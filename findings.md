# 分析发现

## 2026-08-10：开票重申请 Cannot POST 定位
- 前端调用地址为 `/api/invoices/{invoiceId}/reapply`，Vite H5 代理转发到本地 API 3100；后端全局前缀为 `/api`，正确路由是 `POST invoices/:id/reapply`。
- 截图中的 `Cannot POST` 不是业务状态校验失败，而是运行中的旧 API 实例未加载新增路由。重启后，同一路径对不存在编号返回 `404 原开票申请不存在`，说明路由已生效。
- 真实成功链路由 `p1-write-operations.spec.ts` 覆盖：驳回 → 重申请 → 再驳回 → 再重申请 → 管理端通过，且使用新申请编号和 `retryOfInvoiceId` 保留审计链路。

## 2026-08-09：P2 授权账号输入风险

- 管理端学员档案的关系授权接口本身已有后端校验和审计，但原前端要求管理员手工输入 `userId`，容易因复制错误或误选账号造成授权风险。
- 已改为搜索启用普通用户、选择账号、选择关系类型、二次确认后提交；接口仍使用原有 `/admin/student-profiles/:id/relationships`，没有绕过权限边界。
- 普通用户访问学员关系管理接口的 403 测试已补齐；运行态浏览器成功/取消路径仍需在服务启动后复核。

## 2026-08-09：确认弹窗二次渲染导致的层级回归防护

- 仅在打开瞬间设置 z-index 仍可能被 uni-h5 后续渲染覆盖；因此确认 Portal 现在会在首帧、下一帧、0ms 和 32ms 重新应用 `!important` 层级，并确保仍挂在 `document.body`。
- 该兜底覆盖修改密码、保存资料、取消报名、支付凭证、开票、报名提交、学员关系和退出登录等所有 `showClientConfirm` 入口，不改变确认按钮的业务逻辑。

## 2026-08-09：确认框被业务弹窗遮挡的根因与处理

- 单纯提高普通 DOM `z-index` 不能完全规避 uni-h5 宿主、页面过渡和 stacking context；截图中的白色“账号与安全”弹窗未被二次确认遮罩覆盖，符合确认节点仍处于下层的特征。
- 已将 H5 确认框升级为浏览器 Top Layer（`HTMLDialogElement.showModal()`），并保留旧 WebView 的 body Portal + `!important` z-index 回退。
- 该改动覆盖修改密码、资料保存、报名、取消报名、支付凭证、开票、学员关系和退出登录等所有 `showClientConfirm` 入口。

## 2026-08-09：C 端二次确认层级复核

- 发现业务弹窗使用 `z-index: 1000` 时，部分 uni-h5 版本原生 `uni.showModal` 默认层级为 `999`，存在确认框落到业务弹窗下方的兼容性风险。
- 当前统一契约为：业务弹窗 `90`；uni-h5 系统弹窗根/遮罩/内容 `2147483000/2147483001/2147483002`；C 端重要确认根/遮罩/内容 `2147483645/2147483646/2147483647`。
- C 端重要确认通过 `document.body` Portal 和 `!important` 内联层级渲染；业务、消息、我的、报名、学员页面均已复核。

## 2026-08-09：当前项目状态复核

- 管理端主要模块已能在 `frontend/admin-react/src/App.tsx` 找到对应 `apiFetch`/`apiUpload` 调用和后端 `/admin/*` 路由；不能再笼统判定为“很多按钮没有实现”。
- 仍需把“代码链路存在”与“可上线”区分：成功后刷新、取消不写入、重复点击门禁、失败重试、权限、脱敏和审计必须有证据。
- C 端重要二次确认已统一使用 `showClientConfirm`；业务弹窗 `1000`，C 端确认层 `2147483645/2147483646/2147483647`，管理端确认层 `2147483646/2147483647`。
- 本地自动化通过不等于服务器可上线；PostgreSQL 迁移、上传文件卷、容器重启、备份恢复和回滚必须在服务器实测。

## 2026-07-31 学员领域重构与第一版脱离 Mock 的基线

- 用户指定的依据文档已核对：当前 `User` 是账号主体，`Order.participants` 是报名快照，`/admin/students` 与 `/admin/enrollments` 当前均由订单展开，尚不存在独立 `Student`/`Enrollment` 数据表。
- 依据文档给出的目标模型，后续实现应采用 `User`（账号）+ `Student`（学员档案）+ `AccountStudent`（账号关系）+ `Enrollment`（报名履历）四层结构；订单继续承担金额、支付、开票和兼容快照职责。
- 迁移必须采用“新增表/索引 → 历史回填 → 事务双写 → 新读旧留 → 对账切换”的渐进路径，不能直接删除 `Order.participants`。
- 本次用户目标不仅是文档更新，还要求以当前代码事实为基线更新功能清单，并把后续工作拆成可执行、可验收、可持续引用的任务规划。
- 当前 C 端大多数业务请求已通过 `frontend/client-uni/src/common/api.ts` 访问 `/api`，`common/mock.ts` 当前未被页面导入；但微信登录仍使用 `mock:` 身份标识，且 H5 开发失败分支会调用后端 mock 适配接口，生产接入仍未完成。
- 当前 `CreateOrderDto` 只接收 `participants[].data`，控制器丢弃潜在的 `studentId`；`MvpService.createOrder()` 只写 `Order.participants` JSON，说明独立学员档案仍是规划而非现状。
- FeatureList 当前有 91 个标准功能、354 个原子功能；`WEB-STU-001` 的绿色实现标记与代码事实不一致，需改为“订单报名快照/兼容入口”，并新增学员主档案、账号关系、报名履历、去重合并、迁移对账和真实 API/数据库联动等规划功能。

## 数据模型

- `User` 是账号主体：`id`、`username`、`passwordHash`、`role`、`name`、`company`、启用状态、积分及注册/更新时间；关联 `Order`、`Invoice`、`Preview`、`Feedback`、`PointLedger`。
- `Course` 是课程主体，包含 `registrationTemplateId`，通过外键关联 `RegistrationTemplate`；一个模板可被多个课程复用，一个课程当前只关联一个模板。
- `RegistrationTemplate` 用 `payload` 字符串保存字段定义 JSON，另有 `version`；字段类型包括 text、phone、select、radio、checkbox。
- `Order` 同时关联 `userId` 和 `courseId`，并以 `participants` 字符串保存该订单下报名人的 JSON 数组，`participantCount` 保存人数；没有独立的 Student/Participant 表。
- `PaymentProof` 只关联订单，不直接关联用户；订单通过 `userId` 间接归属账号。

## 后端行为

- `createOrder(userId, courseId, participants, paymentMethod)` 先读取课程模板，按模板必填字段逐个校验报名人，再把整个数组 `JSON.stringify` 写入一条订单；允许多人报名时一条订单可含多名报名人。
- 课程 `allowMultiParticipant` 为 false 时拒绝多人报名；同时按报名人手机号检查同课程有效订单中的重复报名。
- `listEnrollments()` 读取所有订单，把 `Order.participants` JSON 数组展开成报名明细，并补充账号用户、课程、订单和支付状态；`listStudents()` 目前只是该方法的别名。
- 用户管理 `listUsersPage()` 查询 `User`，展示账号资料，并额外统计订单数和课程预览数；“报名课程数”实际是订单条数，不是去重后的课程数。
- 当前后端认证控制器只有登录、刷新令牌、当前用户查询，没有发现公开注册接口；用户数据来自种子/迁移或其他未在当前模块暴露的管理流程。

## 前端语义

- 管理端“用户管理”调用 `/admin/users`。
- “学员管理”和“报名明细”分别调用 `/admin/students`、`/admin/enrollments`，但后端返回完全相同的展开结果，因此当前学员管理不是独立学员主数据维护。
- 详情面板直接显示订单下的报名人数组，说明报名人资料来源是订单快照，而非 User 表。

## 示例数据

- 种子数据中的每条演示订单都写入一元素 `participants` 数组；课程模板按课程或课程组配置，证明模板字段与报名数据均通过 JSON 快照承载。

## 2026-07-31 本次改造现状

- C 端底部 Tab 在 `frontend/client-uni/src/pages.json` 中仍显示“业务”，业务页面标题和文案也使用“我的业务”；需要统一改为“订单”。
- C 端“我的”页目前只有简单弹窗：个人资料只能修改姓名，账号与安全是直接重置演示密码，尚未支持姓名、联系方式、性别、企业等完整资料编辑。
- `User` 当前只有 `name`、`company`、`avatarText` 等基础字段，没有 `phone`、`gender`、`email`、微信身份标识等资料字段；认证控制器也只有账号密码登录，没有微信一键登录接口。
- `Course` 当前没有图片字段；首页和详情页通过前端课程编号映射本地 SVG，首页轮播第一张还使用固定 banner 图。管理端课程编辑器没有课程图片上传控件。
- 报名汇总后端已返回每门课程的总人数、已支付人数、未支付人数，但管理端目前只渲染表格，没有图表；汇总行也没有详情操作。
- 报名明细后端已有 `/admin/enrollments`，可按订单展开报名人信息；可复用于报名汇总详情中的已支付/未支付分区。

## 2026-07-31 独立学员档案复核

- 当前 `Order.participants` 是 JSON 字符串，`MvpService.createOrder()` 只写订单快照；`listStudents()` 直接复用 `listEnrollments()`，所以“学员管理”不是独立主数据。
- `CreateOrderDto` 将 `participants[].data` 映射为报名对象，当前接口尚未接收 `studentId`；独立档案改造需要在兼容旧请求的前提下增加可选 `studentId`。
- 当前重复报名校验从订单快照中提取手机号，未建立数据库级学员唯一键；迁移时必须保留异常匹配和人工合并入口。
- `Course.registrationTemplateId` 指向当前模板，但订单没有保存模板 ID/版本；新 `Enrollment` 应保存模板版本及当次 `formPayload`，不能用后续模板覆盖历史数据。
- `Order.userId` 表示下单/付款责任账号，不能替代实际参训人；目标模型应拆分 `User`、`Student`、`AccountStudent`、`Enrollment` 四类关系。
- 独立档案上线应采用“历史回填 → 新订单事务双写 → 对账 → 新读旧保留”的渐进式迁移，禁止直接删除 `Order.participants`。

## 2026-07-31 最终基线校正

- 当前仓库已存在 `User.phone`、`User.gender`、`User.email`、`User.wechatOpenId` 和 `Course.image` 字段；此前“尚未存在这些字段”的早期记录仅代表当时检查点，不再作为当前事实。
- 当前 FeatureList 已完成本轮更新：105 条标准功能、402 条原子功能；P1 完成后已实现标记分别为 23 条和 217 条。后续统计和任务规划以更新后的 Excel 为准。
- 当前业务数据链路已基本使用真实 API + Prisma/SQLite；仍需脱离的开发适配边界是 `mock:` 微信身份、H5 开发设备标识回退和未配置渠道时的在线支付占位。
- 本轮已生成 `project_baseline.md`、`implementation_plan.md` 及 `Docs/Summary`/`Docs/Plans` 对应文档；后续开发任务从规划 P1 开始，必须绑定 FeatureList 编号并经过迁移、双写、对账和回滚门禁。

## 2026-07-31 P1 数据模型实施

- 已在 `backend/api/prisma/schema.prisma` 新增 `Student`、`AccountStudent`、`RegistrationTemplateVersion`、`Enrollment` 及 User/Course/Order/Template 反向关系。
- 已新增 `backend/api/prisma/migrations/0002_student_domain/migration.sql`；自定义 `prisma/migrate.js` 现按 0001 → 0002 顺序执行，并将 SQLite `user_version` 提升为 5。
- P1 迁移只新增表和索引，主库 `Order.participants` 保留；主库已有 20 条订单快照，新四张表当前均为 0 行，历史回填留给 P2。
- 已在修改 schema 前生成一致性备份：`backend/api/data/training.pre-p1-20260731.db`。
- 测试数据库和当前主库均已执行迁移检查；四张新表、索引和 `user_version=5` 均存在。
- Prisma schema `validate`、后端构建和 4 个测试套件（10 项）通过；已用当前生成客户端成功查询四个新模型。完整 `prisma generate` 仍受 Windows 查询引擎文件 `EPERM rename` 环境锁影响，需在后续环境清理后重新执行完整生成命令。

## 2026-07-31 P2 历史回填与对账

- `0003_student_backfill_ops` 迁移必须纳入自定义迁移器，否则 P2 脚本缺少批次/异常表；迁移器当前将 `user_version` 提升到 6。
- 使用原生 `node:sqlite` 编写回填脚本，绕开 Prisma Client 生成文件锁问题；脚本按稳定订单游标运行，Student/AccountStudent/Enrollment 使用确定性 ID 和唯一约束保证幂等。
- dry-run 也需要维护进程内手机号候选，否则同一批次中“先出现正常手机号、后出现姓名冲突”的异常会漏报；现已用 `plannedStudentsByPhone` 补齐。
- 手机号冲突不静默合并：同一规范化手机号对应不同姓名时写 `PHONE_NAME_CONFLICT`；坏 JSON 写 `INVALID_PARTICIPANTS_JSON`；缺手机号保留可追溯档案并写 `MISSING_PHONE`。
- 主库现有 20 个订单/21 个报名人全部无异常回填；副本库异常场景验证通过。
- `prisma generate` 的 Windows EPERM 环境问题仍未解决，但不阻断本阶段：schema validate、SQLite 迁移、原生回填、后端测试和构建均可完成。

## 2026-07-31 P3 下单事务双写

- 控制器必须把 `studentId` 与 `data` 分开接收，再在服务层从历史快照中剔除 `studentId`；否则会把权限控制字段写进订单表单快照。
- Prisma transaction 内先创建订单再写履历是可回滚的：越权关系测试确认订单数和履历数均不增加。
- `RegistrationTemplateVersion` 使用 `templateId_version` 复合唯一键 upsert；Enrollment 使用 `orderId + sourceParticipantIndex` 唯一键和确定性 ID。
- 取消/退款以前只更新 Order，已补齐关联 Enrollment 状态更新为 `cancelled`，避免新旧读在状态上分叉。
- P3 测试初次失败原因已修正：测试模板需要必填 `companySize`，复用测试课程需选择测试库已关联模板且有剩余容量的课程。
- 首次运行 `prisma validate` 未设置 `DATABASE_URL` 返回 P1012；随后显式设置 `DATABASE_URL=file:./data/training.db` 重跑通过。该环境错误已解决，不再重复无环境变量命令。

## 2026-07-31 P4 学员 API 与权限

- 为避免破坏现有管理端，保留 `/admin/students` 的订单快照语义，新增 `/admin/student-profiles` 作为独立档案数据源，P5 再切换 UI。
- `Student.phoneNormalized` 不设全局唯一，匹配 API 返回候选；编辑时若命中其他 active 档案直接阻断，避免静默覆盖。
- 合并关系时若目标已有相同账号关系，源关系改为 revoked；若存在相同订单/报名序号履历则阻断事务，避免唯一键冲突和历史丢失。
- 学员列表/导出默认脱敏手机号和邮箱；管理员守卫控制资源访问，关键操作通过既有 `AuditLog` 记录。
- P4 自动化测试验证关系授权、默认关系、解除、匹配、履历、软合并和停用；下一阶段重点是管理端两个模块的数据源与交互切换。

## 2026-07-31 P5 管理端视图拆分

- 为保持旧页面兼容，新增 `/admin/enrollment-records`，没有直接改变旧 `/admin/enrollments` 汇总接口语义；管理端报名明细独立读取 Enrollment。
- 学员管理列表默认只显示 Student 主档案字段；详情再请求 `/admin/student-profiles/:id`，避免把完整关系和敏感数据塞进列表。
- 报名履历详情使用保存的 `formPayload`，不重新套用当前报名模板；模板版本和订单状态同时展示以便审计。
- 管理端构建提示已清除重复 `enrollmentCount` 标签定义；当前 Vite 构建无阻断警告。

## 2026-07-31 P6 C 端学员体验

- `/students` 返回的是当前账号 active `AccountStudent` 关系，C 端无需自行过滤其他账号学员。
- 报名页的模板字段是动态配置，不能假设所有课程都有同名字段；实现采用姓名/手机号/企业等常用 key 映射，无法识别的字段继续由用户填写。
- `studentId` 只作为订单控制字段传输，后端会从 `participants[].data` 快照中剔除并重新校验关系权限。
- 解除学员关系只撤销账号关系，不删除 `Student`、`Enrollment` 或旧订单快照，符合历史可追溯要求。
- P6 初次全量回归暴露 `mvp.controller.ts` 两个同名 `students` 方法，已将旧管理兼容接口改为 `adminStudents`；生产化 5 项回归恢复通过。
- C 端 H5 构建通过，仅保留 Sass legacy `@import` 弃用提示，不属于阻断错误。

## 2026-07-31 P7 双读对账与切换

- Enrollment.orderId 在兼容历史数据中允许为空，对账建立订单索引时必须过滤空 orderId，不能把孤立履历误归入订单。
- 订单取消是唯一必须逐条映射到 Enrollment `cancelled` 的状态；支付状态目前仍以 Order 为权威，因此对账只检查“已支付订单不能没有有效履历”，不强行改写 Enrollment.status。
- 新读切换写入 `SystemConfig(student.readMode)`，默认 legacy；切换 new 前重新执行同一份对账，差异存在时返回 400，切回 legacy 始终允许。
- 兼容接口保留旧路径和旧字段，P7 不删除 `Order.participants`，因此可在观察期内快速回退。

## 2026-07-31 P8 Mock 边界与渠道 adapter

- `frontend/client-uni/src/common/mock.ts` 没有任何 import，属于遗留演示文件；删除后业务源码扫描无静态 mock 引用。
- 微信 fake 身份只能在非生产环境使用；生产默认 real，缺少 `WECHAT_APP_ID/WECHAT_APP_SECRET` 或 code 时明确拒绝登录。
- 支付 adapter 的 `real` 模式当前只负责配置门禁和统一返回结构，未注入 SDK/签名服务时保持 `ready=false`；不会调用 `payOrder` 伪造支付成功。
- `.env.example` 只提供变量名和安全占位值，不把真实密钥写入仓库；生产 `JWT_SECRET` 仍由 `load-env.ts` 强制校验。

## 2026-07-31 P9 发布与回滚

- 全量回归最终为 7 个测试套件、16 个测试通过；生产化 API 5/5、学员域/P7 2/2、adapter 2/2。
- SQLite 主库存在 `training.db-wal`；直接复制主库文件会得到旧 schema/数据，发布备份必须先 checkpoint，再使用 `VACUUM INTO`。
- WAL-safe 备份已只读打开验证：`user_version=6`，`Order=20`、`Student=21`、`AccountStudent=21`、`Enrollment=21`。
- 回滚优先切换 `student.readMode=legacy`，再恢复构建；数据库恢复只能使用已校验 SHA256 的独立备份，不能直接覆盖线上文件。

## 2026-08-03 C 端“我的学员”独立页面需求核对

- 当前 `frontend/client-uni/src/pages/mine/mine.vue` 已调用 `/students`，支持新增、编辑、设为默认和解除关系，但学员列表仍以内嵌卡片展示，入口不在菜单中，也没有独立路由。
- 当前后端 `listAccountStudents()` 仅返回当前账号 active 关系，并按 `isDefault`、更新时间排序；接口已经提供姓名、手机号、企业、部门、职务、关系类型和默认标记，可直接支撑独立页面。
- 报名页 `setMappedStudentFields()` 已按字段别名回填姓名、手机号、性别、邮箱、企业、部门、职务；模板定义中的其他字段没有匹配别名时继续由报名人手动填写。
- 当前主库关系可能存在没有 `isDefault` 的账号学员，因此页面必须先尝试显式默认，再用账号资料匹配当前学员；匹配不到时显示空状态，不擅自写入默认关系。
- 新页面应按姓名拼音首字母分组；中文名需要前端首字母转换，拉丁字母直接大写，数字/无法识别字符归入 `#`。

## 2026-08-03 C 端独立学员页面实现结果

- 新增 `pages/students/students.vue`，复用现有 `/students`、`/students/:id`、`/students/:id/default` 和删除接口，不改变数据模型。
- 页面当前学员优先采用显式默认关系，再按账号资料匹配；当前账号 `demo` 的运行态数据返回 15 个 active 关系、0 个显式默认，因姓名/手机号/企业均未匹配而按要求显示空状态。
- 中文首字母使用 `Intl.Collator('zh-Hans-u-co-pinyin')` 与代表字比较，不使用错误的 Unicode/GBK 直接编码区间；避免中文姓名全部落入 `#`。
- H5 构建、根目录 verify、接口读取和 diff 检查均通过；仅有既存 Sass 弃用警告。

## 2026-08-03 单机部署分析

- 根目录脚本确认：`build:api`、`build:admin`、`build:client` 和 `verify` 可作为发布前门禁；生产 API 入口为 `backend/api/dist/main.js`。
- `backend/api/src/main.ts` 将全局前缀设为 `/api`，默认端口为 `3100`；生产建议只监听本机，由 Nginx 反向代理。
- 管理端和 C 端构建产物的 HTML 当前引用绝对 `/assets/...`，因此两个前端应使用两个域名或独立站点；同域名子路径部署需要额外配置 base 后重新构建验收。
- `backend/api/prisma/migrate.js` 使用 SQLite 兼容迁移；有数据的生产环境不可直接运行含 seed 的 `db:init`。SQLite WAL 数据库不可直接复制，必须 checkpoint 后使用 `VACUUM INTO` 备份。
- 当前 `WECHAT_ADAPTER`/`PAYMENT_ADAPTER` 是生产配置门禁；支付 `real` 尚不代表已接入真实支付 SDK。上线文档已明确该边界。

## 2026-08-03 C 端登录页需求复核

- 登录页原先在 `login.vue` 中硬编码 `demo`、`123456`，并提供微信登录、三类快捷账号和演示密码提示；这些属于演示入口，不应出现在正式 C 端登录界面。
- 本次仅移除前端登录入口和误导性文案，保留后端账号密码登录接口，避免影响既有认证链路。
- “按照图 2 框架”落地为居中的品牌 + 登录卡片、圆角账号/密码输入框和单一登录按钮；隐私政策、开放平台/扫码登录和演示入口均未保留。

## 2026-08-03 注册、找回密码与迁移边界

- 注册功能可在测试环境直接落库并签发 JWT；账号唯一性由 `User.username` 约束保证，密码使用现有 scrypt 哈希策略。
- 找回密码不会把验证码明文存入数据库；开发 fake adapter 只用于内测，生产必须改为短信/邮件 webhook，或明确禁用该功能。
- 用户提供个人微信/支付宝收款信息只能用于人工线下收款凭证测试，不能实现标准在线支付；正式支付需要微信支付商户号/证书或支付宝开放平台应用、异步回调和验签。
- SQLite 到 PostgreSQL/MySQL 不能仅改 Prisma provider：必须先确定目标数据库、导出策略、数据量、停机窗口和回滚副本，再做测试库迁移、数量/金额对账、双读观察和生产切换。当前优先推荐 PostgreSQL。
- 真实学员档案流程的代码基础已完成 P1-P9，但仍需按“本人、单账号多人、跨账号授权、模板差异回填、取消/退款、越权、合并、对账”逐条做内测验收，默认 `student.readMode=legacy`，对账无差异后才切换 `new`。
- 现场检查发现 3100 原先仍运行旧 API，新增找回密码接口返回 404；执行兼容迁移后数据库为 `user_version=7`，重启 3100 项目实例后接口已恢复 201。说明“代码已改”与“运行中的服务已加载新代码”必须分别验收。

## 2026-08-04 在线支付真实化复核

- 发现并修正高风险缺口：旧 `/orders/:id/pay` 只校验登录账号，不校验支付平台结果，任何用户都可把自己的待支付订单改成已支付。现在该接口对 online 一律拒绝，订单只能由已验签的微信/支付宝异步通知确认。
- 支付意图使用 `PaymentTransaction.outTradeNo=orderId`，保存渠道、金额、回调交易号和状态；重复回调在已支付状态下幂等返回。
- 微信支付 v3 回调要求 `Wechatpay-*` headers、平台证书、API v3 Key；支付宝回调要求支付宝公钥和 RSA2 签名验证。缺少配置时不会伪造支付成功。
- C 端旧的伪 QR 网格和“我已完成支付”直写订单逻辑已移除，改为真实支付链接/原生支付和服务端状态轮询。
## 2026-08-04 项目现状核验

- SQLite 只读核验：`user_version=8`；`User=12`、`Course=6`、`Order=21`、`Student=22`、`AccountStudent=22`、`Enrollment=22`，`PaymentTransaction=0`、`PasswordResetChallenge=0`。
- 代码和文档一致的部分：注册、找回密码、独立学员档案、报名履历、管理端档案 API、C 端我的学员、支付意图/回调状态机已实现。
- 尚未完成的不是数据库表，而是业务闭环：真实学员场景验收、商户支付配置和回调验收、PostgreSQL/MySQL 测试迁移与生产切换。
- 个人微信/支付宝收款码只能作为线下凭证审核测试；不能提供标准在线支付的订单号、异步通知、验签、退款和幂等能力。
- 本次核验时未发现 3100/5174/5185 端口监听，机器上仍有多个 Node 进程；服务状态必须以启动后的健康接口和页面访问为准。
- 已新增核验报告：`Docs/Summary/2026-08-04_项目现状核验与内测到生产执行清单.md`。
## 2026-08-04 PostgreSQL 选择后的新发现

- 当前 `PrismaService` 原实现无条件将 `DATABASE_URL` 转换为 `file:` 路径，直接迁移 PostgreSQL 会失败；已改为识别 `postgresql://`/`mysql://` 并保留 SQLite 兼容。
- `prisma/migrate.js`、`prisma/seed.js`、`prisma/seed.ts` 均为 SQLite 专用，已增加 URL 保护。
- H5/小程序内测可访问内网 API，但微信/支付宝公网回调不能访问 `192.168.0.162`；真实支付回调还需要公网 HTTPS 隧道或正式域名。
- 服务器 root 登录凭据不是数据库实例信息；需要独立的 PostgreSQL host/port/database/user/password/schema/SSL 配置。
## 2026-08-04 小程序支付场景核验

- 微信小程序 JSAPI 下单必须有当前用户 `wechatOpenId`；仅有账号密码或个人收款码不能生成合法 JSAPI 支付参数。
- 现在 `WECHAT_PAY_PRODUCT=jsapi` 时，后端会从 User 读取 `wechatOpenId`，缺失则返回不可用；成功时返回 `appId/timeStamp/nonceStr/package/signType/paySign`。
- 微信登录场景已支持小程序 `code2Session`、H5/公众号网页 OAuth，但当前登录页面仍是账号密码页面，未重新展示微信一键登录入口。
## 2026-08-04 回归结果

- 全量后端测试 11/11、25/25 通过；API、C 端 H5、管理端构建和根目录 verify 通过。
- `192.168.0.162:22` 网络可达，但尚未认证登录；不能据此推断 PostgreSQL 已安装。
- 真实支付和生产迁移仍缺少商户密钥、HTTPS 回调地址、PostgreSQL 实例和可回滚维护窗口，因此目标保持未完成。
## 2026-08-04 迁移脚本核验

- SQLite 导出实际结果：`user_version=8`；User=12、RefreshToken=115、Order=21、Student=22、AccountStudent=22、Enrollment=22、PaymentTransaction=0、PasswordResetChallenge=0。
- 导出采用只读连接，包含 SHA-256；导入前仍需先做 WAL-safe `VACUUM INTO` 备份。
- PostgreSQL 导入脚本只能在 `db:prepare:postgres` 生成 PostgreSQL Client 后运行，不能在当前 SQLite Client 上直接执行。
- PostgreSQL 导入默认跳过 `RefreshToken` 和 `PasswordResetChallenge`，避免迁移旧会话或旧验证码。
- 首版 `.mjs` 脚本因 CommonJS/ESM 格式不匹配而失败，已改为 `.cjs` 并完成语法检查。
## 2026-08-04 服务器连接状态

- SSH 探测结果：网络可达，但当前会话没有可用公钥认证；不能据此判断 root 密码是否正确，也不能读取服务器 PostgreSQL 状态。
- 已提供只读诊断脚本，下一步需要用户在服务器终端运行并返回非敏感输出，或配置临时 SSH 公钥后再继续远程部署。

## 2026-08-04 服务器基础信息回传

- 服务器主机名为 `ctc-Vostro-3910`，运行 Ubuntu 24.04 系列内核 `6.17.0-19-generic`。
- Node.js 为 `v22.22.1`，满足项目当前运行时要求。
- `psql` 命令不存在，说明 PostgreSQL 客户端未安装；不能仅据此断定 PostgreSQL 服务端未安装。
- 下一步必须检查 `systemctl status postgresql`、`dpkg -l | grep postgresql`、`ss -ltnp | grep 5432` 和 `pg_isready`，再决定安装或创建实例。

## 2026-08-04 个人收款码线下闭环补齐

- 管理端新增微信/支付宝收款码图片上传，图片存放在 `storage/payment-settings`，公开接口只返回图片 URL，不暴露服务器路径。
- C 端报名账单和支付记录凭证弹窗展示收款码，并明确“付款后上传凭证、审核通过后到账”。
- 新增文件类型、大小、路径穿越校验及服务层回归测试；个人收款码仍不会被当成在线商户支付回调。

## 2026-08-04 渠道配置自检

- 新增管理员接口 `GET /api/admin/integration-readiness`，只返回适配器模式、缺失配置项、产品类型和 HTTPS 状态，不返回任何密钥值。
- 自检覆盖微信登录、微信支付 H5/Native/JSAPI、支付宝 WAP 和密码找回 webhook；生产 fake/disabled 配置会被明确标记为不具备生产条件。
- 管理端新增“系统管理 → 渠道自检”页面，直接展示生产可用/内测可用/未配置状态和缺失字段，避免管理员手工拼接接口请求。
- 新增 `Docs/runtime/install-postgres-ubuntu24.sh`，用于 Ubuntu 24.04 本地安装 PostgreSQL、创建应用角色和数据库；脚本不记录密码，已存在数据库不会删除或覆盖。
- 新增 `Docs/runtime/migrate-sqlite-to-postgres.sh`，带 `CONFIRM=YES` 门槛，自动执行 WAL-safe 备份、SQLite 导出、PostgreSQL schema/migration、导入和对账证据生成。
## 2026-08-04 本地到服务器部署手册核对结果

- 目标服务器为 Ubuntu 24.04 系列，Node.js v22.22.1 已安装，`psql` 尚未安装；当前未执行远程写入。
- 现有单机指南末尾存在重复的“第 13 节完整步骤”，包含 Windows 直接使用 `rsync` 和旧图片说明；新手册应作为权威版本，不继续扩充旧文档。
- PostgreSQL 初始化脚本为 `Docs/runtime/install-postgres-ubuntu24.sh`，默认创建 `training_app`、`training_management`、5432、public schema，密码仅交互输入。
- SQLite → PostgreSQL 脚本为 `Docs/runtime/migrate-sqlite-to-postgres.sh`，必须 `CONFIRM=YES`，会先 WAL-safe 备份、导出 SHA-256 快照、准备 PostgreSQL schema、部署 migration、导入并回报行数。
- API 生产入口为 `backend/api/dist/main.js`，全局前缀为 `/api`；微信回调为 `/api/payments/wechat/notify`，支付宝回调为 `/api/payments/alipay/notify`；渠道自检为管理员接口 `/api/admin/integration-readiness`。
- H5 和管理端构建产物均使用绝对 `/assets`，无域名内网环境宜先用同一 IP 的端口或两个 server 块做内测；正式渠道回调必须公网 HTTPS，LAN IP 不能直接接收平台异步通知。
- 文档不得写入或回显用户此前提供的 root 密码、数据库密码、AppSecret、API v3 Key、证书私钥、支付宝应用私钥或完整 `DATABASE_URL`。
- 用户服务器截图显示项目直接位于 `/opt/training-management`，没有 `/opt/training-management/current`，且当前 `.env` 使用 SQLite `DATABASE_FILE`；手册必须允许直接目录部署，不应假设 current 软链接已经存在。
- 截图中 JWT_SECRET 曾以明文出现在编辑器画面；应要求用户立即轮换 JWT_SECRET，不能复用截图中的旧值。

## 2026-08-04 Docker 部署评估

- 当前仓库没有 Dockerfile/Compose 配置，也没有 Redis 依赖或业务调用；Redis 不应为了“完整部署”而强行加入。
- 当前 API 运行时是 Node.js 22 + Prisma，SQLite 适合本地单实例，不适合作为容器内生产持久库；目标仍应切换 PostgreSQL。
- 推荐单机采用 Docker Compose 管理 API、PostgreSQL、Nginx（可选），使用命名卷/主机目录保存 PostgreSQL 数据、支付凭证、课程图片和备份；不要把数据库数据打进镜像，也不要把全部服务塞进一个容器。
- Docker 能解决环境一致性和 GitHub 发布重复性，但不会自动解决迁移、备份、支付 HTTPS 回调和密钥管理；内测可容器化，生产仍需独立备份和回滚演练。
- 已落地 `docker-compose.yml`、`docker/Dockerfile.api`、`docker/Dockerfile.web`、两个 Nginx 配置、`.dockerignore` 和 `.env.docker.example`；API 构建阶段使用占位 PostgreSQL URL 生成 Prisma Client/migration，运行时由 Compose 注入真实 URL。
- Compose 使用 `api-migrate` 一次性服务、PostgreSQL 健康检查、API 健康检查和两个前端端口（8080/8081）；Docker 内 API 连接主机名为 `postgres`。
- 本地 Docker CLI 可用但 daemon 未运行，镜像构建无法在本机完成；`docker compose config`（验证环境）和根目录 `pnpm run verify` 已通过。
- 服务器 root 执行 `git pull` 时触发 Git `detected dubious ownership`：仓库位于 `/opt/training-management`，文件所有者为 `training`。优先用 `runuser -u training` 执行 Git；若 training 没有 GitHub 凭据，再仅为该目录添加 `safe.directory` 例外。
- 用户以 `runuser -u training -- git -C /opt/training-management status --short` 成功执行，说明 training 对仓库具备 Git 读写权限；唯一输出是被跟踪的生成文件 `frontend/admin-react/tsconfig.tsbuildinfo` 有本地修改，拉取前应先查看差异并决定保留或恢复。
- `git diff --stat` 显示该文件只有 1 次插入、1 次删除；这只能证明内容发生变化，不能证明是人工编辑。由于 `tsconfig.tsbuildinfo` 是 TypeScript 增量构建缓存，优先按自动生成文件处理，但恢复前仍应查看实际 diff。
- 用户查看 diff 后，`tsconfig.tsbuildinfo` 已恢复；新的 `?? .lesshst` 是 training 用户在仓库目录生成的 less 历史文件，不属于项目代码，不会阻止 pull，可在确认后删除。
- 用户执行 `git pull` 已进入 GitHub SSH 首次主机指纹确认，说明仓库 remote 正在使用 SSH 或 Git 正尝试 SSH 连接；当前尚未到 GitHub 仓库授权验证阶段。
- 用户按示例检查 `id_ed25519.pub` 时得到 `No such file or directory`，原因是 training 用户的 SSH 密钥文件尚未创建（且此前用 `Ctrl+C` 取消了 GitHub 主机指纹确认），不是仓库目录权限问题。
- 用户已成功生成 training 用户的 Ed25519 密钥，并输出了单行 `.pub` 公钥；下一步只需将 `ssh-ed25519 ... training-management@192.168.0.162` 整行添加到目标 GitHub 仓库的 Deploy keys，保持只读。
- 用户当前 GitHub 账号的 SSH and GPG keys 页面已有 `Company-Server` 账号密钥（Read/write）；不必重复寻找 Deploy keys。需要先比较其指纹与当前服务器 `training/.ssh/id_ed25519.pub`，不一致时在同一账号页面新增当前公钥，旧 key 暂不删除。
- SSH 密钥按 Linux 用户分开：root 使用 `/root/.ssh`，training 使用其 home 下的 `.ssh`；`runuser -u training -- git ...` 只会优先使用 training 的密钥，不能自动使用 root 的密钥。现有 GitHub `Company-Server` key 可能来自旧部署用户或旧密钥，不能仅凭名称判断属于当前 training。
- 用户已实际核对指纹：GitHub `Company-Server`/root 公钥为 `SHA256:1Vp20x...`，training 公钥为另一个 `SHA256:U+Ioz...`；因此现有 GitHub Key 确实属于 root，training 拉取必须新增其公钥或改为 root 拉取。
- 用户服务器已成功执行 `runuser -u training -- git ... pull --ff-only`，输出包含 `docker-compose.yml`、API/前端 Dockerfile 和 Nginx 配置的 create mode，说明 Docker 代码已拉到服务器；通常无需重启 Ubuntu 主机。
- 用户服务器 `git status` 输出 `?? .ssh/`：由于 training 的 home 当前被设置为项目根目录，SSH 私钥目录落在 Git 工作区内。必须移出到 `/var/lib/training/.ssh` 并通过 `HOME`/SSH 配置使用，不能只做 Git 忽略；Docker 构建上下文也已补充排除 `.ssh`。
- 用户已成功将 `id_ed25519`、公钥和 known_hosts 移至 `/var/lib/training/.ssh`；随后命令返回 `env: git: 权限不够`，原因是复制多行命令时反斜杠位置导致 `env` 调用异常，不代表 Git 仓库权限不足。应改用单行命令或 `sh -lc`。

## 2026-08-06 管理端操作闭环核验

- 当前管理端不是“没有后端接口”，而是部分按钮把业务操作压缩在 `operate`/`secondaryOperate` 中直接调用，导致退款、开票通过、用户密码重置等动作缺少确认和明确结果。
- 开票前端原先自动生成 `MOCK-${Date.now()}` 作为发票号，这不符合真实财务流程；现改为要求操作人员输入实际发票号码，驳回仍走后端 `processInvoice`。
- 学员档案 API 已支持 `PATCH /admin/student-profiles/:id`、状态切换、账号关系默认设置和解除关系；此前详情页只展示关系，未提供这些入口，现已接入并在操作后刷新详情。
- 管理端操作统一增加 `operationKey` 锁，避免同一批列表按钮在请求尚未完成时重复提交；请求异常通过现有 notice 区域反馈。
- 当前退款接口仍是系统订单状态/名额/履历处理；它不是官方支付渠道退款 API。由于用户明确暂缓真实支付，本阶段只保证管理端状态操作可审计，不宣称支付退款已生产闭环。
- 学员详情现在组合读取 `/admin/student-profiles/:id` 与 `/admin/student-profiles/:id/enrollments`，可在同一弹窗查看主档案、授权账号和报名履历；授权账号使用后端已有 `POST /relationships` 接口，未新增重复数据模型。
- 管理端登录页此前仍预填 `admin/123456` 并展示默认密码；这属于上线前安全/体验缺口，现已改为空白输入、标准 autocomplete 和表单回车提交。开发种子账号仍只用于本地测试，不作为生产凭据。
- 静态验收脚本现覆盖关键管理操作 API 和危险演示痕迹，后续新增按钮必须同步增加对照断言，避免“有按钮但无真实接口”的回归。
- P10 专项场景验证了一个账号代理多个学员报名和默认学员切换；模板必填字段失败时订单、Student 和 Enrollment 数量保持不变，证明创建事务回滚有效。
- 消息模块目前只有站内消息存储和展示数据模型，没有短信/邮件投递适配器；本轮已将预留渠道改为显式不可用，而不是让运营人员误以为消息已发送。
- 发现并修复学员详情操作后的状态丢失问题：详情刷新必须同时读取 profile 和 enrollments；保存失败不能关闭编辑弹窗。
- 报名明细本身没有独立的状态变更接口，支付审核/退款属于订单域；因此列表动作应明确为“查看详情”，不应显示“处理”或伪造处理入口。

## 2026-08-06 上线前非支付功能审计（本轮）

- 当前基线验证通过：根目录 `pnpm.cmd run verify`、管理端 `build`、管理端 `test:ui` 以及后端 12 个 Jest 套件/29 项测试均通过。
- 发现管理端 `audits` 审计日志列表仍复用默认“处理”按钮，但 `operate` 对该模块没有真实操作，只会提示“已打开详情视图”；这是明确的无效按钮，应改为只读列表并隐藏操作列。
- 发现 `enrollment-details` 和 `students` 同时传入 `onDetail` 与 `onOperate`，两者都显示“查看详情”，会产生重复按钮；应保留单一详情入口。
- 发现订单列表对“待支付/已取消/已退款”等不可操作状态仍显示“审核 / 退款”按钮；应按状态仅在“待审核/已支付”显示可执行操作，其余只保留查看详情。
- 发现课程和 Banner 删除按钮未做二次确认；上线前应加入确认，避免误删内容。
- 后端已有 `POST /admin/student-profiles/:id/merge` 合并档案接口，但管理端详情没有入口；这是已具备后端能力却未暴露的非支付功能，可补充“合并到其他档案”操作并在成功后刷新详情。
- 后端已有学员读取模式/对账接口，但属于迁移运维控制，不应在普通业务列表中伪装成可执行按钮；继续保留接口和文档，另行做受控运维入口。
- 已修复上述管理端问题：审计日志隐藏操作列；报名明细/学员列表只保留一个详情入口；订单仅在待审核/已支付状态显示可执行动作；开票仅在待处理状态显示处理/驳回；课程与 Banner 删除增加确认；学员详情新增基于已有后端接口的“合并到其他档案”。
- 后端补齐课程报名状态门禁：`报名中/名额紧张`、截止时间、剩余名额由 `quote` 和 `createOrder` 统一校验；课程视图新增 `registrationOpen` 和关闭原因，管理端保存课程时校验状态和截止时间格式。
- 新增课程状态/截止时间回归测试；后端测试从 29 项增加到 30 项并全部通过，管理端构建和静态验收仍通过。
- P1 报名模板增强已落地：新增复制（新建模板，不修改原模板）和安全删除接口；删除会拒绝仍被课程使用或已有历史版本的模板，避免破坏报名历史。
- 模板编辑弹窗新增“另存为副本”和“删除模板”按钮，删除由前端二次确认、后端关联约束共同保护；新增后端回归测试后全量达到 31 项通过。
## 2026-08-06 继续推进非支付上线版本：基线盘点

- 当前管理端共覆盖课程、Banner、报名模板、报名汇总/明细、订单、开票、学员档案、用户、优惠规则、反馈、站内消息、积分、系统配置、审计和渠道自检。
- 后端已有大部分基础 CRUD/处理接口；当前明显的业务增强缺口不是“所有按钮都没有接口”，而是部分模块缺少完整的生命周期能力：优惠规则无独立删除/冲突检查，积分列表只显示余额不显示流水，消息没有目标范围/阅读状态，审计仅按 action 过滤且 detail 是字符串，学员导出默认最多 100 条且脱敏/权限策略需进一步收紧，发票仅有状态和发票号没有文件状态预留。
- 当前 UI 静态验收已经能阻止若干已修复问题回归（无效按钮、重复详情、状态误操作、删除无确认、模板复制/删除等），但尚未覆盖上述 P1 增强项。
- 当前 Prisma 模型中的 Message、PointLedger、AuditLog、Invoice、DiscountRule 字段可支持第一阶段增强；消息阅读状态/目标范围和发票文件状态需要新增持久化字段或结构化 payload 兼容方案，必须同步 SQLite/PostgreSQL 迁移。
- 用户目标明确暂缓支付，因此本阶段优先把管理端非支付功能做成可验收闭环，再进行 Docker PostgreSQL 迁移和发布演练。

## 2026-08-06 P1-1 结果：优惠规则生命周期

- `DiscountRule` 无需新增数据库字段即可支持本阶段增强：新增删除接口，启停仍通过保存接口更新 `enabled`，历史订单已保存金额/折扣，不会因删除规则而改变。
- 冲突定义为：两个启用规则的 `minPeople` 相同且课程范围重叠；空课程范围代表全课程。不同人数门槛按现有“最高门槛优先”逻辑保留，不误报为冲突。
- 管理端规则列表增加冲突规则列；编辑弹窗增加删除入口；列表增加启用/停用入口；保存响应出现冲突时给出明确提示。
- 通过证据：`mvp.spec.ts` 10/10、API build、管理端 build、`test:ui`、根目录 `verify`、`git diff --check`。
- 下一步：积分流水查询与来源展示，然后处理消息目标/阅读状态和审计筛选差异。

## 2026-08-06 P1-2 结果：积分流水查询

- 现有 `PointLedger` 已保存每次增减的用户、积分、原因和时间，因此无需改表；新增分页查询接口返回当前余额和逐笔流水。
- 管理端积分列表新增“查看流水”操作，与“调整积分”分开；流水弹窗明确正负变更和原因，避免只看余额无法追溯。
- 通过证据：`mvp.spec.ts` 11/11、API build、管理端 build、`test:ui`、根目录 `verify`、`git diff --check`。
- 下一步：消息目标范围/阅读状态，再做审计日志按操作者/时间/业务对象筛选和变更差异展示。

## 2026-08-06 P1-3 结果：审计筛选

- 审计查询现在支持操作类型、操作者模糊匹配、关键字（操作人/动作/详情）和时间范围；非法日期会被忽略，不阻断列表查询。
- 管理端审计筛选栏新增操作者、开始时间、结束时间控件，并保留原有操作类型筛选；审计列表继续只读，不新增伪处理按钮。
- 积分调整审计详情现在包含变更原因，便于从流水追溯业务依据。
- 通过证据：`mvp.spec.ts` 12/12、API build、管理端 build、`test:ui`、根目录 verify、`git diff --check`。
- 下一步：消息目标范围与阅读状态；之后再处理学员导出权限/脱敏和发票文件状态预留。

## 2026-08-06 本轮总回归

- 后端全量 Jest：12 个套件、36 项测试通过。
- API 构建、管理端构建、管理端静态 UI 验收、根目录 `verify` 和 `git diff --check` 通过。
- 未将消息阅读/展示范围、导出脱敏、发票文件状态或浏览器人工验收误标为完成。

## 2026-08-06 P1-4 消息目标范围与阅读状态

- `Message.payload` 继续采用结构化 JSON，包含 `targetUserIds`、`targetCourseIds`、`startsAt`、`endsAt`；无需为目标范围重复增加固定列。
- `MessageRead` 使用 `(messageId,userId)` 唯一约束，按用户返回 `readAt` 和 `unreadCount`；导出/导入脚本和 SQLite 0006 migration 已同步。
- 管理端输入的用户/课程 ID 在保存时转数组并在后端校验存在性，避免拼写错误造成“消息保存成功但无人可见”。
- 课程目标可见性同时读取未取消订单和未取消 `Enrollment`，兼容历史回填数据和新订单数据。
- `markMessageRead` 先复用可见性过滤，再写入阅读记录；指定用户消息不能被其他账号标记，防止越权制造阅读数据。
- Prisma Client 生成命令在 Windows 因 `query_engine-windows.dll.node` 被占用而无法 rename，但当前生成目录中的 `index.d.ts`/运行时数据模型已包含 `MessageRead`；API 测试与构建是当前有效证据，后续应在无服务占用的维护窗口重试生成。

## 2026-08-06 P1-5/P1-6 结果

- 学员导出接口此前固定调用分页 100 条且不带页面筛选；现改为接收关键字/状态、最多 1000 条，返回 `truncated`、`sensitiveFieldsMasked` 和导出时间。
- 默认导出使用 `studentView(..., false)`，手机号、邮箱保持脱敏；明文仅保留给受控管理员 API 调用，管理端不提供明文按钮。
- 导出审计详情记录导出数量/总数、脱敏状态和截断状态，便于追责和核对。
- 发票模型已有 JSON `payload`，因此用结构化 payload 字段预留 `invoiceFileStatus`、`invoiceFileName`、`invoiceFileUrl`、`invoiceFileUploadedAt`，无需破坏现有 SQLite/PostgreSQL 表结构。
- 发票状态转换：新申请 `未生成` → 通过 `待上传`；驳回 `不适用`。真实文件上传/电子发票平台仍未接入，不应把“待上传”当作已开具文件。
- `processInvoice` 后端现在拒绝重复处理已完成申请，并要求通过时提供真实发票号；前端状态隐藏只是辅助保护。

## 2026-08-06 P2/P3 回归与 Docker 状态

- HTTP 生产套件现覆盖：普通用户访问管理 API 返回 403、刷新令牌旧值失效、支付凭证受控访问、订单/开票/退款状态链路、消息目标与阅读、学员脱敏导出、重复开票拒绝和应用重启持久化。
- `docker-compose.yml` 的管理端/H5 对外端口是 `18080/18081`，因为目标服务器已有其他进程占用 `8080/8081`；访问地址和防火墙文档已同步。
- 新增 `Docs/runtime/backup-postgres-docker.sh`：从 Compose PostgreSQL 容器生成 custom-format dump，写入 SHA-256 文件并限制权限；不会打印 `.env.docker` 或密码。
- 本机 Docker CLI 可用但 Docker daemon 未运行，不能把镜像构建或容器恢复演练标为通过。服务器侧此前 API/PostgreSQL 容器已启动，但端口调整后的 admin/h5、迁移对账和恢复演练仍需用户在服务器执行并回传证据。
- P2 自动化门禁已满足：生产 HTTP 套件 5/5、后端全量 12/36、API/管理端构建和静态验收均通过；剩余阻断点全部属于服务器外部状态，不是本地代码测试缺口。

## 2026-08-06 可上线非支付版本续作审计

- 当前管理端模块已覆盖课程、Banner、模板、报名汇总/明细、订单、开票、学员档案、用户、优惠、反馈、站内消息、积分、系统配置、审计和渠道自检；按钮/API 静态验收已通过。
- 仍有两个值得补齐的生命周期缺口：消息目前主要通过编辑表单改变启用状态，缺少显式启停/删除动作；反馈处理接口可以重复覆盖已处理反馈，缺少状态门禁。
- `MessageRead.message` 使用 `onDelete: Cascade`，消息删除可以安全级联清理阅读记录，不需要新增数据库字段或迁移。
- C 端消息接口 `/api/messages` 与 `/api/messages/:id/read` 已存在，但仍缺少独立消息展示入口，列入下一阶段非支付增强。
- 发票目前只有文件状态预留，没有真实文件上传与下载链路；不能在发布说明中宣称电子发票文件能力已完成。
- 浏览器逐按钮人工验收尚未有可接管的 localhost 环境；当前证据仍以自动化测试、构建、静态按钮/API 对照和 HTTP 运行为主。

## 2026-08-06 阶段 1 实施结果

- 管理端新增消息启用/停用接口 `POST /admin/messages/:id/enabled`，操作带确认、请求锁、成功刷新和审计。
- 管理端新增消息删除接口 `DELETE /admin/messages/:id`，删除前明确提示用户不可见且阅读记录会清理；数据库关系级联删除 `MessageRead`。
- 反馈处理增加后端状态门禁：已处理反馈不能再次回复，且回复内容不能为空，避免原处理结论被覆盖。
- 回归证据：`mvp.spec.ts` 15/15，API build、管理端 build、`test:ui` 和 `git diff --check` 通过。

## 2026-08-06 阶段 2 前置：C 端消息中心

- C 端新增 `/pages/messages/messages`，从现有 `/messages` 接口读取当前账号可见的站内消息，按未读状态展示，并在打开详情时调用 `/messages/:id/read`。
- “我的”页新增“我的消息”入口和未读数量徽标；页面位置不影响既有“我的学员”入口和积分入口。
- 已更新 `pages.json`、C 端 API 封装和 H5/小程序共用页面；未接入短信/邮件，仍只展示站内消息。
- H5 构建已通过（保留既有 Sass 弃用警告）；下一步继续处理发票文件上传状态链路和管理端展示动作。

## 2026-08-06 阶段 2 实施结果

- 管理端已将“已开票、待上传”的记录接入真实文件选择和上传 API；支持 PDF、PNG、JPEG，默认上限 10MB，上传后状态变为 `已上传`。
- 后端文件保存在 `UPLOAD_DIR/../invoices`，数据库 payload 记录原文件名、MIME、大小、上传时间和受保护读取地址；管理员和所属用户读取时均做权限校验。
- 已新增管理端/用户端发票文件读取路由；用户只能读取自己的发票，管理员可以读取管理范围内文件。重复上传会被拒绝，避免无确认覆盖原始财务文件。
- C 端业务页展示电子发票文件状态和文件名；真实下载交互仍可在浏览器/小程序人工验收阶段确认。
- `mvp.spec.ts` 16/16、API build、管理端 build、`test:ui` 通过；阶段 2 的功能实现已完成，仍需全量回归和人工验收。
## 2026-08-06 可上线非支付版本续作：管理端按钮复核

- 当前工作区仍保留前序未提交改动；本轮不触碰 `.git`，不重置用户修改。
- 管理端 `frontend/admin-react/src/App.tsx` 的列表操作大部分已有 API，但发现两个真实 UI 闭环缺口：
  1. `enrollment-details` 和 `students` 将 `canOperate` 设为禁用，却没有传递 `onDetail`，导致操作列没有“查看详情”按钮。
  2. `feedbacks` 对已处理记录仍显示“回复处理”，只能依赖后端拒绝，前端没有状态门禁。
- 已修复：详情处理器覆盖 `orders/invoices/feedbacks/enrollment-details/students`；反馈仅 `待处理` 记录显示回复操作。
- 已验证：管理端 `test:ui`、管理端 build、后端 Jest 全量 12 套件/39 项通过。
- 浏览器运行态：可接管已有管理端标签页 `http://localhost:5174/login`，当前管理员工作台可见；C 端 `5185` 当前未监听，需单独启动后验收。
- 浏览器验收发现第一次修复后学员列表会重复显示两个“查看详情”；已恢复详情专用模块的通用操作禁用条件，现每行只保留一个详情按钮。
- 学员详情实际可打开，并显示基础资料、授权账号、默认关系/解除关系、报名履历、编辑/启停用/合并入口；接口返回明文仅在受保护详情请求中使用，列表仍脱敏手机号。
- 报名明细列表每行已验证只显示一个“查看详情”；详情可展示学员、课程、订单、模板版本和当次表单快照。
- 反馈管理已验证：待处理记录显示“查看详情 + 回复处理”，已处理记录只显示“查看详情”，前端状态门禁生效。
- 开票管理人工验收发现列表实际缺少“发票文件状态”列，虽然后端已返回该字段；已把 `invoiceFileStatus` 和 `invoiceFileName` 加入管理端开票列定义，待刷新运行态复核。
- 刷新管理端后列仍未出现，初步判断当前 `3100` API/管理端运行态未加载最新后端字段（源码 `invoiceView` 已明确返回 `invoiceFileStatus`）；需重启本地 API 后再复核，不能把旧运行态结果当作代码未实现。
- 重启本地 `3100` API 后复核通过：开票列表现在显示“发票文件状态”，已开票/待上传显示“上传发票”，待处理显示“开票通过/驳回”，已驳回不显示处理按钮。旧运行态差异已确认，不是代码缺失。
- 消息通知人工验收：列表为空时“新增消息”入口仍可用；表单明确限制为站内消息，短信/邮件选项禁用，并提供目标用户、目标课程、开始/结束展示时间和启用状态字段。
- 积分管理人工验收：用户列表提供“调整积分”和“查看流水”两个真实入口，积分流水入口按用户返回当前积分和明细；导航分组折叠时需先展开分组，这是当前统一导航交互而非按钮缺失。
- 本轮实现请求锁：报名模板保存/复制/删除、Banner 保存/删除、消息保存、系统配置保存、积分调整、反馈处理、课程删除和支付设置保存均接入 `runOperation`；课程保存增加 `courseSubmitting/operationKey` 门禁。
- 回归：管理端 `test:ui`、管理端 build、根目录 `verify`、后端 Jest 12 套件/39 项、`git diff --check` 均通过。
- 用户、优惠、课程和 Banner 人工验收：用户启停用/重置、优惠编辑/启停用、课程编辑、新增入口、Banner 编辑/启停用均显示；用户列表的 `role` 列已修正为“用户角色”，不再误标为“职务”。
- 系统配置人工验收：新增/编辑入口可用，配置键编辑时锁定键名，保存走后端 `PUT /admin/configs/:key`；操作审计为只读列表，操作类型、操作者、时间范围筛选可见且没有无意义处理按钮。

## 2026-08-06 阶段 3 浏览器验收启动

- 已接管用户当前打开的管理端标签页 `http://localhost:5174/login`；页面实际处于管理员登录态，并显示操作审计列表。
- 运行态页面显示审计筛选（操作类型、操作者、起止时间）、导出当前页、只读表格和分页信息；当前数据为 62 条，说明管理端与 API/持久化数据已连通。
- 浏览器验收不能只依赖静态按钮断言；后续按课程、报名、订单、开票、学员、用户、优惠、反馈、消息、积分、系统配置、审计和 C 端页面逐项记录真实点击与响应。
- 当前接管标签页的课程管理入口可见、可用且 DOM 坐标正常，但 Playwright 语义点击、DOM 点击和坐标点击均未触发页面切换；控制台无错误，页面仍停留在操作审计。该现象记录为浏览器控制层限制，不据此判定课程功能失败。
- 2026-08-06 自动化回归补充：后端 Jest 12 套件/39 项通过，管理端 `test:ui` 通过，根目录 `verify` 通过。重新接管浏览器标签后，工作台、课程管理和报名汇总可切换；首次点击未生效是导航组未先展开/控制层时序问题，不能作为按钮未实现证据。
- 课程管理运行态检查：列表实际返回 6 条课程，状态筛选、导出、新增课程和逐行编辑按钮均可见；新增课程弹窗包含模板选择、报名状态/截止时间、名额、富文本和保存接口入口，打开后可正常关闭，未写入测试数据。
- 业务与运营模块运行态抽查：报名汇总/明细均有唯一“查看详情”入口；订单仅对已支付显示退款，详情弹窗能读取课程、报名人、金额、支付状态和凭证；开票显示查看详情、通过、驳回和待上传发票操作；学员详情可读取授权账号和报名履历；用户、优惠、反馈、积分、渠道自检、系统配置、审计的对应入口均可见，已处理反馈不显示回复操作。
- 后端风险修复：`reviewOffline` 原先只检查凭证是否存在，未限制订单/凭证状态；现限制为订单 `待审核` 且凭证 `pending`，重复审核返回明确错误并保留原审核结果。
- 回归证据：`mvp.spec.ts` 17/17、API build、管理端 build 通过；本轮未创建或删除业务数据（仅打开/关闭弹窗和查看列表）。
- C 端静态链路检查：业务页已包含支付记录、浏览记录、开票记录；线下凭证弹窗分为对公转账信息和上传凭证两部分，上传调用受保护接口；消息、我的学员、个人资料、反馈和开票状态均有明确页面入口。H5 构建通过，仅保留 Sass 弃用警告。
- 全量回归：后端 12 套件/40 项通过，管理端 `test:ui`、根目录 `verify`、`git diff --check` 通过。剩余 C 端人工点击和服务器 Docker/PostgreSQL 证据尚未形成。

## 2026-08-06 追加审计结论

- 发现修改密码接口存在默认密码回退：空请求会被控制器转换成 `123456`；已删除该回退并在服务层增加缺失/长度校验，避免生产环境误设置弱密码。
- 发现电子发票上传后返回管理员专用文件地址，C 端即使拿到文件名也无法安全打开；已统一返回所属用户可访问、管理员兼容的受保护路由，并补充 C 端查看入口。
- 目前未发现新的“显示按钮但没有后端能力”的管理端模块；剩余主要是人工设备验收、服务器迁移与恢复证据，而不是继续增加静态按钮。
- 追加发现：电子发票已上传后管理端只展示文件名，没有复核入口；已增加“查看发票”并按状态区分驳回/查看，接口使用现有受保护文件读取能力。
- 全量回归更新为后端 12 个套件、41 项通过；其中生产化 API 5/5 覆盖发票上传后普通用户受保护读取。

## 2026-08-06 订单关闭补齐

- 功能清单列出了“超时/手动关闭待支付订单”，但现有管理端只有待审核审核和已支付退款，待支付记录没有真实操作入口。
- 当前订单数据模型使用“已取消”作为取消/退款/关闭共用终态；因此关闭订单不新增状态，而通过审计动作“关闭待支付订单”区分业务原因。
- 新增 `MvpService.closeUnpaidOrder` 和 `POST /admin/orders/:id/close`。事务同时更新 `Order`、`Course.enrolled` 和 `Enrollment.status/cancelledAt`，并拒绝重复关闭，满足幂等性和名额一致性要求。
- 管理端订单筛选删除无数据来源的“已退款”枚举，待支付行显示“关闭订单”；已取消行只保留详情查看。
- 回归结果：后端 12/42、生产化 API 5/5、管理端静态 UI、管理端 build、根目录 verify 均通过。

## 2026-08-06 C 端 H5 运行态验收

- 本地端口检查确认 `3100`、`5174`、`5185` 均处于监听状态；C 端不再是此前“未启动”的空白环境。
- 以种子测试账号登录后，首页课程数据来自 API，课程状态和报名入口均可见；未执行新增报名等写操作。
- 我的学员运行态符合预期：当前账号没有匹配的默认学员时显示空状态，其他学员按 A/B/L/S/X/Y/# 分组展示，支持编辑、设为默认和解除关系。
- 业务页运行态加载出订单、支付记录、开票记录和浏览记录；待支付订单可见微信/支付宝及线下凭证入口，已支付订单可见申请开票入口。
- 线下凭证弹窗人工打开成功，确认对公转账信息和上传凭证模块为两个独立区域；未选择文件、未提交任何订单变更。
- 由于当前是 H5 浏览器而非微信小程序真机，暂不把本次证据扩展为小程序兼容性结论；下一阶段需要在服务器内测地址或真机补充截图/网络记录。

## 2026-08-07：管理端标签页与上线范围判断

- 管理端“已访问标签页”不需要后端接口：它保存的是当前管理员浏览上下文，不是业务数据；使用 `sessionStorage` 可以避免不同账号之间长期混用导航历史。
- 标签集合始终包含不可关闭的“首页”；其余标签按首次访问顺序去重。关闭当前标签时优先切换到左侧相邻标签，没有左侧标签时切换右侧，最后回到首页。
- 标签页按钮与侧边栏、工作台卡片共用 `navigate`，从不同入口进入模块都会维护同一份访问历史。
- 本次功能只改变前端导航状态，不改变课程、订单、报名、发票、学员或支付数据，无需数据库迁移和后端回滚。
- 当前代码审查未发现新的“显示管理按钮但完全没有后端能力”的模块；后续应把精力放在浏览器真实点击、写操作审计和服务器部署证据上。

### 运行态限制记录

- 本机 `3100` 已有进程监听，`5174` 也被旧 Vite 实例占用；本轮新启动的管理端自动使用 `5176`，没有对未知进程执行终止操作。
- 浏览器控制层对当前 `localhost:5174/login` 标签返回 URL 策略拒绝，不能通过替代浏览器、裸 CDP 或绕过方式继续；因此标签页的运行态点击验收必须在用户可接管的浏览器环境中完成。
- 只读 HTTP 检查显示已有管理端 `http://[::1]:5174/login` 返回 200，API `http://127.0.0.1:3100/api/courses?page=1&pageSize=1` 返回 200；这证明旧运行实例可达，但不能证明它已加载本轮标签页代码，需在维护窗口重启或使用用户手动刷新确认。

## 2026-08-07：紧凑列表布局与导出选择实现

- 页面固定策略仅作用于桌面端非工作台页面，避免窄屏筛选区被强行压缩；移动端仍允许自然页面滚动。
- `page-main` 使用列式 flex 布局，表格区设置 `flex: 1`、`min-height: 40vh` 和 `overflow: auto`，因此列表变长时滚动条出现在列表容器而不是整个页面。
- 复选状态存储在当前模块的 React 状态中，切换模块时清空；表头全选只作用于当前已加载列表，避免误把分页之外的记录标为选中。
- 导出规则明确为：无勾选=筛选结果；有勾选=选中结果。学员档案无勾选时走服务端脱敏导出，不通过前端列表绕过敏感字段保护。
- 需要注意：服务端分页导出目前上限为 1000 条，超过上限会在导出 JSON 的 `truncated` 字段和页面提示中标明，后续可按业务需要改为异步导出。

## 2026-08-07：顶部标签页、面包屑与退出登录布局修正

- 原布局将标签导航和退出按钮放在同一行，标签较多时会挤压最后一个标签。
- 最终布局为：标签导航单独占顶部一行；非工作台页面的面包屑与紧凑文本形式的退出按钮同排；工作台继续在顶部操作区显示退出按钮。
- 标签区负责横向滚动，面包屑通过 flex 占用剩余宽度，退出按钮保持固定小宽度；移动端允许行内元素换行。

## 2026-08-07：管理端路由闭环与部署文档复核

- 通过逐项对照前端操作按钮与 `mvp.controller.ts`，当前管理端模块的新增、编辑、删除、启停、审核、详情、导出和流水查询入口均有真实路由或明确只读行为；静态审计未发现新缺口。
- README 的 Docker 内网访问地址已过期，曾写为 8080/8081；当前 Compose 实际映射为 18080/18081，已同步修正，避免服务器部署后按错误端口排查。
- 服务器 Docker/PostgreSQL 迁移和恢复仍不能由本地静态代码审计代替，必须保留服务器命令输出、容器健康状态、数据对账和恢复报告。

## 2026-08-07：面包屑与内容边界对齐

- 参考页面样式后，面包屑不再使用卡片边框/白色背景；使用纯文本和斜杠分隔，减少顶部视觉占用。
- 顶部行显式使用与列表容器相同的 10px 内缩，保证面包屑左端和退出按钮右端与页面其他内容对齐；标签页另起一行，不再与按钮争用空间。
## 2026-08-07：搜索区与标签页缓存复核

- 搜索区原默认控件尺寸偏大，已在桌面非工作台页面设置 32px 控件高度和更小内边距；移动端仍保留纵向布局，避免窄屏不可用。
- 当前 React 管理端没有独立路由组件树，因此采用内存快照实现 Keep-Alive 等价行为，不额外引入路由依赖；状态恢复范围覆盖筛选、分页、表单、弹窗、详情、勾选和表格滚动。
- 运行态仍需在可控浏览器中手工确认输入框和滚动恢复的视觉效果；静态、构建和后端自动化验证已通过。
## 2026-08-07：Docker/PostgreSQL 发布证据补强

- 服务器曾出现 Prisma OpenSSL 检测警告；仅依赖基础 `node:22-bookworm-slim` 不足以作为稳定运行证据，现已在 API 镜像两个阶段显式安装 OpenSSL/CA 证书。
- 服务器迁移必须使用仓库最新 `docker-compose.yml`、`Dockerfile.api` 和 `backend/api/prisma/import-postgresql.cjs`；旧文件会分别导致 `api-migrate` 工作目录重复拼接、OpenSSL 警告或模板外键导入失败。
- 新增非破坏性 smoke check，禁止在未显式配置 `PAYMENT_ADAPTER=disabled` 时误发布非支付版本；它不会打印数据库密码、JWT 或支付密钥。
- 当前工作机 Docker daemon 未运行，不能把本地静态构建当作服务器容器验收；需在 `192.168.0.162` 生成 Compose、迁移、对账、备份和恢复输出后才能关闭部署阶段。
## 2026-08-07：本地运行态证据

- 3100 API 和 5185 H5 可通过 IPv4 访问；管理端实例绑定 `[::1]`，因此 `[::1]:5174/login` 与 `[::1]:5176/login` 返回 200，而 `127.0.0.1:5174` 失败属于绑定地址差异，不是前端构建故障。
- 当前两套管理端实例同时存在：5174 为旧实例、5176 为本轮新实例。发布前应在维护窗口停止旧实例后只保留最新实例，避免浏览器缓存旧代码造成“按钮没实现”的假象。

## 2026-08-07：新一轮可上线审计发现

- 当前管理端 18 个模块的主要按钮仍能在 `App.tsx` 找到对应 `mvp.controller.ts` 路由；未发现新的“显示按钮但完全没有接口”的缺口。
- 发现学员报名明细接口 `listEnrollmentRecords` 直接返回完整手机号和 `formPayload`，与学员域设计中“默认脱敏、详情受控查看”的要求不一致。下一步改为列表脱敏，新增管理员受保护的单条明细接口，详情点击时再读取完整快照。
- 后端全量 Jest 当前 12 个套件/43 项全部通过；该安全修复需要新增回归断言，不能只依靠静态 UI 检查。
- 详情读取现在写入 `查看报名履历` / `查看学员档案` 审计动作；列表读取仍不记录明文数据，避免日志中出现手机号或完整表单快照。

## 2026-08-07：服务器发布阶段阻塞点

- `192.168.0.162:22` 网络可达。
- 当前本地 Codex 环境没有可用于 `root@192.168.0.162` 的 SSH 公钥/代理凭据，非交互 SSH 返回 `Permission denied (publickey,password)`；没有在命令行传递或记录服务器密码。
- 因此服务器 Docker/PostgreSQL 构建、迁移、对账、备份和恢复不能被本地代执行或冒充已通过；下一步需要用户在服务器终端执行发布清单，或为当前本地环境配置已授权的 SSH key 后再做只读复核。
- 复核部署文档时发现容器内 Prisma 命令仍有旧的 `--dir backend/api`/`node backend/api/prisma` 写法，会在镜像工作目录下重复拼接路径；已改为容器内工作目录对应的短命令。
## 2026-08-07：移动端遮挡与操作确认需求定位

- `pages/students/students.vue` 同时使用 UniApp 原生导航栏和自定义 `students-topbar`，会产生重复吸顶标题；应移除自定义栏并保留页面内容入口。
- C 端 Tab 页面（课程/订单/我的）底部预留空间偏小；`mine.vue`、`business.vue` 的内容及底部弹窗需要增加安全区和 Tab 栏预留，弹窗遮罩层级应高于原生 Tab 栏。
- `mine.vue` 的个人资料、账号安全保存按钮以及 `business.vue` 的取消报名当前直接执行接口；本轮统一增加 `uni.showModal` 二次确认。
- `detail.vue` 的课程简介标题行包含用户无须了解的“富文本内容”徽标，应删除模板节点及对应样式。

## 2026-08-07：确认弹窗层级与订单按钮复核

- 账号与安全、支付凭证、开票、报名和学员页面的业务遮罩均使用约 1000 的层级；`uni.showModal` 是挂载在页面外层的全局节点，若不显式提高层级，二次确认会被当前业务弹窗遮挡。
- 已在 `App.vue` 的非 scoped 全局样式中将 H5 的外层 `uni-modal`、`.uni-mask`、`.uni-modal` 和 `.uni-actionsheet` 设为 2000，并固定 `.uni-modal` 定位，覆盖所有现有二次确认调用；不能只写在页面 scoped 样式中，否则会生成带 `data-v-*` 的选择器而匹配不到全局节点。
- 订单操作按钮此前只依赖 `.order-actions button` 的通用样式，且部分语义类没有独立样式；现已补齐 outline/primary/offline 三种按钮风格，取消报名使用无边框文字样式。
- 当前源码中已检索不到“富文本内容”，若用户仍看到旧标签，应重启 H5 开发服务并强制刷新浏览器，清除旧 bundle 缓存。

## 2026-08-07：二次确认弹窗层级恢复

- 业务弹窗层级统一为 `1000`；系统确认弹窗使用独立的 `3000/3001` 层级，避免“修改密码”确认框显示在账号安全窗口下面。
- 层级规则必须放在 `App.vue` 非 scoped 全局样式中；放入页面 scoped 样式会生成 `data-v-*` 选择器，无法匹配 UniApp 挂载到页面根节点的全局弹窗。
- 运行态计算样式已确认：`uni-modal=3000`、`.uni-mask=3000`、`.uni-modal=3001`；测试确认框已取消，未提交密码修改。

## 2026-08-07：管理端保存/处理确认复核

- 代码审计发现：管理端主要删除、退款、关闭、启停和密码重置已有确认，但部分保存与处理按钮直接提交，用户取消无法阻止请求。
- 已在 `frontend/admin-react/src/App.tsx` 增加统一 `confirmAction`，覆盖课程、模板、Banner、优惠规则、消息、配置、积分、反馈、学员档案、线下支付凭证审核和开票通过等重要非支付操作。
- 该改动只增加前端确认，不改变后端接口、数据模型或状态门禁；取消确认时不会发出写请求。
- 静态验收脚本已增加确认规则断言，管理端 build、UI 静态验收、根目录 verify 和目标后端回归通过。

## 2026-08-07：全量回归结果

- 后端 Jest 全量 13 个套件、48 项测试通过；API build、管理端 build、管理端 `test:ui`、C 端 H5 build 和根目录 `verify` 均通过。
- C 端只有既有 Sass/uni-app 弃用警告，不影响产物生成。
- 发布结论仍受两项外部证据约束：可接管浏览器的 D 阶段人工验收，以及服务器 PostgreSQL/Compose 的真实迁移、对账、备份恢复和回滚。

补充：C 端当前 `App.vue` 已将确认弹窗统一为外层/遮罩 `4000`、内容 `4001`；文件中更早的 `2000`/`3000` 记录保留为历史过程，不是当前有效值。
## 2026-08-07：非支付可上线版本审计基线

### 当前已具备

- 管理端已经有 18 个模块：工作台、Banner、课程、报名模板、报名汇总/明细、订单、开票、学员档案、用户、渠道自检、支付设置、优惠规则、反馈、消息、积分、系统配置、审计。
- 管理端前端多数按钮已经绑定真实 API：课程、Banner、报名模板、订单审核/关闭/退款、开票处理/文件、用户启停/密码重置、学员档案/关系/合并、优惠规则、反馈、消息、积分、配置等。
- 后端控制器已提供上述主要接口，已有 `admin-operations.spec.ts` 覆盖课程/Banner/模板、优惠/消息/配置/积分、用户/反馈三组非支付操作闭环；另有 P10 学员档案场景测试。
- 管理端已有多页签缓存、表格筛选/分页/导出、二次确认、操作锁、审计入口和静态 UI 验证脚本。

### 仍不能直接宣称上线的风险

- 当前“按钮已接 API”不等于真实浏览器闭环已验收；需要逐模块登录后点击验证成功、失败、取消、重复点击和刷新后的状态。
- 管理端仍大量使用浏览器原生 `window.confirm`/`window.prompt`，不适合统一的可访问性、移动端和自动化验收；重要操作的二次确认虽存在，但交互仍需统一。
- 课程、模板、Banner、消息等保存接口需要补充输入边界、重复提交、权限和审计验证；现有测试主要验证 HTTP 成功，不覆盖完整前端操作。
- 订单、开票、支付属于支付相关范围，本阶段只保证非支付管理能力可用，支付适配器继续保持待接入/禁用，不作为本阶段上线门槛。
- 部署仍有 SQLite/PostgreSQL、Docker Compose、备份恢复和真实浏览器验收未完成的外部门槛；本地构建通过不能替代服务器验收。
- 数据安全上线前必须确认：敏感字段默认脱敏、管理员权限、审计记录、上传文件访问控制、生产密钥和 `PAYMENT_ADAPTER=disabled`。

### 首轮实现顺序

1. 建立“模块—按钮—前端调用—后端路由—测试—人工验收证据”矩阵，先找真正无动作/错误动作的按钮。
2. 优先完成内容与用户运营模块：课程状态/发布、模板、Banner、用户、学员档案、反馈、消息、积分、配置、审计。
3. 为每个模块补后端边界测试和前端静态断言，再做真实浏览器回归。
4. 最后执行非支付上线门禁：生产配置自检、数据库迁移/备份恢复、Docker 健康检查、权限与敏感数据检查。
## 2026-08-07：二次确认弹窗 z-index 审计结论

- uni-app H5 的 `uni.showModal` 会渲染到 `data-type="systemDialog"` 的系统对话页，该外层页面带有内联 `z-index: 999`。只修改内部 `.uni-modal-dialog` 不足以跨越业务页面弹窗的层叠上下文。
- 已在 `frontend/client-uni/src/App.vue` 同时修复外层系统对话页、遮罩和内容层级，并兼容新旧 uni-h5 class 命名。
- 业务页面自定义 `.modal-mask` 统一为 1000；系统确认框使用 4000/4001，层级关系明确且不依赖页面 scoped 样式。

## 2026-08-07：确认框层级规则恢复

- 发现历史 CSS 注释与选择器在同一行，可能使 `systemDialog`、`.modal-mask` 和 `uni-modal` 规则失效；已拆分注释和选择器并保留全局作用域。
- 修改密码、取消报名、提交凭证、报名提交、学员保存/解除等 `uni.showModal` 二次确认均走同一套 4000/4001 层级；业务自定义弹窗保持 1000。
- 结论：源码构建层面已恢复，仍需在真实 H5/小程序设备上确认关闭后弹窗不会残留。

## 2026-08-07：管理端按钮功能矩阵审计结论

- 管理端并非“多数按钮没有实现”：18 个模块的主要入口均能在 `App.tsx` 找到操作分发，并能在 `mvp.controller.ts` 找到对应的 `/admin/*` 路由；管理路由统一受 `JwtGuard + AdminGuard` 保护。
- `admin-operations.spec.ts` 已覆盖内容维护、优惠、消息、配置、积分、用户启停/重置和反馈处理；`production.spec.ts`、`mvp.spec.ts`、`student-api.spec.ts`、`p10-scenarios.spec.ts` 覆盖订单、开票、学员档案及状态门禁的不同层级证据。
- 现有证据仍不足以证明上线：真实浏览器逐按钮验收、失败响应、取消不发写请求、脱敏网络响应、审计输出和服务器迁移/恢复尚未全部完成。
- 新增回归证据确认两条关键门禁：普通用户无法访问管理接口，待发布课程不会从公共课程接口泄露；这两项仍需在真实浏览器网络面板中补充截图证据。
- 本轮构建和专项回归均通过；全量 Jest 曾因单个管理端套件启动耗时超过默认 120 秒而超时，拆分执行后该套件及其余专项均通过，后续应保留分组执行方式或优化测试启动耗时。

## 2026-08-08：上线规划续审发现

- 当前管理端确认交互已经从历史的 `window.confirm` 改为 `requestDialog/confirmAction` 应用内弹窗，但 `frontend/admin-react/scripts/verify-ui.mjs` 仍检查旧字符串 `const confirmAction = (message: string) => window.confirm(message)`。
- 因此 `pnpm.cmd --dir frontend/admin-react test:ui` 当前真实失败，失败项为“管理端重要保存操作缺少统一二次确认”；该失败是验收脚本与实现不同步，不代表保存按钮没有二次确认。
- 修复验收门禁后必须重新执行管理端 UI 验收、管理端构建和根目录 `verify`，并把结果写入本轮进度。
- 当前目标仍是非支付版本；在线支付配置和回调暂不进入本轮功能实现，但线下凭证、开票、订单关闭等非支付业务仍需保持可验收。

## 2026-08-08：P0/P1 实施结果

- P0 已完成：验收脚本改为检查现行应用内确认弹窗，管理端 UI 静态验收、管理端构建和根目录 verify 恢复通过；后端全量回归为 13 套件/49 项通过。
- P1 第一批已完成：课程、Banner、报名模板的后端边界校验已补齐，并在 `mvp.spec.ts` 增加非法输入回归。前端校验不能替代后端校验，直接 API 请求也会被拒绝。
- `pnpm.cmd --dir backend/api exec tsc --noEmit` 不可执行，因为该工作区脚本没有可直接调用的 `tsc` 二进制；随后使用项目正式 `pnpm.cmd --dir backend/api build` 完成类型/生产构建验证并通过。后续不再重复该失败命令。

## 2026-08-08：P2/P3 第一批边界加固

- `setUserEnabled` 原先对不存在用户直接调用 Prisma update，现已先查询并返回明确 404 业务错误。
- `updateStudentProfile` 原先对管理端直连 API 缺少姓名、手机号和邮箱校验，且允许修改已合并档案；现已补齐边界和状态门禁。
- 学员关系授权和档案合并现拒绝已合并档案作为源/目标，并拒绝空关系类型。
- `saveDiscountRule` 原先会把非法门槛/折扣静默裁剪，现改为拒绝非法值并校验关联课程存在。
- 回归：`student-api.spec.ts` 4/4、`mvp.spec.ts` 22/22、API build 通过。

## 2026-08-08：P4 管理端原生对话框收口

- 富文本编辑器的“插入链接”原先调用 `window.prompt`，图片类型/大小错误原先调用 `window.alert`，与管理端统一确认弹窗不一致。
- 现通过 `CourseModal -> RichTextEditor` 传入 `promptAction`/`onNotify`，复用应用内弹窗和通知。
- 静态验收脚本新增禁止原生 `window.prompt/window.alert` 的断言；管理端 UI 验收和 build 通过。

## 2026-08-08：P2 会话失效与学员权限复核

- 旧实现只在停用/重置时吊销 refresh token，JWT access token 在未过期前仍可被 JwtGuard 接受；这与管理端“旧登录令牌会失效”的提示不一致。
- 通过 `User.sessionVersion` 修复：令牌签发版本与数据库版本必须一致；任何账号状态切换或密码变更都会递增版本。旧令牌即使 JWT 尚未过期也会返回 401。
- C 端学员接口已按 `userId + active relation` 过滤；新增测试确认未授权账号不能读取/修改/设默认/解除其他账号的学员档案。
- 迁移注意：SQLite 现有库由 `migrate.js` 条件补 `sessionVersion`；服务器 PostgreSQL 发布前必须重新运行 `db:prepare:postgres` 生成包含新字段的 schema/migration，不能直接沿用旧生成目录。

## 2026-08-08：C 端确认弹窗层级回归

- C 端所有二次确认均通过 `uni.showModal`（修改密码、保存资料、取消报名、提交支付凭证、提交开票、删除报名人员、学员档案操作及退出登录等）。这些弹窗并不渲染在页面的 scoped DOM 内，而是由 uni-h5 动态挂载到 `body > #u-a-m > uni-modal`。
- 业务弹窗仍统一使用 `z-index: 1000`。系统确认层必须同时提升根节点、遮罩和内容节点；只提升内容节点或只写页面 scoped 选择器，在部分 uni-h5 版本中会出现确认框被业务弹窗覆盖的现象。
- 当前修复使用全局 App 样式和独立高层级 stacking context：系统遮罩 `2147483000`、确认内容 `2147483001`，兼容 `#u-a-m`、`data-type="systemDialog"` 及旧版类名，且不覆盖 `display`，避免破坏 `uni.showModal` 的关闭状态。
- 验证结果：H5 构建通过；待服务重新启动后，需在浏览器逐项确认确认框可见、按钮可点击、取消不产生请求。
## 2026-08-08：非支付上线审计续审

- 当前自动化基线已重新执行并通过：管理端 `test:ui`、管理端生产构建、API 构建、后端 Jest 13 套件/53 项测试均通过。
- 这组结果证明主要按钮的前端分发、后端路由和关键业务边界存在，但不能替代真实浏览器逐按钮验收，也不能替代服务器 Docker/PostgreSQL/备份恢复证据。
- 管理端当前主要模块可在 `frontend/admin-react/src/App.tsx` 找到对应操作分发，后端管理路由集中在 `backend/api/src/mvp/mvp.controller.ts`；“按钮未实现”的判断必须以运行态 HTTP 请求和数据变化为准，不能仅凭按钮文字判断。
- 非支付版本的剩余上线风险集中为：真实浏览器 D 阶段、普通用户/管理员权限和敏感字段网络响应复核、服务器 PostgreSQL 导入对账、备份恢复/回滚、上传文件持久化，以及生产环境变量和健康检查。
- 支付适配器继续保持禁用或待配置，不作为本轮非支付上线阻塞项；线下支付凭证审核属于订单运营能力，仍需纳入浏览器验收。

## 2026-08-08：非支付可上线规划与管理端按钮复核

- 管理端当前共有 18 个模块；`App.tsx` 的新增、编辑、删除、启停、审核、导出和详情操作均能找到具体处理函数，控制器中也能找到对应管理路由。
- 当前不能把“按钮能看到”直接等同于“功能已上线”：还需要运行态 HTTP 状态、成功/失败反馈、数据库变化、取消无写请求、权限和审计证据。
- 已将本轮范围拆为 P0 基线、P1 内容/报名、P2 用户/学员、P3 运营/系统、P4 发布恢复、P5 内部测试发布；在线支付保持禁用并单独留待商户材料验收。
- 运行态浏览器当前可进入管理端并看到用户管理列表和已访问标签页，说明前端服务与登录态可用；后续按模块采集真实验收证据，优先查找实际无动作或请求失败的按钮。

## 2026-08-08：C 端二次确认层级运行态复核

- 统一检查 `business`、`messages`、`mine`、`register`、`students` 五个页面的业务弹窗，均保留 `1000` 兜底并使用 `--client-business-modal-layer`。
- `uni.showModal` 的实际 H5 结构为 `body > #u-a-m > uni-modal`；当前运行态计算结果为根节点/遮罩 `2147483640`、确认内容 `2147483641`，业务弹窗为 `1000`，修改密码确认框已位于账号安全弹窗之上。
- 新增根目录 `verify` 门禁，防止任一业务弹窗层级回退或遗漏统一变量；H5 构建、verify 和 `git diff --check` 均通过。

## 2026-08-08：C 端修改密码确认框层级恢复（本轮）

- 截图反馈的现象是确认框在“账号与安全”业务弹窗下面；根因是不同 uni-h5 版本的系统弹窗承载节点和类名不完全一致，不能只依赖页面 scoped 的 `.modal-mask` 或单一子节点选择器。
- 现行层级关系统一为：C 端业务弹窗 `1000`；`#u-a-m`/`systemDialog` 根节点和遮罩 `10000`；确认框内容 `10001`。
- 已补齐 `#u-a-m > uni-modal`、mask/container 兼容选择器，并保留 uni-app 的 `display:none` 关闭逻辑。
- 运行态读取到 `--client-system-modal-mask-layer:10000`、`--client-system-modal-dialog-layer:10001`；构建和根目录 verify 通过。
- 旧 H5 进程或浏览器缓存仍可能显示旧层级，更新后需重启 C 端 H5 服务并强制刷新；小程序端需重新编译预览。

## 2026-08-08：运营管理输入边界加固

- `saveMessage` 现在限制标题 120 字、内容 10,000 字、目标用户/课程各不超过 500 个，并拒绝非布尔的启用状态；直接调用 API 不再把字符串 `"false"` 当成启用。
- `adjustPoints` 现在要求安全整数、非零、原因不超过 500 字，并检查调整后的积分不会超过 Prisma `Int` 可保存范围；超范围在事务前返回明确 400。
- `saveSystemConfig` 现在限制配置键格式（字母开头，可含字母/数字及 `._:-`）、值 10,000 字、说明 500 字，减少误写和数据库异常。
- 新增 `mvp.spec.ts` 运营边界回归；该套件 23/23 通过，API build 通过。全量基线随后应更新为 13 套件/56 项。

## 2026-08-08：账号与我的学员资料边界加固

- `updateProfile` 现在校验姓名、手机号、邮箱格式和跨账号重复绑定，并将手机号/邮箱规范化后保存。
- `createAccountStudent` 现在校验邮箱并保存规范化手机号/邮箱，避免同一学员因空格或大小写形成错误匹配。
- 新增学员 API 回归：非法邮箱被拒绝，带空格手机号会规范化；学员/认证专项 10/10 通过。

## 2026-08-08：二次确认弹窗层级复核结论

- C 端所有二次确认统一由 `uni.showModal` 产生，页面业务弹窗为 `z-index: 1000`；确认框不能只提升内部内容，必须同时提升 `body > #u-a-m`/`systemDialog` 根节点、遮罩和内容容器。
- 当前固定关系为：业务 `1000` < 系统根节点/遮罩 `10000` < 系统内容 `10001`。内容容器额外固定定位并居中，兼容 uni-h5 默认 `.uni-modal` 的 `z-index: 999`。
- 当前源码和 H5 产物已覆盖修改密码、保存资料、退出、取消报名、支付凭证、开票、报名提交、删除报名人员和学员操作等确认路径；旧 bundle 仍可能导致浏览器看到旧层级，需重启服务并强制刷新。

## 2026-08-08：确认框直接子节点层级加固

- 仅提升 `#u-a-m` 外层并不足以抵御旧版 `.uni-modal { z-index: 999 }`，现已对 `#u-a-m > uni-modal`、`uni-modal > .uni-mask`、`uni-modal > .uni-modal` 增加显式层级规则。
- 层级关系保持为业务弹窗 `1000` < 系统根节点/遮罩 `10000` < 系统确认内容 `10001`，覆盖所有 `uni.showModal` 二次确认场景。
## 2026-08-08：C 端二次确认层级复核

- 修改密码确认框的实际 DOM 挂载在 `body > #u-a-m > uni-modal`，不能只依赖页面内部 `.modal-mask` 的层级。
- 当前统一层级为：业务弹窗 `1000`；系统确认根节点/遮罩 `11000`；确认内容 `11001`。该顺序高于页面吸顶、底部操作和业务弹窗。
- 已对所有 `uni.showModal` 二次确认入口复核：修改密码、保存资料、退出登录、取消报名、提交支付凭证、提交开票、报名确认、删除报名人员、学员保存/设为默认/解除关系均命中统一规则。
- 浏览器运行态实测：修改密码确认框显示在“账号与安全”弹窗上方，计算层级为 `#u-a-m=11000`、遮罩 `11000`、内容 `.uni-modal=11001`。
- 构建和静态验收通过；小程序真机仍需补充一次验收，因为不同运行容器可能使用另一套系统弹窗 DOM。
## 2026-08-08：管理端 17 个模块运行态复核

- 在 `http://localhost:5174/login` 使用管理账号登录后，按导航分组逐项展开并打开 17 个模块。
- 每个模块都生成对应的已访问标签并加载列表或只读状态；运行态未出现加载失败、请求失败或错误提示。
- 订单、开票、学员、用户、优惠、反馈、消息和积分列表均出现可用操作入口；渠道自检和操作审计按设计为只读模块，不显示无效处理按钮。
- 因而当前“很多按钮未实现”的判断不能仅凭页面观感下结论；下一阶段应重点记录按钮点击后的接口响应、数据变化、失败恢复和审计结果。
## 2026-08-08：GitHub 部署脚本缺失风险修复

- 原 `/Docs/` 整体忽略规则会让服务器 `git pull` 后缺少 PostgreSQL、备份、恢复和 Compose 验收脚本，导致部署时只能手工 `scp`。
- `.gitignore` 现改为继续忽略文档/运行产物，仅放行 `Docs/runtime/*.sh`、`*.ps1`、`*.cmd`；新增 `.gitattributes` 保证 Linux shell 脚本使用 LF 行尾。
- README 与服务器验收清单已补充 `git ls-files Docs/runtime/*.sh` 检查。提交前必须把脚本加入 Git 并推送，服务器再 `git pull --ff-only`。
- Docker API runtime 已包含 `openssl`，可避免 Prisma 在容器内回退到错误的 libssl 检测结果；服务器必须用最新代码重新构建镜像。

## 2026-08-08：非支付管理端运行态验收与交互缺口

- 管理端真实登录态已验证课程、报名模板、Banner、报名汇总、报名明细、订单、开票、学员、用户、消息和反馈模块可以加载列表/详情；没有发现导航后页面空白或按钮完全没有分发的证据。
- 课程新增空表单会给出“请完整填写课程基础信息”；订单关闭、用户启停、开票驳回均先弹出应用内二次确认，点击取消后弹窗关闭且列表状态保持不变。
- 开票驳回流程先要求填写理由，再进入二次确认；报名汇总的已支付/未支付详情和报名明细单条详情均可打开。
- 发现 `.modal-actions` 原本随长内容滚动，修复为 sticky 底部操作栏，并保持弹窗内容内部滚动；`app-dialog-actions` 同步处理。
- 发现学员档案编辑弹窗未把 `busy` 传给外层 `SimpleModal`，提交期间可能关闭弹窗；已补齐。支付设置表单提交期间也已禁用输入和上传控件（支付仍不作为当前上线门槛）。

## 2026-08-08：P1 课程管理刷新闭环缺口

- 现象：管理端课程表显示课程 ID，但服务端课程关键字搜索此前只匹配标题、副标题和讲师；保存/编辑后按 ID 查询为空，容易误认为保存按钮无效。
- 修复：课程公共列表和管理端列表的关键字匹配增加课程 ID。
- 证据：管理端操作回归新增“编辑后按 ID 刷新可见、删除后按 ID 刷新不可见、维护/删除审计可查”，`admin-operations.spec.ts` 5/5 通过。
- 风险边界：这只证明 API 和数据刷新闭环；仍需在可控浏览器中补充真实点击、失败响应、取消不写入和页面反馈记录。

## 2026-08-08：二次确认弹窗层级再次统一

- C 端修改密码确认框下沉的风险来自系统确认节点 `#u-a-m` 与页面业务弹窗不在同一 DOM 层级；仅提高 `.uni-modal` 内部层级并不能覆盖旧版 uni-h5 的包装节点。
- 现行层级固定为业务弹窗 `1000`、系统根节点/遮罩 `2147483646`、确认内容 `2147483647`；`#u-a-m` 直接子节点、`uni-modal`、mask、container 和 `role="dialog"` 均有全局兼容规则，未覆盖 `display`，保留 uni-app 的关闭逻辑。
- 管理端确认框继续 Portal 到 `document.body`，并使用同一高层级，确保修改用户密码等确认不会被编辑/详情弹窗覆盖。
- 自动校验和构建全部通过；运行态仍需刷新 5185 页面确认旧缓存已清除。

## 2026-08-08：C 端确认弹窗 z-index 兼容性加固

- 运行态确认 `uni.showModal` 当前挂载为 `body > #u-a-m > uni-modal`；页面业务弹窗为 `1000`，系统根节点/遮罩为 `2147483646`，确认内容为 `2147483647`。
- 本轮补齐 `uni-modal__mask`、`uni-modal__container` 及直接子节点规则，兼容新旧 uni-h5 DOM 类名，避免修改密码确认框再次落到“账号与安全”业务弹窗下方。
- 该规则不覆盖 `display`，继续由 uni-app 控制打开/关闭；所有现有 `uni.showModal` 二次确认共用同一层级契约。
- H5 构建、根目录静态验收和 `git diff --check` 已通过；仍需用户重启/强刷当前 5185 服务后做最终视觉验收。

## 2026-08-08：非支付可上线版本基线审计

- 当前代码已经具备管理端 17 个模块、C 端业务/我的/报名/学员页面以及对应 API；“很多按钮没实现”不能仅凭按钮是否显示判断，必须核对点击后的 HTTP 响应、数据库变化、刷新状态和审计。
- 已有较强自动化基础：后端 Jest、管理端 UI 静态验收、C 端 H5 构建、根目录 verify 和 Docker/PostgreSQL 脚本均已纳入工作区；但 P1 写操作的真实运行证据仍不完整。
- 当前最重要的非支付缺口是订单关闭/取消、线下支付凭证审核、开票处理和报名明细刷新闭环；其次是用户/学员的跨账号权限、脱敏和旧令牌失效证据，再其次是消息/积分/优惠/配置等运营模块的幂等验收。
- 当前上线边界：`PAYMENT_ADAPTER=disabled`；支付商户号、私钥、AppSecret、API v3 Key 和 HTTPS 回调不进入本轮开发或验收门槛。
- 执行顺序固定为 P1 → P2/P3 → P4/P5；每完成一个阶段必须同步更新 `task_plan.md`、`progress.md` 和 `Docs/Summary`，并保留可复现命令与结果。

## 2026-08-08：P1 订单/凭证/开票闭环结果

- 修复已取消订单重复取消：重复请求现在返回 400，不会再次扣减课程名额或重复修改报名履历。
- 修复审核中支付凭证重复上传：订单处于“待审核”时后端拒绝再次上传，C 端改为只显示审核中提示；审核驳回后仍可重新上传。
- 修复被驳回开票申请永久占用订单：重复开票校验排除“已驳回”记录，C 端同样不把已驳回订单标记为已申请。
- 新增隔离数据库 HTTP/Multipart 回归，验证订单取消/关闭/退款、凭证审核、报名明细、开票处理、发票文件和审计；测试 2/2 通过。
- P1 接口闭环已达到代码级完成；真实浏览器点击和服务器部署证据仍是发布门禁，不把自动化测试直接等同于上线。

## 2026-08-08：真实接口基线复核结果

- 本地实际运行的 API、管理端和 C 端地址均可访问；管理员令牌请求 18 个管理模块入口最终全部为 HTTP 200。
- 报名明细和学员档案的列表接口默认脱敏：报名明细不返回 `formPayload`，手机号以掩码返回；单条报名详情通过管理员保护接口返回完整快照并写入审计。
- 这次复核没有发现“按钮存在但对应管理接口不存在”的新问题；此前对部分入口的瞬时失败是重复开发进程占用端口造成的运行环境问题，不是业务接口缺失。
- 仍不能据此宣称上线：写操作尚缺逐项浏览器成功/失败/取消/重复点击/审计证据，服务器 Docker/PostgreSQL 迁移、对账、备份恢复和回滚也尚未完成。

## 2026-08-08：首条非支付写操作运行证据

- Banner 真实接口闭环已验证：非法空标题返回 400；新增返回 201；刷新列表出现临时记录；删除返回 200；再次刷新记录消失。
- 操作审计中可见对应的 Banner 维护和删除记录，临时测试数据已清理。
- 该证据证明至少一组管理端按钮具备真实写入、刷新、删除和审计闭环；不能外推为所有模块已完成，其他写操作仍需逐项验证。

## 2026-08-08：课程与报名模板运行态复核

- 课程闭环已验证：非法输入 400；“待发布”课程新增 201；管理端可见、公共课程隐藏；编辑和删除均成功，刷新后记录消失。
- 报名模板闭环已验证：重复字段 400；新增、编辑、删除均成功，刷新后记录消失。
- 复核过程中发现 PowerShell `Invoke-RestMethod -Body` 的默认字符串编码会让中文 `status` 产生假性“课程状态不合法”；改用显式 UTF-8 字节请求后通过。浏览器正常发送 UTF-8，不需要修改业务状态枚举。
- 临时测试数据已清理；订单、开票、报名明细、学员和运营模块仍需按同一标准继续验收。
- 为避免旧进程加载旧逻辑，已重启当前 API 并恢复监听 `3100`；健康请求和管理员登录已复测通过。

## 2026-08-08：C 端修改密码二次确认层级复核

- 问题原因是 C 端业务弹窗使用 `z-index: 1000`，而 uni-h5 的 `uni.showModal` 会挂载到页面外部的系统浮层容器；部分版本使用 `#u-a-t`，旧规则只覆盖 `#u-a-m`，因此确认框可能被账号与安全业务弹窗遮挡。
- 已在 `frontend/client-uni/src/App.vue` 统一层级契约：业务弹窗 `1000`；系统确认根节点/遮罩 `2147483646`；确认内容 `2147483647`。同时兼容 `#u-a-m`、`#u-a-t`、`uni-modal`、`.uni-mask`、`.uni-modal__mask`、`.uni-modal__container` 和 dialog 角色节点。
- 管理端 `frontend/admin-react/src/styles.css` 同步使用业务弹窗 `1000`、统一确认遮罩 `2147483646`、确认内容 `2147483647`，并继续通过 Portal 挂载确认框。
- 该修复覆盖修改密码、保存资料、退出登录、取消报名、支付凭证提交、开票提交和我的学员操作等二次确认场景；不强制覆盖 `display`，保留框架对弹窗显隐的控制。

## 2026-08-08：P2 管理端权限与敏感数据边界回归

- 管理端读取和写入入口均使用 `JwtGuard + AdminGuard`；新增回归覆盖课程、Banner、模板、订单、开票、学员、用户、支付设置、优惠、反馈、消息、积分、配置和审计等路径，普通用户全部返回 403。
- 报名履历列表默认只返回掩码手机号且不包含 `formPayload`；管理员打开单条履历详情后才返回完整手机号和报名表单快照，并记录查看审计。
- 学员档案列表默认掩码手机号、隐藏 `phoneNormalized`，邮箱仅显示部分字符；受保护档案详情才返回完整手机号和规范化手机号。
- 新增 `backend/api/test/admin-security-boundaries.spec.ts`，2/2 通过；测试使用隔离库创建临时订单，避免空种子数据造成错误结论。
- 同一专项回归补充账号停用和管理员重置密码后的旧 access token 立即失效；非所属账号不能编辑、设默认或解除学员关系。专项测试现为 3/3 通过。

## 2026-08-08：P3 反馈输入边界加固

- 反馈提交原先只校验非空，且会把客户端附带的任意字段原样写入 `Feedback.payload`；现仅保存规范化的 `category`、`content`，并限制分类 80 字、内容 5,000 字。
- 管理端反馈回复增加 5,000 字长度门禁，空回复和重复处理仍明确拒绝。
- `mvp.spec.ts` 新增超长内容、超长回复和任意字段不落库回归，当前套件 24/24 通过。
- 管理端反馈列表/处理返回值也改为白名单字段，历史 payload 中的额外字段不会继续通过接口外泄。
## 2026-08-08：二次确认弹窗动态宿主层级复核

- 运行态截图说明问题不是业务弹窗 `z-index: 1000` 过高，而是 `uni.showModal` 的动态宿主可能参与了较低的页面 stacking context。
- 只给 `.uni-modal` 内容加层级不能覆盖宿主层级；因此同时固定 `#u-a-t` 与兼容宿主 `#u-a-m`，并覆盖其直接子节点、遮罩和 dialog/container/content。
- 统一层级契约：业务弹窗 1000；系统确认根/宿主 2147483000；遮罩 2147483001；确认内容 2147483002。
- 尚未把浏览器刷新后的人工点击结果当作完成证据；需重新加载 5185 后逐项验证确认、取消、重复打开和状态不变。
## 2026-08-08：P3 运营模块并发与审计边界补强

- `adjustPoints` 改为事务内 `updateMany` 带积分上下限条件的原子更新；并发请求不会绕过 SQLite/Prisma Int 边界，余额与流水仍在同一事务中写入。
- `POST /admin/messages` 拒绝客户端主动传入 `id`；`PATCH /admin/messages/:id` 必须携带已有资源 ID，避免创建接口被误用为更新接口。
- 审计 `from/to` 非法日期和开始时间晚于结束时间现在明确返回 400，不再静默忽略筛选条件。
- 用户反馈审计只记录反馈 ID，不再把用户提交的原文内容写入审计详情，降低手机号等敏感内容外泄风险。
- 新增回归覆盖积分上限、消息创建/更新语义、审计日期边界和反馈审计脱敏；`mvp.spec.ts` 25/25 通过。
## 2026-08-08：C 端修改密码确认框层级复核

- 现象：修改密码二次确认框可能落在“账号与安全”业务弹窗下方。
- 根因：`uni.showModal` 的动态宿主和业务弹窗不在同一 DOM 层级，仅调整页面 `.modal-mask` 不足以覆盖不同 uni-h5 版本。
- 处理：统一 `#u-a-m`、`uni-modal`、`systemDialog`、旧/新版 mask、dialog/container 的层级；并在 body 挂载后通过 `MutationObserver` 兜底。
- 结论：源码层级契约和构建验证已通过，人工浏览器/小程序点击证据仍需补充，不能仅凭静态检查宣称最终验收完成。

## 2026-08-08：C 端二次确认层级运行时加固复核

- uni-h5 的 `#u-a-m` 与 `uni-modal` 可能分多个渲染周期创建，且后续补充 class/style；只在首次插入时设置 z-index 不足以覆盖所有运行态。
- 当前归一化契约：业务弹窗 `1000`；系统确认根/宿主 `2147483000`；遮罩 `2147483001`；确认内容 `2147483002`。
- 运行时监听节点、class、style、`data-type` 变化，并在 `requestAnimationFrame` 中重校正；写入前比较现有值，避免 MutationObserver 自触发循环。
- 本轮静态、构建和 verify 均通过；真实浏览器强制刷新后的“修改密码→保存新密码→确认/取消”仍是待补证据，不提前宣称验收完成。

## 2026-08-08：管理端按钮运行闭环第二轮审计

- 现有管理端主要按钮均已找到对应 `/admin/*` 接口；本轮发现的实际缺口不是“没有接口”，而是课程保存、凭证审核和学员档案操作在“写入成功但刷新失败”时会误报失败。
- 已将课程保存和凭证审核纳入统一 `runOperation` 请求锁；学员档案操作现在分别处理写入、详情刷新和列表刷新结果，避免重复提交或错误提示掩盖真实状态。
- 订单、开票、Banner、模板、优惠、消息、积分、反馈、配置和用户操作继续沿用统一请求锁、二次确认和状态门禁；后端接口与专项回归已存在。
- 当前仍不能宣称可上线：服务器 Compose/PostgreSQL 的最终重建、数据导入对账、上传持久化、备份恢复、回滚演练和真实浏览器逐按钮证据尚未全部完成。
- 在线支付继续不作为本阶段放行条件，必须保持 `PAYMENT_ADAPTER=disabled`；当前非支付内测路径是线下收款信息、凭证上传和管理端人工审核。
- 运行证据补充：管理端可登录，工作台统计和课程管理/Banner/报名模板/报名汇总页面可加载；后端隔离回归 3 套/11 项通过。快速连续切换标签页不能替代逐页人工验收，P5 仍需按模块留痕。
## 2026-08-08：当前运行时管理端核心写操作核查

- API `3100` 可使用管理员账号登录；工作台、课程、Banner、报名模板、报名汇总、报名明细、订单、开票、学员、用户、渠道自检、支付设置、优惠、反馈、消息、积分、配置和审计接口均返回 HTTP 200。
- 课程临时记录完成新增（201）、编辑（200）、删除（200），刷新后不存在。
- Banner 临时记录完成新增（201）、编辑（201）、删除（200）。
- 报名模板临时记录完成新增（201）、编辑（200）、删除（200）。
- 审计接口检索到上述维护和删除记录，证明写入不是前端假按钮。
- 当前仍不能据此宣称可上线：订单/凭证/开票高风险场景、浏览器取消/重复点击证据、Docker/PostgreSQL 导入对账和备份恢复仍需完成。

## 2026-08-08：非支付上线目标重新盘点（本轮）

- 自动化基线：根目录 `pnpm.cmd run verify` 通过；管理端 `test:ui` 和生产构建通过；后端 Jest 15 套件、64 项通过。
- 运行态：本机已有 API `3100`、管理端 `5174`、C 端 `5185` 监听；管理端现有会话可进入工作台，导航和统计数据正常显示。
- 当前模块矩阵显示 17 个管理端模块均有列表/详情或编辑入口，未发现明显的空 `onClick`；需要继续用真实点击核对写入、刷新、取消、重复点击和失败反馈，而不能把按钮数量当作完成度。
- 本轮上线边界：在线支付不纳入放行条件，继续保持 `PAYMENT_ADAPTER=disabled`；非支付内部版本必须完成内容维护、订单/线下凭证/开票、用户学员、运营、审计和发布恢复门禁。

## 2026-08-08：管理端课程导航与写操作运行证据（本轮）

- 管理端导航采用分组折叠：分组关闭时子按钮虽然存在于 DOM，但被 `max-height: 0; overflow: hidden` 隐藏；真实点击前必须先展开分组。展开后课程管理路由、面包屑和已访问标签页均正常更新。
- 课程新增、编辑、删除均能触发真实接口并刷新列表；删除确认取消不会发出删除请求，确认后记录从列表消失，证明相关按钮不是静态占位。
- 订单、线下凭证审核、开票处理、重复操作和权限边界的隔离回归仍为 5/5 通过；下一步应优先补充真实浏览器中的状态门禁和失败反馈，而不是重复盘点按钮数量。

## 2026-08-08：P1 高风险管理操作运行态证据（本轮）

- 订单详情、关闭确认取消、支付凭证审核备注必填、开票驳回理由与二次确认取消、报名履历详情均已通过真实管理端会话核验。
- 报名明细详情是异步加载：点击后需等待详情接口返回，不能以短时间内未出现弹窗判定按钮失效。
- 本轮所有取消操作均未改变现有业务记录；成功写入路径继续由隔离后端回归覆盖，避免污染演示订单。

## 2026-08-08：P4 上传持久化门禁

- Compose 已将 `upload_data` 挂载到 API 的 `/app/backend/api/storage`，但仅看 volume 声明不能证明重启后文件仍可读。
- 新增 `Docs/runtime/verify-upload-volume.sh`，通过临时标记写入、API 重启、健康等待、读取和清理完成可重复验证；不依赖真实支付或业务订单。
- 服务器上需要在 PostgreSQL/API/前端均健康后执行该脚本；失败时不得进入 P5 内部发布。

## 2026-08-08：C 端修改密码确认框层级回归定位

- 业务弹窗的层级为 `1000`；uni-h5 默认 `uni-modal` 为 `999`，不同版本还可能通过 `#u-a-m`、`#u-a-t` 或 `systemDialog` 页面包装确认框。
- 先前运行时守护将 `uni-modal` 子节点从 Vue 挂载根搬到 `body`，存在被 Vue 下一次渲染重新挂回默认层级的风险，这解释了“修改密码确认框偶尔出现在账号与安全弹窗下面”的现象。
- 结论：只提升系统确认根页面（确实嵌套在应用内部时），不搬动 `uni-modal`；对根/遮罩/内容分别固定为 `2147483000/2147483001/2147483002`，业务弹窗保持 `1000`。

## 2026-08-08：确认宿主插入时的瞬时层级窗口

- 发现仅依赖 `requestAnimationFrame` 归一化仍可能让动态确认宿主在首帧沿用框架默认层级；这不是业务弹窗层级错误，而是宿主插入与框架样式写入存在时序差。
- 已改为 MutationObserver 回调先同步归一化，再异步补偿；同时避免无变化的 data 属性写入，防止观察器递归触发。

## 2026-08-08：C 端二次确认弹窗层级修复结论

- 统一层级关系为：业务弹窗 `1000` < Toast `2000` < 系统确认根/遮罩/内容 `2147483000/2147483001/2147483002`。
- 根因不是修改密码页面单独设置错误，而是 uni-h5 在不同版本下会改变确认宿主结构和挂载位置；因此修复必须覆盖 portal、custom element、systemDialog 和动态样式写入。
- 运行时只提升真正嵌套的 `systemDialog` 根页面，不移动 Vue 管理的 `uni-modal` 子节点，避免被后续渲染重新挂回默认层级。
- 自动验证已通过；真实页面验收仍受当前浏览器没有有效 C 端登录会话限制，不能把静态构建结果替代人工点击证据。
## 2026-08-08：非支付上线目标基线盘点

- 管理端导航当前包含 18 个模块：工作台、Banner、课程、报名模板、报名汇总、报名明细、订单、开票、学员、用户、渠道自检、支付设置、优惠规则、反馈、消息、积分、系统配置、审计。
- 当前代码已为课程、Banner、报名模板、优惠规则、消息、系统配置、积分、反馈、用户启停/密码重置、学员档案和部分订单/开票操作提供真实接口调用；不能把“有按钮”直接视为上线完成。
- 非支付发布范围应保留线下收款提示、支付凭证上传与人工审核，但在线支付适配器继续保持 disabled，避免把未完成商户配置误当作上线条件。
- 当前主要待验收风险：浏览器逐按钮成功/取消/重复点击/失败反馈；订单和开票高风险写操作；学员关系与合并；导出敏感数据边界；写入成功但列表刷新失败时的状态提示；Docker/PostgreSQL 重启后的持久化与回滚。
- 自动化基线已有 `pnpm.cmd run verify`、管理端 `test:ui`、管理端生产构建及后端 Jest 套件；这些只能证明代码级契约，不能替代真实浏览器操作证据。

## 非支付发布验收矩阵（初版）

| 域 | 必须可验收的操作 | 当前证据 | 下一步 |
|---|---|---|---|
| 内容 | 课程/Banner/报名模板新增、编辑、删除、启停、刷新后结果 | 接口回归和 UI 代码存在 | 浏览器逐项验收并补失败态 |
| 运营 | 优惠规则、消息、积分、反馈处理 | 接口回归存在 | 检查并发、重复提交和审计 |
| 用户 | 用户启停、重置密码、学员档案编辑/状态/关系/合并/导出 | 后端边界测试存在 | 浏览器验收敏感字段和权限 |
| 业务 | 报名汇总/明细、订单关闭/凭证审核、开票通过/驳回/上传发票 | 关键接口已有 | 完成状态机和真实操作证据 |
| 系统 | 权限、审计、配置自检、Docker/PostgreSQL、备份恢复 | 部分自动化证据 | 做发布演练和回滚演练 |
## 2026-08-08：P0 管理端运行态盘点完成

- 使用真实管理端会话逐项进入 17 个业务模块；面包屑、已访问标签和列表/状态区域均随模块切换更新，未发现加载失败。
- 订单、开票、反馈、学员和报名明细的操作入口与状态门禁符合当前数据：已取消订单无关闭/退款，已处理反馈无回复，已开票记录仅显示上传发票，敏感列表默认脱敏。
- 记录已保存到 `Docs/Summary/2026-08-08_P0管理端运行态审计记录.md`；P0 盘点完成，下一步执行 P1/P2 写操作和失败/取消/重复点击证据。
## 2026-08-08：P1 浏览器验收第一条证据

- 订单关闭确认框的取消路径可见且可操作；取消后列表数据和状态不变，未发现误关闭。
- 课程新增空提交未创建课程；成功写入、刷新可见和删除确认仍需隔离临时数据完成。
## 2026-08-08：C 端反馈失败路径加固

- 发现 C 端问题反馈的异步提交没有 catch/finally，失败时用户无明确提示且可能重复点击。
- 已增加输入边界、二次确认、提交锁和失败恢复；根目录 verify 已将该契约纳入静态门禁。
## 2026-08-08：P1 课程管理真实成功闭环

- 浏览器真实会话完成临时课程新增、刷新可见、删除确认、刷新消失；审计可按标题查到维护和删除记录。
- 这证明课程按钮不是静态占位；其余写操作仍需分别保留成功/取消/失败/重复点击证据。
## 2026-08-08：P1 内容管理浏览器闭环扩展

- Banner 与报名模板新增、刷新可见、删除确认、刷新消失均已在真实管理端会话中完成；审计记录可检索。
- P1 内容管理的主要“按钮无动作”疑虑已用运行态证据排除，下一阶段转向订单/凭证/开票状态机。

## 2026-08-08：P1 反馈处理取消路径

- 真实管理端进入反馈管理，打开待处理反馈的回复弹窗并点击取消，弹窗关闭且列表内容、状态保持不变。
- 该结果证明反馈处理按钮具备真实弹窗交互和取消门禁；成功回复与异常恢复仍以隔离后端测试作为证据，避免直接修改演示反馈。
## 2026-08-08：C 端确认弹窗层级回归

- 运行态发现旧兼容规则误把 `uni-modal__hd/bd/ft/btn` 识别为确认内容，导致内容节点全部 `position: fixed`，弹窗高度变为 0。
- 修复后仅提升确认宿主、遮罩和真正的 dialog/container 节点；标题、正文和按钮恢复默认布局。
- 实际 DOM：业务弹窗 `1000`，确认宿主 `2147483000`，遮罩 `2147483001`，内容 `2147483002`。
- H5 真实点击“修改密码 → 保存新密码”确认框已显示在业务弹窗上方；取消后未发起密码修改请求。
- 发布前仍需刷新/重启 H5，并在小程序容器单独复核。
## 2026-08-08：P4 管理端用户运营与配置运行态证据

- 学员详情与关系操作均有真实确认框；取消后详情和关系状态保持不变，未触发写入。
- 优惠规则新增/删除链路可用，删除后临时数据清理，维护/删除审计均保留。
- 积分调整存在非零整数和原因校验；合法调整进入确认框，取消不写入；成功路径需隔离库验证。
- 系统配置新增无删除入口，临时配置通过受控数据库清理删除，审计记录继续保留；产品上如需长期维护应补充“删除/停用”策略。
- 操作审计筛选和重置可用，能定位本轮临时操作记录。

## 2026-08-08：学员档案编辑的脱敏回写风险

- `GET /admin/student-profiles` 和报名明细列表默认返回脱敏手机号/邮箱；单条详情才允许受控读取明文。
- 管理端 `openStudentEditor` 原先把 `137****0031` 通过正则拼成 `1370031`，保存时会触发后端手机号格式校验；邮箱也可能被截断成不完整值。
- 已改为：识别包含掩码的值后在编辑框留空；保存 payload 默认不携带 `phone`/`email`，只有操作员重新输入完整值时才提交。这样不修改敏感字段不会破坏原档案。
- 该修复属于“按钮有接口但运行态容易失败”的边界问题，后续其他敏感字段编辑应沿用同一策略。
## 2026-08-08：确认框被业务弹窗遮挡的根因与修复

- 根因不是密码接口，而是 uni-h5 `showModal` 在不同版本下挂载到框架 portal，portal 可能处于低于页面业务弹窗的 stacking context；只提升内容节点的 `z-index` 不足以保证跨堆叠上下文生效。
- 修复方案：H5 重要确认使用 body-level `client-confirm-root`，遮罩和内容均带明确层级；非 H5 保留原生 `uni.showModal`。
- 已覆盖密码修改、资料保存、报名提交/删除、支付凭证提交、开票申请、学员保存/设默认/解除关系等确认入口。
- 回归要求：刷新 H5 后确认确认框显示在业务弹窗上方、遮罩可点击、取消不写入、确定只触发一次；小程序端需用真机/开发者工具单独复核。

## 2026-08-08：二次确认弹窗 z-index 回归复核

- 复核结论：问题属于弹窗 portal/stacking context 与后续框架内联样式竞争，不是修改密码接口或权限问题。
- 所有页面业务弹窗统一为 `1000`；H5 共享确认根节点、遮罩、内容统一为 `2147483000/2147483001/2147483002`，并在运行时与 CSS 两侧同时加固。
- 本次补强将共享确认三层的 z-index 设置改为内联 `!important`，对 uni-h5 延迟写入的默认 z-index 具备覆盖能力。
- 运行态未提交真实密码变更，仅使用临时匹配密码触发确认框并核对 DOM 计算层级。
- 风险边界：小程序端继续使用原生 `uni.showModal`，需在开发者工具/真机单独复核；在线支付仍保持 disabled。

## 2026-08-08：确认层级常量统一补强

- 发现 `App.vue` 和 `common/confirm.ts` 原先分别维护相同数字，后续容易出现确认根节点、遮罩和内容层级漂移。
- 新增 `common/modal-layer.ts` 作为唯一层级契约，两端引用同一组 `2147483000/2147483001/2147483002` 常量；业务弹窗仍固定为 `1000`。
- 管理端确认弹窗继续通过 document body Portal 使用 `2147483646/2147483647`，高于所有管理端业务弹窗 `1000`。

## 2026-08-08：C 端错误态与重试闭环

- 发现首页、订单、消息、学员档案和报名页面在接口失败时存在“清空/静默降级为无数据”的风险，用户无法区分网络异常与真实空列表。
- 已为首页、课程详情、订单/开票、消息、学员档案、报名和个人中心增加失败状态、错误提示和重新加载入口。
- 订单和首页刷新失败时保留已有数据；消息标记已读失败只反馈，不影响用户打开消息详情。
- 报名模板失败阻止继续填写并提供重试；报价和收款信息失败分别显示局部错误，不把接口异常当成支付成功或报价为零。
- 运行态正常数据页面已检查通过；失败模拟和重试成功仍需在隔离环境完成，不能用正常接口返回替代失败证据。

## 2026-08-08：C 端待支付订单支付方式展示边界

- 新建订单在账单弹窗选择线下转账前，后端仍保持待支付且没有支付渠道；旧前端直接把空渠道回退显示为“微信支付”。
- 这会把“尚未选择方式”误导成“已选择微信支付”，与非支付内测流程不一致。
- 已改为：有 `paymentChannel` 才显示微信/支付宝；`paymentMethod=offline` 显示对公转账；待支付且无渠道显示“待选择支付方式”；历史已支付但无渠道显示“在线支付”。
## 2026-08-09：二次确认层级运行态证据

### 本轮复核补充

- 修改密码弹窗被业务弹窗遮挡的问题在当前运行态未复现；确认框使用 body-level Portal，避免页面 stacking context 影响。
- C 端层级契约为：业务 `1000` < 系统 `2147483000~2147483002` < C 端确认 `2147483600~2147483602`。
- 管理端层级契约为：业务 `1000`/参与人明细 `1100` < 确认遮罩 `2147483646` < 确认内容 `2147483647`。
- 若用户仍看到旧层级，优先重启/刷新 5185 H5 服务并清理旧 HMR 页面；源码和运行态层级已一致。

- 修改密码弹窗被压住的原因已排除：不是密码接口或权限问题，而是确认层与账号安全业务弹窗处于不同 stacking context。
- 当前 H5 运行态证据：`.modal-mask` 的计算 z-index 为 `1000`；`.client-confirm-root/.client-confirm-mask/.client-confirm-dialog` 分别为 `2147483600/2147483601/2147483602`；`elementFromPoint` 返回确认内容节点。
- 所有 C 端重要二次确认统一走 body-level `showClientConfirm`；非 H5 平台保留 `uni.showModal`，需在小程序容器单独复核。
- 管理端 `app-dialog-backdrop/app-dialog` 通过 `2147483646/2147483647` 位于业务弹窗 `1000` 之上，没有发现低层级确认入口。

## 2026-08-09：非支付上线主线重新盘点

- 管理端源码中的主要操作入口均能对应到 `apiFetch`/`apiUpload` 和后端 `/admin/*` 路由；当前更大的风险是运行态成功、取消、失败、重复点击和刷新证据不完整，而不是所有按钮都没有接口。
- 管理端当前运行会话可正常显示工作台，统计数据和 18 个导航模块均已加载；这证明读取链路可用，但不能替代写操作验收。
- 下一步按高风险顺序核验订单关闭/退款、支付凭证审核、开票通过/驳回/发票文件、用户启停/重置密码和学员关系/合并；临时数据必须隔离并清理。

## 2026-08-09：本轮恢复后的基线证据

- 根目录 `verify` 返回所有静态门禁为 `true`，包含 C 端确认层、写入锁、状态门禁、错误重试、管理端刷新错误分离等检查。
- `frontend/admin-react` 的 `test:ui` 通过；后端 Jest 15 套件、64 项测试全部通过。
- 管理端工作台运行态可加载，统计数据和导航模块正常；该证据只证明读取链路，不替代高风险写操作的成功/取消/失败/刷新/审计核验。
- 当前最短推进路径是 P1：以唯一临时订单和开票记录验证状态机，记录成功、取消、非法输入、重复提交、刷新和审计，然后清理临时数据。

## 2026-08-09：P1 运行态发现与修复

- C 端线下凭证链路已在真实会话完成：报名 → 线下转账 → 上传凭证 → 管理端审核通过 → C 端刷新为已支付。
- 管理端凭证驳回在空备注时被前端阻止，填写理由后才进入二次确认；取消确认不改变订单状态。
- C 端开票申请成功后曾出现“弹窗仍打开但已选择 0 笔订单”的真实缺陷。根因是 `submitInvoice` 在 `invoiceSubmitting=true` 时调用 `closeInvoiceDialog()`，而该函数为防止提交中误关闭会直接返回。
- 已改为请求成功后直接将 `invoiceDialogOpen.value = false`，并新增 `clientInvoiceClosesAfterSuccess` 静态门禁；后续需重新创建一个隔离订单对该修复做一次运行态复验。
- 管理端开票通过、发票号码填写和电子发票上传均已在同一临时申请上验证，列表显示“已开票/已上传”。
## 2026-08-09：P1/P2 隔离回归复核

- `admin-operations.spec.ts`、`admin-security-boundaries.spec.ts`、`p1-write-operations.spec.ts`、`p10-scenarios.spec.ts` 共 4 套件、13 项通过，当前非支付核心写操作没有发现回归。
- 新增导出边界断言后确认：普通管理员导出默认只返回脱敏手机号/邮箱；只有管理员显式请求 `reveal=true` 才可揭示明文，响应同时标记 `sensitiveFieldsMasked`，并写入导出审计。
- 因此“管理端按钮全部未实现”的判断不成立；剩余风险集中在浏览器运行态的失败/刷新/重复点击证据以及 Docker/PostgreSQL 重启恢复，而不是缺少后端接口。
- 运行时检查 `GET /api/courses`、管理端和 C 端页面均可访问；API 没有统一 `/health` 路由，不能把 `/api/health` 的 404 误判成服务未启动，应使用业务接口或 Compose 健康检查。

## 2026-08-09：API 健康检查门禁补强

- 原先 Compose 通过 `/api/courses` 判断 API 健康，无法明确证明数据库连接正常。
- 已新增 `GET /api/health`，执行数据库轻量查询；数据库不可用时返回 503，而不是伪装成正常。
- Compose、生产化测试和根目录静态 verify 已统一使用该健康接口。后续服务器验收应先看 `curl http://127.0.0.1:3100/api/health`，再判断迁移、前端和业务接口。
- 历史阻断：已运行的 3100 进程曾是旧实例，直接访问新路径返回 404；代码和隔离测试已通过，需重启项目 API 后再判断新版本。
- 该阻断已解除：重启项目 API 后 `/api/health` 返回 `status=ok,database=ok`，课程接口、管理端和 C 端页面均可访问。该证据仅覆盖本机运行态，不替代服务器 Compose 验收。

## 2026-08-09：发布脚本探针一致性

- Compose、API 健康接口和 `verify-compose-release.sh` 之前可能使用不同探针；现已统一为 `/api/health`，并检查数据库状态字段。
- 这样服务器验收可以区分：容器存活、API 可达、数据库可用和前端端口可达，减少“服务启动但数据库未连接”的误判。
- 本机 Docker daemon 当前不可用，未执行真实 PostgreSQL 导入或卷重启；这属于尚未取得服务器运行证据，不是代码通过的替代证明。

## 2026-08-09：P3 运营状态门禁补强

- 新增管理端运营回归：同人数/课程范围的优惠规则返回冲突 ID；站内消息重复标记已读保持幂等且阅读数不重复增加；非法系统配置键和零积分调整均返回 400。
- 后端全量回归现在为 15 套件、66 项测试通过，管理端 UI 验收和构建继续通过。
## 2026-08-09：C 端确认层提升至浏览器最高安全整数

- 修改密码确认框的遮挡风险不是密码接口问题，而是确认 portal 与业务弹窗处于不同 stacking context，且原 C 端层级没有与管理端最高层级统一。
- C 端确认现在使用 `2147483645/2147483646/2147483647`（根/遮罩/内容），业务弹窗保持 `1000`；确认根和遮罩同时设置可见性内联兜底。
- 所有重要 C 端操作继续走 `showClientConfirm`；普通积分说明和反馈正文输入仍是原生信息输入，不应误判为二次确认。
- 已通过静态 verify、H5 构建和 diff 检查；刷新 5185 后仍需用真实会话点验修改密码、取消和确定分支。
## 2026-08-09：修改密码确认框被业务弹窗遮挡的复核结论

- 根因是旧 H5 页面或旧确认层级仍使用 `2147483600/2147483601/2147483602`，而不是密码接口、权限或业务弹窗本身。
- 当前源码的统一契约为：业务弹窗 `1000`；C 端确认根/遮罩/内容 `2147483645/2147483646/2147483647`；管理端确认遮罩/内容 `2147483646/2147483647`。确认层级高于 C 端系统弹窗 `2147483000~2147483002`。
- 当前浏览器实测 DOM 与源码一致，确认内容节点的计算层级为 `2147483647`，可覆盖账号与安全弹窗。历史记录中的旧数值仅代表修复前证据，不代表当前实现。
## 2026-08-09：非支付版本当前差距复核

- 管理端 `App.tsx` 的主要写操作入口均已对应真实 `apiFetch`/`apiUpload` 和后端 `/admin/*` 路由；当前不能再笼统判断为“很多按钮未实现”。
- 仍然缺少的主要证据是浏览器运行态：订单关闭/退款、报名明细刷新失败与重试、用户/学员安全操作、运营配置和审计筛选的成功/取消/失败/重复点击/刷新分支。
- 自动化测试覆盖后端权限、幂等和边界，但不能替代管理端真实会话中的写入结果、界面提示、按钮禁用和审计可见性。因此当前下一阶段按 P1 → P2 → P3 逐项补运行态证据，再进入服务器发布验收。
## 2026-08-09：P1 管理端运行态登录阻断

- 管理端页面 `http://localhost:5174/login` 可正常加载；使用常见的 `admin/admin` 仅得到“账号或密码错误”，未继续重复尝试，避免锁定或污染账号状态。
- 下一步需从项目本地测试账号说明、种子数据或用户提供的测试账号取得已授权凭据，再继续真实浏览器写操作验收；这不影响后端自动化测试和源码链路判断。
## 2026-08-09：P1 浏览器选择器边界

- 订单页的标签关闭按钮“关闭订单与支付标签”与行操作“关闭订单”存在名称前缀重叠；未使用精确匹配时会误点标签关闭按钮，结果仅关闭当前标签并返回工作台，未写入订单。
- 后续运行态验收统一使用精确可访问名称或按表格行限定作用域，避免把导航标签操作误判为业务按钮失效。
## 2026-08-09：P1 管理端订单、开票与凭证运行态证据

- 使用本地授权管理账号进入 `5174` 后，订单列表可加载 28 条数据，筛选和分页可用。
- 订单关闭：`HX-1786086133218-e6b377` 先验证二次确认取消且保持“待支付”，再确认后变为“已取消”，页面提示“待支付订单已关闭，课程名额已回退”。
- 退款：`HX-1786238416666-4afa87` 先验证取消不写入，再确认后变为“已取消”，页面提示“订单已退款，课程名额已回退”。
- 开票驳回：`INV-1786238594395-605271` 验证填写理由、第一次确认、第二次确认和成功后变为“已驳回”；凭证驳回空理由会被前端阻止并提示“驳回凭证时请填写原因”。
- 线下凭证审核：`HX-4797620266` 验证空驳回理由校验、二次确认取消；填写理由并确认后从“待审核”筛选中移除，页面提示“支付凭证已驳回”。
- 本批次使用的是本地验收数据，不涉及在线支付，不上传外部文件，也未执行不可逆的真实商户操作。

## 2026-08-09：P1 报名明细详情与刷新恢复证据

- 本地服务当前实际监听 3100、5174、5185；`GET /api/health`、`GET /api/courses`、管理端登录页和 C 端入口均返回 200。
- 管理端“报名明细”已验证真实详情弹窗：可读取报名履历、模板版本和当次表单快照；详情关闭后列表保持正常。
- 刷新管理端后“报名明细”已访问标签仍保留，切回标签后列表重新请求并显示 29 条记录；这补齐了报名明细详情和刷新一致性证据。
- 失败态/重试尚未做真实浏览器证据，原因是当前 API 已由既有进程监听，不能通过重复启动制造故障；后续应在隔离进程或可控代理下执行，不应停止用户正在使用的服务。

## 2026-08-09：P2 用户与学员档案运行态证据

- 用户 `demo01` 的重置密码流程已完成二次确认、成功反馈和列表刷新；临时密码只用于本轮恢复操作，随后已通过个人密码接口恢复为 `123456`。
- `demo01` 的启用/禁用操作已分别确认成功，最终状态恢复为启用，未影响管理端当前会话。
- 学员档案 `stu-0401e1f0eeb72f12a9cbd5fa` 的详情、资料保存和成功刷新已验证；保存使用原有业务字段，未引入数据变更。
- 操作审计页能检索本轮用户密码、用户启停、学员查看和档案更新记录，说明高风险操作具备留痕。
- 关系授权/解除、默认关系、合并、脱敏导出和跨账号权限尚未形成完整运行态证据，P2 继续保持进行中。

## 2026-08-09：P4 服务器 SSH 阻断

- 只读连接 `root@192.168.0.162` 返回 `Permission denied (publickey,password)`，当前本地会话没有可用的服务器登录密钥或密码认证权限。
- 未执行任何远程写操作，也没有继续尝试猜测密码；服务器 Docker/PostgreSQL 发布、迁移、备份恢复和回滚需要用户先在服务器配置授权 SSH 公钥，或直接执行部署手册中的命令并回传非敏感结果。
## 2026-08-09：二次确认层级复核结论

- 原问题表现为确认框落在“账号与安全”业务弹窗下方，根因是 uni-h5 原生确认 Portal 与页面业务弹窗处于不同 stacking context，不能只依赖框架默认层级。
- 当前已统一为 body-level fixed Portal：业务层 `90` < 系统层 `2147483000~2147483002` < C 端确认层 `2147483645~2147483647`；管理端确认层为 `2147483646~2147483647`。
- 所有 C 端重要操作均复用同一确认实现；信息展示“我的积分”和反馈正文输入仍保留原生系统弹窗，不属于二次确认入口。
- 自动化静态门禁与 H5 构建均通过；服务器/小程序容器尚未作为本次本地改动的运行态证据。

## 2026-08-09：非支付上线主线管理端功能盘点

- 管理端当前导航包含 18 个模块：工作台、Banner、课程、报名模板、报名汇总、报名明细、订单、开票、学员、用户、渠道自检、支付设置、优惠规则、反馈、消息、积分、系统配置和操作审计。
- 已确认的真实接口覆盖：课程/Banner/模板的新增、编辑、删除；订单关闭、线下凭证审核、退款；开票通过、驳回和电子发票上传/查看；用户启停和密码重置；学员档案编辑、启停、账号授权/默认/解除、合并、履历和脱敏导出；优惠、消息、积分、反馈和系统配置写操作；审计查询。
- 管理端列表已具备加载失败提示、重新加载、分页、筛选、导出和重复提交锁；确认弹窗通过统一 Portal 渲染，不存在“按钮只弹提示但不调用接口”的确定性缺口。
- 当前真正影响非支付上线的缺口不是静态按钮数量，而是：P1/P2/P3 各关键写操作的成功、取消、重复点击、刷新失败恢复和审计可见性需要按模块补齐运行态证据；服务器 Compose/PostgreSQL 的迁移、备份恢复和回滚还不能以本机结果替代。
- 在线支付本批次保持 `PAYMENT_ADAPTER=disabled`，支付设置页只作为配置入口和渠道自检，不纳入内部测试放行条件。
- 管理端仍需重点补强的产品级边界：高风险操作前端确认与后端状态门禁一致、敏感导出默认脱敏、普通用户不能访问管理接口、写成功但列表刷新失败时显示可重试状态、所有临时验收数据可追踪和清理。

## 2026-08-09：P1.1 列表刷新失败恢复实现

- 原管理端 `load()` 在刷新请求失败时直接 `setData(null)`，导致已有列表瞬间消失；这会把短暂网络/数据库故障误认为“没有数据”或“按钮没有实现”。
- 现改为保留最近一次成功响应：首次加载失败显示完整错误态；已有数据刷新失败时显示局部错误提示和“重新加载”，同时保留可查看的旧列表。
- 该修改只影响管理端展示层，不改变写接口、权限或数据库状态；写操作成功但刷新失败时仍会通过 `runOperation` 给出明确提示，用户可以重试列表加载。

## 2026-08-09：P2 管理员账号安全边界实现

- 发现 `setUserEnabled` 原先没有保护管理员自停用和最后管理员停用，生产环境可能出现所有管理账号都被禁用的不可恢复状态。
- 现增加后端校验：停用目标是当前操作员本人时返回 400；启用管理员数量小于等于 1 时拒绝停用，并保留已有 sessionVersion/refresh token 吊销逻辑。
- 专项验证结果：`admin-security-boundaries.spec.ts` 4/4 通过；后端全量 15 套件、67 项测试通过。

## 2026-08-09：课程报名人数一致性实现

- 发现课程编辑表单允许直接改写 `Course.enrolled`，会绕过创建订单、取消报名和退款的事务，导致名额统计与报名履历不一致。
- 现将前端字段改为只读，后端只保留业务事务维护的既有值；编辑请求携带不同报名人数返回 400，新课程非零报名人数也被拒绝。
- 现有容量校验仍保留：课程名额不能小于当前报名人数；报名人数变更继续由订单/履历状态流转维护。
- 回归中发现旧测试依赖“已报名人数不能超过名额”提示，已保留该错误契约；只有在名额关系合法后才提示新课程初始报名人数必须为 0。

## 2026-08-09：管理端学员档案合并交互差距

- 合并接口和后端校验已经存在，但旧管理端入口通过手工输入目标档案 ID，属于“接口已实现、产品交互不完整”的差距，容易造成误合并。
- 现改为 Portal 搜索选择器：请求 `/admin/student-profiles?status=active`，排除当前档案和已合并档案；选择目标后才进入二次确认，确认后仍调用原有合并接口。
- 该改动不改变数据库模型或合并事务，只收紧目标选择和前端可操作性；失败时保留选择器重试，写入成功但刷新失败时沿用已有局部恢复提示。

## 2026-08-09：P3 反馈处理并发边界

- 原 `resolveFeedback` 先查询状态再执行普通 `update`，在两个请求并发到达时存在后写覆盖先写的理论窗口。
- 现改为 `updateMany` 携带 `id + status='待处理'` 条件的原子迁移；更新数量不是 1 时直接返回重复处理错误，再读取保存结果。
- 新增并发测试确认两个同时处理请求只能一个成功，数据库只保留一个回复；专项 26/26、API 构建、根目录 verify 和差异检查通过。
- 这类后端状态门禁与前端 `operationKey` 请求锁互为补充：前端减少重复点击，后端保证绕过 UI 的重复请求也不会覆盖业务状态。

## 2026-08-09：报名明细失败恢复运行态证据

- 通过隔离 API 将报名明细接口置为 503，首次加载显示错误态；接口恢复后点击“重新加载”可以恢复列表。
- 在已有列表上再次制造 503，管理端没有把数据误显示为空，而是保留旧记录并显示刷新失败提示和重试入口。
- 该结果证明此前“按钮没反应/列表没实现”的一类问题实际是失败态展示问题；剩余真实环境证据仍需在授权管理账号和服务器环境中补齐。
## 2026-08-09：二次确认弹窗层级复核补充

- 当前 C 端确认框使用 body-level fixed Portal，避免被账号与安全等页面业务弹窗的 stacking context 遮挡。
- 层级顺序为：业务弹窗 `90` < uni-h5 系统弹窗 `2147483000~2147483002` < C 端确认内容 `2147483647`。
- 信息展示类 `uni.showModal`（积分说明、反馈输入）不属于二次确认，不纳入确认 Portal 扫描范围。

## 2026-08-09：P1/P2 本地运行态接口复核续接

- API 使用 `PORT=3199` 隔离启动后，`GET /api/health` 返回 `200` 且数据库状态为 `ok`；管理端 `5174/login` 返回 200。这证明当前代码和本地 SQLite 运行环境可启动，但不替代目标服务器 Compose 验收。
- 管理会话只读复核结果：`/admin/orders` 返回 `total=28`；`/admin/enrollment-records` 返回 `total=29`；`/admin/student-profiles` 返回 `total=27`；`/admin/audits` 返回 143 条记录及操作类型集合；审计记录中能看到密码重置、用户启停、学员档案查看/更新。
- 报名明细返回 `templateId/templateVersion`、`orderStatus`、`accountUsername`、脱敏 `phone` 等字段；学员档案返回账号关系和履历数量，说明数据源不是静态按钮占位。
- 本轮浏览器控制尝试被本地 URL 安全策略拒绝，未采用换浏览器、原始 CDP 或其他绕过方式。后续需要在安全策略允许的本地会话中继续真实点击验收；当前 API、静态和自动化证据不能单独关闭 P1/P2。

## 2026-08-09：P3 运营模块只读运行态接口证据

- 管理会话只读访问 `/admin/messages`、`/admin/points`、`/admin/configs`、`/admin/discount-rules`、`/admin/feedbacks` 和 `/admin/audits` 均返回 200；当前数据量分别为 6、13、6、6、3 和 143（审计同时返回操作类型集合）。
- P3 当前确认了真实数据源、接口和响应结构，尚未确认浏览器写操作的成功提示、取消分支、重复提交锁、失败重试和审计筛选结果；因此不能把 P3 标记完成。

## 2026-08-09：全量后端回归复跑

- `pnpm.cmd --dir backend/api test -- --runInBand` 通过 15 个测试套件、68 项测试。
- 回归覆盖认证、学员域、管理操作、权限边界、P1 写操作、反馈并发、迁移回填、支付完整性和渠道适配器；支付相关结果只证明安全门禁和适配器契约，不代表真实商户支付已启用。
## 2026-08-09：修改密码确认层级复核补充

- 仅提高确认内容节点的 z-index 不能覆盖父级 stacking context，因此本次同时固定业务弹窗层级、确认根节点挂载位置和根节点层级。
- 运行态复核前必须重启/刷新 5185 H5，避免旧 HMR 页面继续使用旧弹窗实现。

## 2026-08-09：修改密码确认层级真实运行态证据

- 真实 H5 页面已验证“账号与安全 → 保存新密码”的确认框覆盖关系：确认 Portal 位于父业务弹窗之上，运行时层级为根 `2147483645`、遮罩 `2147483646`、内容 `2147483647`。
- 取消确认后确认 Portal 被移除，父弹窗和输入内容保留；没有执行最终密码修改提交，因此本次证据只覆盖确认显示和取消分支。
- 当前 `confirm.ts`/`App.vue` 的实际实现以 HTML 根节点 Portal 为准；此前记录中“body-level”描述属于历史中间状态，不能作为当前实现依据。

## 2026-08-09：P2 学员关系浏览器证据

- `operator/123456` 可登录本地管理端；`admin/123456` 当前返回 401，但不影响使用现有管理员账号继续验收，也未修改数据库密码。
- 学员关系选择器能够搜索并选择 `demo01`，授权、默认关系和解除关系均经过二次确认；授权和解除后的详情状态与审计筛选结果一致。
- 浏览器导出事件在当前控制面未捕获，可能是前端通过 Blob/锚点下载而非标准 download 事件；应在允许下载的会话或直接验证导出文件内容时补证据，不能仅凭按钮存在关闭该项。

## 2026-08-09：退款与导出权限续接接口证据

- 订单 `HX-1786210554512-579e76` 的当前服务端状态为“已取消”；按 `退款完成` 筛选审计可见 `operator` 对该订单的记录，重复退款请求返回 400。该结果证明状态、审计和终态拒绝在接口层闭环，但不替代管理端页面刷新和筛选点击。
- 学员档案导出接口的默认结果确实为脱敏：`sensitiveFieldsMasked=true`、手机号掩码、无 `phoneNormalized`；管理员显式 `reveal=true` 才返回完整手机号、`phoneNormalized` 和 `sensitiveFieldsMasked=false`。
- `demo01/123456` 普通用户访问 `/admin/student-profiles` 返回 403。账号 `demo` 的密码曾在既有验收中被重置，未继续猜测或修改该账号。
- 当前浏览器绑定异常只影响页面级证据采集，不影响上述本地 API 复核；发布结论仍必须把页面级证据和服务器 Compose/PostgreSQL 证据分别列出。

## 2026-08-09：P1/P2 浏览器续接结果与支付配置纠偏

- 浏览器绑定已恢复。P1 订单刷新、退款审计筛选、凭证/开票/报名履历审计筛选和 C 端确认 Portal 均完成真实页面复核。
- 报名明细使用独立隔离服务完成真实浏览器的 503 错误态、重新加载恢复和已有列表刷新失败保留旧数据验证；该证据专门证明 UI 恢复行为，不替代真实数据库数据。
- P2 合并选择器能够搜索临时目标并进入最终确认，最终数据写入没有执行；因为合并会迁移履历且标记源档案为已合并，需在继续执行前获得对该明确临时源/目标的单独确认。
- P2 脱敏导出按钮成功给出 4 条脱敏提示，审计筛选显示 `4/4 条，脱敏`；下载事件未捕获，因此“页面触发导出”和“已检查文件字节内容”仍需区分。
- 本地 `.env` 已显式设置 `PAYMENT_ADAPTER=disabled`。重启 API 后 readiness 和公共支付设置均确认在线微信/支付宝为禁用，修复了本地开发默认 `fake` 导致的非支付门禁不明确问题。

## 2026-08-09：5185 H5 刷新后确认 Portal 运行态证据

- 通过现有 `demo01/123456` 登录 H5 后，修改密码确认 Portal 在真实 DOM 中为 `client-confirm-root`，确认内容层级为 `2147483647`，根节点为 `2147483645`；账号与安全业务弹窗遮罩层级为 `90`。
- 取消确认后 `确认修改密码` 节点数量为 0，账号与安全弹窗仍打开，两个密码输入值仍为临时值；未执行不可逆的密码提交。
- 该证据证明当前重启/刷新后的 H5 运行态实现与源码层级契约一致；后续仍需继续 P2/P3 写操作证据和 P4 服务器环境验收。

## 2026-08-09：导出文件观测边界

- 管理端学员导出按钮的真实页面路径已经再次触发，但 In-app Browser 的标准 download 事件没有收到，本机 Downloads 目录也没有新文件。
- 当前可靠证据仍是：导出接口默认响应脱敏、页面提示“已导出 4 条脱敏学员档案”、审计显示 `4/4 条，脱敏`；缺少的是浏览器落盘文件字节的独立检查，不应混淆为同一结论。

## 2026-08-09：P3 浏览器写操作证据

- 反馈处理的前端取消分支不会调用写接口；确认后列表刷新保留回复文本并隐藏重复处理按钮，与后端原子状态门禁一致。
- 消息新增使用二次确认，成功后真实列表显示新消息；停用同样使用确认并刷新显示 `启用状态=否`。临时消息标题带验收日期，便于后续清理或审计检索。
- 积分调整使用正负变更记录而非直接覆盖余额；本轮 `+1`/`-1` 后余额恢复，流水按时间倒序显示两条记录和原因，证明余额和流水共同刷新。
- P3 仍未完成：优惠规则、系统配置、审计筛选以及各模块失败重试/重复提交证据仍需继续补齐。

## 2026-08-09：P3 优惠、配置与审计运行态

- 优惠规则表单在浏览器中拒绝人数门槛小于 1 或折扣超出 0~1；有效临时规则创建后可在分页列表中找到，停用确认刷新后状态为否，且审计记录带有 `enabled=true/false` 两次维护结果。
- 系统配置保存使用二次确认，写入后列表刷新可见；随后恢复 `invoiceDays=7`，避免改变本地默认配置。
- 审计筛选“处理反馈”返回单条本轮记录 `FB-DEMO-008`；最新审计列表还直接显示了用户积分调整、优惠规则维护和系统配置维护，证明写操作不是仅前端提示。

## 2026-08-09：发布门禁与服务器阻塞复核

- 当前自动化证据：根目录 verify、管理端 UI 静态验收、管理端构建、C 端 H5 构建、后端 15 套件/68 项测试和 `git diff --check` 全部通过。
- 当前支付运行态证据：管理 readiness 的支付总适配器、微信支付适配器和支付宝支付适配器均为 disabled；公共配置的在线微信/支付宝开关均为 false。微信登录/密码找回等 fake 模式不等同于在线支付启用，需在发布报告中分开说明。
- 目标服务器端口 22 可达，但 BatchMode SSH 被服务器关闭；没有新的服务器证据，不能用本地 SQLite、构建或 API 结果替代 P4 Compose/PostgreSQL 验收。

## 2026-08-09：优惠规则冲突语义

- 当前后端/前端对同门槛规则的处理是允许保存并返回/展示 `conflicts`，而不是返回 400；停用冲突记录后冲突集合重新为空。
- 因此“同门槛冲突”属于当前已实现的软警告边界。P3 只能声称已验证边界提示和停用恢复，不能声称已验证“冲突提交被拒绝”或“网络失败自动重试”。

## 2026-08-09：跨账号管理入口证据补充

- 普通账号在管理端登录入口被拒绝，管理员会话可恢复；普通用户管理接口 403 的服务端证据仍需保留为权限判定依据。

## 2026-08-09：P3 审计筛选运行态

- “停用消息”筛选返回临时消息 `MSG-1786282970119-152ce0`；“用户积分调整”筛选返回 `demo01` 的 `+1/-1`；“优惠规则维护”筛选返回冲突规则的启用与停用；“系统配置维护”筛选返回 `invoiceDays` 改为 8 和恢复 7。
- 这些筛选结果证明浏览器写入之后审计记录可见，且操作详情可追溯到具体业务对象；剩余缺口是网络级失败恢复和重复提交的独立浏览器证据。

## 2026-08-09：续接服务恢复与浏览器限制

- API 通过现有开发启动流程恢复在 3100，健康检查返回 `status=ok,database=ok`；H5 5185 在启动过程出现短暂端口占用提示，等待后确认端口恢复监听。
- 应用内浏览器重新接管 5185 标签并刷新时触发 URL 安全策略拒绝。本轮没有尝试绕过或改用原始 CDP；此前 H5 重启/刷新后的修改密码确认框证据仍以已有总结文档为准。
- 因浏览器策略限制，不能把本轮重新接管失败误报为新的页面验收；P3 网络失败重试与重复提交证据仍保持未完成。

## 2026-08-09：P3 重复提交竞态修复

- `runOperation` 原先只依赖 React state `operationKey`，而 state 更新不是同步操作；快速双击可能在第一次 state 更新生效前重复进入写操作。
- 当前改为 `operationKeyRef` + state 双层保护。ref 负责同一事件循环的同步互斥，state 负责 UI 的禁用和“处理中”显示；finally 按 key 清理，避免旧请求误清理后续状态。
- 该结论由源码复核、UI 静态断言、TypeScript/Vite 构建和全量后端回归共同支持；仍不等同于浏览器实际双击事件证据。

## 2026-08-09：导出异常短号码脱敏缺口与修复

- 默认导出 HTTP 响应声明 `sensitiveFieldsMasked=true`，但旧逻辑对长度小于 7 的手机号直接返回原值，实际泄露了历史值 `123132`。
- 修复后只对符合 `^1\d{10}$` 的手机号保留前三后四；异常/短值全部替换为星号。默认导出再次检查到 27 个手机号字段，未掩码数量为 0，且不含 `phoneNormalized`。
- 该修复同时覆盖列表、报名履历和导出使用的共享 `maskPhone` 路径；管理员显式 `reveal=true` 的明文边界保持不变。

## 2026-08-09：导出附件契约标准化

- 导出接口已增加标准 `Content-Disposition` 文件名，运行态返回 `student-profiles-masked.json`。
- 这使 HTTP 层具备标准附件语义，并可被客户端/代理验证；但当前前端仍通过授权 fetch 后生成 Blob 下载，应用内浏览器的实际 download 事件与磁盘文件仍需单独验收。

## 2026-08-09：明文导出文件名边界

- 附件文件名现在与敏感字段状态一致：默认脱敏为 `student-profiles-masked.json`，仅管理员显式 `reveal=true` 为 `student-profiles.json`。
- 这只是文件命名与 HTTP 契约修正，不改变明文导出的权限边界；浏览器落盘文件仍需独立验收。

## 2026-08-09：最新运行态证据

- 当前本地 API 与数据库健康；支付总适配器、微信支付、支付宝支付均禁用，公共在线支付开关均关闭。
- 导出附件命名与脱敏状态一致，默认和管理员明文路径均返回 HTTP 200；这仍不提供浏览器实际落盘文件证据。

## 2026-08-09：HTTP 下载文件字节证据

- 标准 HTTP 附件响应已下载到一次性临时文件并读取实际字节：`13651` 字节。
- SHA-256：`25273DD028EB26041A2ECA772FB017C8CAF6A53B8B38CE2E4E55F309E217E82D`。
- 内容检查：`sensitiveFieldsMasked=true`；手机号字段 27 个；未掩码数量 0；不含 `phoneNormalized`；临时文件已清理。
- 证据边界：这是 HTTP 响应文件的字节级证据，不是应用内浏览器标准 `download` 事件或 Downloads 目录落盘证据。

## 2026-08-09：应用内浏览器 URL 策略阻断复核

- 重新接管现有 `http://127.0.0.1:5185/#/pages/mine/mine` 标签时被应用内浏览器 URL 安全策略拒绝。
- 没有尝试绕过该策略，也没有读取 Cookie、存储或密码；因此无法新增 P2 浏览器下载和 P3 页面级网络失败/重复点击证据。

## 2026-08-09：续接后的自动化与本地运行态复跑

- 根目录 `verify`、管理端 UI 静态验收、管理端生产构建、C 端 H5 构建、后端 15 套件/68 项测试和 `git diff --check` 全部通过。
- `/api/health` 返回 HTTP 200，`status=ok,database=ok`；公共支付设置返回 HTTP 200，`onlineWechatEnabled=false`、`onlineAlipayEnabled=false`。
- 工作区 `.env` 中 `PAYMENT_ADAPTER=disabled`。管理 readiness 端点本轮因缺少 Bearer token 返回 401，不能以未授权请求推断其状态；既有授权 readiness 证据仍单独保留。

## 2026-08-09：P3 剩余缺口代码覆盖审查

- 管理端 `runOperation` 已使用同步 `operationKeyRef` 与 React state 双层请求锁；运营模块消息、积分、优惠、配置和反馈写操作均接入该统一入口。
- 管理端列表加载已区分首次加载失败与刷新失败：刷新失败保留最近成功数据，并显示局部错误提示和“重新加载”入口。
- 以上属于源码和静态验收证据；仍不能替代真实页面中的快速双击、网络中断后重试和旧数据恢复操作证据。

## 2026-08-09：发现学员档案专用操作锁竞态

- `operateStudentDetail` 原先只检查异步 React state `operationKey`，没有同步检查 `operationKeyRef`；其快速重复点击仍存在同一事件循环竞态。
- 该路径覆盖学员启停、默认关系、解除关系、授权关系和档案合并等高风险操作，需与通用 `runOperation` 使用同一同步互斥门禁。

## 2026-08-09：学员档案操作锁竞态修复结果

- `operateStudentDetail` 现在先检查 `operationKeyRef.current || operationKey`，再占用 `studentOperationKey`；finally 仅在 ref 仍属于当前 key 时清理。
- 修复覆盖学员启停、默认关系、解除关系、授权关系和档案合并路径，并补充 `verify-ui.mjs` 静态断言。
- 管理端 UI 静态验收、管理端构建、根目录 verify、后端全量 15 套件/68 项和 `git diff --check` 均通过。
- 证据边界不变：浏览器 URL 策略仍阻止独立页面双击验证。

## 2026-08-09：发现学员详情刷新缺少直接重试入口

- `operateStudentDetail` 能区分详情刷新失败和列表刷新失败，但 `DetailPanel`/`StudentProfileDetailPanel` 没有接收详情重载回调。
- 详情失败时当前提示要求用户“重新打开详情”，而列表失败有局部“重新加载”按钮；这使详情恢复路径不对称，影响学员关系/合并等写操作后的失败恢复闭环。

## 2026-08-09：学员详情刷新直接重试入口修复结果

- 新增 `reloadStudentDetail`，调用现有详情与报名履历接口并更新当前详情面板。
- `StudentProfileDetailPanel` 新增“重新加载详情”按钮，且在管理端写操作忙碌时禁用；详情/列表同时失败时提示用户分别重试。
- 静态验收、管理端构建、根目录 verify、后端全量回归和差异检查均通过。
- 仍缺可控浏览器中的真实详情接口失败注入与按钮点击证据。

## 2026-08-09：管理端写操作路径复核

- 课程、Banner、模板、支付设置、优惠、消息、配置、积分、反馈、订单审核、退款、开票、用户启停/重置和发票上传均已接入 `runOperation`；学员档案资料/关系/合并使用 `operateStudentDetail`。
- 课程图片和收款码上传使用独立上传函数，需单独确认是否有同步上传锁和重复点击保护。

## 2026-08-09：发现管理端上传操作未接入统一请求锁

- 课程图片上传的文件输入未使用 `submitting` 禁用，上传函数直接调用 `apiUpload`。
- 收款码文件输入使用 `busy` 禁用，但上传函数本身没有占用 `operationKeyRef`；与其他管理端写操作相比，仍存在快速触发多个上传请求的窗口。

## 2026-08-09：管理端上传重复提交门禁修复结果

- 课程图片和收款码上传均改为调用 `runOperation`，因此共享同步 `operationKeyRef`，并在请求期间由 busy/submitting 状态禁用相关输入。
- 管理端静态断言、TypeScript/Vite 构建、根目录 verify、后端全量回归和差异检查均通过。
- 该结果证明源码层和自动化门禁已覆盖上传锁；仍不等同于浏览器实际快速重复选择文件证据。

## 2026-08-09：C 端业务写操作锁复核

- C 端凭证上传、取消报名、开票申请和在线支付均使用 Vue `ref` 状态作为同步忙碌锁，并在请求期间禁用相应按钮/关闭动作。
- C 端 `loadAll` 失败时不会清空已有订单、发票、课程或预览数组；仍需确认模板是否在刷新失败时保留旧内容并提供明确的局部重试入口。

## 2026-08-09：C 端刷新恢复与写锁静态证据确认

- `business.vue` 在 `loadAll` 失败时保留已有数组，错误横幅提供“重新加载”；无数据首次失败时不会误显示“暂无记录”。
- 根目录 `mvp/scripts/verify.mjs` 已检查 `invoiceSubmitting`、`cancellingOrderId`、`payingOrderKey` 以及错误页重试文案和不清空 `orders` 的契约。
- 该结果是源码/静态证据；C 端真实断网后点击重试仍需可控浏览器运行态验证。

## 2026-08-09：C 端构建与服务器权限只读复核

- C 端 H5 构建通过；根目录 verify 的 C 端写锁、错误重试和支付状态门禁全部为 true。
- 本地 `/api/health` 返回 200、数据库为 ok；公共支付设置仍为 `onlineWechatEnabled=false`、`onlineAlipayEnabled=false`。
- `192.168.0.162:22` TCP 可达，但 `ssh-add -l` 报 SSH agent 不可用；这不能替代服务器 SSH 登录，P4 继续保持外部权限阻塞。

## 2026-08-09：管理端列表网络失败与恢复重试证据

- 管理端的未提交 `frontend/admin-react/.env.local` 指向 3199；若只启动固定 3100 API，管理端会显示 `Failed to fetch`，这属于本地运行配置不一致，不应误判为业务接口缺失。
- 真实管理端学员列表在 3199 API 停止期间点击“查询”，显示“刷新失败：Failed to fetch”，并保留刷新前的 5 条旧列表数据；恢复 API 后点击“重新加载”，列表恢复为 27 条。
- 该结果证明管理端刷新失败与旧数据恢复路径在当前页面可操作；它不证明 C 端故障恢复，也不证明所有写操作均已完成网络失败重试验收。
- 学员导出点击仍未产生应用内浏览器标准 `download` 事件；HTTP 响应字节级脱敏证据和浏览器落盘证据必须继续分开。

## 2026-08-09：5185 H5 运行态续接边界

- 服务层面 5185 仍在监听，不能据此推断页面级验收已经完成。
- 应用内浏览器重新建立后无法创建新的可控标签；本轮没有用替代 URL、其他浏览器或底层调试接口绕过该限制。
- 当前证据边界保持不变：既有修改密码确认 Portal 运行态证据可引用，但 C 端下载、断网重试和重复点击仍缺独立页面证据。

## 2026-08-09：C 端消息与课程详情旧数据保留修复

- 消息页原先在刷新失败时进入 `v-else-if="loadError"`，导致已有消息列表被错误隐藏；现改为“无旧数据显示完整错误，有旧数据显示局部错误并保留列表”。
- 课程详情页原先在加载失败时将 `course` 置空，导致刷新失败后详情消失；现只在首次无数据失败时显示完整错误，已有详情刷新失败时保留内容并提供重试。
- 静态门禁已覆盖两条契约；页面级断网注入和重试点击仍需可控浏览器会话，不能由构建结果替代。

## 2026-08-09：C 端学员关系重复提交缺口及修复

- `students.vue` 原先只有 `savingStudent`，且在确认弹窗返回后才设置；`makeDefault` 与 `removeStudent` 没有任何同步锁，快速点击可能在同一事件循环内发起多个请求。
- 现以页面级 `studentOperationKey` 作为同步门闩，保存/默认/解除分别使用唯一 key；锁覆盖确认等待期，并在 `finally` 中按 key 清理，避免旧操作误清理后续操作。
- 相关控件增加禁用/忙碌视觉与保存按钮 `disabled` 状态；静态门禁和 H5 构建通过。
- 该修复证明源码层竞态已收敛，不等同于应用内浏览器双击事件、请求次数或失败重试的独立运行态证据。

## 2026-08-09：C 端“我的”页确认前重复提交缺口及修复

- `mine.vue` 的资料保存、密码修改和反馈提交原先都在确认返回后才设置 busy 状态；反馈还允许在可编辑输入框等待期间再次打开。
- 现统一使用 `mineOperationKey`，在确认/输入等待前占用，所有提前返回和请求结束路径都通过 `finally` 释放；保存和关闭控件在锁定期间禁用。
- 这是与学员关系页相同的确认前同步门闩策略，静态门禁覆盖三条 key、释放语义和模板禁用状态。
- 回归验证通过；页面级双击和失败重试请求次数仍需可控浏览器证据，不能由静态检查替代。

## 2026-08-10：H5 服务恢复但页面接管仍受阻

- 本轮初检发现 5185 没有监听；按仓库现有启动脚本启动后，HTTP 根页面返回 200。
- 浏览器已有标签仍是启动前的 `ERR_CONNECTION_REFUSED` 错误页。尝试从该标签访问同一 `http://127.0.0.1:5185/#/pages/mine/mine` 时，浏览器安全策略阻止了从 `data:` 错误页导航。
- 这证明服务层已恢复，但不证明页面已加载，更不证明修改密码确认、学员关系、失败重试、重复点击或下载落盘路径；未采用任何绕过手段。

## 2026-08-10：新 H5 标签验证修改密码确认层级

- 在 H5 服务恢复后，新建应用内浏览器标签成功加载登录页；使用测试账号 `demo01` 登录并进入“我的 → 账号与安全”。
- 输入两次相同的新密码并点击“保存新密码”后，DOM 中同时存在父业务 `.modal-mask` 和共享确认 Portal。
- 计算样式证据：父业务弹窗 `z-index=90`；确认根 `2147483645`、遮罩 `2147483646`、内容 `2147483647`，且确认根挂载在 `HTML` 下。
- 点击“取消”后确认根数量为 0、业务弹窗数量为 1，页面仍包含“账号与安全”；未点击“确定”，未执行密码修改。

## 2026-08-10：学员解除关系确认前锁运行态证据

- 新 H5 标签进入 `/pages/students/students` 后，当前显示 2 条其他学员。
- 点击第一条“解除”后，确认 Portal 存在；DOM 中 6 个“编辑/设为默认/解除”操作均带 `action-disabled`，说明确认等待期已同步锁定页面操作。
- 未点击“确定”，没有执行解除关系写请求；保存和设为默认仍需独立取消/失败/重复点击证据。

## 2026-08-10：学员关系取消分支运行态证据补充

- 解除关系确认取消后，确认根节点消失，页面仍显示 2 个解除入口。
- 设为默认确认框能够打开；取消后页面恢复，未改变默认关系。
- 新增学员填写临时姓名并点击保存后，确认框能够打开；取消后保存编辑弹窗仍保留，未创建临时档案。
- 三条路径均未点击确认按钮，因此没有产生关系或档案写入；失败注入、双击请求数和成功后刷新对账仍未取得。

## 2026-08-10：3100 网络失败注入未生效

- 3100 监听 PID 为 Node 进程；停止该单个 PID 后，开发监视器自动生成新 PID 并恢复监听，健康接口仍为 200。
- 该尝试不能证明 C 端失败态，也不能替代页面级断网重试证据；未继续终止未知进程树。

## 本轮续接：应用内浏览器可控标签与下载证据

- 5185 服务已恢复且根页面 HTTP 200；3100 健康接口同时返回 HTTP 200、数据库状态 `ok`。
- 原有 H5 错误页标签的刷新动作仍被应用内浏览器 URL 安全策略拒绝。该失败只说明错误页恢复路径受策略限制，不说明 5185 服务不可用。
- 同一浏览器会话中的空白标签可以正常加载 `http://127.0.0.1:5185/`，并显示 H5 首页；进入“我的”页面后真实显示账号 `demo01`，因此后续 H5 验收应复用该标签，而不是从错误页导航。
- 管理端学员页的“导出”按钮可点击，但标准 `download` 事件等待 10 秒无事件；本轮未取得浏览器 Downloads 文件路径或文件字节。既有 HTTP 下载响应的文件名、脱敏正文和字节哈希证据仍不能升级为浏览器落盘证据。
- 本轮没有产生业务数据写入，也没有执行学员档案合并。

## 本轮补充：Downloads 导出字节证据

- 用户 Downloads 目录存在 `student-profiles-masked (5).json`，大小 `19910` bytes，写入时间为 `2026-08-09 23:50:50.8308766 +08:00`。
- 原始文件正文可直接检查：`exportedAt=2026-08-09T15:50:50.825Z`、`total=27`、手机号字段 27 个且未掩码数量为 0、`sensitiveFieldsMasked=true`、`truncated=false`，不含 `phoneNormalized`，JSON 以 `}` 结束。
- SHA-256 为 `C89AAD9D67619F0261B1672943690FF3BF2EF0599BFC452FFDF586A52942EFC0`。
- 因此 P2 的浏览器下载文件落盘/字节证据已经取得；应用内浏览器的 `download` 事件在当前重放中仍超时，不能把事件捕获缺失误写成文件未落盘。

## 本轮补充：H5 密码确认取消

- 可控 H5 标签真实进入“账号与安全”，打开保存新密码确认框。
- 确认根 `.client-confirm-root` 挂载在 `HTML`，层级为 `2147483645`；确认内容 `.client-confirm-dialog` 层级为 `2147483647`；父业务弹窗层级为 `90`。
- 点击“取消”后确认根移除、父业务弹窗保留，未执行密码修改写入。

## 本轮续接：C 端反馈快速双击

- H5 真实提交唯一临时反馈 `C端双击幂等验收-1786291386284`，最终确认按钮执行快速双击。
- 页面显示一次“反馈已提交”；随后只读查询 SQLite `Feedback` 表，唯一内容仅有 1 条记录：`FB-1786291402614-77aeb3`。
- 这是页面操作到数据库结果的端到端幂等证据；由于浏览器 API 不暴露网络计数，不能声称已取得请求数为 1 的底层证据。

## 本轮错误记录：独立 H5 故障注入启动失败

- 为不影响现有 5185，尝试在 5190 启动临时 uni-app H5 并将代理指向不可用 3198。
- 后台启动方式因 PowerShell `PATH` 环境键冲突失败；前台 pnpm/Vite 方式分别遇到 bin 不存在、5185 固定端口占用和 uni-app 依赖解析错误。
- 临时配置已删除，当前服务状态未被改变；该过程不产生 C 端网络失败证据。

## 2026-08-10：C 端消息页网络失败、旧数据保留与重试恢复证据

- 可控 H5 标签先加载消息页，基线显示 6 条消息。
- API 短时停止后，点击页面“刷新”真实得到 `刷新失败：请求失败：500`，并出现“重新加载”；消息列表仍保留 6 条旧记录，未退化为空列表。
- API 恢复后点击“重新加载”，错误提示消失，列表恢复显示 6 条消息；健康接口同时恢复为 HTTP 200、数据库 `ok`。
- 这是消息页级别的真实运行态证据。不能据此宣称课程详情、报名/订单、学员关系等所有 C 端页面均已完成相同门禁。

## 本轮新增边界：请求计数与服务器访问

- 浏览器页面评估环境不提供可用的 `performance` 对象，无法从页面资源条目可靠统计刷新双击产生的 API 请求次数；不以两次 UI 点击推断请求数。
- 反馈快速双击仍只有“页面一次提示 + 数据库最终一条记录”的端到端幂等证据。
- `192.168.0.162:22` 本轮只读 SSH 检查超时；没有猜测密码、读取凭据或执行远程修改，P4 服务器验收继续保持未完成。

## 2026-08-10：C 端课程详情刷新运行态证据

- 课程详情页此前已有“失败时保留 `course`”的代码契约，但缺少用户可触发的刷新入口；本轮新增顶部“刷新”入口，并在加载期间阻止重复刷新。
- 真实 H5 课程详情加载后，API 短时停止；点击“刷新”得到 `刷新失败：请求失败：500`，同时旧标题、费用、报名人数和课程简介仍可见。
- API 恢复后点击“重新加载”，错误提示消失，详情恢复；该证据覆盖课程详情页的旧内容保留与重试闭环。
- 不扩大解释为报名、订单、凭证和学员关系页面已完成相同门禁；这些页面仍需独立验证。

## 2026-08-10：C 端订单业务页刷新运行态证据

- 订单页原有 `loadAll` 错误提示和重试逻辑，但缺少正常状态下的刷新入口；本轮新增入口并增加 `loadInFlight` 门闩。
- 真实 H5 订单页加载 2 条订单后，API 短时停止；点击“刷新”显示业务数据失败和“重新加载”，订单标题、订单号、状态、金额及凭证提示仍保留。
- API 恢复后点击“重新加载”，错误提示消失，订单列表恢复；该证据覆盖订单业务页刷新失败恢复，不扩大解释为订单写操作门禁已全部完成。

## 2026-08-10：C 端学员档案刷新运行态证据

- 学员档案页原有失败后重试逻辑，但缺少正常状态下的刷新入口；本轮新增入口和 `loadInFlight` 门闩。
- 真实 H5 加载 2 条其他学员后，API 短时停止；点击“刷新”得到学员档案加载失败提示，2 条学员记录和编辑/默认/解除入口仍保留。
- API 恢复后点击“重新加载”，错误提示消失，列表恢复；该证据覆盖学员档案页刷新失败恢复，不扩大解释为关系写操作已全部验收。

## 2026-08-10：续接后浏览器标签恢复

- 旧的可控 H5 标签绑定返回“标签不属于当前浏览器会话”；读取浏览器故障规范后保留浏览器绑定并重新获取标签，未切换到替代浏览器，也未绕过安全策略。
- 新标签导航到 `http://127.0.0.1:5185/` 后，真实页面标题为“六边形培训”，DOM 可见课程卡片、搜索入口和底部“我的”，说明 H5 页面层已恢复可控。
- 本发现只证明页面连接和首页加载，不证明登录、学员关系写操作或管理端状态已完成。

## 2026-08-10：H5 测试会话进入“我的”

- 真实 DOM 显示账号 `demo01`（陈晓雯），并显示“我的学员”入口；当前标签已具备继续进行学员关系运行态验收的登录会话。
- 本记录不把入口存在视为写操作完成，后续仍需验证确认、请求结果、失败恢复和刷新对账。

## 2026-08-10：我的学员默认关系成功路径准备

- 真实页面显示 `demo01` 当前没有默认学员，另有 2 条 active 代理报名关系；本轮选择第一条关系做“设为默认”成功路径。
- 该动作不涉及学员档案合并；如需恢复原始默认关系，将在取得成功结果和刷新对账后再按实际状态处理。

- 第一条关系的确认框真实可见，文案明确说明默认关系会影响后续报名回填；本轮确认前未发生关系写入，具备继续验证成功路径的前置证据。

- 确认后真实页面显示“默认学员已更新”，演示学员2进入当前学员区并标记当前，演示学员8保留为其他学员；这是默认关系成功写入后的页面级运行态证据。

- 成功后点击页面刷新并重新读取真实列表，默认关系仍保持演示学员2，说明写入状态可从 API 刷新恢复；该证据不涉及档案合并。

- 新增学员入口含全角加号，精确文本“添加学员”无法匹配，正则可唯一定位“＋ 添加学员”；后续将使用唯一临时数据，避免误改现有档案。

- 新增学员表单真实显示独立档案保存说明和可选默认回填选项；本轮将使用唯一临时姓名/手机号，避免与演示学员或待合并档案混淆。

## 2026-08-10：uni-app H5 表单定位错误

- `getByPlaceholder` 无法命中新增学员输入框，因为运行时生成的 `<input class="uni-input-input">` 没有 placeholder 属性。
- 真实 DOM 顺序确认：`input[0]` 姓名、`input[1]` 手机号、`input[2]` 企业、之后依次为部门/职务/邮箱；后续使用该结构定位，不重复尝试失效占位符选择器。

- 唯一临时资料已填入但尚未写入；保存确认 Portal 真实可见，文案明确说明会更新学员档案，当前具备确认前证据。

- 保存确认后真实页面显示“学员已保存”，临时档案出现在其他学员列表；该成功写操作没有修改当前默认学员，也没有执行合并。

- 刷新后临时档案仍在其他学员列表，默认学员仍为演示学员2；这提供了保存写入可从 API 重新加载的运行态证据。

- 通过唯一临时姓名定位到对应行后，确认该行仅包含自身的解除入口；清理范围是 `demo01` 与临时档案的账号关系，不是档案合并。

- 临时关系解除成功，确认框明确保留历史报名记录；页面反馈“已解除关系”，清理没有影响当前默认学员或演示学员8。

- 刷新后临时档案关系仍不存在，默认学员仍为演示学员2；本轮新增的临时账号关系已完成可验证清理，未执行合并。

## 2026-08-10：默认关系双击事件边界

- 浏览器控制层的 `dblclick` 对 uni-app H5 “设为默认”按钮没有触发确认框，页面保持演示学员2为当前学员；不能据此推导底层请求次数或门闩有效。
- 将以两个紧邻的普通点击作为替代尝试，并在出现确认框后取消，避免写入演示学员8。

- 两个紧邻点击未改变页面，不能证明重复请求门闩；按钮 DOM 为可见的 `<span>设为默认</span>`，控制台无异常。当前把该项保留为未闭环，避免用静态门禁替代运行态证据。

- 通过父级操作节点触发“设为默认”后，确认 Portal 正常出现；点击取消后 Portal 消失，演示学员2仍是当前学员。该证据覆盖取消分支，不覆盖底层请求计数或快速双击请求去重。

- 新增学员失败恢复测试已重新打开空表单，暂未提交任何有效档案；预期先验证姓名校验错误，再使用唯一临时数据恢复保存。

- 空姓名校验失败后真实 DOM 同时包含错误段落和“新增学员”表单，未创建新档案；该证据覆盖保存失败后的输入恢复前置状态。

- 失败后填入第二个唯一临时资料，保存确认框再次出现，说明表单没有被失败路径锁死或关闭；该恢复路径尚未点击最终确认。

- 失败恢复后的最终确认成功，临时学员 02 出现在其他学员列表；该路径没有改变演示学员2的默认关系。

- 临时学员 02 的解除入口已按唯一行定位，确认框文案与第一次清理一致；下一步完成确认并刷新对账。

- 临时学员 02 解除成功并在刷新后保持移除，默认学员仍为演示学员2；本轮所有临时测试关系均已清理。

## 2026-08-10：P2 学员关系成功/失败恢复运行态证据

- 默认关系：确认“设为默认”后演示学员2进入当前区，刷新后仍保持当前。
- 保存关系：唯一临时学员 01 保存成功、刷新后仍存在；随后解除并刷新清理。空姓名校验失败后表单保留，修正为唯一临时学员 02 后再次保存成功，再解除并刷新清理。
- 解除关系：确认文案说明历史报名记录保留，成功后页面反馈“已解除关系”，刷新后关系仍移除。
- 快速双击：当前浏览器控制层的 `dblclick` 和直接 span 点击没有形成可靠确认/请求证据；父级节点点击可打开确认框，取消后状态不变。未把静态门禁或 UI 点击次数推断成底层请求次数。

## 2026-08-10：自动化复跑与服务状态

- `pnpm.cmd run verify`、后端 15 套件/68 测试、管理端 UI 静态验收、C 端 H5 构建、`git diff --check` 均通过。
- API 3100 健康检查为 HTTP 200、数据库 `ok`；H5 5185 根页面为 HTTP 200；`PAYMENT_ADAPTER=disabled` 未改变。
- 管理端 5174 未恢复监听；启动方式触发当前 PowerShell 环境的重复 `Path/PATH` 键错误，未修改环境变量或配置来掩盖该边界。

## 2026-08-10：管理端服务延迟恢复与登录权限边界

- 延迟启动完成后，管理端 `http://127.0.0.1:5174/` 和配置 API `http://127.0.0.1:3199/api/health` 均可访问；真实登录页显示管理账号、密码和登录按钮。
- 当前没有已授权的管理端测试凭据；历史 `admin/admin` 失败结果继续保留，不猜密码、不重复尝试，也不把登录页存在当作管理端业务验收完成。

## 2026-08-10：非支付开关与未授权管理端边界

- 配置 API `3199` 的 `/api/payment-settings/public` 返回 HTTP 200，在线微信和支付宝开关均为 `false`；该运行态证据与工作区 `PAYMENT_ADAPTER=disabled` 一致。
- 未携带 Bearer token 访问 `/api/admin/integration-readiness`、`/api/admin/student-profiles` 均返回 HTTP 401；这证明权限门禁存在，但不替代已授权管理端业务验收或 readiness 内容审计。

## 2026-08-10：管理端真实登录恢复

- README 明确规定演示管理账号为 `admin/123456`；用该凭据登录本地 5174 后，真实 DOM 显示工作台和完整管理导航。
- 当前管理端业务运行态验收具备授权会话；后续仍需逐项记录真实请求结果、失败恢复和审计变化，不能把登录成功等同于 P1 完成。

- 订单导航真实 DOM 是带 `title="订单与支付"` 的按钮，第一次点击未造成可见路由/标签变化；这不是订单功能失败证据，后续改用精确按钮和状态检查。

- 精确按钮点击和 DOM CUA 节点点击均没有产生订单页或访问标签，页面仍为工作台首页；当前记录为标签交互异常，不把它升级为订单业务缺陷。

## 2026-08-10：续接后服务占用与 H5 页面状态

- `Invoke-WebRequest` 实测 `http://127.0.0.1:3100/api/health`、`http://127.0.0.1:3199/api/health`、`http://127.0.0.1:5174/`、`http://127.0.0.1:5185/` 均返回 HTTP 200；两个 API 的健康正文均为 `status=ok,database=ok`。
- 重新启动尝试遇到已有监听：API 3100/3199、管理端 5174 和 H5 5185 均出现 `EADDRINUSE` 或 Vite 端口占用提示；该结果说明旧服务仍在运行，不是服务未启动证据。
- 应用内浏览器接管现有 H5“我的学员”页面后，真实 DOM 显示当前学员为空、其他学员 2 人（演示学员2、演示学员8），可继续使用既有登录会话。
- 当前证据仍未证明账号安全弹窗覆盖关系，也未扩展为管理端订单业务完成；下一步必须取得“保存新密码”确认 Portal 的实际可见性和取消分支证据。

## 2026-08-10：C 端修改密码确认层级真实验证

- 真实 H5 页面进入“账号与安全”，两个密码输入框均填入临时一致值后点击“保存新密码”。
- 页面截图显示“确认修改密码”确认框位于账号与安全业务弹窗之上，遮罩覆盖底层页面，取消和确定按钮真实可见。
- 通过可见 DOM 节点点击“取消”后，确认层消失，账号与安全表单仍在；没有点击最终确定，因此本轮没有执行密码修改。
- 该项现在具备运行态覆盖关系和取消分支证据；不把它扩大解释为密码修改成功或其他页面确认入口已全部验收。

## 2026-08-10：P1 管理端订单退款与关闭

- 管理端订单模块需要先展开“报名与交易”，订单入口点击后创建“订单与支付”标签；重新登录后直接切换该标签可进入订单列表。
- 退款订单 `HX-1786204869428-cecd61`：已支付 → 确认退款 → 页面提示退款并回退名额 → 刷新后已取消，退款按钮不再出现。
- 待支付订单 `HX-1785832507481-c41cc5`：确认关闭 → 页面提示关闭并回退名额 → 待支付列表 8 条变 7 条 → 刷新后订单不再出现在待支付列表。
- 当前证据证明成功写入和刷新持久化，不证明重复退款/关闭请求的底层失败响应；该点保留为独立待办。

## 2026-08-10：线下凭证审核的种子文件缺失

- 管理端筛选“待审核”后唯一订单为 `ORDER-DEMO-002`；点击“审核凭证”真实返回“支付凭证文件不存在”。
- 数据库种子 `backend/api/prisma/seed.js` 为该订单引用 `payment-proofs/demo-proof-002.png`，但当前 `backend/api/storage/payment-proofs` 只有 `.gitkeep` 和运行中生成的测试文件，没有 `demo-proof-002.png`。
- 这属于本地演示数据资产缺失，不把它误判为审核接口成功/失败路径已完成；先补齐与种子引用一致的本地演示 PNG，再重新验证审核流程。

## 2026-08-10：线下凭证审核驳回与重新上传

- 以种子引用的 `demo-proof-002.png` 补齐本地演示资产后，`ORDER-DEMO-002` 凭证审核弹窗可正常预览。
- 管理端驳回时必须填写原因；填写“凭证验收驳回：测试文件不清晰”并确认后，订单从“待审核”离开列表，页面提示“支付凭证已驳回”。
- 同一 H5 `demo01` 会话显示订单回到“待支付”、保留驳回原因；上传新的本地测试 PNG 并确认后，订单变为“待审核”，页面显示等待审核提示且隐藏重复提交入口。
- 文件选择器超时的首次尝试发生在尚未打开上传弹窗时；按页面真实状态拆成“打开弹窗 → 选择图片 → 提交确认”后成功，不把首次异常记录为业务上传失败。

## 2026-08-10：P1 凭证通过、审计与开票失败恢复

- `ORDER-DEMO-002` 凭证审核通过后，管理端搜索显示“已支付 / offline”；C 端刷新显示“已支付”并出现开票入口。
- 操作审计真实记录本轮 `退款完成`、`关闭待支付订单`、`线下支付驳回`、`上传支付凭证`、`线下支付审核通过`；筛选“退款完成”可得到 11 条结果。
- `INV-DEMO-005` 通过发票号处理并上传合法 PNG，刷新后保持“已上传”。
- `INV-DEMO-006` 无效文本文件被真实拒绝，随后合法 PNG 上传成功并在刷新后保持“已上传”，完成开票文件失败→恢复路径。

## 2026-08-10：续接本地服务与浏览器会话

- 本地服务已有实例仍在运行；健康检查返回 `status=ok,database=ok`，重复启动只产生端口占用，不构成服务失败。
- 管理端现有标签可见开票管理列表，H5 现有标签可见订单页；浏览器会话未丢失，可继续做运行态验收。
- 当前不把“标签存在”扩大解释为新业务通过；后续仍需逐项取得 P2 跨账号/导出/审计与 P3 运营模块的成功、失败、取消、刷新和幂等证据。

## 2026-08-10：P2 账号停用与旧令牌运行态验收

- `demo01` 旧访问令牌在账号停用期间访问 `/profile` 返回 401；恢复启用后该旧令牌仍为 401，新登录为 200。
- 本轮操作审计真实记录 `禁用用户`、`启用用户`，账号已恢复为启用，不留下停用测试状态。
- 该证据覆盖账号状态与旧令牌失效，不扩大解释为跨账号订单/学员数据访问；后者仍需使用两个账号的实际资源 ID 单独验证。

## 2026-08-10：P2 跨账号读取与脱敏导出运行态验收

- `demo01` 与 `demo02` 的订单列表相互隔离：对方订单号查询返回 0 条，跨账号支付状态读取返回 404；本人列表可正常返回。
- 脱敏导出默认掩码手机号；API 的 `reveal=true` 由 `request.user.role === 'admin'` 控制，因此当前 `operator`（数据库角色同为 admin）也能明文导出。标准管理端导出按钮只调用脱敏参数路径；这不是本轮擅自修改的权限策略，已作为发布前权限决策项保留。
- 导出和账号启停均写入操作审计；未授权导出返回 401。

## 2026-08-10：P3 反馈管理取消、处理与刷新运行态验收

- `FB-DEMO-006` 的处理弹窗取消后状态未变；确认回复后状态变为“已处理”，回复内容真实显示。
- 查询刷新后该反馈仍为“已处理”，说明管理端成功写入和刷新持久化有效；该项未扩展为所有 P3 模块已完成。

## 2026-08-10：P3 消息与积分管理运行态验收

- 消息启用/停用均有二次确认；取消不改变状态，启用后再停用并刷新，演示消息最终恢复为停用。
- 积分调整确认后能实时更新列表和流水；`u-demo-02` 由 52 临时加到 53，再扣回 52，刷新后持久化为 52。
- 一次连续自动化操作在管理端会话回到登录页时发生选择器超时；读取页面后重新登录，拆分确认步骤复跑成功，未重复提交未知状态。
- 审计筛选分别返回：处理反馈 2 条、启用消息 1 条、停用消息 2 条、用户积分调整 4 条（含本轮记录）。

## 2026-08-10：本轮验收复核

- 根目录 `pnpm.cmd run verify` 通过，客户端确认层、写操作门闩、刷新保留旧数据、管理端错误重试等静态门禁均为 true。
- `git diff --check` 无错误；LF/CRLF 提示属于工作区既有换行转换提示。

## 2026-08-10：5185 H5 重启后真实运行态证据

- 5185 进程被安全地按监听端口精确定位并重启；新 Vite 实例 ready，HTTP 根页面 200。
- 重启后的 H5 登录页没有保留旧登录态；重新登录 `demo01` 后真实进入首页和“我的”。这证明验证发生在重启后的新页面实例，而不是旧 DOM。
- “确认修改密码”截图显示确认层和遮罩压在“账号与安全”业务弹窗上方，取消/确定按钮可见；点击取消后确认层节点消失，业务弹窗和两个密码输入值仍在，未改变密码。
- 标签接管过程中旧标签句柄失效；使用 `user.openTabs()` 精确匹配当前 5185 标签并 `claimTab` 成功，未读取浏览器存储或使用替代控制面。

## 2026-08-10：P1 当前运行 API 失败态证据

- 真实 3199 API 对已完成订单/发票的重复审核、重复退款、重复关闭和重复开票处理均返回 HTTP 400，错误信息分别指向状态不允许重复操作。
- `p1-write-operations.spec.ts` 在隔离测试库中 2/2 通过；运行 API 的失败请求不改变已完成状态。
- 审计查询可正常返回 HTTP 200；重复失败请求本身不伪造成功审计，成功动作的审计记录沿用已有证据。

## 2026-08-10：非支付内部测试发布冻结结论

- 已形成最终本地发布报告和支付后续接入清单。当前证据支持进入本地/内网非支付内部测试，不支持生产部署或目标服务器发布。
- `PAYMENT_ADAPTER=disabled`、微信/支付宝在线开关为 `false`、API/管理端/H5 健康检查和自动化回归均已记录；本轮没有调用真实支付渠道。
- 学员档案合并仍未执行，原因是缺少用户明确确认；这不是技术失败，也不能通过默认确认替代授权。
- 页面级快速双击的底层请求计数在当前浏览器评估环境中无法可靠取得；已保留静态同步门闩、失败恢复和数据库幂等证据，但不声称请求次数为 1。
- P4 目标服务器仍受 SSH 权限阻塞；不猜测凭据、不远程写入，不能把本地运行态结果扩大为服务器部署验收。
- 当前数据库中 `operator` 与 `admin` 均为 `admin` 角色，`reveal=true` 可返回明文导出；已作为生产前权限决策项保留，未在本轮擅自调整角色或数据。

## 2026-08-10：operator 敏感字段权限修复与真实运行态验证

- 根因确认：JWT 和登录服务把所有非 `admin` 值归一为 `user`，同时演示数据库把 `operator` 存成 `admin`，因此无法表达“可管理但不可明文揭示”的边界。
- 已新增角色规范化：`admin`、`operator`、`user`；`AdminGuard` 允许 admin/operator，普通 user 仍被拒绝。管理端登录和用户角色筛选同步支持 operator。
- 学员档案详情、报名履历详情和导出接口均以 `request.user.role === 'admin'` 作为明文揭示门禁；operator 的 `reveal=true` 被强制降级为脱敏响应。
- 真实本地库已将 `u-operator` 从 `admin` 校正为 `operator`，并递增 `sessionVersion`；3199 登录返回 `operator`，dashboard 返回 200，operator 详情与导出返回掩码手机号和 `student-profiles-masked.json`。
- 管理员详情仍返回明文手机号；权限、迁移、生产和 MVP 相关 38 个测试通过，API 构建通过。
- 该权限决策项已从发布阻断中移除；剩余阻断为页面级底层双击请求统计、学员合并明确确认和目标服务器 P4。
## 2026-08-10：反馈最终确认快速双击证据更新

- 先刷新 H5 标签再定位父级 `uni-view.menu-row`，可稳定打开“问题反馈”弹窗；此前未刷新时的点击无效属于标签/交互时序问题，不能作为业务失败证据。
- 真实页面流程为“填写反馈 → 首层确定 → 自定义二次确认 → 最终确定快速双击”；最终页面只显示一次“反馈已提交”。
- 本地 SQLite 只读查询确认测试内容 `页面级快速双击幂等性验证-20260810` 仅有 1 条 `Feedback` 记录，ID `FB-1786305748966-315ff2`，状态“待处理”。
- 该结果证明本次快速双击没有造成重复反馈落库；底层浏览器网络请求次数仍无法可靠统计，因此保留“请求计数未证明”的边界。
## 2026-08-10：H5 标签绑定恢复后的当前事实

- 应用内浏览器当前存在一个旧错误页标签和一个真实“我的”页标签；应丢弃旧绑定并接管真实标签。
- 接管后的 DOM 与截图均显示 `demo01` 的“我的”页面，说明 5185 服务、登录态和页面渲染可继续用于运行态验收。
## 2026-08-10：管理端当前可控会话

- 现有管理端标签不是登录页，而是已授权的订单与支付页面；可继续使用其真实业务会话进行 P1 运行态验收。
- 订单列表分页、状态筛选、详情按钮和审计入口均在 DOM 中可见；下一步选择报名明细故障注入/重试路径，并记录数据刷新与审计边界。
## 2026-08-10：报名明细页面基线事实

- 当前管理端报名明细页可控且已加载真实数据；页面提供“查询”按钮和分页，详情操作为唯一“查看详情”按钮。
- 目标验证是接口失败后保留旧列表、显示明确错误和重试入口，恢复服务后重试并核对记录；需要在不污染业务数据的前提下执行。
## 2026-08-10：报名明细断网失败态证据

- 3199 进程停止后，管理端页面的请求失败提示为 `刷新失败：Failed to fetch`，并提供唯一“重新加载”入口。
- 页面仍保留原有 5 条报名明细，而不是误显示空列表；这与 `frontend/admin-react` 的保留旧数据契约一致。
- 当前故障注入是本地可恢复运行态证据，未产生业务写入或数据污染。
## 2026-08-10：报名明细失败恢复结果

- 3199 恢复后服务健康；页面重试成功，错误提示和重试按钮均移除，列表恢复到 29 条。
- 该证据覆盖“已加载数据保留 → API 失败提示 → 重试恢复”的真实管理端页面路径，强度高于仅有静态门禁或接口测试。
- 仍需单独确认管理端审计页面对成功业务操作的可见性；失败请求本身不应伪造成功审计记录。
## 2026-08-10：管理端审计页面事实

- 审计页不是只读空壳：当前真实返回 230 条记录，支持按操作类型、操作者和时间过滤。
- 首屏已看到 `提交反馈`、`导出学员档案`、`查看学员档案` 等记录；下一步查询 `退款完成` 或 `关闭待支付订单`，验证 P1 操作记录可定位。
## 2026-08-10：退款审计筛选结果

- “退款完成”筛选返回 11 条真实审计记录，包含本轮已有订单退款记录；页面显示操作人、订单详情和时间。
- 审计筛选路径可用，且没有把报名明细断网失败误记成成功业务操作；P1 的页面失败恢复和审计可见性证据现已分别成立。
## 2026-08-10：当前门禁与支付边界

- 本轮运行态故障注入恢复后，自动化和构建门禁未回归；后端仍为 15/15 套件、68/68 测试通过。
- 3199 公共支付设置仍明确关闭；没有启用真实微信/支付宝，也没有执行学员档案合并。
- 当前证据支持本地/内网非支付内部测试，不支持目标服务器生产发布。

## 2026-08-10：续接后浏览器会话事实

- 通过现有应用内浏览器标签重新接管管理端 5174（标签 `52`）和 H5 5185（标签 `54`），未读取 Cookie、Storage 或其他浏览器凭据。
- 管理端当前位于“操作审计”，筛选“退款完成”真实返回 11 条记录；页面显示操作人、订单编号和时间。
- H5 当前为 `demo01` 的“我的”页，页面可见“账号与安全”“我的学员”“问题反馈”等入口；此前的 5185 重启后确认层级证据仍可复用，不重复执行。
- 本轮后续优先补 P3 运营模块的失败恢复/重试和重复提交边界；P4 目标服务器仍不具备远程验收权限，不以本地结果替代。

## 2026-08-10：P3 反馈模块入口初查

- 管理端当前操作审计页的 DOM 中可见唯一“反馈管理”按钮；按可访问名称点击后页面仍停留在操作审计，未据此判断反馈接口失败。
- 该现象与此前订单导航首次点击未切换的交互时序问题相似，下一步改用精确按钮定位/页面刷新后的分步操作诊断；不重复提交业务数据。

## 2026-08-10：P3 反馈列表断网失败态

- 确认 3199 监听 PID 为 `24252` 后，仅停止该单个 Node 进程；没有停止 3100、5174、5185，也没有执行数据库写操作。
- 反馈管理点击“查询”后真实显示 `刷新失败：Failed to fetch` 与“重新加载”，原有 5 条反馈记录仍保留；失败请求没有把列表错误清空。
- 使用 `backend/api/dist/main.js`、`PORT=3199` 恢复服务时，第一次误以仓库根目录为工作目录，服务解析到新建的空 `data/training.db`，导致管理端进入登录页；该实例已停止，未用于业务验收。随后按 `backend/api` 工作目录重新启动并恢复授权。

## 2026-08-10：P3 反馈服务恢复边界

- 首次从仓库根目录启动 `dist/main.js` 会解析到根目录空的 `data/training.db`，导致 `admin/123456` 无法登录；已停止该错误实例，未把空数据库用于验收，并清理了本次误启动产生的零字节数据库文件。
- 按 `backend/api` 作为工作目录、`PORT=3199` 重新启动后，健康检查正常，使用 README 记录的 `admin/123456` 登录成功，工作台回到原有演示数据（课程 7、报名人数 29、已支付 11、待支付 7）。
- 一次反馈页入口定位因正则同时命中页面按钮和“关闭反馈管理标签”按钮而失败；改用精确可访问名称，不修改业务代码。

## 2026-08-10：本轮自动化与发布边界复核

- 根目录 `pnpm.cmd run verify` 通过，所有静态门禁为 `true`。
- 后端 Jest 15 个测试套件、68 个测试全部通过；管理端 `test:ui`、管理端生产构建和 C 端 H5 构建全部通过。
- `git diff --check` 通过；仅有既有 LF/CRLF 转换提示。
- 运行态：3100、3199、5174、5185 均在监听；3199 `/api/health` 返回 HTTP 200 且 `status=ok,database=ok`。
- 根目录 `.env` 明确为 `PAYMENT_ADAPTER=disabled`；3199 `/api/payment-settings/public` 返回 HTTP 200，`onlineWechatEnabled=false`、`onlineAlipayEnabled=false`。
- 本轮未启用真实微信/支付宝、未执行学员档案合并、未向目标服务器写入；发布结论仍限于本地/内网非支付内部测试。

## 2026-08-10：新一轮 5185 H5 重启后会话事实

- 当前 5185 H5 根页面 HTTP 200；应用内浏览器中从现有真实标签 `57` 重新接管 `http://127.0.0.1:5185/#/pages/mine/mine`，未读取 Cookie、Storage 或其他浏览器凭据。
- 重启后的实际页面显示账号 `demo01`、积分 36、消息 6，以及“账号与安全”“我的学员”“问题反馈”等入口；本轮将继续验证确认层取消分支，不执行真实密码修改。

## 2026-08-10：H5 账号安全入口定位

- 直接点击可见文本“账号与安全”未触发页面切换；这属于 uni-app H5 文本节点与父级交互节点的定位差异，未据此判断业务失败。
- 页面中真实存在 6 个 `uni-view.menu-row` 父级入口，后续按父级行精确匹配“账号与安全”继续操作。

## 2026-08-10：H5 账号安全父级行结构

- 父级行文本真实为“账号与安全登录账号和密码›”，已确认唯一匹配行；常规 Playwright `click()` 仍未打开业务弹窗。
- 该交互时序/事件触发问题需要进一步检查页面实际可点击节点或使用浏览器支持的 tap 语义；未继续重复提交或修改密码。

## 2026-08-10：H5 入口事件诊断错误

- 读取“账号与安全”行的真实 HTML，确认其由 `uni-view.menu-row` 包含 `menu-title`、`menu-hint` 和箭头组成。
- 尝试在页面上下文调用 `el.click()` 失败，因为该抽象节点没有可调用的 DOM `click` 方法；未继续使用绕过式脚本，改回浏览器原生交互能力寻找 tap 语义。

## 2026-08-10：H5 浏览器交互能力核对

- 当前浏览器控制层提供 `locator`、`getByText`、`getByRole`、页面截图和 DOM 快照等能力；未发现独立的 tap 方法。
- 后续将检查 locator 的原生交互方法和元素可点击状态；仍限定在可见页面交互，不读取浏览器存储或使用 CDP。

## 2026-08-10：H5 账号安全行状态

- 账号安全父级行唯一匹配，`count=1`、可见且可用；定位器确实公开原生 `click` 方法。
- 但 click 后页面未变化，说明问题更可能是 uni-app 事件绑定/当前页面状态或交互桥接，而不是节点不存在；继续保持不提交密码的范围。

## 2026-08-10：H5 账号安全交互层进一步证据

- 截图显示“账号与安全”整行在可视页面中，父级行 `pointer-events=auto`，位置和尺寸正常，没有发现遮罩覆盖。
- 键盘 `Enter` 语义操作因应用内浏览器焦点目标不匹配而失败；该失败未改变页面和数据。当前证据指向 uni-app H5 事件桥接/标签状态问题，尚不能把确认层验证扩展为本轮新成功证据。

## 2026-08-10：H5 刷新后账号安全弹窗恢复

- 对真实 5185 H5 标签执行页面刷新后，重新按 `uni-view.menu-row` 父级行点击“账号与安全”，弹窗成功打开。
- 弹窗真实显示账号 `demo01`、两个密码输入框和“保存新密码”；这证明刷新后页面交互恢复，前一轮未切换是页面交互状态问题。

## 2026-08-10：管理端 P3 会话恢复

- 当前管理端真实标签可重新接管，停留在“反馈管理”，已授权会话显示 14 条反馈；首两条为待处理测试记录，未新增或处理数据。
- 下一步从现有会话继续 P3 消息/积分/优惠/配置模块的页面级失败和重复操作验证。

## 2026-08-10：管理端消息模块基线

- 展开“用户与运营”后，精确点击“消息通知”成功切换真实标签；列表返回 7 条消息。
- 首行是既有临时测试消息 `MSG-1786282970119-152ce0`，当前启用状态为“否”，可用于启用后恢复原状态的重复点击/确认分支；本轮尚未执行写操作。

## 2026-08-10：管理端消息启用快速双击与恢复

- 对既有停用临时消息 `MSG-1786282970119-152ce0` 打开“启用消息”确认层，并对最终“确定”执行快速双击；页面只出现一次“消息已启用”，列表状态从“否”变为“是”。
- 随后通过一次“停用消息”确认恢复为“否”，列表仍为 7 条，未新增消息；该证据支持页面级确认重复点击门禁和状态恢复。
- 该结果不声称底层请求次数为 1；请求次数仍未在当前浏览器环境可靠统计。

## 2026-08-10：管理端消息列表故障注入准备

- 确认 3199 当前监听 PID 为 `6920` 后，仅停止该单个 Node 进程；未停止 3100、5174、5185，也未执行数据库写操作。
- 下一步在已加载的消息列表点击“查询”，验证失败提示、旧列表保留和重试入口；服务恢复后再点击重试。

## 2026-08-10：管理端消息列表失败与重试恢复

- 停止 3199 后点击消息页“查询”，页面真实显示 `刷新失败：Failed to fetch` 与“重新加载”，原有 5 条消息及其启用状态仍保留。
- 按 `backend/api` 工作目录恢复 3199，健康检查返回 HTTP 200、`status=ok,database=ok`；点击“重新加载”后错误提示消失，列表恢复为 7 条，临时消息仍为停用。
- 该证据覆盖 P3 消息列表的失败、旧数据保留、直接重试、刷新恢复和数据状态不变；未产生额外业务写入。

## 2026-08-10：H5 密码保存入口定位边界

- 两个密码输入框均已真实填入临时值 `TestPassword-2026`；未点击最终确认。
- 直接按可见文本点击“保存新密码”未弹出确认层，随后读取该文本节点 HTML 的定位操作超时；未继续重复点击或提交密码，暂不把它判断为业务保存失败。

## 2026-08-10：密码保存按钮实际状态

- 页面 DOM 显示“保存新密码”对应的真实节点是 `<uni-button class="primary-btn" disabled="true" loading="true">`；文本点击可能已进入操作忙状态，但确认层未在快照中出现。
- 当前不再点击该按钮，先等待并读取可见状态，避免在未知请求状态下重复提交或修改密码。

## 2026-08-10：密码保存按钮忙状态未解除

- 等待 2.5 秒后，页面仍停留在账号安全弹窗；保存按钮仍带 `disabled="true"`/`loading="true"`，但没有确认层或成功/失败提示出现在 DOM 快照中。
- 当前未再次点击，也未点击任何最终确认；需要通过截图确认是否存在未被快照暴露的确认层，并继续记录为潜在交互/忙状态异常。

## 2026-08-10：H5 密码确认层截图证据

- 页面截图真实显示“确认修改密码”确认层及遮罩，位于“账号与安全”业务弹窗之上；“取消”和“确定”按钮均可见。
- DOM 快照没有暴露该系统确认层，但截图证实其实际可见；当前仍未点击最终“确定”，下一步只点击“取消”验证表单保留。

## 2026-08-10：H5 密码确认层取消分支闭环

- “取消”按钮唯一可见，点击后确认层消失；账号与安全业务弹窗仍保留，两个密码输入框和临时值仍在，未提交密码修改。
- 该证据覆盖重启后真实 H5 页面、确认层可见性、取消安全分支和表单保留；不扩大解释为密码修改成功。

## 2026-08-10：本轮最终回归核对

- `pnpm.cmd run verify` 和 `git diff --check` 通过；3199 健康检查为 `status=ok,database=ok`。
- 公共支付设置仍为 `onlineWechatEnabled=false`、`onlineAlipayEnabled=false`，四个本地端口 3100/3199/5174/5185 均在监听。
- 当前证据支持本地/内网非支付内部测试；未执行真实支付、学员档案合并或目标服务器写入。

## 2026-08-10：新一轮管理端标签恢复

- 旧管理端标签绑定失效后，从现有用户标签重新接管 `58`；真实消息通知页仍显示 7 条，临时消息状态为“否”。
- 未读取浏览器存储、未重新初始化服务；后续可继续在该授权页面验证 P3 其他模块。

## 2026-08-10：系统配置模块基线

- 展开“系统管理”并进入“系统配置”成功，真实列表返回 6 条配置；`invoiceDays` 当前值为 `7`。
- 本轮选择修改既有 `invoiceDays` 并恢复原值，验证取消、确认、刷新和重复提交边界，不新增配置项。

## 2026-08-10：系统配置非法值分支

- 编辑既有 `invoiceDays=7`，将配置值临时填为 `not-a-number` 后点击保存，页面先进入“操作确认”层；未点击确认，因此没有把非法值写入数据库。
- 下一步取消确认并关闭编辑，随后以合法值变更/恢复验证保存、刷新和审计边界；该现象说明当前校验在确认前未阻止字符串输入，需保留为产品/接口边界观察。

## 2026-08-10：系统配置非法值取消恢复

- 取消操作确认后，编辑弹窗仍保留非法临时值；再取消编辑后回到列表，`invoiceDays` 仍为 `7`。
- 未产生系统配置写入或审计副作用；该路径覆盖非法输入的确认取消和编辑取消安全分支。

## 2026-08-10：系统配置列表故障注入准备

- 确认 3199 当前监听 PID 为 `4188` 后，仅停止该单个 Node 进程；未停止其他服务，未执行配置写入。
- 下一步在已加载的系统配置列表点击“查询”，验证旧配置保留和重试恢复。

## 2026-08-10：系统配置列表失败与重试恢复

- 3199 停止后点击系统配置“查询”，真实显示 `刷新失败：Failed to fetch` 和“重新加载”，原有 5 条配置仍保留，`invoiceDays=7` 未变。
- 按 `backend/api` 工作目录恢复 API，健康检查返回 HTTP 200、数据库 `ok`；点击“重新加载”后错误消失，列表恢复 6 条，配置值仍为原值。
- 本轮未产生配置写入；该证据覆盖系统配置读取失败、旧数据保留、重试恢复和刷新对账。

## 2026-08-10：本轮 P3 回归与发布边界

- 根目录 `verify`、后端 15/68、管理端 UI 校验、两端构建和 `git diff --check` 全部通过。
- 3199 数据库健康为 `ok`，公共支付开关仍为 `false`/`false`；本轮未启用真实支付、未执行学员合并、未远程写入。
- 当前最明确的剩余工作是 P3 积分/优惠及其他重复提交边界；P4 目标服务器仍需真实 SSH 权限，P5 只能批准本地/内网非支付内部测试。

## 2026-08-10：继续执行前的浏览器会话

- 真实管理端标签已重新接管，未读取 Cookie、Storage 或其他浏览器凭据。
- 当前页面为系统配置，导航可见“积分管理”和“运营优惠”；系统配置列表与既有证据一致，`invoiceDays=7`。

## 2026-08-10：积分入口定位

- “积分管理”定位器唯一匹配，外层 HTML 为 `<button title="积分管理">`，按钮可见。
- 一次普通点击未切换页面；这只是入口交互定位现象，尚未执行任何积分写操作。

## 2026-08-10：积分入口第二次交互观察

- 强制 UI 点击后 URL 仍为管理端 SPA 的 `/login`，页面 DOM 仍为系统配置；这表明当前浏览器会话的菜单点击事件未切换当前模块。
- 代码静态核对显示积分模块真实接口为 `/admin/points`，但未用静态结果替代页面验收；下一步继续通过可访问键盘交互或重新接管标签恢复模块。

## 2026-08-10：积分入口键盘交互结果

- `Enter` 后积分按钮出现活动态标记，但主内容仍为系统配置，说明该次键盘事件没有完成 `navigate` 状态切换。
- 静态代码显示导航按钮的 `onClick={() => navigate(item.key)}` 与积分端点存在；保留该异常作为浏览器交互证据，继续采用页面内已访问标签作对照。

## 2026-08-10：积分管理页面已进入

- 页面级真实列表加载成功，共 14 条；第一行 `u-demo / 培训用户 / 128`，具备“调整积分”和“查看流水”操作。
- 入口异常已定位为导航组未展开时的交互状态问题；未产生写入，后续使用可恢复的 +1/-1 流程。

## 2026-08-10：积分调整取消分支

- 真实页面取消分支保留列表旧值，`u-demo` 从 `128` 未发生变化。
- 该分支证明临时输入不会在取消后写入；下一步执行 +1 后再 -1 恢复原值。

## 2026-08-10：积分调整快速重复确认

- 真实页面确认层出现，快速双击“确定”后只出现一次成功提示，`u-demo` 当前积分为 `129`。
- 该证据覆盖确认层与页面级重复点击门禁；底层请求次数当前浏览器环境无法可靠统计，后续以积分流水、审计和恢复值核对。

## 2026-08-10：积分恢复第一次定位错误

- 失败原因：恢复步骤仍使用旧值 `128` 的行名称，而真实页面已经显示 `129`；定位器无匹配，未触发按钮和写入。
- 后续改为当前值定位，继续执行 `-1` 恢复。

## 2026-08-10：积分恢复完成

- `u-demo` 已从 `129` 恢复到 `128`，没有遗留净积分变化。
- 页面成功提示只出现一次；下一步查看该用户流水，核对正负调整记录与审计筛选。

## 2026-08-10：积分流水核对

- 积分流水弹窗真实显示当前积分 `128`，并记录本轮 `+1`、`-1` 两条流水，未被快速双击重复生成额外同值记录。
- 该证据可以与操作审计中的积分调整动作交叉核对；未声称底层网络请求次数。

## 2026-08-10：积分审计入口定位错误

- 关闭流水弹窗的严格定位因两个同名按钮产生多匹配错误；这是交互定位错误，不是业务失败，也未造成写入。
- 后续使用同名按钮的第一个可见实例继续操作。

## 2026-08-10：积分操作审计筛选

- 审计筛选“用户积分调整”后共 6 条，顶部两条为本轮 `demo` 的正负调整；未出现重复的同一临时操作记录。
- 页面级审计证据已补齐，下一阶段仅做积分列表的失败/重试，不再写积分。

## 2026-08-10：积分列表失败与重试恢复

- 3199 断开时，积分页面保留 14 条旧数据并显示 `刷新失败：Failed to fetch`/“重新加载”；恢复后重试成功，列表回到 14 条。
- 失败期间没有清空列表，也没有产生积分写入；`u-demo` 始终为 `128`。

## 2026-08-10：运营优惠页面进入

- 真实规则列表共 8 条；测试对象 `rule-1786283589509-4a7ab8` 原状态为“否”，其余规则保持不变。
- 本模块将覆盖编辑取消、状态切换快速重复操作、恢复原状态、审计筛选和列表失败/重试。

## 2026-08-10：优惠规则编辑取消分支

- 取消编辑后，第一条规则的原值 `2 / 0.95` 和停用状态“否”均保留。
- 未产生优惠规则维护审计或数据库写入；继续验证状态切换和恢复。

## 2026-08-10：优惠规则启用快速重复确认

- 快速双击确认后只出现一次成功提示，第一条规则从停用变为启用，列表仍为 8 条。
- 启用后冲突规则列真实出现 `rule-2`，说明页面状态与业务冲突计算均有变化；后续恢复停用以避免改变原始状态。

## 2026-08-10：优惠规则恢复第一次定位错误

- 失败原因是第一条规则编号同时出现在第二条的“冲突规则”列，行定位产生两行、两个同名按钮；未触发停用。
- 后续改用第一条完整行文本进行精确定位。

## 2026-08-10：优惠规则恢复完成

- 第一条规则已恢复原停用状态，冲突列也恢复为“无”；页面成功提示仅一次。
- 该模块的状态切换临时变化已闭环，接下来核对维护审计并做列表失败/重试。

## 2026-08-10：优惠规则操作审计筛选

- “优惠规则维护”筛选真实返回 7 条，本轮启用/停用各一条，详情中的冲突列表与页面观察相符。
- 未发现快速双击产生的额外同值审计记录；下一步仅验证读列表失败/重试。

## 2026-08-10：优惠规则列表失败与重试恢复

- 3199 断开时优惠规则页面保留 8 条旧数据并显示 `刷新失败：Failed to fetch`/“重新加载”；恢复后重试成功，列表回到 8 条。
- 第一条规则的原值、停用状态和无冲突状态均未被失败读取改变；本轮未产生额外写入。

## 2026-08-10：P3 积分/优惠专项与回归收尾

- 积分页面：取消不写入，`+1/-1` 后净值恢复 `128`，流水和“用户积分调整”审计可交叉核对；断网保留 14 条并可重试恢复。
- 优惠页面：编辑取消保持 `2 / 0.95 / 否`，启用/停用快速重复确认后恢复原状，维护审计可见冲突变化；断网保留 8 条并可重试恢复。
- 自动化和构建回归全部通过；当前仍不把快速双击结果解释为底层请求次数为 1，也不改变 P2/P4/P5 边界。

## 2026-08-10：继续会话 H5 复核开始

- 接管的真实 H5 标签显示 `demo01`、账号安全弹窗和两个临时密码框；未读取 Cookie/Storage，未点击最终确认。
- 运行标签与错误标签并存；后续仅重启明确的 5185 服务并刷新健康标签，避免使用错误标签作为证据。

## 2026-08-10：H5 重启刷新后账号安全入口

- 5185 重启后真实页面可刷新并恢复 `demo01` 会话；账号安全父级菜单行唯一匹配，弹窗真实可见。
- 重启/刷新没有把临时密码值自动带回，当前两个密码框为空；后续临时填值仅用于确认层验证，禁止点击最终确认。

## 2026-08-10：H5 密码确认触发

- 重新填入临时密码后，保存按钮唯一匹配并触发；DOM 中业务弹窗和输入值仍在，未观察到密码写入或成功 toast。
- 系统确认层需要视觉证据核对，不能仅依据 DOM 快照判断其不存在。

## 2026-08-10：H5 密码确认层视觉证据

- 真实截图确认系统确认层居中覆盖业务弹窗，遮罩层级正确，按钮“取消/确定”可见；这证明重启/刷新后层级问题未复发。
- 直接按业务弹窗 `uni-button` 查找“取消”未命中系统层，未因定位失败而重复点击或提交密码。

## 2026-08-10：H5 重启刷新后密码确认取消闭环

- 重启/刷新后的真实截图证明确认层级正确；取消后系统层消失，业务弹窗与两个临时输入值仍在。
- 未执行最终确认，因此没有修改密码；本轮 H5 遗留运行态已闭环。

## 2026-08-10：P2 运行态读取初次诊断

- `demo01` 与 `demo02` 跨账号读取对方订单支付状态均返回 404；普通用户读取管理端导出返回 401。
- 管理员导出响应文件名为 `student-profiles.json`，运营人员为 `student-profiles-masked.json`；解析响应时因 JSON 重复键失败，需补字段级诊断后再形成完整证据。
- `demo/123456` 登录失败，未猜测或重置其密码；改用当前已确认的 `demo01` 作为普通用户权限证据。

## 2026-08-10：P2 跨账号/脱敏导出运行态复核完成

- 真实 3199 读取确认：admin 详情揭示明文手机号与 `phoneNormalized`，operator 详情保持脱敏且不揭示 `phoneNormalized`。
- 标准导出默认脱敏；仅 admin 的 `reveal=true` 返回明文导出，operator 即使显式请求仍为脱敏；普通用户管理端导出 403。
- `demo01` 和 `demo02` 跨账号订单支付状态均 404；导出审计接口可返回记录。
- 该结果修正了旧总结中“operator 与 admin 等同”的历史描述；当前运行态以角色分离结果为准。

## 2026-08-10：本轮最终回归校验

- `verify`、后端 15/68、管理端 UI 校验和 `git diff --check` 全部通过；测试日志仅含既有密码重置/短信模拟输出和换行提示。
- H5 与 P2 复核均为读取/临时确认取消路径，没有留下新的业务写入；支付、合并和远程服务器边界未改变。

## 2026-08-10：续接后的本地回归与健康检查

- 根目录 `verify` 的静态门禁仍全部通过，`git diff --check` 无实际差异错误。
- API 服务的健康路由由 `backend/api/src/mvp/mvp.controller.ts` 定义为 `/api/health`；3100 和 3199 实测均为 `200 / status=ok / database=ok`。
- 3100/3199 根路径返回 404 不能用来判断 API 健康；5174/5185 根页面实测均为 200。
- 该验证没有改变演示数据或任何发布边界；仍不能替代目标服务器 P4 验收，也不构成生产发布批准。

## 2026-08-10：P3 消息启停并发状态门禁

- 原 `setMessageEnabled` 采用“先读取、再更新”，并发请求可能都成功并各写一条启停审计。
- 已改为 `updateMany` 搭配 `id + enabled != desired` 条件的原子状态迁移；更新计数为 0 时区分消息不存在和已处于目标状态，后者返回 400。
- 新增并发测试实测 `201/400`，最终状态为目标状态，相关启用审计只有一条；该证据比页面按钮/提示更接近底层写入门禁。
- 本轮完整后端测试从 68 增至 69 个，15 个测试套件全部通过；不把该结果扩展解释为浏览器底层请求次数已可统计。

## 2026-08-10：P3 规则/配置重复保存审计去重

- `saveDiscountRule` 与 `saveSystemConfig` 原先对相同值重复提交仍会重复写维护审计；现改为已有记录的目标值条件更新。
- 竞争请求中只有真正改变目标值的请求产生维护审计；相同值重复提交仍返回成功并读取最终状态。
- 新增回归覆盖优惠规则和系统配置两类重复保存；当前完整后端测试为 15 个测试套件、70 个测试。
- 初始测试误按优惠规则 ID 查询审计，得到 0 条是因为原审计详情不含规则 ID；改按操作类型统计增量后验证通过。
- 积分调整没有套用该去重策略，继续保留每次独立流水，避免把真实业务操作错误合并。

## 2026-08-10：P4 Compose 环境预检

- Compose 文件通过 `POSTGRES_PASSWORD` 和 `.env.docker` 强制要求服务器专用环境文件；当前仓库没有该文件，符合不提交秘密的安全边界。
- PostgreSQL 导入/导出/对账脚本语法均可解析；脚本真正连接 PostgreSQL、迁移对账、备份恢复和上传卷重启仍必须在目标服务器执行。
- 新增 `.env.docker.example` 仅描述配置形状和非支付默认值，不能作为真实密码或生产 JWT 使用。

## 2026-08-10：P4 预检收尾证据

- 根目录静态 verify、三个 PostgreSQL 脚本语法检查和 `git diff --check` 均通过。
- API 3100/3199 健康为 `200 / status=ok / database=ok`，支付公共设置仍为微信/支付宝 `false/false`。
- `docker compose config` 因工作区不存在 `.env.docker` 而拒绝，保留为正确的服务器秘密前置条件；未通过占位值伪造 Compose 发布成功。

## 2026-08-10：P2/P3/P4 当前状态审计

- 既有 Downloads 落盘文件证实标准导出内容为完整 JSON、27 条记录全部脱敏、未掩码手机号数量为 0，且不含 `phoneNormalized`。
- P3 现已具备页面与后端的成功/取消/失败重试/刷新/审计及重复提交边界证据，计划可标记完成；底层浏览器请求计数仍保留为限制。
- P2 合并操作仍不可在无明确授权时执行；P4 只能在目标服务器配置 `.env.docker` 后进行真实迁移、卷、备份与回滚验收。

## 2026-08-10：目标服务器网络可达性复核

- `192.168.0.162:22` TCP 探测成功；当前阻塞点从“网络是否可达”收敛为“缺少 SSH 授权和服务器 `.env.docker`”。
- 未使用猜测凭据或执行 SSH 命令，因此 Docker/PostgreSQL、迁移、上传卷、备份恢复和回滚仍没有运行态证据。

## 2026-08-10：P4 shell 脚本检查限制

- Windows 环境调用 `bash -n` 和 `wsl.exe --status` 均被 Bash/WSL 服务以 `E_ACCESSDENIED` 拒绝。
- 该错误来自本地 shell 服务权限，不代表 `Docs/runtime/*.sh` 的语法错误；目标服务器 Linux 验收时应先执行 `bash -n Docs/runtime/*.sh`，再执行发布脚本。

## 2026-08-10：本轮 H5 与本地门禁执行证据

- `pnpm.cmd run verify`、PostgreSQL 三个 Node 脚本语法检查和 `git diff --check` 均通过；四个本地服务端口均可达。
- 真实 H5 登录 `demo01` 后刷新，URL 仍在“我的”页面且显示账号信息，证明当前本地会话可恢复。
- 密码确认弹窗必须用视觉证据判断：截图显示系统确认层、遮罩和“取消/确定”在业务账号安全弹窗之上；取消后业务弹窗及两个临时输入值仍存在，未发生密码写入。
- 浏览器旧标签绑定曾报“Tab not found/不属于当前会话”；重新获取当前浏览器会话后测试恢复。该限制不改变应用运行态结论，也不作为底层请求次数统计依据。

## 2026-08-10：完整门禁复跑发现

- 当前代码的后端测试实际为 15 个套件、70 个测试，全部通过；规划文件中较早的 15/68、15/69 记录属于历史批次。
- 后端构建、管理端 UI 校验/构建、H5 构建和根目录 verify 全部通过。
- Sass/uni-app 的弃用提示没有转化为编译错误；如果后续维护，可单独安排依赖升级，但不构成本轮非支付内测阻塞。
- 这组本地证据不能替代目标服务器 PostgreSQL/Compose/备份恢复验收，也不能授权执行不可逆学员合并。

## 2026-08-10：计划状态一致性审计

- 规划文件顶部曾保留早期的 P2/P4/P5 未开始标记，与末尾最新证据不一致；已按当前证据修正为 P2/P4 进行中、P5 本地/内网非支付冻结完成。
- P5 冻结结论有范围限定：允许本地/内网非支付内部测试，不批准生产或目标服务器发布。
- 端口探测初次超时属于检查命令行为，不是服务失败；改用短超时 TCP 客户端后四个端口均确认可达。PowerShell 管道解析错误同样已修正。

## 2026-08-10：外部阻塞审计收尾

- 当前已没有可在不扩大权限范围的前提下完成的本地高优先级事项。
- 合并是不可逆业务写操作，必须获得明确源/目标档案授权；服务器验收必须在真实目标主机、专用环境文件和可用 SSH 身份下执行。
- 多轮重复核对均保持相同阻塞条件，因此不能把任务继续留在“仍在尝试”状态，也不能把本地预检写成服务器发布完成。

## 2026-08-10：恢复后的外部状态复核

- 恢复目标后的只读检查确认：目标 SSH 端口可达，但本机没有运行中的 SSH agent；仓库中仍没有服务器专用 `.env.docker`。
- 没有新的合并授权信息；不可逆学员合并继续保持未执行。
- 本机 SSH 配置目录读取被权限拒绝，不能据此推断存在可用密钥；该工具限制不改变服务器端口可达性结论。

## 2026-08-10：短信注册与忘记密码现状整理

- 当前已实现 `/auth/register/sms/request`、`/auth/register/sms/confirm`、`/auth/password-reset/request` 和 `/auth/password-reset/confirm`，开发/内测 fake 模式会返回 `devCode`，真实 webhook 模式才会调用外部短信 relay。
- 注册验证码和密码找回验证码均为 10 分钟有效、最多 5 次错误、挑战一次性使用；密码找回成功会递增会话版本并吊销旧 refresh token。
- 真实短信所需配置为 `PHONE_REGISTRATION_ADAPTER=webhook`、`PHONE_REGISTRATION_WEBHOOK_URL/TOKEN`，以及 `PASSWORD_RESET_ADAPTER=webhook`、`PASSWORD_RESET_WEBHOOK_URL/TOKEN`；当前服务器模板仍为 fake，真实短信尚未配置。
- 已将上述事实、测试数据、SSH 信息交付方式和执行步骤写入新的 Summary 指南。
## 2026-08-10：本地登录错误诊断
- 当前 5174 管理端实际登录 `admin/123456` 成功，进入工作台；当前 5185 H5 实际登录 `demo01/123456` 成功，截图错误未能在更新后的运行态复现。
- H5 刷新后 `demo01` 会话恢复；账号安全密码保存动作显示系统确认层，取消后未发生密码写入。
- 旧错误最可能来自 seed 修正前的旧密码哈希或旧运行态；现有 seed 已对已有用户更新 `passwordHash`。
- 3100 与 3199 为独立本地 API 运行实例；3100 登录 operator 后角色仍为 user，3199 为 operator，需维护窗口/用户确认后再重启 3100。
- 本轮只做本地运行态验证，没有连接或写入 `192.168.0.162`。
## 2026-08-10：目标服务器非支付阻断证据
- 目标服务器 22 端口可达，但 SSH root 认证返回 `Permission denied (publickey,password)`；本机没有 plink、Posh-SSH、Paramiko，WSL 启动也因 `E_ACCESSDENIED` 失败，未把密码写入文件或命令历史。
- 目标 3100 旧 API 的 `/api/health` 为 404、课程接口为 200；5174/5185/5432 不可达，不能作为当前 Compose/PostgreSQL 版本验收。
- 目标 `/api/payment-settings/public` 返回 `onlineWechatEnabled=true`、`onlineAlipayEnabled=true`，与非支付上线门禁冲突。
- 目标 admin 登录返回 500；未擅自修改支付设置、迁移数据库、重启容器或执行备份恢复。
## 2026-08-10：非支付公开开关门禁补强
- 本地 `MvpService.getPublicPaymentSettings()` 已有 adapter 逻辑，本轮补充测试锁定生产 `PAYMENT_ADAPTER=disabled` 时 `onlineWechatEnabled=false`、`onlineAlipayEnabled=false`。
- 该测试与完整后端回归均通过；目标服务器实际 `true/true` 说明其未加载当前非支付配置/版本，不能宣称服务器已通过。

## 2026-08-10：C 端退出登录弹窗视觉复核
- 截图中原问题不是业务逻辑失败，而是 H5 html portal 上的确认层关键样式没有稳定覆盖，导致按钮退化为浏览器默认样式。
- 退出确认现在使用 `danger` 变体，并以内联重要样式锁定布局、圆角、按钮高度、颜色和字体；其他确认入口继续使用默认变体。
- 真实 5185 运行态显示退出图标、居中标题、说明文字、浅色取消按钮和红色确认按钮；取消和确认两个分支均已验证。

## 2026-08-10：课程管理新增课程需求
- 管理端原来使用单个自由文本 `date`，后端仍兼容字符串，但管理员无法可靠选择开始/结束时间。
- 后端 `saveCourse` 要求 `registrationTemplateId` 必须对应已存在的报名模板；前端模板尚未加载或未选择时会提交空值并返回 400。
- 已增加 `courseStartAt`、`courseEndAt` 两个 `datetime-local` 字段，保存时组合为后端兼容的时间段字符串。
- 已增加新增课程草稿缓存、关闭后恢复、重置清空、开始/结束时间校验和报名模板前置校验。
- 管理端课程列表已改为 `createdAt desc`，保证最新新增课程在表格第 1 页最前面。

## 2026-08-10：本地管理端登录错误复核
- “只写了 DB 没有执行”不是本轮现象：seed 命令已实际运行并完成 SQLite 数据库更新。
- 本地 API 健康检查和管理员登录均成功，截图中的错误不能由当前本地数据库账号状态复现。
- 用户需要在 `http://localhost:5174/login` 执行强制刷新（Ctrl+F5）后重新输入 `admin/123456`；若仍报错，应检查浏览器开发者工具中请求是否指向 `http://127.0.0.1:3199/api/auth/login`。
- `192.168.0.162` 是独立服务器环境；此前 SSH 认证失败，当前未对其执行 seed、迁移、重启或其他写入操作。

## 2026-08-10：管理端时间控件与 Banner 课程搜索
- 课程报名截止时间、Banner 开始/结束时间原来是普通文本；已统一为 `datetime-local`，消息通知和审计筛选原本已使用该控件。
- Banner 关联课程已改为搜索选择器，按课程名称或编号过滤，编辑时保留当前选择并支持清除。

## 2026-08-10：驳回发票重新申请
- 后端原有普通 `POST /invoices` 已排除非驳回重复订单，但 C 端没有针对已驳回记录的入口。
- 新增 `POST /invoices/:id/reapply`：仅原申请人、仅已驳回记录可调用；复用原订单范围，生成新申请编号，原记录和驳回理由保留，并记录 `retryOfInvoiceId` 与审计。
- C 端已驳回发票显示“修改后重新申请”，自动带出原抬头、税号、邮箱和订单，可修改后重新提交。

## 2026-08-10：C 端支付弹窗遮挡修复
- 报名页支付弹窗打开时，底层报名表的原生报名档案选择控件仍可见，H5 独立绘制层会盖到弹窗上方。
- 已在支付弹窗打开状态隐藏并禁用底层页面节点，只保留业务弹窗和遮罩可见可交互。
## 2026-08-10：开票重申请闭环

- 重申请不能覆盖旧记录，否则无法保留驳回理由和审核历史；新记录通过 `retryOfInvoiceId` 关联旧记录并保持 `待处理` 状态。
- 登录提醒必须排除已有后续重申请的旧驳回记录，否则用户每次登录都会被重复提醒；当前仅提醒最新可处理的驳回记录。
- 管理端已有 `/admin/invoices/:id/process` 状态门禁，待处理的新记录可再次审核通过或驳回；专项回归已验证完整链路。

## 2026-08-10：H5 确认层刷新恢复与 P2 专项复跑
- 当前 5185 H5 确认层根节点、遮罩、内容层级分别为 `2147483645/2147483646/2147483647`；账号安全业务弹窗位于其下方，取消分支保留临时输入值且没有发起密码修改。
- 关闭业务弹窗后刷新页面，URL 仍为 `#/pages/mine/mine`，账号 `demo` 和账号安全入口存在；这证明本地会话刷新恢复，不证明最终密码修改成功。
- P2 `student-api.spec.ts`、`admin-security-boundaries.spec.ts` 共 10 个测试通过，覆盖档案关系、默认/解除、合并边界、脱敏导出和跨账号权限拒绝。
- 管理端页面级导出本轮未取得新浏览器证据，原因是已有管理端标签不属于当前自动化会话；未读取 Cookie/存储、未使用 CDP 或其他绕过手段。

## 2026-08-10：重启后 H5 与 P2 真实请求复核

- 5185 进程重启后通过 `cmd.exe /c pnpm.cmd dev:h5` 恢复；此前 PowerShell 直接启动 `pnpm.cmd` 会因 `Path/PATH` 键冲突失败，改用不同启动路径解决。
- 重启后的 H5 刷新仍保持 demo 会话；账号安全确认层覆盖业务弹窗，取消后临时输入保留，关闭并刷新后页面回到“我的”。当前标签控制台错误为空。
- 3100 与 3199 的真实登录角色均符合预期：`admin` 为 admin、`operator` 为 operator、`demo01/demo02` 为 user。
- 真实接口读取补充确认：普通用户访问 `/admin/student-profiles` 返回 403；operator 请求 `reveal=true` 的导出响应文件名仍为脱敏版本；demo01 访问 demo02 订单支付状态返回 404。
- 管理端页面级下载仍无法作为本轮新增证据：当前浏览器旧管理端标签反复无法接管；这是浏览器会话生命周期限制，不是应用接口失败。未读取 Cookie/Storage，未使用 CDP 或其他绕过。
- 一次 P2 PowerShell 汇总脚本因数组/异常响应处理错误中止，随后改用显式列表脚本完成权限和跨账号检查；两次脚本均未产生业务写入。

## 2026-08-10：材料提交边界整理
- 用户需要的是如何向 Codex 提供继续目标所需材料，而不是直接把秘密粘贴到聊天中。
- 合并必须由用户提供源档案 ID、目标档案 ID和明确授权；推荐先只读预览，再由用户二次确认执行。
- 服务器网络可达但 `root/123456` 认证失败；推荐使用 SSH 公钥和本机 ssh-agent，聊天中只提供用户名、项目目录、只读测试结果和权限范围。
- 服务器真实 `.env.docker` 应由用户在服务器本地配置；可以提供脱敏变量状态，不提供数据库密码、JWT secret、短信 Token 或完整连接串。
- 管理端导出接口和脱敏校验已有证据，当前缺少独立浏览器下载事件；可由用户手工导出后提供页面成功截图、文件元数据、SHA-256和脱敏校验结果，不必上传原始学员文件。

## 2026-08-10：新 H5 标签刷新恢复复核
- 当前浏览器会话可以新建并控制 5185 标签；旧的用户标签仍不一定能被当前自动化会话接管，因此本轮使用新标签完成独立复核。
- `demo01` 登录成功后，真实刷新仍停留在 `#/pages/mine/mine`，页面显示账号信息和“账号与安全”。
- 账号安全确认层通过 `[role=dialog][aria-label="确认修改密码"]` 真实存在；运行时 `root/mask/content` 的层级为 `2147483645/2147483646/2147483647`，说明确认层确实位于业务弹窗上方。
- 取消确认后临时输入值保留，关闭业务弹窗并刷新后确认层不存在；没有点击“确定”，因此没有修改测试账号密码。

## 2026-08-10：VSCode SSH 配置差异
- `C:\Users\EDY\.ssh\config` 中的 `guanxiang-server` 实际指向 `49.234.113.222`，不是之前指定的 `192.168.0.162`。
- `guanxiang-server` 显式设置 `PreferredAuthentications password`，而本机没有可用 ssh-agent；VSCode 成功很可能依赖交互式密码输入或已有连接。
- SSH 目录只包含 `config`、`known_hosts`、`known_hosts.old`，未发现常见私钥文件；没有读取任何敏感文件内容。
- 两台服务器 22 端口均可达，但 BatchMode 只读登录均返回 Permission denied；因此不能用 VSCode 登录另一台主机的结果替代目标服务器 P4 验收。

## 2026-08-10：VSCode 目标服务器交互式认证确认
- 用户确认 VSCode 成功登录的是 `192.168.0.162`；读取最近 Remote-SSH 日志后得到直接证据：Resolver 目标为 `ssh-remote+192.168.0.162`，使用 Windows OpenSSH，出现 root 密码提示并收到密码响应。
- 日志随后显示远程系统为 Ubuntu/Linux，VSCode Server 启动成功并完成端口转发；目标服务器 SSH 认证可用。
- BatchMode 失败与此不矛盾：该模式禁止密码输入，当前机器又没有 ssh-agent/私钥，只能返回 Permission denied。
- P4 阻塞已收敛为“需要在用户已建立的交互式 SSH 会话中执行/回传只读检查，以及提供服务器 `.env.docker` 状态”，不再记录为 SSH 网络或认证不可用。

## 2026-08-10：P2 学员导出页面文件证据
- 管理端新标签进入学员管理并点击导出后，Downloads 目录新增 `student-profiles-masked (7).json`。
- PowerShell `ConvertFrom-Json` 的错误来自当前终端编码/解析路径；使用 Node 按 UTF-8 读取后 JSON.parse 成功，文件包含 29 条记录。
- 文件校验确认 `sensitiveFieldsMasked=true`、`truncated=false`、不含 `phoneNormalized`，所有手机号均为掩码形式；页面级导出和落盘证据已补齐，独立浏览器 download 事件仍单独标注未捕获。
- 用户回传的 `id`/Linux `find` 报错显示命令是在本地 `PS D:\Projects\TrainingManagementSystem>` 执行，不是 VSCode SSH 远程终端；服务器检查尚未开始。

## 2026-08-10：服务器远程终端前置条件
- 新回传的命令提示符和结果来自远程 VSCode 终端：`uid=0(root)`、docker 组、`ctc-Vostro-3910`、`/opt/training-management`、Compose 文件存在。
- 远程 Docker 版本为 29.1.3，Compose 为 2.40.3；与本机 Docker 版本不同，证明这次确实是在服务器环境执行。
- 下一步优先检查服务器 `.env.docker` 仅存在性/键存在性、`PAYMENT_ADAPTER`、Compose config、容器状态和 API health；全部属于只读检查。

## 2026-08-10：H5 隔离 API 验证与服务器版本差异
- 本地 3100 由 VSCode Remote-SSH 端口转发占用，实际命中服务器旧 API；本地 3199 返回当前代码健康响应。不能通过停止 VSCode 进程来解决端口冲突。
- 通过重启 5185 H5 并临时设置 `VITE_API_BASE_URL` 指向 3199，取得了当前 H5 的真实浏览器证据：登录、刷新会话恢复、账号安全确认层显示、层级覆盖业务弹窗、取消不提交和输入保留。
- 服务器 HEAD `5f41bf7` 与本地 HEAD `16ff2b8` 不一致，服务器镜像创建于 2026-08-05；服务器 API 容器内部 `/api/health` 404 与当前仓库路由/Compose healthcheck 不一致，说明服务器需要先同步正确部署源或明确使用的版本。
- 服务器存在未跟踪的 PostgreSQL 导出文件、PostgreSQL schema 目录和 `docker-compose.override.yml`；它们属于服务器现有材料，不能在部署前清理或覆盖。
- Git 关系检查确认服务器 `5f41bf7` 是本地 `16ff2b8` 的祖先；因此服务器旧镜像可通过部署本地后续版本获得健康路由，但当前工作区未提交修改不能在没有发布源确认的情况下直接上传。
- 本轮构建与回归门禁：H5/API/管理端构建通过，后端 15 个套件、70 个测试通过，根目录 `verify` 全部静态门禁为 true；Sass 仅报告弃用警告。
