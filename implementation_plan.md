# 脱离 Mock 的前后端数据库联动任务规划

> 规划基线：2026-07-31
>
> 目标：在不破坏现有订单和支付能力的前提下，将项目从 COP/MVP 演示状态推进为前后端分离、真实 API、真实数据库可联调且能够测试上线的第一版小程序。
>
> 执行约束：后续开发任务必须引用本规划中的阶段编号和 FeatureList 编号；未完成验收门禁前，不得把功能清单状态改为“已实现”。

## 一、当前状态与工作边界

### 已确认的现状

- C 端和管理端核心业务已通过 API 访问 NestJS 后端，后端使用 Prisma + SQLite 持久化。
- 课程、报名模板、订单、支付凭证、发票、预览、反馈、积分、审计已有真实读写链路。
- `frontend/client-uni/src/common/mock.ts` 当前没有被页面业务代码导入。
- 微信登录仍使用开发 `mock:` 身份适配；在线支付意图仍是 `ready: false` 占位；两者必须被隔离在显式 adapter 边界内。
- 学员管理仍由 `Order.participants` 展开，尚未有 `Student`、`AccountStudent`、`Enrollment`。

### 不在本轮直接做的事情

- 不直接删除 `Order.participants`；
- 不把历史 JSON 一次性强制合并成单一档案；
- 不在没有渠道配置、回调验签和退款规则时伪造“在线支付生产完成”；
- 不通过修改 FeatureList 勾选状态掩盖未完成的代码或测试。

## 二、阶段总览

| 阶段 | 主题 | 绑定功能 | 状态 | 主要输出 |
|---|---|---|---|---|
| P0 | 基线、业务口径与备份 | 全局；`WEB-STU-007` | 已完成（基线/备份） | 规则确认、数据库备份、验收基线 |
| P1 | 学员域数据模型 | `WEB-DATA-001` | 已完成 | Prisma schema、迁移、索引 |
| P2 | 历史快照回填与对账 | `WEB-DATA-003`、`WEB-STU-007` | 未开始 | 回填脚本、异常清单、对账报告 |
| P3 | 下单事务双写 | `WEB-DATA-002` | 未开始 | 兼容 DTO、事务双写、回滚测试 |
| P4 | 后端学员 API 与权限 | `WEB-STU-003`~`WEB-STU-008` | 未开始 | 档案、关系、履历、合并和审计 API |
| P5 | 管理端拆分视图 | `WEB-ENR-002`、`WEB-ENR-003`、`WEB-STU-003`~`008` | 未开始 | 独立学员档案页、报名明细页 |
| P6 | C 端学员体验 | `MP-ENR-007`、`MP-ME-007` | 未开始 | 选择已有学员、临时填写、我的学员 |
| P7 | 新读切换与兼容期 | `WEB-DATA-004`、`WEB-STU-007` | 未开始 | 双读对账、切换开关、观察期报告 |
| P8 | Mock 脱离与外部渠道 adapter | `WEB-DATA-005` | 未开始 | 环境配置、微信/支付 adapter、生产门禁 |
| P9 | 首版回归、发布与回滚 | `WEB-DATA-006` | 未开始 | E2E、构建报告、发布清单、回滚演练 |

## 三、详细任务

### P0：基线、业务口径与数据库备份

**目标**：冻结“账号、学员、报名履历、订单”的定义，避免迁移过程中反复改变口径。

**任务**：

1. 确认学员去重键、手机号缺失规则、企业代报名授权规则、重复报名和取消报名规则；
2. 明确 `Student` 基础字段、敏感字段、状态和合并状态；
3. 导出当前 SQLite 数据库和 schema 版本，记录备份校验值；
4. 冻结 FeatureList 105/402 的基线，不因讨论直接修改实现勾选；
5. 为迁移、双写、新读和回滚分别建立环境开关。

**完成标准**：业务规则得到书面确认；备份可恢复；本阶段记录写入 `Docs/Plans/progress.md`、根 `progress.md` 和阶段总结。

### P1：Student、AccountStudent、Enrollment 数据模型

**绑定**：`WEB-DATA-001`。

**任务**：

1. 在 Prisma 新增 `Student`、`AccountStudent`、`Enrollment`；
2. 为规范化手机号、关系唯一性、课程/学员、订单/序号建立索引和约束；
3. 为 `Enrollment` 保存模板 ID、版本、`formPayload`、订单责任账号和状态；
4. 保留 `Order.participants`，必要时增加模板版本/快照辅助字段；
5. 生成可重复执行的 migration，不在本阶段删除旧字段。

**完成标准**：空数据库可迁移；现有数据库迁移后原有 API 和种子仍能启动；schema 通过 TypeScript/Prisma 校验；迁移失败可回滚。

### P2：历史 participants JSON 回填、幂等与对账

**绑定**：`WEB-DATA-003`、`WEB-STU-007`。

**任务**：

1. 逐订单解析 `participants`，记录订单号和数组序号；
2. 规范化手机号，按手机号/姓名/公司生成匹配候选；
3. 自动匹配无歧义记录，冲突、缺字段、损坏 JSON 进入人工清单；
4. 创建 `Student`、`AccountStudent`、`Enrollment`，保存原始快照和匹配方式；
5. 使用批次号和游标支持断点续跑，重复执行不产生重复数据；
6. 输出订单人数、履历数、课程汇总、支付状态差异报告。

**完成标准**：试运行和正式回填均有批次报告；重复运行结果相同；异常可定位到订单和报名人序号；通过备份恢复演练。

### P3：下单事务双写与兼容 DTO

**绑定**：`WEB-DATA-002`。

**任务**：

1. `CreateOrderDto` 支持每个报名人的可选 `studentId`，继续兼容仅传 `data` 的旧请求；
2. 校验 `studentId` 是否属于当前账号或有授权关系；
3. 无 `studentId` 时按规则创建/匹配档案，手机号冲突进入明确错误或人工路径；
4. 在同一 Prisma transaction 中写入订单、课程人数、关系和履历；
5. `Order.participants` 与 `Enrollment.formPayload` 保存同一份提交快照；
6. 任一写入失败时整体回滚，并覆盖重复提交和并发容量测试。

**完成标准**：旧客户端仍可下单；新客户端可带 `studentId`；订单和履历不会半成功；重复请求具备幂等策略或明确错误。

### P4：后端学员 API、权限、合并和审计

**绑定**：`WEB-STU-003`~`WEB-STU-008`。

**任务**：

1. 提供学员分页、详情、编辑、停用接口；
2. 提供账号关系查询、授权、解除、默认报名人接口；
3. 提供跨课程报名履历查询和状态联动；
4. 提供手机号规范化、匹配候选、人工确认、软合并和合并审计；
5. 按角色控制查看、编辑、合并和导出，手机号等敏感字段脱敏；
6. 保留旧 `/admin/students` 响应兼容期，并记录旧读新读差异。

**完成标准**：正常、越权、冲突、重复提交和审计测试齐全；普通管理员只能访问授权范围；合并后历史订单和履历可追溯。

### P5：管理端拆分“学员档案”和“报名明细”

**绑定**：`WEB-STU-003`~`WEB-STU-008`、现有报名管理功能。

**任务**：

1. “学员管理”改为 `Student` 分页列表和档案详情；
2. “报名明细”改为 `Enrollment`/订单明细列表；
3. 档案详情显示授权账号、跨课程履历、状态和统计摘要；
4. 报名详情显示模板版本、历史表单快照、订单和支付状态；
5. 增加冲突、合并、停用、敏感字段脱敏和审计入口；
6. 保持加载版本守卫，避免旧请求覆盖新模块数据。

**完成标准**：两个模块数据源不同且可在 UI 中验证；分页、筛选、详情、权限和空状态通过静态验收及 API 联调。

### P6：C 端“我的学员”和报名选择

**绑定**：`MP-ENR-007`、`MP-ME-007`。

**任务**：

1. 报名页读取当前账号有权限的 `AccountStudent` 列表；
2. 支持选择已有学员并按当前模板补齐字段；
3. 支持新建学员档案或一次性临时报名人；
4. 提交时携带 `studentId`，仍保存本次表单快照；
5. “我的”增加学员列表、新增、编辑、默认报名人和解除关系；
6. 对手机号冲突、无权限学员和模板缺字段提供可理解提示。

**完成标准**：本人报名、单账号代多人报名、已有学员复用、临时填写四条路径均能落库并在管理端追溯。

### P7：新读切换、双读对账和兼容期

**绑定**：`WEB-DATA-004`、`WEB-STU-007`。

**任务**：

1. 新旧查询同时运行，输出差异而不立即改变用户结果；
2. 校验订单人数、履历数量、课程统计和支付状态；
3. 连续多个批次无未解释差异后，按环境开关切换学员档案和报名明细新读；
4. 保留旧字段、回退开关和旧接口一段观察期；
5. 形成切换、回退和异常处理手册。

**完成标准**：对账差异全部有结论；切换后关键页面和 API 回归通过；回退开关可在不改数据的情况下恢复旧读。

### P8：Mock 脱离、环境配置和外部渠道 adapter

**绑定**：`WEB-DATA-005`。

**任务**：

1. 清理页面业务对静态 mock 的隐式依赖，构建时扫描禁止引用；
2. 把 API base URL、上传目录、JWT、数据库 URL、支付和微信配置纳入环境模板；
3. 将微信 `code2Session`、微信支付、支付宝支付封装为服务端 adapter；
4. 开发环境可使用显式 fake adapter，但生产启动时缺少渠道配置必须失败或进入明确禁用状态；
5. 支付回调验签、幂等、订单状态机和退款规则必须有测试。

**完成标准**：测试/生产构建不依赖 `mock:` 身份或演示数组；无配置时不会伪造支付成功；外部渠道可替换且业务服务不直接依赖 SDK 细节。

### P9：端到端回归、发布和回滚演练

**绑定**：`WEB-DATA-006`。

**任务**：

1. 建立登录、课程、模板、单人/多人报名、已有学员、支付凭证、审核、发票和履历查询 E2E；
2. 执行后端单元/集成测试、前端 TypeScript/Vite/UniApp 构建；
3. 检查数据库迁移、备份、日志、权限和上传目录；
4. 在测试环境演练发布、迁移失败、回滚和旧读切换；
5. 生成首版发布清单，确认已实现项与 FeatureList 一致。

**完成标准**：关键路径在干净测试数据库通过；构建产物可启动；发布和回滚演练有记录；所有 P0 阻断项关闭后才允许上线。

## 四、每个开发任务的固定交付格式

每次实现必须在提交说明或阶段总结中包含：

1. 关联阶段和 FeatureList 编号；
2. 修改文件、数据库迁移和接口变更；
3. 自动化测试命令及结果；
4. 手工验收路径、截图或 API 请求样例；
5. 兼容性、回滚和数据影响说明；
6. 同步更新：
   - `Docs/FeatureList/培训管理系统功能清单.xlsx`；
   - `Docs/Plans/progress.md`；
   - 根目录 `progress.md`；
   - 对应 `Docs/Summary` 阶段总结。

## 五、推荐验证命令

在项目根目录执行：

```powershell
pnpm.cmd --dir backend/api test -- --runInBand
pnpm.cmd --dir backend/api build
pnpm.cmd --dir frontend/admin-react build
pnpm.cmd --dir frontend/client-uni build:h5
git diff --check
```

如果需要微信小程序构建，先确认 UniApp/Vue 版本兼容性，再执行对应命令；构建失败必须记录为环境门禁，不得把失败状态标成已实现。

## 六、当前执行顺序

P0 基线和 P1 数据模型已完成；下一项允许开始的开发任务是 P2 历史快照回填与对账。除非任务明确属于线上故障修复，否则不应跳过 P2/P3 直接修改 C 端页面来模拟独立学员档案。

本规划与以下文件共同作为后续实现依据：

- `Docs/Summary/2026-07-31_学员域重构与真实联动项目现状总结.md`
- `Docs/Summary/用户管理_学员管理_报名信息存储关系分析.md`
- `Docs/Plans/2026-07-31_学员域数据库设计方案.md`
- `Docs/FeatureList/培训管理系统功能清单.xlsx`
# P2 当前状态补充（2026-07-31）

P2 已完成：新增 `0003_student_backfill_ops`、原生 SQLite 回填脚本、dry-run/异常清单/幂等游标和正式对账；P3 下单事务双写、P4 学员 API 与权限、P5 管理端视图拆分也已完成，下一阶段为 P6 C 端学员体验。
