# 项目关系分析与文档整理计划

## 2026-08-10：本轮运行态修复
- [x] 修复开票重申请接口运行实例未加载导致的 `Cannot POST`，重启本地 3100 API 并完成接口存在性复核。
- [x] 同步 verify 门禁与 C 端业务弹窗层级 1000，统一残留页面的旧层级值。
- [x] 完成后端 15 个测试套件/70 个用例、H5 构建、根目录 verify 和差异检查。

## 目标

基于当前项目代码，核实用户管理、学员管理、课程报名模板、订单及报名信息的实际关系，并生成中文 Markdown 说明文档到 `Docs/Summary`。

## 阶段

- [x] 阶段 1：盘点 Prisma 数据模型与后端业务逻辑
- [x] 阶段 2：核对管理端/客户端页面及接口语义
- [x] 阶段 3：整理结论、边界和优化建议
- [x] 阶段 4：生成并校验 Markdown 文档
- [x] 阶段 5：核对本次 C 端与管理端现状
- [x] 阶段 6：实现 C 端导航、我的页面与账号资料/安全功能
- [x] 阶段 7：实现课程图片上传及 C 端图片展示
- [x] 阶段 8：调整学员列表与报名汇总详情/图表
- [x] 阶段 9：构建、测试并核对运行时接口
- [x] 阶段 10：完善独立学员档案的业务意义、数据模型、迁移步骤与验收标准

## 2026-08-09：非支付可上线主计划（当前执行基线）

### 目标

在不依赖微信/支付宝在线支付的前提下，完成管理端与 C 端可供内部测试的业务版本。每个阶段必须有代码、接口、运行态证据、自动化校验和 `Docs/Summary` 留痕；不能只以“按钮存在”作为完成依据。

### 阶段与放行条件

- [x] P1 订单、线下支付凭证、开票、报名明细：成功/取消/重复点击/失败重试/刷新/审计闭环；在线支付保持 disabled。
- [/] P2 用户与独立学员档案：启停、重置密码、关系授权/默认/解除、合并、履历、脱敏导出和跨账号权限；除不可逆合并授权外的当前运行态证据已补齐。
- [x] P3 消息、积分、优惠、反馈、系统配置、审计：成功/取消/失败/重复提交/刷新及边界校验；底层浏览器请求次数仍不作声称。
- [/] P4 Docker/PostgreSQL：迁移对账、上传卷持久化、容器重启、健康检查、备份恢复和只读回滚；本地预检已完成，目标服务器仍待 SSH 授权。
- [x] P5 非支付内部测试冻结：本地/内网自动化、双端运行态、权限/脱敏/审计清单、回滚方案和支付后续清单已形成；不代表生产放行。

### 2026-08-09 续接记录

- 已补充订单 `HX-1786210554512-579e76` 的接口级刷新持久化、`退款完成` 审计筛选和重复退款 400 证据；P1 仍保持进行中，因为管理端页面刷新/筛选点击证据尚未在当前浏览器会话补齐。
- 已补充学员档案默认脱敏、管理员明文揭示和普通用户 403 的接口级证据；P2 仍保持未完成，因为导出文件实际内容、合并浏览器路径和跨账号浏览器证据仍需完成。
- 当前浏览器标签页控制出现会话绑定异常；未用替代自动化方式绕过，恢复可控浏览器后优先完成 H5 和管理端运行态证据。

### 当前证据与未闭环项

- 2026-08-09 续接：5185 H5 重启/刷新后已使用 `demo01/123456` 完成“我的 → 账号与安全 → 保存新密码”真实复核；确认 Portal 根/遮罩/内容运行时层级为 `2147483645/2147483646/2147483647`，父业务弹窗为 `90`，取消后父弹窗和输入保留且未提交密码修改。
- 2026-08-09 续接：P3 已补反馈取消/处理、消息新增/停用、积分调整/恢复的真实管理端路径；临时消息已停用，临时积分已恢复，反馈处理记录作为本地既有测试数据的真实状态变更保留并可审计。
- 2026-08-09 续接：P3 又补优惠规则边界/新增停用、系统配置取消/变更/恢复和“处理反馈”审计筛选；仍需完成剩余审计筛选、失败重试/重复提交证据和阶段自动化复跑后再关闭 P3。

- 自动化基线已通过：根目录 `verify`、管理端 UI 静态验收、管理端构建、后端 Jest、C 端 H5 构建。
- 管理端 18 个模块的主要按钮已经对应真实 `/admin/*` 接口；当前执行重点是写操作的状态门禁、失败恢复、审计可见性和真实会话验收，不再把“按钮存在”直接等同于功能完成。
- P1 订单关闭、退款、凭证驳回、开票驳回已有运行态证据；报名明细详情打开与刷新恢复已补齐，报名明细加载失败/重试、审计可见性仍未补齐。
- P2 用户/学员取消确认和启停、重置密码、学员档案保存已有运行态证据；关系授权/解除、合并、脱敏导出和跨账号权限仍需逐项验证。
- 服务器 Docker/PostgreSQL 不能用本地结果替代，必须在 `192.168.0.162` 重新执行迁移、重启、备份恢复和回滚验收。

### 本轮执行顺序

- [x] P1.1：补报名明细/订单/凭证/开票的错误态重试、重复提交锁和审计检索证据。
- [/] P2.1：补学员关系授权、默认/解除、合并、脱敏导出和普通用户越权证据；已先完成管理员自停用/最后管理员保护。
- [x] P3.1：消息、积分、优惠、反馈、系统配置的成功/取消/失败/幂等、刷新和审计验证已完成；后端状态门禁与重复保存审计去重已补齐。
- [ ] P4.1：在服务器 Compose 环境执行 PostgreSQL 导入、卷持久化、健康检查、备份恢复和回滚。
- [ ] P5.1：冻结非支付内部测试版本，生成发布清单、已知限制和支付待办清单。

### 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 3199 恢复时误以仓库根目录为工作目录 | 1 | 产生零字节根目录数据库并导致演示管理员无法登录；已停止错误实例、删除该零字节测试产物，按 `backend/api` 工作目录重新启动并恢复授权 |
| 反馈管理入口正则同时命中页面按钮和关闭标签按钮 | 1 | 改用精确可访问名称；未修改业务代码 |

### 2026-08-10 续接进展

- [x] 核对当前规划、进度、发现、最新发布报告、工作区和四个本地服务。
- [x] 补反馈管理列表的真实断网失败态：错误提示、重试入口和旧列表保留。
- [x] 恢复 3199 正确工作目录并重新登录管理端，确认反馈列表恢复为 14 条且演示数据未丢失。
- [x] 复跑根目录 `verify`、后端 15 套件/68 测试、管理端 UI 校验、管理端构建、H5 构建和 `git diff --check`。
- [x] 核对 `PAYMENT_ADAPTER=disabled`、公共支付设置两个在线开关为 `false`，并确认 3100/3199/5174/5185 均在监听。
- [ ] P2：仍等待用户明确确认后才执行学员档案合并；其余关系/导出/跨账号证据按最新报告保留。
- [ ] P3：反馈、消息和系统配置的页面级失败/旧数据保留/重试已补；消息快速双击与系统配置非法值取消分支也已补；积分、优惠及剩余重复提交边界仍需继续收敛。
- [ ] P4/P5：目标服务器 SSH、Docker/PostgreSQL、备份恢复/回滚仍未验收；仅允许本地/内网非支付内部测试。

### 2026-08-10 当前状态汇总（以本节为准）

- [x] P1 已完成本地/内网非支付运行态、失败重试、重复操作门禁和审计闭环。
- [ ] P2 保持进行中：关系授权/默认/解除、脱敏导出、跨账号权限和 Downloads 文件内容已有证据；不可逆学员档案合并仍等待用户明确确认。
- [x] P3 已完成：页面成功/取消/失败重试、流水/审计、消息启停原子门禁、优惠规则/系统配置重复保存审计去重均已验证。
- [ ] P4 未完成：目标服务器 SSH、Compose/PostgreSQL 迁移对账、上传卷、备份恢复和只读回滚尚未执行。
- [/] P5 本地/内网非支付冻结材料已完成；最终发布仍受 P2 合并授权、P4 服务器验收和底层请求统计限制，不批准生产发布。

### 2026-08-10 当前轮补充

- [x] 5185 H5 重启/刷新后真实进入“我的”，账号安全确认层截图可见，取消后表单和临时密码保留，未提交密码修改。
- [x] 管理端消息通知：既有临时消息快速双击启用后只显示一次成功提示，随后停用恢复原状态。
- [x] 管理端消息通知：3199 断网时保留 5 条旧列表并显示错误/重试；恢复 API 后重试恢复 7 条，临时消息仍停用。
- [x] 管理端系统配置：非法临时值取消后 `invoiceDays` 保持 7；3199 断网保留 5 条旧配置，恢复后重试恢复 6 条。

## 2026-07-31：学员域与真实联动规划

- [x] 复核 `User`、`Order.participants`、报名模板和管理端学员入口的当前事实
- [x] 使用 artifact-tool 更新 `Docs/FeatureList/培训管理系统功能清单.xlsx`
- [x] 生成 `Docs/Summary/2026-07-31_学员域重构与真实联动项目现状总结.md`
- [x] 生成 `Docs/Plans/2026-07-31_脱离Mock的前后端数据库联动任务规划.md`
- [x] 保存根目录副本 `project_baseline.md` 与 `implementation_plan.md`
- [x] 完成 Excel inspect、公式错误扫描、目标范围渲染和视觉检查

### 本次工具/校验记录

| 问题 | 处理 |
|---|---|
| PowerShell 直接调用 `pnpm` 被执行策略拦截 | 使用 `pnpm.cmd` 执行管理端构建 |
| artifact-tool 脚本在导出/渲染后返回 teardown 错误 | 已确认导出 xlsx、inspect 和四张目标范围预览均已生成，并用独立校验脚本复核 |
| 全表 render 可能产生超大位图 | 仅渲染 `使用说明`、标准/原子功能前段和模块统计目标范围 |

## 2026-07-31：学员域数据库设计方案

- [x] 核对现有 Prisma schema、学员域现状总结和 P0–P9 任务规划
- [x] 编写 `Docs/Plans/2026-07-31_学员域数据库设计方案.md`
- [x] 同步根目录副本 `database_design.md`
- [x] 校验 Markdown 编码、章节、Mermaid/Prisma 代码块、核心模型和迁移内容

## 2026-07-31：按规划 P1 实施学员域数据模型

- [x] 备份当前 SQLite 主库并记录 P1 备份文件
- [x] 新增 Prisma `Student`、`AccountStudent`、`RegistrationTemplateVersion`、`Enrollment` 模型及反向关系
- [x] 新增 `0002_student_domain` 迁移，保留 `Order.participants`
- [x] 更新自定义迁移器到 0001 → 0002、SQLite `user_version=5`
- [x] 在测试库和当前主库执行迁移并核对表、索引、旧订单快照
- [x] 增加迁移回归断言并通过后端测试/构建

### P1 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| `prisma generate` 在替换 `query_engine-windows.dll.node` 时返回 `EPERM rename` | 使用 pnpm 脚本和直接 Prisma CLI 各尝试一次 | `prisma validate`、格式化、后端构建和测试均通过；记录为 Windows 查询引擎文件锁，后续释放占用后重新执行完整 generate |
| PowerShell 内联 `node -e` 的 `VACUUM INTO` 引号解析失败 | 1 | 改用仓库内一次性备份脚本执行一致性备份，备份成功 |
| 清理临时 P1 测试数据库的显式删除操作被审批层拒绝 | 1 | 保留在 `backend/api/data/training.p1-migration-test.db`，不影响主库和代码；未再次尝试删除 |

## 2026-07-31 学员域后续阶段执行状态

- [x] P1：Student、AccountStudent、RegistrationTemplateVersion、Enrollment 模型与 0002 迁移
- [x] P2：历史 participants 回填、异常记录、幂等游标和订单/课程对账
- [x] P3：CreateOrderDto 兼容 `studentId`，订单与学员域事务双写
- [x] P4：后端学员 API、授权关系、合并和审计
- [x] P5：管理端学员档案与报名明细拆分
- [x] P6：C 端我的学员、已有学员选择和临时填写
- [x] P7：双读对账、新读切换与回退开关
- [x] P8：Mock 边界收敛、微信/支付 adapter 与环境门禁
- [x] P9：端到端回归、发布、备份和回滚演练

### P6 完成（2026-07-31）

- [x] C 端报名页复用已有学员、临时填写、默认学员回填并提交 `studentId`
- [x] “我的”页新增/编辑/默认/解除学员关系
- [x] 更新 FeatureList、阶段总结和计划留痕（36/105、260/402）
- [x] C 端 H5、管理端 Vite、Nest 构建与生产化回归通过
- [x] P7 双读对账、新读切换与回退开关

### P2 验收留痕

- 主库：20 orders / 21 participants / 21 Student / 21 AccountStudent / 21 Enrollment；issues=0、差异=0。
- 副本库：dry-run、坏 JSON、手机号冲突、正式回填和重复执行均已验证。
- 自动化：`backend/api/test/backfill.spec.ts` 通过。
- 报告：`Docs/Summary/2026-07-31_P2_历史学员回填正式对账报告.json`。

### P3 验收留痕

- 旧请求和新 `studentId` 请求均可下单；新建档案、账号关系和报名履历同步落库。
- 越权 `studentId`、手机号冲突、容量不足和重复报名均有明确错误；越权测试确认订单/履历整体回滚。
- 取消/退款会同步履历状态；后端全量 5 套件、12 项测试通过，构建通过。
- 下一步：P4 暴露分页档案、关系维护、履历查询、合并和审计 API。

### P4 验收留痕

- `/admin/student-profiles` 系列接口覆盖档案、关系、履历、匹配、合并、停用和脱敏导出；旧 `/admin/students` 保持兼容。
- `backend/api/test/student-api.spec.ts` 通过；全量后端 5 套件、12 项测试和构建通过。
- 下一步：P5 管理端切换到独立档案列表/详情和报名明细数据源。

### P5 验收留痕

- 学员管理使用 `/admin/student-profiles`，报名明细使用 `/admin/enrollment-records`；旧接口保留兼容。
- 详情分别展示账号关系/履历摘要与模板版本/表单快照；管理端 Vite 构建通过。
- 下一步：P6 改造 C 端报名页和“我的”页。

## 2026-08-08：C 端确认弹窗层级修复留痕

- [x] 核对业务弹窗、系统确认根节点、遮罩和内容容器的实际层级关系
- [x] 修复 `uni.showModal` 内容容器沿用默认 `z-index: 999`/定位导致的遮挡风险
- [x] 将所有 C 端二次确认统一约束为业务 `1000`、系统根/遮罩 `10000`、内容 `10001`
- [x] 更新 `mvp/scripts/verify.mjs` 和 `Docs/Summary/2026-08-08_C端二次确认弹窗层级复核.md`
- [x] 完成 C 端 H5 构建、根目录 verify 和差异检查
- [ ] 重启 H5 服务后完成浏览器人工回归：修改密码、保存资料、取消报名、支付凭证、开票、报名提交、学员关系操作

## 2026-08-03：C 端独立“我的学员”页面优化

### 目标

将“我的学员”从“我的”页内嵌列表升级为独立页面入口，放在“账号与安全”下方、“我的积分”上方；页面展示当前学员和其他学员，当前学员优先匹配账号资料，其他学员按姓名拼音首字母分组排序。

### 阶段

- [x] 核对报名模板回填字段与未匹配字段的填写边界
- [x] 核对现有 `/students` 接口、账号关系和默认学员能力
- [x] 新增独立学员页面与路由
- [x] 调整“我的”页入口并移除内嵌列表
- [x] 实现当前学员匹配、空状态和拼音首字母分组
- [x] 构建、接口和页面静态验收，更新总结留痕

### 验收结果

- 新增 `pages/students/students` 页面；“我的”页入口位于“账号与安全”下方、“我的积分”上方。
- 当前学员按显式默认、手机号、姓名、企业匹配；当前账号无匹配时显示空状态。
- 其他学员按中文拼音排序器转换首字母并分组，拉丁字母直接归一化，数字归入 `#`。
- C 端 H5 构建、根目录 `verify`、`git diff --check` 和 `/profile`、`/students` 运行态核验通过。

### 决策

- 报名模板仍以模板字段定义为准；只有识别到姓名/手机号/性别/邮箱/企业/部门/职务等字段别名时自动回填，其余字段保留手动填写。
- 当前学员排序优先级为：账号关系 `isDefault` → 学员手机号/姓名/企业与当前账号资料匹配 → 无当前学员空状态；不自动修改数据库默认关系。
- 其他学员使用当前账号 `/students` 返回的 active 关系，只做前端分组展示，不改变跨账号授权和历史报名数据。

## 2026-08-03：单机服务器部署规划

- [x] 核对 API、管理端、C 端 H5 的生产入口、构建产物、环境变量和端口
- [x] 形成单机部署拓扑、systemd 守护、Nginx 双站点和防火墙方案
- [x] 形成 SQLite 初始化/迁移、WAL-safe 备份、回滚和 P9 读模式门禁
- [x] 新增 `Docs/Summary/2026-08-03_单机服务器部署指南.md`
- [x] 新增 `Docs/Plans/2026-08-03_单机部署实施方案.md`
- [ ] 获取服务器操作系统、SSH 授权、域名/HTTPS 和真实渠道配置后执行远程部署

## 2026-08-03：C 端登录界面简化

- [x] 将“演示账号”改为“账号”，取消账号和密码自动填充
- [x] 删除微信一键登录、C 端用户/运营人员/管理人员快捷入口和演示密码提示
- [x] 按截图框架重做登录卡片，移除隐私政策与开放平台/扫码登录相关内容
- [x] 同步清理“账号与安全”中的微信绑定提示
- [x] 完成 H5 构建、根目录 verify、差异检查和本地页面视觉复核

## 2026-08-03 注册/找回密码与生产化准备

- [x] 后端注册、密码找回挑战、验证码策略和审计
- [x] C 端注册页、找回密码页、登录入口和路由
- [x] 注册/找回密码自动化测试与后端构建
- [x] 写入生产支付、真实微信、短信渠道和 PostgreSQL 迁移前置条件
- [ ] 停止占用 Prisma 查询引擎的旧 Node 服务后重新执行 `pnpm.cmd --dir backend/api db:generate`
- [ ] 用户确认目标数据库、服务器/域名、微信登录场景和正式/沙箱支付渠道后执行下一阶段真实联调

## 2026-08-04 真实支付安全门禁与渠道适配

- [x] 增加 PaymentTransaction 模型、迁移和内测库升级到 user_version=8
- [x] 禁止客户端直接确认在线支付，新增状态查询和回调入账
- [x] 实现微信支付 v3 H5/Native、支付宝 WAP 签名下单和回调验签代码
- [x] C 端支付改为真实意图/跳转/原生支付及服务端状态轮询
- [x] 支付安全专项测试、构建和运行态路由验收
- [ ] 注入真实商户配置并用沙箱/小额订单完成回调验收

### 本阶段错误与处置

| 错误 | 处置 |
|---|---|
| Prisma generate 在 Windows 重命名查询引擎时 EPERM | 确认 3100 仍被 Node 进程占用；未擅自停止服务，保留现有已含新模型的生成客户端，待维护窗口重试 |
| 全量 Jest 5 分钟无输出超时 | 新增专项测试已通过；记录为全量回归环境门禁，后续停止旧服务/清理锁后分组重跑 |
| 首次 C 端构建超过默认 120 秒 | 延长到 300 秒后成功，只有既存 Sass 弃用警告 |
## 2026-08-04 项目现状核验与后续阶段

- [x] 读取并核对现有 Docs/Summary、Docs/Plans、代码、Prisma schema 和 SQLite 实例
- [x] 区分学员档案、注册/找回密码和支付功能的“代码完成”与“真实业务验收”状态
- [x] 写入当前核验报告和用户待提供信息清单
- [ ] P10：按本人/多学员/跨账号授权/模板差异/异常回滚等场景完成学员档案内测
- [ ] P11：在独立 PostgreSQL 测试库完成 SQLite WAL-safe 备份、导入和对账
- [ ] P12：完成微信/支付宝沙箱或小额商户订单的下单、回调验签、幂等和失败回滚验收
- [ ] P13：完成维护窗口内生产数据库切换、只读回滚副本和发布验收

### 本阶段错误与限制

| 问题 | 处理 |
|---|---|
| PowerShell 读取历史 Markdown 显示乱码 | 以文件实际 UTF-8 内容和代码/数据库只读证据为准，新报告使用 UTF-8 写入 |
| 直接读取 SQLite 主文件存在 WAL 风险 | 只读检查可以继续，发布备份必须使用 checkpoint + `VACUUM INTO` 的 WAL-safe 流程 |
| 当前未发现 3100/5174/5185 监听 | 不根据 Node 进程数量判断服务状态，启动后再做健康接口和页面验收 |
## 2026-08-04 用户确认 PostgreSQL 与内测环境

- [x] 确认 PostgreSQL、H5+小程序、内部验证码和内网服务器边界
- [x] 识别并修复 DATABASE_URL 被误当 SQLite 文件路径的问题
- [x] 增加 PostgreSQL 专用 schema/client/migration 准备脚本及 SQLite 流程保护
- [x] 生成数据库实例说明和无域名支付回调限制文档
- [x] 已确认服务器为 Ubuntu 24.04 系列、Node.js v22.22.1；`psql` 客户端尚未安装
- [ ] 在服务器确认 PostgreSQL 安装、版本、数据库、应用账号和 SSL 配置
- [ ] 执行 SQLite WAL-safe 导出、PostgreSQL 测试库导入与对账
- [ ] 获取公网 HTTPS 回调地址后完成微信/支付宝沙箱或小额支付验收
## 2026-08-04 小程序支付补齐

- [x] 增加微信 JSAPI 下单和小程序支付参数签名
- [x] 从当前账号读取 `wechatOpenId`，缺失时拒绝伪造支付
- [x] 增加微信登录场景参数（小程序/H5/公众号）
- [x] 增加 JSAPI 签名测试并完成 API 构建回归
- [ ] 配置真实小程序 AppID/AppSecret、商户证书和公网回调后做小额实测

## 2026-08-04 个人收款码线下闭环补齐

- [x] 管理端可分别上传微信/支付宝个人收款码图片
- [x] C 端报名账单和订单支付记录展示收款码
- [x] 付款凭证上传、待审核、审核通过/驳回链路保留并完成专项测试
- [ ] 真实在线支付仍待企业/个体工商户商户配置和公网 HTTPS 回调

## 2026-08-04 渠道配置自检

- [x] 新增管理员 `/api/admin/integration-readiness` 自检接口
- [x] 覆盖微信登录、微信支付、支付宝支付和找回密码渠道
- [x] 自检结果不包含密钥内容，并完成接口回归测试
- [ ] 仍待真实商户配置和公网回调进行最终支付验收
- [x] 管理端“系统管理 → 渠道自检”已接入并完成构建验证

## 2026-08-04 PostgreSQL 服务器初始化准备

- [x] 新增 Ubuntu 24.04 PostgreSQL 安装/初始化脚本
- [x] 脚本采用独立应用账号，不使用 Linux root，不输出数据库密码
- [ ] 在目标服务器本地执行脚本并确认 PostgreSQL 实例信息
- [x] 新增带 `CONFIRM=YES` 门槛的 SQLite → PostgreSQL 测试迁移脚本
## 2026-08-04 完成度审计回归

- [x] 后端全量测试 11/11、25/25 通过
- [x] API/C 端/管理端构建和 verify 通过
- [x] 记录服务器仅 SSH 可达，尚未确认 PostgreSQL 实例
- [ ] 获取数据库实例非敏感信息后执行远程安装/迁移
- [ ] 获取公网 HTTPS 回调后完成真实支付验收
## 2026-08-04 SQLite → PostgreSQL 迁移工具核验

- [x] 新增只读 SQLite 导出和 SHA-256 校验
- [x] 新增 PostgreSQL 按依赖顺序导入和行数回报
- [x] 修复迁移脚本 ESM/CommonJS 扩展名问题
- [x] 实际验证 SQLite 导出和脚本语法
- [ ] 在独立 PostgreSQL 实例执行导入与对账

## 2026-08-04 本地到服务器部署及渠道实施手册

- [x] 核对现有单机部署指南、实施方案、服务器脚本、API 环境变量和支付回调入口
- [x] 明确 Windows 本地上传、Ubuntu 服务器安装、systemd/Nginx、PostgreSQL 迁移和回滚边界
- [x] 新增当前权威实施手册到 `Docs/Summary`
- [x] 完成 UTF-8、敏感信息、旧 `rsync`/图片说明和 `git diff --check` 检查
- [x] 根据服务器截图补充“直接部署在 `/opt/training-management`、没有 `current`”的兼容步骤和 JWT_SECRET 轮换提醒
- [ ] 用户在 `192.168.0.162` 执行 PostgreSQL 初始化并回传非敏感自检结果
- [ ] 在独立 PostgreSQL 测试库完成导入、行数/金额/订单状态对账
- [ ] 获取公网 HTTPS 回调后完成微信/支付宝沙箱或小额支付闭环验收

## 2026-08-04 Docker Compose 单机部署

- [x] 核对现有构建入口、Prisma PostgreSQL 生成流程和前端资源路径
- [x] 新增 API/管理端/H5 Dockerfile、Compose、Nginx 配置、`.dockerignore` 和环境变量模板
- [x] 新增 Docker 单机部署、SQLite → PostgreSQL 迁移、备份回滚实施方案
- [x] 更新 README 的 Docker 快速入口和 SQLite/生产 PostgreSQL 说明
- [x] `docker compose config`（验证环境变量）和根目录 `pnpm run verify` 通过
- [ ] 在服务器安装 Docker Engine/Compose 并执行真实镜像构建
- [ ] 在 Docker PostgreSQL 卷中完成迁移、对账和 P10 内测

### Docker 本地验证限制

| 问题 | 处理 |
|---|---|
| 本地 Docker CLI 可用但 Docker daemon 未运行 | Compose 静态配置已验证；镜像构建需在服务器或启动 Docker Desktop 后执行 |

## 2026-08-06 管理端上线前完善（支付暂缓）

### 目标

在不等待真实微信/支付宝配置的前提下，先把管理端已存在的业务接口真正接入为可安全操作、可反馈、可复核的功能，完成上线前的非支付业务闭环。

### 阶段

- [x] 盘点管理端模块、按钮、后端接口和当前风险
- [x] 完善订单审核/退款、开票处理、用户启停用/密码重置的确认、错误处理和重复提交保护
- [x] 完善学员档案详情中的资料编辑、状态、账号关系和报名履历操作入口
- [x] 对课程、Banner、模板、反馈、消息、积分等操作做统一交互验收（静态按钮/API 对照脚本已增加）
- [x] 对未接入的短信/邮件通知渠道做明确门禁，避免管理端误报发送成功
- [ ] 完成管理端构建、后端测试、静态按钮/API 对照和浏览器验收（构建、测试和静态/API 已完成；浏览器接管受本机 URL 安全策略阻止）
- [x] 更新 `Docs/Summary` 阶段总结、上线差距清单和下一步规划

### 本阶段边界

- 微信/支付宝真实支付、商户回调和退款 API 暂缓，不作为本阶段完成条件。
- 线下支付凭证审核仍属于本阶段业务，可继续验收。

### 错误记录

| 问题 | 尝试 | 处理 |
|---|---:|---|
| 浏览器控制层拒绝接管 `localhost:5174` | 1 | 不绕过安全策略，改用本地 HTTP 运行态、静态验收和构建测试；待可访问浏览器环境再做逐项视觉验收 |
| 沙箱无法创建 `.git/index.lock` | 1 | 不修改 `.git` 权限；保留工作区改动，由用户在正常 Git 终端提交 |

## 2026-08-06 P10 学员档案场景回归

- [x] 增加一个账号代理多个学员报名和默认学员切换专项测试
- [x] 增加模板必填字段失败事务回滚专项测试
- [ ] 在管理端和 C 端浏览器中完成本人/代理多人/跨账号授权的人工验收

## 2026-08-06 上线前非支付功能审计与补齐（当前目标）

### 目标

在支付商户申请期间，基于当前代码和运行证据，逐项消除管理端“有按钮但无真实能力、重复入口、状态不允许仍可操作”的问题，先形成可上线的非支付版本。

### 阶段

- [x] 建立审计范围：管理端按钮、后端接口、自动化验证；支付暂不纳入
- [x] 完成基线验证：根目录 verify、管理端构建/UI 静态验收、后端全量测试
- [x] 识别审计日志无效按钮、报名明细重复详情按钮、订单不可操作状态仍显示操作按钮、删除缺少确认、学员合并接口缺少管理端入口
- [x] 修复管理端按钮能力矩阵与状态条件，删除无效/重复入口
- [x] 接入学员档案合并入口并补充冲突/刷新/审计验收
- [x] 为非支付关键管理操作增加接口与 UI 回归断言
- [x] 补齐课程报名状态、截止时间和剩余名额的后端统一门禁，并增加回归测试
- [x] 完成阶段构建、测试、运行态检查并更新 `Docs/Summary` 总结与下一步规划

### 当前阶段遗留

- [ ] 在可接管的浏览器环境完成逐模块人工验收；当前本机浏览器控制层此前拒绝 localhost
- [ ] 用户在正常 Git 终端提交/推送工作区变更
- [ ] 后续进入 P1 非支付业务增强、P2 PostgreSQL/Docker 迁移与备份回滚验收

## 2026-08-06 继续推进：可上线非支付版本（当前目标）

目标：在支付申请期间，继续把管理端和非支付业务从“已有基础接口”推进到“可验收、可追溯、可发布”的版本；支付商户接入不作为本轮完成条件。

### 计划

- [x] 重新核对当前工作区、管理端模块、后端管理接口、数据模型和已有验证证据
- [x] P1-1：补齐优惠规则停用/删除与冲突提示
- [x] P1-2：补齐积分流水查询与来源展示
- [x] P1-3：补齐审计筛选（操作类型、操作者、关键字、时间范围）
- [x] P1-4：补齐站内消息目标范围、展示时间和阅读状态，并验证不可见消息越权
- [x] P1-5：收紧学员导出权限、范围和敏感字段脱敏
- [x] P1-6：为发票文件状态建立可迁移的状态预留和管理端展示
- [x] P2：完成新增功能的权限、异常、重复提交和跨模块回归验收
- [ ] P3：在 Docker PostgreSQL 测试卷完成迁移、行数/金额/订单状态对账，以及备份恢复/回滚演练（Compose/脚本已准备，服务器实测待完成）
- [ ] P4：完成构建、接口、运行态和可访问浏览器逐模块验收，形成非支付内测发布记录

### 本阶段约束

- 微信/支付宝真实支付、商户回调和官方退款 API 暂缓。
- 不把未接入的短信、邮件、电子发票平台显示为已完成。
- 所有新增危险操作必须有状态门禁、二次确认、请求锁、错误反馈和审计记录。

### 当前证据与限制

- 基线：根目录 verify、管理端构建/UI 静态验收、后端 12 套件/31 项测试此前通过。
- 当前工作区存在未提交修改，`.git` 写入受本地沙箱限制；不得擅自替用户提交或推送。
- 浏览器控制层此前无法接管 localhost；需以可访问浏览器或用户回传结果完成视觉/点击验收。

### P1-4 验收留痕（2026-08-06）

- [x] `MessageRead` 模型和 SQLite `0006_message_reads` 迁移已接入；PostgreSQL 导出/导入顺序已包含消息阅读表。
- [x] 管理端消息表单支持目标用户 ID、目标课程 ID、开始/结束展示时间，保存时转换为数组，编辑时回填。
- [x] 后端拒绝不存在的目标用户/课程，按启用状态、时间窗口、用户目标和报名课程过滤消息。
- [x] 用户消息接口返回 `readAt`/`unreadCount`，标记已读前验证消息对当前账号可见。
- [x] 回归覆盖全量、指定用户、指定课程、时间范围、已读和不可见越权；后端 12 套件/36 项通过，API/管理端构建和 UI 静态验收通过。

### P1-4 错误记录

| 问题 | 尝试 | 处理 |
|---|---:|---|
| Windows Prisma 查询引擎 DLL 被占用，`db:generate` 返回 EPERM rename | 2 | 确认现有生成客户端已包含 `MessageRead`，API 测试/构建均通过；保留问题，待维护窗口停止占用进程后重试，不反复强杀服务 |

### P1-5 错误记录

| 问题 | 尝试 | 处理 |
|---|---:|---|
| 导出回归夹具未填写 `course-2` 模板必填的企业规模 | 1 | 补齐测试数据，不放宽正式报名模板校验后重跑 |

### P1-5/P1-6 验收留痕（2026-08-06）

- [x] 学员导出接受当前关键字/状态筛选，最多导出 1000 条并返回 `truncated`；默认不返回手机号/邮箱明文，响应标记 `sensitiveFieldsMasked=true`。
- [x] 导出审计记录包含实际导出数/总数、脱敏状态和是否截断；明文模式仅由管理员受控调用，管理端默认不暴露明文导出按钮。
- [x] 开票申请创建时初始化 `invoiceFileStatus=未生成`；通过后为 `待上传`，驳回为 `不适用`，管理端新增文件状态列。
- [x] 后端只允许“待处理”开票申请继续处理，开票通过必须提供非空发票号码。
- [x] P1-5/P1-6 专项测试、API/管理端构建和 UI 静态验收通过。

### P2/P3 当前证据（2026-08-06）

- [x] 生产 API HTTP 回归覆盖普通用户 403、刷新令牌轮换、订单/开票/退款、凭证受控访问、消息阅读、学员脱敏导出和应用重启持久化。
- [x] 生产 API 套件 5/5 通过；Docker Compose 静态配置通过，Compose 前端宿主端口已调整为 18080/18081，避开服务器现有 8080/8081 占用。
- [x] 新增 `Docs/runtime/backup-postgres-docker.sh`，生成 PostgreSQL custom dump 和 SHA-256 校验，默认不输出密钥。
- [ ] 本地 Docker daemon 未运行，无法在本机完成镜像构建/容器迁移；需在 `192.168.0.162` 服务器执行真实迁移、对账和恢复演练。

### P2 验收留痕（2026-08-06）

- [x] `production.spec.ts` 5/5 通过；覆盖权限拒绝、令牌轮换、订单/开票/退款、凭证文件受控访问、消息阅读、脱敏导出、重复开票和重启持久化。
- [x] 后端全量 12 套件/36 项通过，API/管理端构建、管理端 UI 静态验收、根目录 verify 和 diff 检查通过。

## 2026-08-06：可上线非支付版本主线（当前执行计划）

### 目标

在微信/支付宝商户申请期间，先完成不依赖在线支付的管理端、C 端和 API 业务闭环，解决“按钮存在但能力不完整、重复操作会覆盖状态、缺少人工验收证据”等问题。个人收款码线下凭证审核可以继续保留，但真实在线支付不作为本阶段门槛。

### 阶段

- [x] 阶段 0：基线审计、模块/按钮/API 对照、现有测试证据汇总
- [x] 阶段 1：管理端剩余生命周期闭环（消息启停/删除、反馈重复处理门禁及错误反馈）
- [x] 阶段 2：非支付业务增强（C 端消息中心、发票文件上传状态链路、导出/审计细节）
- [ ] 阶段 3：管理端和 C 端逐模块浏览器人工验收，记录请求、响应、数据库变化和审计结果
- [ ] 阶段 4：Docker/PostgreSQL 测试迁移、备份恢复和非支付内测发布
- [ ] 阶段 5：支付申请完成后另行进行商户沙箱/小额回调验收（不阻塞本主线）

### 当前决策

- 当前服务器和本地环境的 `PAYMENT_ADAPTER` 继续保持 `disabled`；不得用前端跳转或个人收款码直接宣称在线支付成功。
- 所有管理端写操作必须具备：后端状态门禁、前端二次确认（危险操作）、请求锁、错误反馈、成功后刷新和审计留痕。
- 可删除数据必须明确是否会影响历史业务；消息可删除时级联删除阅读记录，课程/模板等继续沿用已有历史引用保护。

### 阶段 1 验收标准

- 已处理反馈不能再次被回复覆盖，重复处理返回明确错误并保留原审计记录。
- 消息支持启用/停用和安全删除；删除后用户不可见，阅读记录级联清理并写审计。
- 管理端按钮/API 静态对照、后端回归、前端构建和 `git diff --check` 全部通过。

### 当前错误与限制

| 问题 | 处理 |
|---|---|
| 本地浏览器控制层此前拒绝接管 `localhost` | 不绕过安全策略；先用 HTTP、静态检查和自动化回归，待可接管环境再补人工验收 |
| 工作区 `.git` 索引在当前沙箱不可写 | 不擅自修改权限；代码和留痕由用户在正常 Git 终端提交 |
## 2026-08-06：可上线非支付版本续作（当前计划）

### 目标

支付商户申请期间，先完成管理端、C 端和 API 的非支付业务闭环；所有管理端按钮必须有明确的后端能力、状态门禁、重复提交保护和错误反馈。真实在线支付不作为本轮上线条件。

### 当前阶段

- [x] 审计管理端模块、按钮与 API/状态门禁映射
- [x] 修复报名明细/学员列表缺失详情入口
- [x] 修复已处理反馈仍显示回复操作
- [ ] 补齐剩余关键保存/删除操作的请求锁、错误反馈和详情查看
- [ ] 完成管理端逐模块浏览器人工验收
- [ ] 启动 C 端 H5 并完成消息、学员、业务、发票状态验收
- [ ] 完成 Docker/PostgreSQL 非支付发布、备份恢复和回滚验收

### 2026-08-07 本轮继续执行

- [x] 修复报名明细“关联课程”筛选参数错误：前端和导出统一发送 `courseId`，后端列表接口按课程过滤。
- [x] 为报名明细课程筛选增加生产化 API 回归断言。
- [x] 报名明细筛选选项改为完整课程列表，导出元数据与实际筛选字段保持一致。
- [x] 修复服务端分页模块导出只取 100 条的问题，按页汇总至 1000 条上限并保留截断提示。
- [x] 修复学员新读模式下报名汇总详情缺少 `paymentStatus` 导致支付状态分类错误的问题。
- [x] 管理端 build、`test:ui` 和生产化 API 回归通过。
- [ ] 继续审计其他管理端筛选、导出和写操作的参数/状态闭环。

### 本轮回归结论

- [x] 已完成报名明细筛选、服务端分页导出、学员新读模式兼容和分页读取修复。
- [x] 已完成后端、管理端、C 端 H5 和根目录回归。
- [ ] 运行态人工验收与服务器 Docker/PostgreSQL、备份恢复证据仍待完成。

### 2026-08-06 本轮增量完成

- [x] 删除修改密码空请求回退 `123456`，补充后端回归测试。
- [x] 修正电子发票用户访问地址，并在 C 端增加受保护的“查看电子发票”入口。
- [x] 通过 `mvp.spec.ts` 18/18、管理端 `test:ui`、根目录 `verify` 和 C 端 H5 构建。
- [x] 管理端已上传电子发票增加受保护“查看发票”按钮，并补充 UI 静态断言。
- [x] 全量后端回归达到 12 套件/41 项，生产化 API 5/5；API/管理端构建、根目录 verify 和 diff 检查通过。

### 本轮错误记录

| 问题 | 尝试 | 处理 |
|---|---:|---|
| 报名明细/学员列表操作列为空 | 1 | 详情处理器被条件排除；已将两个模块接入唯一查看详情入口 |
| 已处理反馈仍显示回复按钮 | 1 | 前端未设置 `canOperate` 状态门禁；已限制为待处理状态 |

## 2026-08-06 可上线非支付版本继续执行（本轮）

### 本轮目标

把“管理端按钮是否实现”从静态存在性推进到运行态可观察、后端有状态门禁、失败可反馈，并优先修复会造成业务状态被重复覆盖的缺口。

### 本轮结果

- [x] 自动化回归：后端 12 套件/39 项、管理端 `test:ui`、根目录 `verify` 通过。
- [x] 浏览器运行态抽查：工作台、课程管理、报名汇总/明细、订单、开票、学员、用户、优惠、反馈、消息、积分、渠道自检、系统配置、审计页面均能进入；课程新增、订单详情、学员详情、反馈回复弹窗可打开并关闭。
- [x] 修复线下支付凭证审核后端门禁：只有“待审核”订单且凭证为 `pending` 时可以审核，防止重复审核覆盖已处理结果；补充 17 项 `mvp.spec.ts` 回归并通过。
- [x] API build、管理端 build 通过。

### 下一步顺序

1. 补完 C 端消息、学员、业务、发票状态的运行态验收，并记录真实请求/响应。
2. 在 `192.168.0.162` 完成 Docker/PostgreSQL 测试卷迁移、行数/金额/状态对账。
3. 执行备份、临时恢复库恢复和回滚演练，确认上传目录/数据库卷持久化。
4. 形成非支付内测发布清单；`PAYMENT_ADAPTER=disabled` 保持不变。

### 已知限制

- 当前人工浏览器控制存在偶发导航时序问题，需要先展开导航组再点击子模块；不把偶发控制层失败判定为业务失败。
- 服务器 Docker/PostgreSQL、真实浏览器逐按钮全量验收仍需要外部运行环境证据；未完成前不标记为正式上线。

## 2026-08-07 增量：管理端紧凑自适应列表布局与导出选择

- [x] 非工作台页面切换为独立 `page-main` 视口容器，桌面端固定在 `100vh` 内，工作台保留独立滚动。
- [x] 搜索区、操作区、面包屑和分页区压缩高度，列表区使用 `flex` 占据剩余空间，表格容器承担横向/纵向滚动。
- [x] 列表首列增加复选框和“全选当前列表”；未勾选时导出全部筛选结果，勾选后仅导出勾选记录。
- [x] 学员档案导出继续使用脱敏接口；服务端分页模块无勾选时按筛选条件请求最多 1000 条并提示截断。
- [x] 管理端 build、`test:ui`、根目录 `verify`、后端 12 套件/42 项和 `git diff --check` 通过。

### 下一步

1. 在最新管理端实例手动验证不同分辨率下页面不滚动、表格内部滚动和复选导出。
2. 继续阶段 D 的 C 端写操作/发票查看人工证据，再进入服务器 PostgreSQL 对账和备份恢复。

## 2026-08-07 增量：顶部面包屑/退出登录与独占标签行

- [x] 顶部第一行使用 `header-top-row`，页面面包屑从最左侧开始，退出登录按钮固定在最右侧。
- [x] 顶部第二行由已访问标签页独占，保留横向滚动，不再与退出按钮争用宽度。
- [x] 退出登录保持紧凑文本形式；桌面端释放列表区垂直空间，窄屏允许面包屑行自动换行。
- [x] 管理端 build、`test:ui` 和 `git diff --check` 通过。

## 2026-08-07 增量：部署入口一致性审计

- [x] 对照管理端 18 个模块、前端操作入口和 `mvp.controller.ts` 路由，未发现新的“按钮完全没有后端接口”缺口。
- [x] 发现 README 仍写旧的 8080/8081 地址，与当前 Compose 使用的 18080/18081 不一致。
- [x] 已修正 README，并明确端口更换时需要同步 Compose 与防火墙规则。
- [ ] 仍需在服务器上完成容器启动、迁移、对账和恢复证据；静态路由审计不能替代运行态验证。

## 2026-08-07 增量：面包屑视觉对齐

- [x] 面包屑移除边框和背景，改为参考图中的纯文本/斜杠样式。
- [x] 面包屑与列表内容使用相同的左右内边距；退出登录固定在同一行最右侧，标签页继续独占下一行。
- [x] 管理端 build、`test:ui` 和 `git diff --check` 通过。

## 2026-08-06 增量阶段：订单关闭能力

- [x] 对照功能清单确认待支付订单手动关闭属于非支付上线必要的订单生命周期能力
- [x] 新增后端关闭接口、状态门禁、名额/报名履历事务联动和审计记录
- [x] 管理端接入待支付“关闭订单”按钮、二次确认、请求锁和成功刷新
- [x] 删除管理端无服务端数据来源的“已退款”筛选项
- [x] 补充单元与 HTTP 回归，完成后端 12 套件/42 项、管理端构建、UI 静态验收和根目录 verify

### 当前下一步

1. 在可接管的浏览器或服务器测试环境完成 C 端登录、消息、我的学员、业务/凭证、发票文件的逐点击验收。
2. 在 `192.168.0.162:/opt/training-management` 执行 Docker Compose PostgreSQL 测试卷迁移、行数/金额/订单状态对账。
3. 运行数据库备份、临时恢复库和回滚演练，验证上传目录与数据库卷持久化。
4. 形成非支付内测发布清单；支付商户申请完成前继续保持 `PAYMENT_ADAPTER=disabled`。

## 2026-08-07：非支付可上线版本总规划与管理端标签页

### 现状结论

- 管理端课程、Banner、报名模板、报名汇总/明细、订单、开票、学员档案、用户、优惠、反馈、消息、积分、系统配置、审计和渠道自检均已具备对应 API 或只读查询；按钮缺失主要不再是静态入口问题，而是需要运行态和服务器证据确认。
- 所有主要写操作已具备后端状态门禁、危险操作确认、请求锁、失败提示、成功刷新和审计记录；订单关闭、凭证审核、重复开票、重复反馈处理等高风险状态已补回归。
- 本轮新增管理端“已访问标签页”：访问页面自动记录、点击切换、关闭标签自动选择相邻页面，首页不可关闭，并在当前浏览器会话中保存。
- 当前版本定位为“非支付内部测试候选版本”，真实在线支付继续排除在本轮上线门槛之外。

### 分阶段实施

- [x] A：管理端按钮/API/状态门禁静态审计与缺口修复
- [x] B：C 端消息、我的学员、业务、发票文件和线下凭证链路补齐
- [x] C：管理端标签页体验增强及构建/静态验收
- [ ] D：管理端与 C 端真实运行态逐模块验收，记录请求、响应、数据和审计变化
- [ ] E：服务器 Docker/PostgreSQL 测试迁移、行数/金额/状态对账
- [ ] F：备份、临时恢复库、回滚及上传目录/数据库卷持久化演练
- [ ] G：非支付内测发布清单、监控和故障处理手册
- [ ] H：支付商户资料齐备后再做微信/支付宝沙箱或小额回调验收

### 当前发布门槛

必须同时具备 D、E、F、G 的证据后，才可以称为“非支付内测可发布”；在此之前不得宣称正式生产上线。支付配置保持 `PAYMENT_ADAPTER=disabled`。

### 运行态限制

- 本机旧 API/Vite 进程占用 `3100`/`5174`，新启动的管理端自动使用 `5176`；不强杀未知进程，避免破坏用户当前环境。
- 浏览器控制策略暂时拒绝接管 `localhost` 页面；标签页交互需在可接管浏览器或用户手动点击后补充证据，不能用绕过方式替代。

### 本轮验证

- 管理端生产构建通过。
- 管理端 UI 静态验收通过，包含标签页保存、切换、关闭和样式断言。
- 根目录 `verify` 通过。
- `git diff --check` 通过（仅有既有 LF/CRLF 提示，无空白错误）。
- 后端全量回归再次执行通过：12 个套件、42 项测试全部通过。
## 2026-08-07 增量任务：搜索区紧凑化与 Keep-Alive 等价缓存

- [x] 缩小非工作台搜索区输入框、下拉框、按钮和间距，保留窄屏自适应。
- [x] 保存/恢复多页签模块状态，并清理关闭标签和退出登录后的缓存。
- [x] 保存/恢复列表横向与纵向滚动位置。
- [x] 完成管理端、根目录和后端自动化回归。
- [ ] 在可控浏览器中补充不同分辨率下的人工视觉验收证据。
## 2026-08-07 增量任务：部署可靠性加固

- [x] API Docker build/runtime 阶段显式安装 OpenSSL 和 CA 证书，降低 Prisma 运行时兼容风险。
- [x] 新增 `Docs/runtime/verify-compose-release.sh`，检查非支付门禁、Compose、迁移、PostgreSQL/API、管理端和 H5 入口。
- [ ] 在 `192.168.0.162` 使用最新仓库文件执行容器构建、迁移、对账和 smoke check。
- [ ] 生成 PostgreSQL 备份、临时库恢复和回滚演练证据。

## 2026-08-07 增量任务：报名明细敏感信息边界加固

- [x] 复核报名明细列表发现完整手机号和 `formPayload` 直接返回，记录为上线前隐私缺口。
- [x] 列表接口改为手机号脱敏且不返回表单快照；新增管理员受保护的单条履历详情接口。
- [x] 管理端详情点击改为读取单条详情，同时保留关联订单和支付凭证展示。
- [x] 新增服务层回归断言，确认列表脱敏、详情可控揭示；管理端/API 构建、UI 静态验收和全量测试通过。
- [x] 详情读取写入学员档案/报名履历审计动作，并保留关联订单和凭证读取。
- [x] 修正 Docker Compose 部署文档中容器工作目录重复拼接导致的 Prisma 命令路径错误。
- [x] 新增非支付服务器执行与验收清单，明确正确容器路径、端口、备份恢复和发布门槛。
- [ ] 在可控浏览器和服务器环境确认脱敏列表、详情权限及审计输出。

## 2026-08-07 增量任务：管理端非支付操作闭环回归

- [x] 新增管理端非支付操作 HTTP 回归，覆盖课程、Banner、模板、优惠规则、消息、配置、积分、用户和反馈。
- [x] 增强管理端 UI 静态验收，增加主要操作 API 路由矩阵检查。
- [x] 后端 13 个套件/46 项、管理端 build、`test:ui`、根目录 verify 和 C 端 H5 build 通过。
- [ ] 在可控浏览器和服务器环境补充截图、请求/响应、审计和迁移恢复证据。

## 2026-08-07 阶段 D：浏览器人工验收准备

- [x] 将管理端、C 端非支付场景拆成 D-01～D-17 可执行验收项。
- [x] 新增 `Docs/Summary/2026-08-07_阶段D浏览器人工验收记录模板.md`，统一记录 URL/状态码、数据变化、审计编号和截图。
- [ ] 在可接管浏览器或用户手动操作环境完成 D-01～D-17，并保留高风险场景失败/回滚证据。
- [ ] D 阶段全部通过后进入服务器 PostgreSQL 迁移、备份恢复和发布门槛检查。

## 2026-08-07 阶段 E/F 前置：Compose 配置门禁

- [x] 发布 smoke 脚本拒绝示例数据库密码和示例 JWT_SECRET。
- [x] 发布 smoke 脚本校验 JWT_SECRET 至少 32 个字符，并校验 `DATABASE_URL` 使用 Compose 的 `postgres` 主机。
- [ ] 在服务器填写真实服务器专用值后执行脚本，归档不含敏感值的结果。

## 2026-08-07 阶段 E：导入后数据对账工具

- [x] 新增 `verify-postgresql-reconciliation.cjs`，校验快照哈希、表行数、订单状态/金额和课程报名人数。
- [x] 新增 `pnpm db:verify:postgres` 命令，并更新服务器执行清单。
- [ ] 在 `192.168.0.162` 的 PostgreSQL 测试库执行并归档 `passed: true` 结果；本地不能替代服务器真实证据。

## 2026-08-07 增量任务：C 端按钮与二次确认层级修复

- [x] 统一订单操作按钮的圆角、边框、颜色、尺寸和 H5 默认伪元素样式。
- [x] 将“取消报名”改为无边框文字按钮，保留足够点击区域和二次确认逻辑。
- [x] 全局提高 `uni.showModal`/`uni-mask`/`uni-modal` 层级，确保修改密码等二次确认覆盖账号安全业务弹窗。
- [x] 核对 C 端源码已不存在“富文本内容”用户提示标签，并重新构建 H5。
- [ ] 在真实 H5/小程序设备上确认确认框视觉层级和按钮点击效果。

## 2026-08-07 增量任务：搜索区中等屏幕紧凑化收口

- [x] 复核桌面端与中等屏幕断点的文本框、筛选下拉框和间距尺寸。
- [x] 将中等屏幕断点收紧为文本框 135px、首个搜索框 155px、筛选下拉框最小 84px。
- [x] 在 `verify-ui.mjs` 增加桌面端/中等屏幕宽度断言，并执行静态验收。
- [x] 直接执行 TypeScript 检查、Vite 生产构建、API 构建、管理端专项 3/3 回归和根目录 `verify`；包装命令的超时不影响底层构建结果。

## 2026-08-07 增量任务：移动端遮挡、重复吸顶与高风险操作确认

- [x] 移除“我的学员”页面与 UniApp 原生导航重复的自定义顶部栏。
- [x] 为课程/订单/我的等移动端内容增加底部 Tab 安全区预留，个人资料、账号安全、学员、支付和报名弹窗提高遮罩层级并限制可视高度。
- [x] 删除课程详情中的“富文本内容”徽标。
- [x] 为取消报名、资料保存、密码修改、学员保存/设默认、支付凭证提交、开票申请、报名提交和报名人员删除增加二次确认；退出登录同步增加确认。
- [x] C 端 H5 构建、根目录 `verify`、`git diff --check` 通过。
- [ ] 在真实 H5/小程序设备上逐项确认弹窗按钮、底部 Tab 不再遮挡及取消操作不发请求。

## 2026-08-07 增量任务：管理端重要保存/处理二次确认

- [x] 复核管理端保存、审核、处理按钮是否统一需要二次确认。
- [x] 为课程、模板、Banner、优惠、消息、配置、积分、反馈、学员档案、凭证审核和开票通过接入确认。
- [x] 增加 `verify-ui.mjs` 静态确认断言。
- [x] 执行管理端 build、UI 静态验收、根目录 verify 和目标后端回归。
- [ ] 在阶段 D 浏览器人工验收中逐项确认“取消”不会发送写请求。
## 2026-08-07：非支付可上线版本规划与实施（当前活动计划）

### 目标

在暂不接入在线支付的前提下，完成管理端核心非支付功能的真实闭环、测试和上线门禁；所有阶段都要留下总结、测试证据和下一步计划。

### 阶段

- [ ] P0：建立管理端功能矩阵，逐一核对按钮、API、权限、状态门禁和测试覆盖，形成缺口清单。
- [ ] P1：内容管理闭环：课程待发布/发布/下架、Banner、报名模板，补状态约束和边界测试。
- [ ] P2：用户与学员闭环：用户启停/密码重置、学员档案编辑/关系/默认/合并，补权限和脱敏测试。
- [ ] P3：运营闭环：反馈处理、站内消息发布/启停/删除/已读、积分调整/流水、系统配置和审计查询。
- [ ] P4：管理端交互统一：替换关键原生 prompt/confirm，统一弹窗、加载、错误、空状态、重复提交和按钮禁用。
- [ ] P5：非支付真实浏览器验收：按模块执行新增、编辑、删除、启停、查询、导出、取消、失败恢复和刷新验收并保存证据。
- [ ] P6：上线门禁：生产环境变量、权限、敏感数据、上传文件、Docker/数据库迁移、备份恢复、健康检查和回滚演练。
- [ ] P7：支付留置：仅记录支付申请/沙箱接入所需材料和后续验收项，不阻塞非支付版本发布。

### 当前状态

- 当前进入 P0，已完成第一轮源码/路由/测试盘点，结果写入 `findings.md`。
- 下一步先生成可执行的管理端功能矩阵，并据矩阵选择第一个真实缺口实现；不能只以静态脚本通过作为完成依据。

### 错误记录

| 问题 | 处理 |
|---|---|
| 历史计划文件内容较旧，无法直接代表本轮上线目标 | 保留历史记录，在文件末尾追加本轮活动计划，后续以本节为准 |

## 2026-08-08：非支付可上线版本规划与实施（当前活动计划）

### 目标

在不等待在线支付商户申请的前提下，逐步把管理端与 C 端非支付功能推进到可内部上线状态。每个阶段都要留下代码变更、自动化结果、运行态证据和下一步计划；支付只保留禁用门禁和后续接入清单。

### 阶段

- [x] P0：修正验收基线并建立真实缺口清单，确保静态检查与当前实现一致。
- [/] P1：内容管理闭环（课程待发布/发布/下架、Banner、报名模板）边界、重复提交和错误恢复。
- [/] P2：用户与学员闭环（用户启停/重置、学员档案/关系/默认/合并）权限、脱敏和审计。
- [/] P3：运营闭环（反馈、消息、积分、系统配置、审计）输入边界、目标范围和幂等。
- [/] P4：管理端统一交互（应用内确认、输入弹窗、加载/错误/空状态、重复点击锁、列表内部滚动）。
- [ ] P5：非支付真实浏览器验收，按模块记录成功/失败/取消/刷新和请求证据。
- [ ] P6：发布门禁（生产环境变量、Docker/PostgreSQL、备份恢复、上传持久化、健康检查和回滚）。
- [ ] P7：支付留置清单；商户资料齐备后再单独进行沙箱/小额回调验收。

### 当前阶段交付标准

P0 必须完成：`frontend/admin-react test:ui`、管理端构建、根目录 `verify` 均通过；功能矩阵明确区分“已接线/接口闭环/待浏览器验收”，不得用静态检查替代运行态证据。

### 错误记录

| 问题 | 当前状态 | 后续动作 |
|---|---|---|
| 管理端 UI 验收脚本仍匹配旧 `window.confirm` 字符串 | 已复现 | 改为检查 `requestDialog`、`confirmAction` 和关键确认文案 |

### P0 完成记录（2026-08-08）

- 更新 `frontend/admin-react/scripts/verify-ui.mjs`，断言现行 `requestDialog`/`confirmAction` 应用内确认实现，不再依赖已删除的 `window.confirm` 字符串。
- 复核 `Docs/Summary/2026-08-07_管理端非支付功能矩阵.md`，确认管理端 18 个模块按“已接线/接口闭环/待浏览器验收”区分，支付不作为本轮上线门槛。
- 验证：管理端 UI 静态验收、管理端生产构建、根目录 `verify` 均通过；后端全量回归 13 套件/49 项通过。

### P1 已完成的第一批加固（2026-08-08）

- 后端课程保存拒绝空标题/分类/时间/地点/讲师、负价格、非整数名额、已报名人数超过名额和非法报名截止时间。
- 后端 Banner 保存拒绝空标题、非法排序、非法时间范围，并校验开始时间不晚于结束时间。
- 后端报名模板保存统一裁剪字段标识/标签/选项，拒绝选择类字段无选项和空白后重复字段标识。
- 新增回归覆盖上述非法输入；`mvp.spec.ts` 当前 22/22 通过，API build 通过。

### P1 下一步

1. 补充内容管理成功/失败/取消/重复提交的浏览器验收记录。
2. 继续进入 P2 用户与学员闭环边界测试，重点检查停用账号、关系越权、合并和敏感字段。

### P2/P3 第一批加固（2026-08-08）

- 用户启停不存在的用户现在返回明确的 `用户不存在`，不再让 Prisma 异常冒泡为 500。
- 学员档案后端拒绝空姓名、非法手机号、非法邮箱；已合并档案不能编辑、启停或再次授权账号；不能合并到已合并目标档案；关系类型不能为空。
- 优惠规则后端拒绝非法人数门槛、超范围折扣比例和不存在的课程，不再静默截断非法值。
- `student-api.spec.ts` 4/4、`mvp.spec.ts` 22/22 和 API build 通过。

### 下一步计划更新

1. P2：补充用户禁用后旧令牌失效、学员关系越权和敏感字段网络响应回归。
2. P3：补充消息目标范围/时间窗、积分流水、反馈处理和配置保存的失败/幂等回归。
3. P4：统一管理端剩余原生 prompt/alert 交互，并进入真实浏览器验收准备。

### P4 第一批加固（2026-08-08）

- 富文本编辑器的链接输入改用管理端统一 `promptAction`，本地图片格式/大小错误改用统一通知，不再调用浏览器原生 `window.prompt/window.alert`。
- `verify-ui.mjs` 增加原生 prompt/alert 禁止断言；管理端 UI 静态验收和 build 通过。
- P4 仍需继续统一运行态焦点、Escape 关闭、空状态和错误恢复表现，并纳入浏览器验收。

### P3 第二批加固（2026-08-08）

- 消息标题/内容/目标数量和启用状态增加后端边界，避免超长或字符串布尔值绕过管理端校验。
- 积分调整要求安全整数、非零原因，并校验结果不超过 Prisma `Int` 可保存范围。
- 系统配置增加配置键格式、值长度和说明长度校验。
- `mvp.spec.ts` 运营边界回归 23/23、API build 通过；P3 尚需真实浏览器验证目标范围、失败提示、重复点击和审计结果。

### P2 第二批加固（2026-08-08）

- 账号资料更新增加姓名、手机号、邮箱格式、重复绑定和规范化存储。
- “我的学员”新增入口增加邮箱校验，并统一规范化手机号/邮箱，避免档案匹配漂移。
- 学员档案与认证专项 2 套件/10 项通过；P2 尚需真实浏览器验证跨账号权限、脱敏网络响应和审计记录。
## 2026-08-07 增量修复：C 端确认框层级

- [x] 修复 `uni.showModal` 系统对话页外层 `systemDialog` 的层级，避免修改密码确认框被业务弹窗遮挡。
- [x] 统一业务弹窗为 z-index 1000、系统确认遮罩/内容为 4000/4001，并兼容不同 uni-h5 class 命名。
- [x] 完成 C 端 H5 构建、根目录 verify 与 diff 检查。
- [ ] 在真实 H5/小程序设备上补充修改密码、取消报名、提交凭证等确认框的视觉点击验收。

## 2026-08-07 增量：确认弹窗 z-index 恢复复核

- [x] 修复 `App.vue` 中注释吞掉 CSS 选择器的问题，恢复全局 z-index 规则。
- [x] 复核所有 `uni.showModal` 使用场景，共用业务 1000 / 系统确认 4000/4001 层级。
- [x] 重新执行 C 端 H5 build、根目录 verify 和 diff 检查。
- [ ] 使用真实 H5/小程序设备逐项验证修改密码等确认框的显示、取消和关闭后清理。

## 2026-08-08 增量：确认弹窗根节点层级加固

- [x] 确认 uni-h5 的 `uni.showModal` 实际挂载根节点为 `#u-a-m`，补齐根节点固定定位和 `z-index: 4000`。
- [x] 保持业务弹窗 `1000`、系统确认遮罩 `4000`、确认框内容 `4001` 的统一层级关系，并限制根节点点击穿透。
- [x] 在 `5185` 运行态复核修改密码确认框，确认框位于账号安全业务弹窗之上。
- [x] 完成 C 端 H5 构建与 `git diff --check`。
- [ ] 在真实 H5/小程序设备逐项验证取消、关闭、重复打开后的清理行为。

## 2026-08-08 增量：P2 会话失效与学员越权回归加固

- [x] 为 `User` 增加 `sessionVersion`，令牌签发和 `JwtGuard` 校验版本。
- [x] 用户启停、个人改密、管理员重置密码、验证码找回密码递增会话版本，使旧 access/refresh token 不能继续使用。
- [x] 增加 SQLite `0008_auth_session_version` 迁移，并兼容已有数据库条件补列；PostgreSQL 由当前 schema 重新生成部署 migration。
- [x] 增加禁用/重置后的旧 token 回归，以及账号跨学员档案越权回归。
- [x] 后端全量 13 套件/53 项、API build、根目录 verify 和 diff 检查通过。
- [ ] 在真实浏览器网络面板中留存 401、403、脱敏字段和审计记录证据。

## 2026-08-07 增量任务：管理端非支付功能矩阵

- [x] 盘点管理端 18 个模块、前端操作分发和后端管理路由。
- [x] 新增 `Docs/Summary/2026-08-07_管理端非支付功能矩阵.md`，标注权限、状态门禁、自动化证据和浏览器验收状态。
- [ ] 按矩阵补齐真实浏览器 D-01～D-17 验收证据，重点覆盖失败路径、取消操作、敏感字段和审计。
- [ ] 根据运行态证据修复仍存在的功能缺口，再进入服务器 Docker/PostgreSQL 迁移、备份恢复和非支付发布门禁。
- [x] 补充管理端权限和课程发布状态回归：普通用户 403、待发布课程管理端可见且公共端隐藏。

## 2026-08-08 增量任务：C 端二次确认弹窗层级

- [x] 统一五个 C 端业务弹窗页面的业务层级变量和 1000 兜底。
- [x] 提升 `uni.showModal` 的 `#u-a-m` 根节点、遮罩和确认内容层级，覆盖修改密码、取消报名、提交凭证、保存资料、提交开票、学员操作等场景。
- [x] 完成 H5 运行态修改密码确认框复核：确认内容位于业务弹窗之上且可交互。
- [x] 完成 H5 构建、根目录 verify 和 diff 检查。
- [ ] 在真实小程序端补充同类确认框的显示、取消、关闭和重复打开验收证据。

## 2026-08-08 增量任务：C 端确认框层级恢复（本轮）

- [x] 复核截图反馈和 uni-h5 实际 DOM，确认业务弹窗 `1000` 与系统确认弹窗存在跨版本承载节点差异。
- [x] 将系统确认根节点/遮罩统一恢复为 `10000`，确认内容为 `10001`，并补齐 `#u-a-m > uni-modal` 与 mask/container 兼容选择器。
- [x] 同步根目录 verify 层级断言，完成 H5 build、verify、git diff 检查。
- [ ] 重启用户当前使用的 C 端 H5 服务并强制刷新，补充“修改密码确认/取消/确认”及其他 `uni.showModal` 场景的人工证据。

## 2026-08-08 增量任务：确认框直接子节点层级加固

- [x] 为系统确认根节点增加 `--client-system-modal-layer` 别名，并覆盖 `#u-a-m > uni-modal`。
- [x] 为旧版实际 DOM 的 `.uni-mask` 与 `.uni-modal` 直接子节点增加 `10000/10001` 显式层级。
- [x] 更新根目录 `verify` 断言，完成源码层面的全量二次确认层级检查。
- [ ] 重启/刷新当前 H5 服务，在真实浏览器和小程序端补充修改密码、取消报名、支付凭证等确认框的人工证据。

## 2026-08-08 当前目标：非支付版本可上线实施计划

### 目标

在不接入在线支付的前提下，把管理端、后端和 C 端核心非支付能力推进到可内部测试、可回滚、可审计、可发布状态；在线支付继续由申请中的商户资料和回调验收单独处理。

### 阶段

- [/] P0 现状审计：核对 18 个管理模块的按钮、接口、权限、状态门禁和运行态反馈。
- [ ] P1 内容与报名闭环：修复真实运行态发现的无动作/错误请求，并补失败、空状态和重复提交回归。
- [ ] P2 用户与学员闭环：补齐跨账号权限、脱敏、旧令牌失效和审计证据。
- [ ] P3 运营与系统闭环：补齐消息、积分、反馈、优惠、配置、审计的错误恢复和幂等验收。
- [ ] P4 发布门禁：Docker/PostgreSQL、迁移对账、上传持久化、备份恢复、健康检查和回滚。
- [ ] P5 非支付内部测试发布：锁定版本、生成验收报告和剩余支付接入清单。

### 当前决策

- 在线支付不作为本轮上线阻塞项，`PAYMENT_ADAPTER=disabled`。
- “按钮没实现”必须通过运行态请求/响应和数据变化确认，不能仅依据按钮文字或静态存在判断。
- 每个阶段完成后更新 `findings.md`、`progress.md` 和 `Docs/Summary` 记录。

### P0 运行态审计错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 重复启动 API 返回 `EADDRINUSE :::3100` | 1 | 通过 `netstat -ano` 确认原 API 已在 PID 40096 监听；不再重复启动，改用现有服务做浏览器验收 |
| `Get-NetTCPConnection` 未返回 3100/5174/5185 | 1 | 使用 Windows `netstat -ano` 复核，确认三个端口实际均在监听；以可访问性和 `netstat` 为准 |
| SSH 连接 `root@192.168.0.162` 返回 `Permission denied (publickey,password)` | 1 | 未继续猜测密码；服务器 P4 只能等用户配置/提供可用 SSH 密钥或在服务器端执行部署命令后继续 |
| 浏览器按按钮名称匹配“课程管理”时同时匹配了导航按钮和“关闭课程管理标签”按钮 | 1 | 改用唯一的 `button[title="课程管理"]` 选择器，避免标签关闭按钮干扰 |
## 2026-08-08：C 端二次确认弹窗层级复核

- [x] 业务弹窗固定为 `1000`，系统确认根节点/遮罩固定为 `11000`，确认内容固定为 `11001`。
- [x] 覆盖 `#u-a-m`、`uni-modal`、`.uni-mask`、`.uni-modal`、`role="dialog"` 及 systemDialog 兼容结构。
- [x] 复核修改密码、保存资料、退出登录、取消报名、支付凭证、开票、报名和学员操作等 `uni.showModal` 场景共用同一套层级规则。
- [x] C 端 H5 构建、根目录 `verify`、`git diff --check` 通过；运行态修改密码确认框位于账号与安全业务弹窗之上且可交互。
- [ ] 仍需在实际小程序容器补充一次真机层级和取消/确认行为验收。

## 2026-08-08：当前活动计划状态更新（以本节为准）

- [x] P0：源码、路由、权限、测试和运行服务基线已盘点，已确认“按钮存在”与“按钮运行闭环”需要分开验收。
- [/] P1：课程、Banner、报名模板、报名汇总/明细、订单和开票的主要运行态入口已验收；仍需补成功写入、接口失败和刷新后状态证据。
- [/] P2：用户与学员的列表、详情、编辑入口、启停确认和脱敏列表已验收；仍需补跨账号 403、旧令牌 401、敏感字段网络响应和审计证据。
- [/] P3：消息、反馈模块已验收入口和空值提示；积分、优惠、配置和定向消息的成功/失败/幂等证据待补齐。
- [/] P4：已修复管理端长弹窗操作栏随内容滚动的问题，所有 `.modal-actions`/`.app-dialog-actions` 改为 sticky 底部栏；仍需完成全模块响应式和空/错状态复核。
- [/] P5：已完成第一轮真实管理端模块巡检和取消路径；继续按矩阵补齐成功/失败/取消/重复点击证据。
- [ ] P6：服务器 Docker/PostgreSQL 迁移、导入对账、上传持久化、备份恢复、健康检查和回滚尚未执行，不能宣称已上线。
- [ ] P7：支付继续保持 `PAYMENT_ADAPTER=disabled`，待商户资料、HTTPS 回调和沙箱/小额订单材料齐备后单独验收。

### 本轮错误记录

| 问题 | 处理 |
|---|---|
| 浏览器首次点击折叠菜单内的课程入口未切换 | 发现菜单组仍处于折叠状态；改用先展开分组、再使用唯一 `button[title]` 选择器验收 |
| 长弹窗按钮随内容滚动 | 在 `styles.css` 增加 sticky 操作栏和移动端边距覆盖；加入 UI 静态门禁 |
| 学员编辑提交期间外层弹窗仍可关闭 | `StudentProfileModal` 向 `SimpleModal` 传递 `busy`，保存期间锁定关闭和输入 |

## 2026-08-08：本轮真实接口基线复核

- [x] 确认本地 API `3100`、管理端 `5174`、C 端 H5 `5185` 均可访问。
- [x] 使用管理员令牌逐项请求 18 个管理端只读入口，最终响应均为 HTTP 200。
- [x] 确认报名明细列表默认返回脱敏手机号且不包含 `formPayload`；单条详情接口才返回完整手机号/表单快照，并记录“查看报名履历”审计。
- [x] 确认学员档案列表默认脱敏手机号/邮箱，详情和受控导出仍走受保护接口。
- [ ] P1～P3 仍需补写操作的真实浏览器证据：成功后的刷新状态、服务端失败反馈、取消无写请求、重复点击门禁和审计记录。
- [ ] P4/P5 仍需在服务器部署环境完成 Compose、PostgreSQL、持久化、备份恢复和发布回滚验收。

### 下一步执行顺序

1. 先在本地管理端完成课程、Banner、模板、订单、开票、学员、用户、消息、积分、配置各一条成功写操作，并记录 HTTP 状态、列表刷新和审计结果。
2. 对同一批操作逐项验证取消、非法输入、重复点击和权限失败。
3. 汇总浏览器证据后，再进入服务器 Docker/PostgreSQL 迁移与恢复门禁；在线支付继续保持禁用。

### 首条写操作运行证据：Banner

- [x] 非法空标题保存返回 HTTP 400。
- [x] 唯一临时 Banner 新增返回 HTTP 201，刷新列表可见 1 条。
- [x] 删除返回 HTTP 200，刷新列表后该条为 0 条。
- [x] 操作审计列表出现“Banner维护”和“Banner删除”记录。
- [ ] 继续补课程、报名模板、订单、开票、学员、用户、消息、积分和配置写操作证据。

### P1 内容管理运行证据补充

- [x] 课程非法空标题返回 HTTP 400。
- [x] 课程新增“待发布”返回 HTTP 201，管理端刷新可见，公共课程列表隐藏。
- [x] 课程编辑返回 HTTP 200，删除返回 HTTP 200，刷新后记录消失。
- [x] 报名模板重复字段返回 HTTP 400；合法模板新增/编辑/删除分别返回 201/200/200，刷新后记录消失。
- [ ] 继续订单、开票和报名明细写操作；其中支付仍只验证线下凭证审核，不验证在线渠道。

## 2026-08-08：C 端确认弹窗 z-index 兼容性加固

- [x] 复核 `body > #u-a-m > uni-modal` 的运行态结构：业务弹窗保持 `1000`，系统根节点/遮罩保持 `2147483646`，确认内容保持 `2147483647`。
- [x] 为 `uni-modal__mask`、`uni-modal__container` 及旧版 `.uni-mask`/`.uni-modal` 补齐直接子节点选择器，覆盖修改密码、保存资料、退出、取消报名、支付凭证、开票、报名、学员操作等所有 `uni.showModal` 场景。
- [x] 静态验收脚本增加新旧 `uni-modal` 类名断言；C 端 H5 构建、根目录 verify、`git diff --check` 通过。
- [ ] 重启/刷新用户当前 5185 H5 服务，在真实浏览器和小程序端补充修改密码及其他确认框的显示、取消、确定和重复打开验收。

## 2026-08-08：非支付可上线版本主计划（当前执行基线）

### 目标

在不接入微信/支付宝在线支付的前提下，将管理端、C 端和 API 推进到可内部发布、可审计、可回滚的版本。在线支付固定保持 `PAYMENT_ADAPTER=disabled`，不把支付商户资料作为本轮完成条件。

### 阶段与完成标准

- [x] P0 基线审计：代码、路由、权限、构建、自动化测试和本地服务已盘点；确认“按钮存在”与“按钮运行闭环”必须分开验收。
- [/] P1 核心业务写操作：订单关闭/取消、线下支付凭证上传与审核、开票处理、报名明细刷新；每项都要有成功、非法输入、取消、重复点击、状态门禁和审计证据。
- [ ] P2 用户与学员：跨账号 403、旧令牌 401、列表脱敏、详情受控揭示、关系维护和审计证据。
- [ ] P3 运营与系统：消息、积分、优惠、反馈、配置、定向消息的成功/失败/幂等/权限验收。
- [ ] P4 交互与发布门禁：管理端响应式、空/错状态、弹窗与操作锁；Docker/PostgreSQL、上传持久化、健康检查、备份恢复和回滚。
- [ ] P5 非支付内部发布：锁定版本、执行发布验收清单、记录已知限制和剩余支付接入清单。

### 当前差距与执行顺序

1. 先用测试库和隔离临时数据验证订单、线下凭证、开票写操作，禁止污染现有业务数据。
2. 将每个操作的 HTTP 状态、数据库状态、刷新结果、取消无写请求、重复请求结果和审计记录写入 `findings.md`、`progress.md` 与 `Docs/Summary`。
3. P1 完成后再推进 P2/P3；全部本地证据齐全后才执行服务器 Docker/PostgreSQL 迁移和回滚演练。

### 本轮错误/限制记录

| 问题 | 当前处理 |
|---|---|
| 旧开发进程可能占用 3100 导致接口版本错觉 | 运行验证前先确认监听 PID 和健康响应，禁止仅依据旧日志判断功能缺失 |
| PowerShell 直接传中文字符串可能产生 UTF-8 假性 400 | 测试写请求使用显式 UTF-8 字节或浏览器 Fetch |
| C 端 H5 旧 bundle 可能保留旧层级 | 修改后必须重启 H5 并强制刷新；小程序需单独真机验收 |

## 2026-08-08：P1 订单/凭证/开票接口闭环完成

- [x] 禁止已取消订单重复取消；管理端关闭待支付、退款已支付订单均具备状态门禁和名额/履历回退。
- [x] 禁止审核中的线下支付凭证重复上传；驳回后允许重新上传，通过后订单进入已支付。
- [x] 开票通过要求发票号码，驳回要求理由；被驳回订单允许重新申请；电子发票上传和重复上传门禁已验证。
- [x] 新增 `backend/api/test/p1-write-operations.spec.ts`，覆盖 HTTP、Multipart、刷新查询、重复操作和审计结果。
- [x] 更新 C 端订单页，审核中的凭证不再显示重复上传入口，被驳回开票订单可重新申请。
- [x] P1 代码/接口回归完成；P1 的真实浏览器人工点击证据仍待补充，完成前不将阶段标记为最终放行。
- [ ] 下一阶段进入 P2：用户/学员跨账号权限、旧令牌失效、脱敏、关系维护和审计证据。

## 2026-08-08：当前执行基线更新（P2 第一批）

- [x] P0：源码、路由、权限、构建、自动化测试和本地服务基线已盘点。
- [x] P1：内容管理、订单关闭/凭证审核/开票处理的代码和 API 状态门禁已完成；真实浏览器写操作证据仍待执行。
- [/] P2：新增管理端权限与敏感数据边界回归；普通用户全部管理读写入口 403，报名履历/学员列表默认脱敏，受保护详情揭示完整数据。
- [ ] P3：运营模块成功/失败/幂等/审计的完整运行证据。
- [ ] P4：Docker/PostgreSQL、上传持久化、备份恢复、健康检查和回滚门禁。
- [ ] P5：非支付内部测试版本冻结、发布验收和已知限制清单。

### 本轮 P2 变更

- 新增 `backend/api/test/admin-security-boundaries.spec.ts`，覆盖全部管理端读取/写入路由的普通用户拒绝，以及敏感列表/详情的揭示边界。
- 同一专项回归补充账号停用/管理员重置密码后的旧 access token 立即失效，以及跨账号学员编辑/设默认/解除均被拒绝。
- 测试使用隔离数据库先创建临时订单和学员履历，避免空种子数据造成假阴性；测试结束自动清理隔离目录。

### 错误记录

| 错误 | 尝试 | 处理 |
| 2026-08-09：目标服务器 SSH | `192.168.0.162:22` TCP 可达，但 BatchMode SSH 被服务器直接关闭，未返回可执行的 Compose 输出 | 记录为外部授权阻塞；不猜密码、不执行远程写操作 |
| 全量后端测试中课程非法名额错误文案不匹配 | 1 | 调整校验顺序和文案，保留“已报名人数不能超过名额”的既有契约后重新验证 |
|---|---:|---|
| 直接使用 `pnpm exec jest` 找不到 jest 二进制 | 1 | 改用项目正式 `pnpm.cmd --dir backend/api test -- ...` 脚本 |
| 新安全测试初次读取种子库无报名履历 | 1 | 在隔离测试中先创建临时订单，继续验证真实脱敏链路 |
| 脱敏手机号正则按错位数编写 | 1 | 按实际 `139****0091` 格式修正为 3 位前缀 + 4 星号 + 4 位后缀 |
## 2026-08-08：C 端确认弹窗层级修复

- [x] 复核账号与安全/修改密码确认框的业务弹窗与动态系统宿主层级。
- [x] 固定 `#u-a-t`/`#u-a-m` 全屏宿主，统一 mask 与 dialog/container/content 层级。
- [x] 更新静态验证脚本，覆盖两套宿主和直接子节点选择器。
- [x] 完成 H5 构建、`node mvp/scripts/verify.mjs` 与 `git diff --check`。
- [ ] 刷新 5185 H5 后完成真实点击回归；小程序容器另行复核。
## 2026-08-08：当前目标规划更新（P3 完成，进入 P4）

- [x] P0：基线审计、模块/接口矩阵和自动化门禁。
- [x] P1：课程、Banner、报名模板、订单关闭、线下凭证审核、开票处理和报名明细刷新闭环（代码/API 已完成，浏览器证据仍需补齐）。
- [x] P2：管理员权限、跨账号学员关系、敏感字段脱敏、旧令牌失效和专项回归。
- [x] P3：反馈、消息、积分、优惠、配置、审计输入边界；本轮补齐积分原子边界、消息 POST/PATCH 语义、审计日期校验和反馈审计脱敏。
- [/] P4：服务器 Docker/PostgreSQL 构建、数据导入对账、上传持久化、备份恢复、健康检查和回滚演练。
- [ ] P5：管理端和 C 端浏览器逐按钮验收，记录成功/失败/取消/重复点击/刷新/审计证据，形成非支付内部测试发布报告。

### P4 下一步执行顺序

1. 在服务器拉取最新 Git 提交并确认 Compose、runtime 脚本和 `.env.docker` 完整。
2. 重建 API/Admin/H5 镜像，确认 API migration 退出码为 0、PostgreSQL healthy、三个前端容器可访问。
3. 使用独立 PostgreSQL 数据卷导入 SQLite 导出，执行行数、订单金额、订单状态和学员履历对账。
4. 验证上传目录/卷可跨容器重启保留，并执行备份、恢复和回滚演练。
5. 通过健康检查和非支付环境门禁后，再进行 P5 浏览器操作验收；在线支付继续保持 disabled。
## 2026-08-08 C 端二次确认层级补强

- [x] 复核修改密码及全部 `uni.showModal` 二次确认场景的层级契约：业务弹窗 1000，系统确认宿主/遮罩/内容 2147483000/2147483001/2147483002。
- [x] 补充 H5 `body` 挂载后的运行时兜底安装，避免 `onLaunch` 早于 body 创建时漏装 `MutationObserver`。
- [x] 完成 C 端 H5 构建、根目录 verify 和 diff 检查。
- [ ] 刷新 5185 后由真实账号人工点击“修改密码 → 保存新密码”，补充取消、确定、重复打开和小程序容器验收证据。

## 2026-08-08：非支付上线计划继续执行

- [x] P0：源码、接口、权限、数据模型、构建和自动化门禁基线已完成。
- [x] P1：课程、Banner、报名模板、订单关闭/凭证审核、开票、报名明细和刷新错误隔离已完成代码闭环；浏览器人工证据仍需补齐。
- [x] P2：用户/学员权限、关系、旧令牌失效和敏感字段脱敏已完成专项回归。
- [x] P3：反馈、消息、积分、优惠、配置和审计输入边界已完成专项回归。
- [/] P4：Docker/PostgreSQL 部署、导入对账、上传持久化、备份恢复和回滚演练进行中。
- [ ] P5：管理端和 C 端逐按钮人工验收，记录成功/失败/取消/重复点击/刷新/审计证据，形成非支付内部测试发布报告。

### P4 执行顺序

1. 服务器拉取最新代码，确认 `.env.docker`、Compose 文件和 runtime 脚本完整。
2. 重新构建 API、管理端和 H5 镜像；确认 migration 退出码为 0、PostgreSQL healthy、端口无冲突。
3. 导出 SQLite 并导入独立 PostgreSQL，核对表行数、订单金额/状态、报名履历和学员档案关系。
4. 验证上传目录/卷跨容器重启保留；执行备份、恢复和只读回滚演练。
5. 通过健康检查和 `PAYMENT_ADAPTER=disabled` 门禁后进入 P5 人工验收。

### C 端弹窗专项补充（代码已完成，待人工验收）

- [x] 复核修改密码及全部 `uni.showModal` 二次确认入口的 z-index 契约。
- [x] 对 uni-h5 动态确认宿主增加运行时归一化、style/class/data-type 监听和数值兜底。
- [ ] P5 人工验收优先验证“我的 → 账号与安全 → 保存新密码”，再覆盖取消报名、支付凭证、开票、资料和学员操作。

## 2026-08-08：非支付上线目标本轮执行记录

- [x] 重新核对自动化门禁：根目录 verify、管理端 UI 验收/构建、后端 15 套/64 项测试均通过。
- [x] 确认本机已有 3100/5174/5185 服务，不重复启动；管理端工作台运行态可加载。
- [/] 继续逐模块人工核对管理端按钮闭环；完成一项就记录成功、失败、取消、重复点击、刷新和审计证据。
- [ ] 在所有非支付按钮证据齐全后，进入服务器 Docker/PostgreSQL、上传卷、备份恢复和回滚门禁。

## 2026-08-08：管理端课程按钮运行态核验记录

- [x] 展开分组后进入课程管理，确认折叠菜单隐藏子项不是功能缺失。
- [x] 完成临时课程新增、确认、编辑、删除取消、删除确认和列表刷新，并清理临时数据。
- [x] 重新执行订单/凭证/开票/权限隔离回归，2 套测试、5 项通过。
- [/] P1 继续补订单、支付凭证、开票和报名明细的真实浏览器状态门禁及错误反馈证据。

### P1 运行态核验补充

- [x] 订单详情、关闭取消、凭证驳回必填、开票理由/二次确认取消和报名履历详情。
- [/] 使用隔离临时订单完成一次真实管理端成功关闭或凭证审核写入，并记录审计与刷新结果。
- [ ] P1 完成后转入 P2/P3 逐模块人工验收。

### P4 发布门禁补充

- [x] 新增 `Docs/runtime/verify-upload-volume.sh`，覆盖上传目录临时写入、API 重启、健康等待、读取和清理。
- [ ] 在服务器实际 Compose 环境执行该脚本并归档结果。
- [ ] 完成 PostgreSQL 导入对账、备份恢复和只读回滚后进入 P5。

### C 端确认弹窗层级回归修复（2026-08-08）

- [x] 复核修改密码及全部 `uni.showModal` 二次确认入口的层级契约：业务弹窗 `1000`，系统确认根/遮罩/内容 `2147483000/2147483001/2147483002`。
- [x] 修复运行时守护不应搬动 `uni-modal` 子节点的问题，仅提升真正嵌套的 `systemDialog` 根页面，避免 Vue 重渲染后层级回退。
- [x] 完成 C 端 H5 构建和根目录 `verify`。
- [ ] 重启或强制刷新 5185 后，人工复核修改密码、取消/确定、重复打开，以及其他确认入口；随后补充小程序容器验证。

### C 端确认弹窗层级即时兜底（2026-08-08）

- [x] 检查全部 `uni.showModal` 二次确认入口与页面业务弹窗层级。
- [x] 增加动态宿主插入后的同步 z-index 归一化，并保留下一帧/延迟兜底。
- [x] 完成 H5 构建、根目录 verify 和 diff 检查。
- [ ] 刷新 5185 后完成修改密码及其他确认入口的真实点击回归；小程序端另行复核。

### C 端二次确认弹窗层级修复留痕（2026-08-08）

- [x] 统一业务弹窗与系统确认根/遮罩/内容层级，并覆盖所有 `uni.showModal` 入口。
- [x] 覆盖 `#u-a-m`、`#u-a-t`、`uni-modal`、`systemDialog` 及新版/旧版 mask、container、dialog 结构。
- [x] 增加 MutationObserver 同步和延迟归一化，避免动态宿主首帧层级回退。
- [x] 通过根目录 verify、C 端 H5 构建和 diff 检查。
- [ ] 使用有效 C 端会话完成修改密码、取消/确定、重复打开及其他确认操作的 H5/小程序人工验收。
## 2026-08-08：非支付可上线版本实施计划

### 目标与边界

在不依赖在线支付商户配置的前提下，将当前项目推进到可供内部真实使用的非支付版本：管理端关键按钮具备真实写入、刷新、失败提示、权限校验和审计留痕；C 端报名、学员、线下凭证和开票展示链路可用；Docker/PostgreSQL 可重启、备份和回滚。在线支付适配器继续保持 disabled。

### 阶段

- [ ] 阶段 A：建立模块/按钮/接口/权限/测试基线与发布阻断清单
- [ ] 阶段 B：内容维护闭环（课程、Banner、报名模板）浏览器验收与缺口修复
- [ ] 阶段 C：运营闭环（优惠、消息、积分、反馈、配置、审计）验收与缺口修复
- [ ] 阶段 D：用户与学员闭环（用户、档案、关系、合并、导出、C 端联动）验收与缺口修复
- [ ] 阶段 E：业务闭环（报名汇总/明细、订单关闭/凭证审核、开票处理/上传）验收与缺口修复
- [ ] 阶段 F：统一交互、安全和发布工程（加载/错误/二次确认、权限、敏感数据、Docker/PostgreSQL、备份恢复）
- [ ] 阶段 G：自动化回归、浏览器冒烟、发布清单和上线前签字材料

### 放行条件

1. 每个写按钮都有真实接口、成功后可刷新验证、失败有明确提示，重复点击不会产生重复写入。
2. 普通用户访问管理接口全部被拒绝；敏感字段列表默认脱敏，导出默认脱敏并有审计。
3. 订单、凭证、开票、学员关系等不可逆操作都有二次确认和服务端状态门禁。
4. API、管理端、C 端和 PostgreSQL 容器重启后数据与上传文件仍可读；备份恢复演练成功。
5. `verify`、管理端 UI 检查、生产构建、后端测试和真实浏览器冒烟全部通过。
## 2026-08-08：P0 管理端运行态盘点完成

- [x] 使用真实管理端会话逐项进入 Banner、课程、报名模板、报名汇总、报名明细、订单、开票、学员、用户、优惠、反馈、消息、积分、渠道自检、支付设置、系统配置和审计模块。
- [x] 核对页面面包屑/已访问标签、列表或状态区域和错误提示；17 个模块均可切换并加载，无加载失败证据。
- [x] 核对订单、开票、反馈、学员和报名明细的操作入口与状态门禁，未发现已取消/已处理记录仍显示对应危险操作的问题。
- [x] 生成 `Docs/Summary/2026-08-08_P0管理端运行态审计记录.md`。
- [ ] P1/P2 继续执行真实写操作的成功、取消、非法输入、重复点击、刷新和审计留痕；服务器发布门禁仍未完成。
## 2026-08-08：P1 课程管理浏览器成功闭环

- [x] 在真实管理端会话中新增唯一临时课程，完成二次确认、查询刷新后可见。
- [x] 编辑并删除该临时课程，完成二次确认、刷新后消失。
- [x] 在操作审计中查到课程维护和课程删除记录，临时数据已清理。
- [ ] 继续 Banner、报名模板、订单/凭证、开票和报名明细的成功/取消/失败/重复点击验收。
## 2026-08-08：P1 内容管理浏览器闭环完成

- [x] 课程、Banner、报名模板均已完成临时新增、二次确认、查询刷新、删除确认和刷新消失。
- [x] 三类内容的维护/删除审计记录均可按标题或名称检索，临时数据已清理。
- [ ] P1 继续验收订单关闭、线下凭证审核、开票处理和报名明细详情/刷新；随后进入 P2 用户与学员。
## 本轮补充：C 端二次确认弹窗层级回归

- [x] 检查 C 端全部 `uni.showModal` 入口与业务弹窗 `z-index` 约定。
- [x] 修复过宽选择器造成的确认框内部节点 fixed 定位和高度塌陷。
- [x] 真实 H5 验证修改密码确认框位于账号与安全业务弹窗上方，取消不写入。
- [x] 通过根目录 verify、C 端 H5 构建和 `git diff --check`。
- [ ] 刷新/重启 5185 后补齐取消、确定、重复打开及其他 C 端确认入口；随后做小程序容器复核。
## P4 管理端用户运营与配置运行态验收

- [x] 学员档案详情、编辑取消、默认关系取消、解除关系取消。
- [x] 优惠规则临时新增、二次确认、分页刷新、删除取消、最终清理和审计。
- [x] 积分零值/原因校验、调整取消、积分流水查看。
- [x] 系统配置编辑取消、临时新增确认、清理和审计留痕。
- [x] 操作审计按类型/操作者筛选与重置。
- [ ] 在隔离数据库补做积分成功写入和刷新对账。
- [ ] 继续 C 端报名/订单/凭证/开票/消息/学员关键状态门禁，再进入服务器 Compose/PostgreSQL 阶段。

## 2026-08-08：非支付可上线版本当前执行批次

- [x] 完成代码、文档、测试和管理端模块基线审计，确认课程/Banner/报名模板及部分运营模块已有真实写入闭环。
- [x] 修复学员档案编辑对列表脱敏手机号/邮箱的错误回写。
- [ ] C 端报名、订单、线下支付凭证、开票、消息和学员关系逐项验证成功/取消/重复/失败/刷新状态。
- [ ] 隔离数据库补做积分成功写入与刷新对账，并核对导出敏感字段边界。
- [ ] 服务器 Docker Compose/PostgreSQL 迁移、上传卷重启持久化、备份恢复和只读回滚演练。
- [ ] 完成自动化、浏览器冒烟和非支付版本发布验收清单；在线支付仍不作为本批次放行条件。
## 2026-08-08：C 端确认框层级修复批次

- [x] 复核所有重要确认入口，统一接入 `showClientConfirm`。
- [x] H5 body-level 确认层固定为 `2147483000/2147483001/2147483002`，高于全部业务弹窗 `1000`。
- [x] 更新静态门禁，禁止确认入口回退为页面内低层级弹窗。
- [x] 完成 `pnpm.cmd run verify` 与 `pnpm.cmd --dir frontend/client-uni build:h5`。
- [ ] 重启/刷新 5185，使用有效 C 端会话完成密码、取消、确定、重复打开和其他确认入口的人工验收。

## 2026-08-08：C 端确认框 z-index 回归加固批次

- [x] 复核所有页面业务弹窗统一为 `1000`，确认根节点/遮罩/内容统一为 `2147483000/2147483001/2147483002`。
- [x] 将共享确认三层的内联 z-index 改为 `!important`，防止 uni-h5 延迟渲染覆盖层级。
- [x] 增加静态门禁，确认根节点、遮罩和内容必须使用内联重要层级。
- [x] 完成运行态密码确认框的 DOM 层级检查，未提交真实密码变更。
- [x] 更新 `Docs/Summary/2026-08-08_C端二次确认z-index回归修复.md`、`progress.md`、`findings.md`。
- [ ] 刷新/重启 5185 后完成人工复核；再继续 C 端业务状态门禁和非支付发布验收。

## 2026-08-08：C 端错误态与重试闭环批次

- [x] 盘点首页、详情、订单/开票、消息、学员、报名和个人中心的加载失败路径。
- [x] 增加错误提示、空数据区分和“重新加载”入口；订单/首页保留刷新失败前的旧数据。
- [x] 为报名模板、报价和收款信息增加独立错误反馈，避免静默失败。
- [x] 增加 `clientErrorStatesHaveRetry` 静态门禁。
- [x] 通过根目录 verify、C 端 H5 构建和 diff 检查；运行态正常数据页面检查通过。
- [ ] 在隔离环境模拟接口失败并补充浏览器重试成功证据。
- [ ] 完成管理端写操作逐模块验收，再进入 Docker/PostgreSQL、上传持久化和备份恢复门禁。

## 2026-08-08：确认层级常量统一补强批次

- [x] 新增 C 端共享弹窗层级契约，避免 `App.vue` 与确认工具的 z-index 漂移。
- [x] 复核管理端确认 Portal 层级高于业务弹窗。
- [x] 通过根目录 verify，并记录本地 H5 编译超时及后续刷新开发服务复核要求。
- [ ] 刷新/重启 5185 后完成密码、取消、确定、重复打开等人工回归。

## 2026-08-08：P1 C 端订单展示与取消闭环批次

- [x] 真实创建临时订单并完成报名确认、账单生成、线下转账信息展示和订单页跳转。
- [x] 验证取消二次确认的取消/确认分支、C 端与管理端刷新状态和审计记录。
- [x] 修复待支付且无支付渠道时误显示“微信支付”的展示边界。
- [ ] 继续进行凭证上传/审核、开票成功路径的浏览器验收；完成后更新 P1 放行状态。
## 2026-08-09：二次确认 z-index 回归完成

- [x] 复核 C 端全部二次确认调用点及业务弹窗层级。
- [x] 运行态验证修改密码确认框位于账号与安全弹窗之上，取消不写入。
- [x] 复核管理端确认 Portal 层级高于业务弹窗。
- [ ] 在小程序开发者工具/真机复核原生 `uni.showModal` 的显示效果；完成后继续非支付业务状态门禁。

## 2026-08-09：非支付可上线版本继续执行

- [x] 完成当前代码、规划、测试基线恢复，明确在线支付暂不作为放行条件。
- [x] 盘点管理端主要操作入口与后端 `/admin/*` 路由对应关系。
- [ ] P1：完成订单、线下支付凭证、开票和报名明细的运行态写操作闭环。
- [ ] P2：完成用户/学员权限、脱敏、关系、合并和重置密码的运行态闭环。
- [ ] P3：完成消息、积分、优惠、反馈、系统配置和审计的成功/失败/幂等核验。
- [ ] P4：完成 Docker/PostgreSQL 迁移对账、上传持久化、备份恢复和回滚演练。
- [ ] P5：形成非支付内部测试发布报告；支付商户配置另行进入后续阶段。

## 2026-08-09：当前执行批次（恢复后）

- [x] 重新读取现有计划、进度和发现，确认当前工作区以非支付内部版本为主线。
- [x] 根目录 `verify`、管理端 UI 静态验收和后端 15 套/64 项测试通过。
- [/] P1：先完成订单、线下凭证、开票和报名明细的运行态成功/取消/失败/重复/刷新/审计证据；优先使用唯一临时数据并清理。
- [ ] P2：用户启停/重置密码、学员状态/关系/合并/导出与跨账号权限证据。
- [ ] P3：消息、积分、优惠、反馈、系统配置和审计的成功/失败/幂等证据。
- [ ] P4：Docker/PostgreSQL 迁移对账、上传卷持久化、备份恢复与回滚演练。
- [ ] P5：非支付内部测试发布报告、阻断项和支付后续依赖清单。

### P2 增量实现状态（2026-08-09）

- [x] 将学员档案合并目标从手工输入 ID 改为可搜索、可选择的活动档案选择器。
- [x] 合并选择后继续统一二次确认，沿用请求锁、详情/列表刷新失败恢复和审计。
- [x] 删除旧手工输入合并实现，补充管理端静态验收门禁。
- [ ] 继续完成关系授权/默认/解除、合并、脱敏导出和跨账号权限的真实浏览器证据。

### 当前放行门槛

### 本轮层级回归任务

- [x] 检查所有 C 端二次确认入口并统一到 `showClientConfirm`。
- [x] 检查 C 端业务弹窗、系统弹窗、确认弹窗的 z-index 顺序及 body Portal 归属。
- [x] 检查管理端确认 Portal 的 z-index，确认高于业务弹窗。
- [ ] 小程序开发者工具/真机复核原生 `uni.showModal` 的视觉层级。

在线微信/支付宝支付继续保持 `PAYMENT_ADAPTER=disabled`，不作为本批次放行条件；线下支付凭证人工审核属于本批次必须完成的业务。

### P1 当前进度（2026-08-09）

- [x] C 端临时报名、线下转账、凭证上传、二次确认和重复提交状态门禁。
- [x] 管理端凭证驳回理由校验、取消确认、审核通过、C 端刷新和操作审计。
- [x] C 端开票申请、管理端开票通过/发票号码/电子发票上传、C 端状态同步。
- [x] 修复开票成功后弹窗残留，并用第二条隔离订单完成回归验证。
- [ ] 订单关闭/退款浏览器成功路径、报名明细刷新/异常恢复及临时数据收口。

## 2026-08-09：P1/P2 隔离回归推进结果

- [x] 运行 `admin-operations.spec.ts`、`admin-security-boundaries.spec.ts`、`p1-write-operations.spec.ts`、`p10-scenarios.spec.ts`：4 套件、13 项全部通过。
- [x] 覆盖订单取消/关闭/退款、线下凭证驳回/通过/重复操作、开票通过/驳回/上传/重复操作、普通用户 403、旧令牌失效、学员关系权限边界。
- [x] 新增学员档案导出安全回归：默认导出必须脱敏，管理员显式 `reveal=true` 才能导出明文，并校验 `sensitiveFieldsMasked` 标识。
- [x] 管理端 UI 静态验收和生产构建通过。
- [x] 后端全量回归通过：15 套件、65 项测试。
- [x] P3 运营门禁补强后全量回归更新为 15 套件、66 项测试。
- [ ] 真实管理端浏览器仍需补订单关闭/退款成功路径、报名明细刷新失败/重试和审计截图；不能用隔离测试替代运行态证据。

### 下一步批次

- [ ] P2：补用户启停/密码重置、学员状态/关系/合并/导出成功与失败提示的浏览器证据。
- [ ] P3：逐项验证消息、积分、优惠、反馈、系统配置、审计的成功/失败/重复点击和刷新。
- [ ] P4：在服务器完成 Compose/PostgreSQL 对账、上传卷重启、备份恢复和只读回滚。
- [ ] P5：冻结非支付版本并形成内部测试发布报告；在线支付保持禁用。

### 2026-08-09 运行态复核续接

- [x] 隔离启动本地 API `3199` 与管理端 `5174`，健康接口和管理端主要只读接口可达。
- [x] 复核订单、报名明细、学员档案和操作审计的数据结构与记录数量；确认审计留痕可从接口读取。
- [x] 重新执行 P1/P2 相关后端专项测试：4 套件、19 项通过；C 端 H5、管理端 UI 静态验收和根目录 verify 通过。
- [ ] 浏览器安全策略阻断了本轮自动化控制，不能将本轮接口结果记为完整浏览器验收；继续保持 P1/P2 未关闭，等待可控浏览器会话完成关系授权/默认/解除/合并/导出及审计筛选。

### P3 只读接口复核

- [x] 消息、积分、系统配置、优惠规则、反馈和审计接口均可由管理会话读取，响应结构和数据源已确认。
- [ ] 继续补 P3 的写操作成功/取消/失败重试/重复提交/审计筛选运行态证据。

### 当前运行阻断

- 历史运行阻断：新增的 `/api/health` 在旧 3100 进程上曾返回 404；本轮已重启项目 API，运行态检查通过。
- 本机 API 已重启并通过运行态健康检查：`/api/health=200`、`database=ok`、`/api/courses=200`；下一步转向服务器 Compose/PostgreSQL 实例。
## 2026-08-09：确认弹窗层级最高值统一

- [x] 将 C 端确认根/遮罩/内容提升为 `2147483645/2147483646/2147483647`，与管理端 Portal 层级保持一致。
- [x] 增加确认根和遮罩的可见性内联兜底，并更新静态 verify 契约。
- [x] 完成 C 端 H5 构建与根目录 verify。
- [ ] 刷新或重启 5185 后完成真实账号的修改密码、取消、确定和重复打开复核。
## 2026-08-09：确认弹窗层级回归门禁

- [x] 修改密码确认框运行态复核通过：业务弹窗 `1000` < C 端确认根/遮罩/内容 `2147483645/2147483646/2147483647`。
- [x] 管理端确认框继续保持 `2147483646/2147483647`，并通过 Portal 挂载到 `document.body`。
- [x] 根目录 `verify` 新增旧层级 `2147483600~2147483602` 的源码回归检查。
## 2026-08-09：非支付版本差距复核结论

- [x] 重新扫描管理端按钮、API 调用与后端 `/admin/*` 路由，确认主要写操作已有真实链路。
- [x] 将“按钮未实现”拆分为“代码链路缺失”和“浏览器运行态证据缺失”，当前优先处理后者。
- [ ] 当前执行顺序固定为 P1（订单/凭证/开票/报名明细）→ P2（用户/学员）→ P3（运营配置）→ P4（服务器）→ P5（发布冻结）。
## 2026-08-09：P1 管理端运行态登录阻断

- [x] 确认 `5174/login` 页面可达且登录表单可交互。
- [x] 记录 `admin/admin` 失败结果，未继续猜测或重复提交。
- [ ] 获取已授权的本地管理端测试账号后继续订单/凭证/开票运行态验收。
## 2026-08-09：P1 浏览器验收选择器修正

- [x] 记录订单操作按钮与标签关闭按钮的名称前缀冲突。
- [ ] 使用精确按钮名称继续订单关闭/退款运行态验收。
## 2026-08-09：P1 管理端高风险写操作回归结果

- [x] 订单关闭取消/确认、退款取消/确认均完成真实浏览器验证。
- [x] 开票驳回理由、二次确认和成功状态完成真实浏览器验证。
- [x] 线下凭证驳回空理由、二次确认和成功状态完成真实浏览器验证。
- [ ] 补审计记录可见性、报名明细刷新失败/重试后，才将 P1 标记为完成。
## 2026-08-09：P2 学员账号授权选择器增量

- [x] 管理端学员详情的授权账号改为搜索启用普通用户并选择，不再手工输入 `userId`。
- [x] 选择关系类型后继续统一二次确认，再调用关系授权接口；失败时保留弹窗和可重试路径。
- [x] 补普通用户访问关系、默认、解除、履历和合并管理接口的 403 测试。
- [ ] 运行态复核授权/取消、默认关系、解除、合并、脱敏导出与审计记录，完成后再关闭 P2。

## 2026-08-09：P3 运营模块实现增量

- [x] 重新核对消息、积分、优惠、反馈、系统配置和审计的前后端真实链路。
- [x] 将反馈处理改为带状态条件的原子更新，防止并发重复回复覆盖原结果。
- [x] 新增并发处理回归测试，专项 `mvp.spec.ts` 26/26 通过，API 构建通过。
- [ ] 继续完成 P3 浏览器运行态成功、取消、失败、重复点击、刷新和审计筛选证据。
- [ ] P4 仍需在目标服务器执行 Docker/PostgreSQL 迁移和备份恢复，不用本机测试替代。
### 本轮确认弹窗层级回归

- [x] 将 C 端确认 Portal 统一挂载到 `document.body` 末尾。
- [x] 复核所有重要二次确认入口与 z-index 契约。
- [x] 完成 H5 构建、根目录 verify 和差异检查。
- [ ] 刷新/重启 5185 后补充修改密码及其他确认入口的运行态点击证据。
### 当前层级修复补充

- [x] 复核 C 端所有重要二次确认入口均使用共享 `showClientConfirm`。
- [x] 统一业务弹窗与确认 Portal 的 z-index、固定定位和 stacking context。
- [x] 通过根目录静态门禁与 H5 构建。
- [ ] 重启 5185 后完成“修改密码”确认框覆盖关系的人工运行态复核。

## 2026-08-09：C 端确认框运行态复核完成

- [x] 重启/恢复 5185 H5 后，真实进入“我的 → 账号与安全”并打开修改密码确认框。
- [x] 验证确认框位于父业务弹窗之上，运行时读取根/遮罩/内容层级为 `2147483645/2147483646/2147483647`。
- [x] 验证点击“取消”后确认框关闭、父弹窗保留，未提交最终密码修改。
- [ ] 继续补齐其他 C 端确认入口（取消报名、凭证、开票、学员关系等）以及小程序容器验证。

## 2026-08-09：P2 学员关系浏览器验收增量

- [x] 使用现有管理员账号完成学员档案真实详情加载。
- [x] 通过搜索选择器授权 `demo01`，验证关系类型、二次确认和授权成功状态。
- [x] 验证设置默认报名人、解除关系及历史报名履历保留。
- [x] 按“授权学员关系”筛选操作审计并看到本次记录。
- [ ] 导出下载文件内容、学员合并和跨账号权限仍需运行态证据。

## 2026-08-09：P1/P2 浏览器验收续接与支付禁用修复

- P1 当前闭环证据已补齐：订单刷新/退款审计、订单关闭、线下凭证通过与驳回、开票处理、上传凭证/电子发票、报名履历审计，以及隔离报名明细 503 → 重试恢复 → 已有数据刷新失败保留旧列表。
- P2 合并选择器已真实搜索并进入最终确认；选中的源 `stu-dacf1b4e270963f1207498aa` 和目标 `stu-307fa4f48d78469bf35c0e77` 均为 UI 验收临时档案，但最终写入尚未执行。
- P2 导出按钮已显示“已导出 4 条脱敏学员档案”，审计筛选显示 `4/4 条，脱敏`；下载文件字节仍待可捕获下载会话验证。
- 本地 `.env` 已明确设置 `PAYMENT_ADAPTER=disabled`；重启 3100 后 readiness 与公共支付设置均确认微信/支付宝在线支付关闭。

## 2026-08-09：续接运行环境与浏览器策略记录

- [x] 按现有运行脚本恢复 API 3100；`GET /api/health` 返回 `status=ok,database=ok`。
- [x] 5185 H5 进程最终恢复监听；此前文档已记录重启/刷新后的修改密码确认 Portal 运行态证据。
- [x] 本轮尝试重新接管 5185 页面时被应用内浏览器 URL 安全策略拒绝；未使用原始 CDP、替代浏览器或其他绕过方式。
- [ ] 继续补 P3 网络失败后的页面重试、旧数据保留/恢复和重复提交的独立浏览器证据；若浏览器策略仍阻止，补充等价的自动化/接口证据并明确区分证据等级。

## 2026-08-09：P3 重复提交竞态修复

- [x] 发现管理端 `runOperation` 仅依赖异步 `operationKey` state，理论上同一事件循环内的快速双击可能同时通过检查。
- [x] 在 `frontend/admin-react/src/App.tsx` 增加 `operationKeyRef` 同步门闩，并在 finally 中按 key 清理，避免旧操作清除新操作状态。
- [x] 在 `frontend/admin-react/scripts/verify-ui.mjs` 增加对应静态回归断言。
- [x] 管理端 UI 静态验收、管理端构建、根目录 verify、后端 15 套件/68 项测试和 `git diff --check` 通过。
- [ ] 仍未获得独立浏览器中的双击/网络断开页面证据，不能把代码修复和静态验证替代为页面级门禁完成。

## 2026-08-09：P2 默认导出短号码脱敏修复

- [x] 只读检查默认导出 HTTP 响应发现历史短号码 `123132` 未被掩码，修复前不能把 `sensitiveFieldsMasked=true` 视为所有手机号已脱敏。
- [x] `maskPhone` 现在只对合法 11 位手机号保留前三后四；其他历史/异常号码全部替换为星号。
- [x] 新增后端权限边界回归：短号码 `123132` 默认导出为 `******`。
- [x] 重启运行 API 后再次检查导出响应：HTTP 200、`sensitiveFieldsMasked=true`、27 个手机号字段中未掩码数量为 0、未返回 `phoneNormalized`。
- [ ] 浏览器标准 download 事件和实际落盘文件仍未捕获；当前新增的是 HTTP 响应正文/字节级证据，不等同于浏览器文件验收。

## 2026-08-09：P2 导出附件契约标准化

- [x] 管理端学员导出接口增加标准 `Content-Disposition: attachment; filename="student-profiles-masked.json"`。
- [x] 新增接口回归断言；重启 3100 后真实 HTTP 响应确认附件文件名、脱敏字段和无 `phoneNormalized`。
- [ ] 应用内浏览器 download 事件及 Downloads 实际文件字节仍需可控浏览器会话确认。

- [x] 修正 `reveal=true` 管理员导出的附件文件名：明文导出为 `student-profiles.json`，默认脱敏导出为 `student-profiles-masked.json`，并补充两种响应头回归断言。

## 2026-08-09：最新运行态复核

- [x] API 健康检查返回 `status=ok,database=ok`。
- [x] readiness 确认总支付适配器、微信支付、支付宝支付均为 `disabled`；公共在线支付开关均为 `false`。
- [x] 重启后默认/明文导出附件文件名分别为 `student-profiles-masked.json` / `student-profiles.json`。

## 2026-08-09：续接后的自动化与本地运行态复跑

- [x] 根目录 `pnpm.cmd run verify` 通过。
- [x] 后端 `pnpm.cmd --dir backend/api test -- --runInBand` 通过：15 套件、68 项测试。
- [x] 管理端 UI 静态验收、管理端生产构建和 C 端 H5 构建通过；`git diff --check` 通过。
- [x] 本地 `GET /api/health` 返回 HTTP 200，`status=ok,database=ok`；公共支付设置返回 HTTP 200，`onlineWechatEnabled=false`、`onlineAlipayEnabled=false`。
- [x] 工作区 `.env` 明确设置 `PAYMENT_ADAPTER=disabled`。
- [ ] `/api/admin/integration-readiness` 当前需要 Bearer token，未使用未经授权的凭据猜测；此前已记录的授权 readiness 证据仍需与本轮公共设置证据分开引用。

## 2026-08-09：HTTP 导出字节证据与浏览器策略复核

- [x] 将一次性 HTTP 下载文件的实际字节证据纳入当前验收记录：字节数 `13651`，SHA-256 为 `25273DD028EB26041A2ECA772FB017C8CAF6A53B8B38CE2E4E55F309E217E82D`。
- [x] 该文件内容检查确认 `sensitiveFieldsMasked=true`，手机号字段 27 个，未掩码数量 0，且不含 `phoneNormalized`；临时文件已清理。
- [x] 明确证据等级：这是 HTTP 下载文件的独立字节检查，不是应用内浏览器 `download` 事件或 Downloads 目录落盘证据。
- [x] 再次尝试接管已有 5185 H5 标签时被应用内浏览器 URL 安全策略拒绝；未使用原始 CDP、替代浏览器、Cookie/存储读取或 URL 绕过。
- [ ] P2 浏览器落盘文件、P3 网络失败重试/旧数据保留/重复点击页面证据、P4 目标服务器验收和 P5 最终放行仍未闭环。

## 2026-08-09：学员档案操作重复提交竞态修复

- [x] 发现 `operateStudentDetail` 仍只依赖异步 `operationKey` state，覆盖学员启停、默认关系、解除关系、授权关系和合并等高风险操作。
- [x] 增加 `studentOperationKey` 与 `operationKeyRef` 同步互斥，finally 仅清理当前 key，避免旧操作清理后续操作。
- [x] 管理端 UI 静态验收、管理端构建、根目录 `verify`、后端 15 套件/68 项测试和 `git diff --check` 全部通过。
- [ ] 该修复仍属于源码/自动化证据；独立浏览器快速双击证据仍受应用内浏览器 URL 策略限制。
## 2026-08-09：详情重试补丁错误记录

- 第一次详情重试补丁未应用：`DetailPanel` 实际函数签名与补丁上下文不完全匹配；未产生部分代码修改。下一次改用精确小块补丁。

## 2026-08-09：学员详情刷新直接重试入口

- [x] 新增 `reloadStudentDetail`，复用详情与报名履历接口并保留原详情数据。
- [x] 学员详情面板新增“重新加载详情”按钮；写入后详情刷新失败提示改为明确指导直接重试，详情与列表同时失败时提示分别重试。
- [x] 管理端 UI 静态验收、管理端构建、根目录 `verify`、后端 15 套件/68 项测试和 `git diff --check` 全部通过。
- [ ] 仍需在可控浏览器中实际制造详情接口失败并点击该按钮，当前应用内浏览器 URL 策略仍阻止该页面级证据。

## 2026-08-09：管理端上传重复提交门禁

- [x] 课程图片上传改为复用 `runOperation('course-image-upload', ...)`，上传期间文件输入禁用。
- [x] 微信/支付宝收款码上传改为复用按渠道区分的 `runOperation` key，上传期间与其他管理端写操作互斥。
- [x] 增加管理端静态断言，并通过管理端 UI 验收、管理端构建、根目录 `verify`、后端 15 套件/68 项测试和 `git diff --check`。
- [ ] 仍需独立浏览器快速重复选择文件的页面级证据；不把静态门禁替代为浏览器证据。

## 2026-08-09：C 端刷新/写锁与服务器权限复核

- [x] C 端 `business.vue` 的 `loadAll` 失败保留已有数组并显示“重新加载”；凭证上传、取消报名、开票和在线支付的 ref 锁由根目录 verify 覆盖。
- [x] C 端 H5 构建和根目录 verify 再次通过；本地 API 健康 200，公共在线微信/支付宝开关均为 `false`。
- [x] 目标服务器 `192.168.0.162:22` TCP 可达，但本机 `ssh-add` 无法连接 SSH agent；未猜测凭据、未执行远程写入。
- [ ] P2/P3 页面级下载、故障注入、重试和重复操作证据，以及 P4 服务器验收仍未闭环。

## 2026-08-09：C 端确认锁补丁错误记录

- 第一次 C 端确认锁补丁未应用：实际 `cancelOrder` 为单行 `try/catch/finally` 结构，补丁上下文不匹配；未产生部分代码修改，后续改用小块精确补丁。

## 2026-08-09：上传路径检索错误记录

- 第一次使用复杂正则检索课程图片/收款码上传路径时因 PowerShell 转义导致 `rg` 正则解析失败；未产生代码修改，后续改用简单文本搜索。

## 2026-08-09：管理端列表网络失败与恢复重试运行态证据

- [x] 发现当前未提交的 `frontend/admin-react/.env.local` 将管理端 API 指向 `http://127.0.0.1:3199/api`；按现有本地启动方式恢复 3199 API，未修改该配置文件。
- [x] 在真实管理端“学员管理”页面停止本地 3199 API，点击“查询”制造网络失败；页面显示“刷新失败：Failed to fetch”，并保留原有 5 条列表数据和脱敏手机号。
- [x] 恢复 3199 API 后点击页面“重新加载”，错误提示消失，列表重新加载为 27 条、当前第 1/6 页。
- [x] 本轮重新通过根目录 `verify`、管理端 UI 静态验收、管理端构建、C 端 H5 构建和后端 15 套件/68 项测试。
- [ ] 该证据覆盖管理端列表刷新失败恢复；仍不能替代 P3 各写操作快速双击/失败重试的独立页面证据。

## 2026-08-09：5185 H5 运行态续接边界

- [x] 当前 5185 H5 服务仍在监听，HTTP 根页面可达；3100/3199 API 健康检查也均为 200、数据库状态为 ok。
- [x] 复核既有修改密码确认 Portal 运行态证据仍可作为当前已保存证据使用。
- [ ] 本轮应用内浏览器重新建立后无法创建新的可控标签；未使用替代 URL、其他浏览器、原始 CDP、Cookie/存储读取或其他绕过方式。
- [ ] 因此本轮不新增 5185 页面级证据，P2 浏览器下载、C 端失败重试和 C 端快速重复操作仍保持未闭环。

## 2026-08-09：C 端消息与课程详情刷新旧数据保留修复

- [x] 修复消息页刷新失败时由 `v-else-if=loadError` 隐藏旧消息的问题；已有消息现在保留，并显示局部“刷新失败/重新加载”入口。
- [x] 修复课程详情刷新失败时主动清空 `course` 的问题；已有课程详情现在保留，并显示局部“刷新失败/重新加载”入口；首次加载失败仍显示完整错误状态。
- [x] `mvp/scripts/verify.mjs` 新增 `clientRefreshPreservesLoadedData` 静态门禁。
- [x] 根目录 verify、C 端 H5 构建、管理端 UI 静态验收和 `git diff --check` 通过。
- [ ] 由于应用内浏览器当前无法建立可控标签，消息/详情真实断网点击证据仍未取得；本次修复不替代页面级故障注入证据。

## 2026-08-09：C 端学员关系操作同步锁

- [x] 为 `frontend/client-uni/src/pages/students/students.vue` 增加页面级 `studentOperationKey` 同步互斥锁。
- [x] 保存学员、设为默认、解除关系均在确认框出现前占用锁，取消/成功/异常后释放。
- [x] 操作期间禁用保存、编辑、设为默认、解除和关闭入口，并增加 `action-disabled` 视觉状态。
- [x] `mvp/scripts/verify.mjs` 新增 `clientStudentsHaveSynchronousWriteLock` 静态门禁。
- [x] 根目录 `verify`、C 端 H5 构建和 `git diff --check` 通过。
- [ ] 仍缺应用内浏览器中的独立快速双击和失败重试运行态证据；本次源码门禁不升级为页面级证据。

## 2026-08-09：C 端“我的”页写操作同步锁

- [x] 为个人资料保存、密码修改和反馈提交增加共享 `mineOperationKey` 同步互斥锁。
- [x] 三条路径均在二次确认/可编辑反馈框打开前占用锁，取消、校验提前返回、成功和异常均释放。
- [x] 操作期间禁用对应保存/关闭入口，并增加 `clientMineHasPreConfirmationWriteLocks` 静态门禁。
- [x] 根目录 `verify`、C 端 H5 构建、管理端 UI 静态验收、后端 15 套件/68 项测试和 `git diff --check` 通过。
- [ ] 仍缺应用内浏览器中的独立双击、失败注入和请求次数证据；源码门禁不替代页面级运行态验收。

## 错误记录：反馈锁静态断言排版耦合

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 新增外层 `mineOperationKey` 后，旧静态断言因 `finally` 缩进/换行变化失败 | 1 | 将断言从固定排版匹配改为检查反馈失败文案、提交状态和释放语义令牌；随后全量回归通过 |

## 2026-08-10：5185 H5 服务恢复与浏览器证据边界

- [x] 发现本轮开始时 5185 无监听，按 `Docs/runtime/start-client-h5.cmd` 安全启动 H5。
- [x] 启动后 `http://127.0.0.1:5185/` 返回 HTTP 200。
- [ ] 应用内浏览器已有标签仍是启动前的 `ERR_CONNECTION_REFUSED` 错误页；从该 `data:` 页面导航到 5185 被 URL 安全策略阻止，未使用替代 URL、其他浏览器或底层绕过。
- [ ] 因此本轮仍不能新增修改密码、学员关系、断网重试、重复点击或下载落盘的页面级证据。

## 2026-08-10：5185 H5 新标签运行态确认框复核

- [x] H5 服务恢复后通过新应用内浏览器标签登录测试账号 `demo01`。
- [x] 进入“我的 → 账号与安全”，填写两次相同的新密码并打开“确认修改密码”。
- [x] 真实 DOM 计算层级为业务弹窗 `90`，确认根/遮罩/内容为 `2147483645/2147483646/2147483647`。
- [x] 点击“取消”后确认根节点消失、账号与安全父弹窗仍保留；本次未确认提交密码修改。
- [ ] 仍需补其他 C 端确认入口、失败重试、快速重复操作及下载落盘证据。

## 2026-08-10：C 端学员解除关系确认运行态复核

- [x] 新标签进入“我的 → 我的学员”，真实加载 2 条其他学员。
- [x] 点击第一条“解除”后确认 Portal 出现，页面 6 个编辑/默认/解除操作均进入 `action-disabled`。
- [x] 当前只验证确认前锁定和取消分支，未点击“确定”，未改变学员关系。
- [ ] 学员保存、设为默认的页面级取消/失败/重复点击证据仍待补齐。

## 2026-08-10：C 端学员关系取消分支运行态补充

- [x] 解除关系确认取消后，确认根节点为 0，列表仍有 2 个“解除”入口，未改变关系。
- [x] 设为默认确认框可打开，操作期间页面操作入口锁定；取消后未改变默认关系。
- [x] 新增学员填写姓名后打开保存确认框；取消后确认根节点为 0，新增学员编辑弹窗仍保留，未写入档案。
- [ ] 页面级网络失败、快速重复点击请求次数和成功后刷新对账仍待补齐。

## 错误记录：3100 故障注入被开发监视器恢复

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 停止监听 3100 的单个 Node PID 后，开发监视器在新 PID 上立即恢复 API，健康接口仍为 200 | 1 | 未继续终止未知进程树；不把该次尝试记为断网证据，保留页面级失败重试缺口 |

## 本轮续接：H5 标签恢复与导出证据边界

- [x] 确认 5185 服务层可用：根页面 HTTP 200，3100 `/api/health` 返回 `status=ok,database=ok`。
- [x] 应用内浏览器原有 `ERR_CONNECTION_REFUSED` H5 标签从错误页刷新仍被 URL 安全策略拒绝；未使用替代浏览器、原始 CDP、Cookie/Storage 读取或其他绕过方式。
- [x] 使用同一浏览器会话中已有的空白标签加载 5185，真实进入 H5 首页并确认 `demo01` 登录态可访问“我的”页面。
- [x] 管理端学员页真实点击“导出”；当前标准 `download` 事件等待 10 秒超时，但 Downloads 文件落盘与字节证据已在后续补充记录。
- [x] 本轮复跑 `pnpm.cmd run verify`、管理端 UI 静态验收、C 端 H5 构建和 `git diff --check` 均通过。
- [ ] C 端网络失败/旧数据保留/重试、C 端快速双击真实请求次数和 P4 目标服务器验收仍未闭环。

## 本轮补充：P2 导出 Downloads 文件落盘证据

- [x] 在用户 Downloads 目录发现最新浏览器导出文件 `student-profiles-masked (5).json`，文件大小 `19910` bytes，写入时间 `2026-08-09T23:50:50.8308766+08:00`。
- [x] 文件正文中的 `exportedAt=2026-08-09T15:50:50.825Z` 与文件时间一致，`total=27`、`phone` 字段 27 个、未掩码数量 0、`sensitiveFieldsMasked=true`、`truncated=false`、不含 `phoneNormalized`，JSON 以 `}` 完整结束。
- [x] 文件 SHA-256：`C89AAD9D67619F0261B1672943690FF3BF2EF0599BFC452FFDF586A52942EFC0`；该项补齐 Downloads 文件字节证据。
- [ ] 当前应用内浏览器 `download` 事件捕获仍超时；事件捕获缺失不再阻止文件落盘证据，但需在总结中分开标注。

## 本轮续接：C 端反馈双击与故障注入边界

- [x] H5 反馈最终确认快速双击后，唯一临时反馈内容在当前 SQLite 数据库中只有 1 条 `Feedback` 记录；页面显示一次“反馈已提交”。
- [x] 保持 `PAYMENT_ADAPTER=disabled`，未执行学员档案合并、支付写入或远程写入。
- [ ] 独立 5190 H5 故障注入未能启动，C 端网络失败/旧数据保留/重试仍待在可控页面中取得；P4 目标服务器验收仍受 SSH 权限阻塞。

## 2026-08-10：C 端消息页网络失败、旧数据保留与重试恢复

- [x] 在可控 H5 标签加载“我的消息”，基线显示 6 条真实消息。
- [x] API 短时停止后点击页面“刷新”，真实显示“刷新失败：请求失败：500”和“重新加载”，原有 6 条消息仍保留。
- [x] 恢复 API 后点击“重新加载”，错误提示消失，6 条消息恢复显示；API `/api/health` 与 H5 根页面随后均返回 200。
- [ ] 该证据覆盖消息页，不扩大解释为所有 C 端页面；课程详情、报名/订单等页面仍需按各自数据状态补充故障恢复证据。

## 本轮新增阻断记录

| 问题 | 尝试 | 结果 |
|---|---:|---|
| 浏览器页面评估环境不暴露 `performance` | 3 | 无法取得刷新双击的底层请求计数；保留消息页失败/重试和已有反馈数据库幂等证据，不声称请求数为 1 |
| 目标服务器 SSH 只读检查超时 | 1 | 未猜测凭据、未远程写入；P4 Docker/PostgreSQL、备份恢复和回滚继续等待可用 SSH 权限 |

## 2026-08-10：C 端课程详情刷新失败、旧内容保留与重试恢复

- [x] 课程详情页新增明确的“刷新”入口；加载期间重复点击被 `loading` 门闩忽略。
- [x] 真实 H5 课程详情加载成功后停止 API，点击“刷新”显示“刷新失败：请求失败：500”和“重新加载”。
- [x] 失败期间课程标题、费用、报名人数和课程简介仍保留；恢复 API 后点击“重新加载”，错误提示消失，详情恢复正常。
- [x] 新增 `clientDetailHasRefreshControl` 静态门禁，根目录 verify 和 H5 构建通过。
- [ ] 该证据覆盖课程详情页；报名、订单、凭证和学员关系页面仍需独立验证相同门禁。

## 2026-08-10：C 端订单业务页刷新失败、旧数据保留与重试恢复

- [x] 订单页新增“刷新”入口；`loadInFlight` 统一门闩阻止刷新期间重复触发。
- [x] 真实 H5 订单页加载出 2 条订单后停止 API，点击“刷新”显示业务数据失败和“重新加载”。
- [x] 失败期间已有订单、订单状态、金额和凭证审核提示仍保留；恢复 API 后点击“重新加载”，错误提示消失、订单恢复。
- [x] 新增 `clientBusinessHasRefreshControl` 静态门禁，根目录 verify 和 H5 构建通过。
- [ ] 订单页的写操作成功/取消/重复点击证据仍按具体操作独立保留；本项只覆盖刷新失败恢复。

## 2026-08-10：C 端学员档案刷新失败、旧列表保留与重试恢复

- [x] 学员档案页新增“刷新”入口；`loadInFlight` 门闩阻止刷新期间重复触发，并在写操作期间显示禁用状态。
- [x] 真实 H5 学员页加载 2 条其他学员后停止 API，点击“刷新”显示学员档案加载失败和“重新加载”。
- [x] 失败期间 2 条学员记录及编辑/默认/解除入口仍保留；恢复 API 后点击“重新加载”，列表恢复正常。
- [x] 新增 `clientStudentsHasRefreshControl` 静态门禁，根目录 verify 和 H5 构建通过。
- [ ] 学员保存、设为默认、解除关系的确认成功/失败/重复点击及刷新对账仍需分别验收；本项只覆盖刷新恢复。

## 2026-08-10：续接验收错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| uni-app H5 新增学员输入框没有运行时 `placeholder` 属性，按占位符定位失败 | 1 | 读取真实 DOM 结构，改用输入顺序定位；未重复执行失效选择器 |

## 2026-08-10：P2 学员关系写操作运行态补充

- [x] 设为默认：确认框、一次确认成功反馈、成功后刷新对账；演示学员2成为当前学员并在刷新后保持。
- [x] 保存学员：唯一临时档案确认、保存成功反馈、成功后刷新对账；临时关系随后已清理。
- [x] 保存失败恢复：空姓名校验错误保留表单，修正为第二个唯一临时资料后成功保存，再解除并刷新确认清理。
- [x] 解除关系：临时关系确认、成功反馈、刷新后关系仍移除；历史报名保留提示已验证。
- [ ] 页面级快速双击的底层请求次数仍无法从当前浏览器环境证明；`dblclick`/直接 span 点击未形成可靠证据，父级点击取消分支已验证。
- [ ] 学员合并仍待用户明确确认；跨账号权限和完整审计/导出浏览器证据继续保留为独立待办。

## 2026-08-10：自动化复跑与本地服务边界

- [x] `pnpm.cmd run verify` 通过，新增 C 端刷新和学员关系门禁均为 true。
- [x] 后端 15 个测试套件、68 个测试全部通过；管理端 UI 静态验收通过；C 端 H5 构建通过；`git diff --check` 通过。
- [x] API 3100 健康检查 HTTP 200、数据库 `ok`；H5 5185 根页面 HTTP 200；`PAYMENT_ADAPTER=disabled` 约束保持不变。
- [x] 管理端 5174 初始即时检查未恢复监听：启动脚本及直接子进程方式曾受当前 PowerShell 环境重复 `Path/PATH` 键影响；未修改环境配置，后续延迟启动已恢复。

- [x] 延迟启动后管理端 5174 和配置 API 3199 均返回 HTTP 200，真实登录页已加载。
- [ ] 缺少已授权管理端测试账号，不能继续管理端订单/审计/关系等浏览器写操作验收；不猜测凭据。
- [x] 3199 `/api/payment-settings/public` 返回 HTTP 200，在线微信/支付宝均为 `false`；未授权管理端 readiness 和学员接口均返回 HTTP 401。

## 2026-08-10：P1 管理端运行态验收收尾

- [x] C 端“账号与安全 → 保存新密码”确认层在真实 H5 中显示于父弹窗之上；取消后未提交密码修改。
- [x] 订单退款成功、刷新持久化；待支付订单关闭成功、刷新后从待支付列表移除。
- [x] 线下凭证驳回、C 端重新上传、管理端审核通过、C 端刷新对账完成。
- [x] 操作审计未筛选可见本轮记录，筛选“退款完成”可返回匹配记录。
- [x] 开票通过、电子发票上传、无效文件失败后合法文件恢复上传、刷新持久化完成。
- [x] `p1-write-operations.spec.ts` 2 个测试通过；`pnpm.cmd run verify`、管理端 UI 静态验收、C 端 H5 构建和 `git diff --check` 均通过。
- [x] 补齐种子引用但缺失的本地演示凭证 `backend/api/storage/payment-proofs/demo-proof-002.png`，未修改审核业务逻辑。
- [ ] P2 学员合并仍需用户明确确认；跨账号权限、完整导出/审计和页面级重复点击证据仍待继续。
- [ ] P3 其他运营模块的浏览器成功/失败/幂等证据仍待继续。
- [ ] P4 目标服务器 Docker/PostgreSQL、备份恢复和回滚仍受真实 SSH 权限阻塞。
- [ ] P5 非支付版本冻结、最终发布报告和支付后续接入清单仍待完成。

## 2026-08-10：目标会话继续执行记录

- [x] 已重新读取当前规划、进度、发现和最近 `Docs/Summary`；未清理或回退既有修改。
- [x] 仅重启占用 5185 的明确 H5 Node 进程；Vite 在 `http://127.0.0.1:5185/` 正常启动，根页面 HTTP 200。
- [x] 重启后通过应用内浏览器新标签重新登录 `demo01`，进入“账号与安全”；确认修改密码层真实位于业务弹窗之上，取消后表单保留且未提交。
- [x] P1 当前运行 API 的重复审核、重复退款、重复关闭、重复开票处理均返回 HTTP 400；P1 自动化写操作测试 2/2 通过。
- [ ] P2 页面级快速双击的底层请求次数仍未取得可靠证据；学员合并仍需用户明确确认，未执行。
- [ ] P4 目标服务器 Docker/PostgreSQL、备份恢复和回滚仍需真实 SSH 权限；不猜凭据、不远程写入。
- [ ] P5 需要基于本轮证据生成最终内部测试发布报告、冻结清单和支付后续接入清单；发布结论必须明确当前外部阻塞和权限决策项。

## 2026-08-10：当前执行批次收尾

- [x] 阶段 4：文档与最终回归整理；发布报告和支付后续接入清单已创建并写入 `Docs/Summary`。
- [x] P5.1：完成非支付内部测试冻结说明，明确可进入本地/内网内部测试，但不批准生产或目标服务器发布。
- [x] P5.2：记录自动化、构建、健康检查、支付禁用和 P1/P2/P3 运行态证据引用。
- [ ] P2.1：页面级快速双击底层请求次数仍缺乏可靠运行态统计；学员档案合并仍等待用户明确确认。
- [ ] P4.1：目标服务器 Docker/PostgreSQL、迁移对账、备份恢复、上传持久化和只读回滚仍需可用 SSH 权限。
- [x] P5.3：`operator` 与 `admin` 的敏感字段明文揭示权限已分离；仅 `admin` 可揭示，operator 运行态保持脱敏。

本批次不调用 `update_goal complete`：仍有外部服务器、权限决策和明确确认事项未完成。

## 2026-08-10：续接后的本地回归与健康检查

- [x] `pnpm.cmd run verify` 通过，客户端确认层级、写入锁、失败重试和状态门禁静态校验均为 `true`。
- [x] `git diff --check` 通过；仅报告工作区既有 LF/CRLF 换行提示。
- [x] 3100/3199 按实际路由 `/api/health` 检查均返回 HTTP 200、`status=ok`、`database=ok`；5174/5185 根页面均返回 HTTP 200。
- [ ] 根路径 3100/3199 返回 404 属于 API 无根路由，不作为健康失败；P2 合并授权、底层请求次数和 P4 目标服务器边界不变。

## 2026-08-10：P3 消息启停并发门禁补强

- [x] 将消息启用/停用改为带目标状态条件的原子迁移；竞争请求返回 400，避免重复状态迁移和重复启停审计。
- [x] 新增管理端并发回归，验证两次同时启用只产生 `201/400`，最终状态正确且启用审计仅 1 条。
- [x] 后端 15 个测试套件、69 个测试，根目录 verify、管理端 UI/构建和 H5 构建全部通过。
- [ ] P3 仍不宣称所有业务写入均具备底层请求级幂等计数；积分调整、学员合并授权和 P4 服务器验收边界保持不变。

### 本轮错误与处理

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 新增并发测试将 POST 成功状态误断言为 200 | 1 | 依据实际 Nest 响应修正为 201；业务实现结果为一次 201、一次 400 |

## 2026-08-10：P3 规则/配置重复保存审计去重

- [x] 优惠规则已有记录的相同值重复保存使用目标值条件更新，不重复新增维护审计。
- [x] 系统配置已有记录的相同值重复保存使用值/说明条件更新，不重复新增维护审计。
- [x] 新增重复/并发保存回归；完整后端测试 15 个套件、70 个测试，verify、UI 校验和双端构建通过。
- [ ] 积分调整仍保留每次独立流水；底层浏览器请求计数、学员合并授权和 P4 服务器验收不因本地去重而关闭。

### 本轮错误与处理

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 优惠规则审计详情不包含规则 ID，按 ID 查询测试得到 0 条 | 1 | 改为比较具体操作类型审计数量增量；不扩展接口未承诺的详情字段 |

## 2026-08-10：P4 Compose 本地预检与环境模板

- [x] PostgreSQL 导入、导出和对账脚本均通过 `node --check`。
- [x] 新增 `.env.docker.example`，不包含真实密码/密钥，并明确 `PAYMENT_ADAPTER=disabled`、PostgreSQL URL、JWT 和上传卷配置形状。
- [x] 服务器验收步骤补充从模板复制 `.env.docker` 后替换服务器专用值的流程。
- [ ] 当前工作区没有真实 `.env.docker`；`docker compose config` 因缺少服务器环境文件拒绝解析，目标服务器 P4 仍需真实 SSH 和服务器环境配置。

### 本轮错误与处理

| 错误 | 尝试 | 处理 |
|---|---:|---|
| `docker compose config` 缺少 `POSTGRES_PASSWORD`/`.env.docker` | 2 | 不创建或猜测秘密；新增安全模板并记录为服务器环境配置前置条件 |

## 2026-08-10：P4 预检与本地边界最终校验

- [x] 根目录 `pnpm.cmd run verify` 通过。
- [x] PostgreSQL 脚本语法检查通过，`git diff --check` 通过；仅有换行格式提示。
- [x] 3100/3199 `/api/health` 均返回 HTTP 200、`status=ok`、`database=ok`；公共支付开关仍为 `false/false`。
- [ ] Compose 真正解析、迁移、PostgreSQL 对账、上传卷、备份恢复和回滚仍必须在配置好 `.env.docker` 的目标服务器执行。

### P4 脚本语法检查限制

- [ ] 本机尝试对 `Docs/runtime/*.sh` 执行 `bash -n`；Windows Bash/WSL 服务返回 `E_ACCESSDENIED`，因此未把本地结果解释为 shell 语法通过或失败。
- [x] 已完成脚本内容静态审阅、Node PostgreSQL 脚本 `node --check`；`.sh` 语法应在目标 Linux 服务器执行验收时一并检查。
- [ ] 只读网络检查确认 `192.168.0.162:22` TCP 可达；未取得 SSH 授权，未尝试猜测凭据，不能据此执行或宣称 P4 完成。

## 2026-08-10：P2 管理角色与敏感字段权限收敛

- [x] 新增 `admin`/`operator`/`user` 角色规范化；operator 仍可访问管理端，但不再被 JWT 映射为 admin。
- [x] 学员档案详情、报名履历详情和导出接口均仅允许 `admin` 揭示明文敏感字段；operator 返回脱敏数据。
- [x] SQLite 种子、迁移保留 operator 角色；本地演示库 `u-operator` 已在事务中校正为 `operator` 并递增会话版本。
- [x] 真实 3199 运行态：operator 登录角色为 `operator`、管理端 dashboard HTTP 200、详情手机号为 `182****0668`、`reveal=true` 导出仍为 `student-profiles-masked.json`。
- [ ] 页面级快速双击底层请求次数、学员合并明确确认和目标服务器 P4 仍未完成。
## 2026-08-10：C 端反馈双击证据补充

- [x] 完成 H5“问题反馈”最终确认按钮快速双击运行态复核，页面仅显示一次“反馈已提交”，本地数据库唯一匹配记录为 1 条。
- [ ] 底层浏览器网络请求次数仍未取得可靠计数；学员档案合并仍需明确确认，目标服务器 P4 仍受 SSH 权限阻塞，支付继续保持禁用。
## 2026-08-10：当前续接收尾状态

- [x] P1：报名明细真实失败态、旧数据保留、重试恢复和退款审计筛选已补齐；自动化/构建门禁复跑通过。
- [x] P5 本地内测材料：非支付发布报告和支付后续清单已存在，并明确只允许本地/内网非支付内部测试。
- [/] P2/P3：已完成本轮 H5 反馈双击、管理端审计和既有权限/脱敏证据；页面底层请求计数及部分逐模块浏览器证据继续单独标注，不用静态结果替代。
- [ ] P2 学员档案合并仍需用户明确确认，禁止擅自执行。
- [ ] P4 目标服务器 Docker/PostgreSQL、备份恢复、上传持久化和回滚仍需真实 SSH 权限；本轮未远程写入。

### 本轮错误与处理

| 错误 | 处理 |
|---|---|
| `Get-NetTCPConnection`/`Get-CimInstance` 读取进程信息被 Windows 权限拒绝 | 改用只读 `netstat -ano` 和 `Get-Process`，确认 3199 的单个 Node PID 后再操作 |
| PowerShell `Start-Process` 因 `Path/PATH` 大小写冲突无法启动 3199 | 改用显式 `cmd.exe` 子进程启动，健康检查恢复 HTTP 200；未修改当前 shell 环境 |

## 2026-08-10：P3 积分与优惠页面级运行态补充

- [x] 积分管理：取消分支不写入；`u-demo` 执行 `+1/-1` 可恢复，快速双击确认各只出现一次成功提示。
- [x] 积分管理：流水显示本轮正负记录，净积分恢复 `128`；操作审计筛选“用户积分调整”可见对应记录。
- [x] 积分管理：3199 断开时保留 14 条旧列表并显示失败/重试，恢复后列表和积分值不变。
- [x] 运营优惠：编辑取消保持 `2 / 0.95 / 否`；第一条规则启用后再停用恢复原状态，快速双击各只出现一次成功提示。
- [x] 运营优惠：操作审计筛选“优惠规则维护”可见启用/停用记录及冲突变化；3199 断开保留 8 条旧规则，恢复后重试成功。
- [/] P3：积分/优惠本轮专项证据已补齐；其他模块的剩余重复提交边界仍需按风险继续收敛。
- [ ] P2 学员档案合并仍需用户明确确认；[ ] P4 目标服务器仍需真实 SSH 权限；支付保持禁用。

## 2026-08-10：继续会话 H5 重启/刷新运行态验证

- [x] 仅重启明确的 5185 Node 进程，根页面 HTTP 200；3100/3199/5174 未停止。
- [x] 真实 H5 刷新后 `demo01` 会话恢复，账号安全父级入口唯一可用，业务弹窗真实显示。
- [x] 临时密码保存触发后，截图确认“确认修改密码”系统层位于业务弹窗之上；未点击最终“确定”，未修改密码。
- [ ] 系统层“取消”按钮尚未通过当前 DOM 语义树完成点击，需继续用可见 UI 交互完成取消分支并核对输入保留。

## 2026-08-10：H5 重启/刷新运行态验证完成

- [x] 重启 5185、刷新真实 H5 标签、恢复 `demo01` 会话并进入账号安全。
- [x] 触发保存后截图确认系统确认层位于业务弹窗之上。
- [x] 取消系统确认层后业务弹窗和临时输入保留，未提交密码修改。
- [ ] 下一阶段转向 P2 跨账号权限/脱敏导出/完整审计证据，或其他仍未闭环的最高优先级项。

## 2026-08-10：P2 读取边界诊断错误

- [x] 已确认 `admin`、`operator`、`demo01`、`demo02` 登录路径可用；未猜测失败的 `demo` 密码。
- [x] 已获得跨账号支付状态 404、普通用户导出 401、管理员/运营导出文件名差异的初步结果。
- [ ] 首次脚本对学员列表字段名和重复 JSON 键处理不正确；需用不泄露敏感值的字段级诊断重新核对详情/导出揭示权限。

## 2026-08-10：P2 跨账号/脱敏导出运行态复核完成

- [x] admin/operator 详情揭示权限已在真实 3199 重新核对：admin 明文揭示，operator 脱敏。
- [x] 标准导出、admin `reveal=true`、operator `reveal=true` 和普通用户 403 已核对；导出审计记录可见。
- [x] `demo01`/`demo02` 订单互读支付状态均 404，账号订单数量与当前演示数据一致。
- [ ] 学员档案合并仍需用户明确确认；[ ] 页面底层请求次数仍不可可靠统计；[ ] P4 目标服务器仍需真实 SSH 权限。

## 2026-08-10：本轮收尾校验完成

- [x] `verify`、后端 15/68、管理端 UI 校验和 `git diff --check` 已复跑通过。
- [x] 规划、进度、发现和 P2/H5 总结文档已同步本轮真实结果。
- [ ] 目标仍保持 active：学员合并需用户明确确认、底层请求计数证据不足、P4 目标服务器需真实 SSH 权限；不得据此批准生产发布。

## 2026-08-10：本轮实际执行结果

- [x] `pnpm.cmd run verify`、三个 PostgreSQL 脚本 `node --check`、`git diff --check` 均通过；3100/3199/5174/5185 端口均可达。
- [x] 重新连接可控浏览器后打开 5185，`demo01` 登录成功；进入“我的 → 账号与安全”，刷新后会话仍恢复。
- [x] 密码确认层真实覆盖业务弹窗；取消后临时输入保留且未提交密码修改。
- [ ] 浏览器会话曾出现旧标签绑定失效，重新获取标签后已恢复；底层快速双击请求次数仍不作声称。
- [ ] P2 学员合并继续等待明确授权；P4 继续等待目标服务器 SSH 授权和 `.env.docker`。

## 2026-08-10：完整门禁复跑结果

- [x] 后端 `pnpm.cmd test`：15 个测试套件、70 个测试全部通过。
- [x] 后端构建、管理端 UI 静态验收、管理端构建、H5 构建和根目录 `pnpm.cmd run verify` 全部通过。
- [x] Sass/uni-app 弃用提示仅为警告，不影响本轮构建成功；未发现新的业务失败。
- [x] P3 当前证据已闭环，保持在线支付禁用；P2 合并授权、P4 服务器验收和底层浏览器请求计数仍是独立未决项。

### 本轮诊断错误与处理

| 错误 | 处理 |
|---|---|
| 首次并行端口探测因单个 `Test-NetConnection` 超时导致整组检查中止 | 改用每端口 1.5 秒超时的 `TcpClient` 只读探测，确认 3100/3199/5174/5185 均可达 |
| PowerShell `foreach`/`finally` 组合产生空管道解析错误 | 改为先收集对象再统一 `ConvertTo-Json`，未影响应用或数据 |

## 2026-08-10：外部阻塞审计结论

- [x] 本地/内网非支付版本的自动化、构建、健康检查、H5 运行态、权限/脱敏/审计和发布材料均已完成或有证据留痕。
- [ ] P2 学员档案合并：该操作会迁移履历并标记源档案，必须由用户明确提供源档案 ID、目标档案 ID 和执行授权。
- [ ] P4 目标服务器验收：`192.168.0.162:22` 网络可达，但缺少可执行 SSH 身份和服务器 `.env.docker`；无法在本地结果上替代 PostgreSQL/Compose/备份恢复/回滚。
- [ ] 页面快速双击底层请求次数：当前浏览器接口无法提供可靠计数，不将页面提示扩大解释为网络次数证据。
- 以上阻塞已在连续多轮中保持不变；在没有用户授权或服务器外部状态变化前，无法安全完成剩余目标。

## 2026-08-10：恢复后的外部状态复核

- [x] 只读确认目标服务器 22 端口仍可达；未取得 SSH agent/密钥授权。
- [x] 只读确认工作区没有真实 `.env.docker`；未创建或写入秘密配置。
- [ ] 当前仍未收到学员合并源/目标档案及明确授权；保持合并未执行。
- [x] 只读检查本机 SSH 配置目录被 Windows 权限拒绝；未绕过权限、读取私钥或猜测 SSH 用户名。

## 2026-08-10：用户继续执行指南

- [x] 生成详细中文指南，回答当前进度、SSH 授权、登录测试数据、短信注册和忘记密码所需信息。
- [x] 记录开发 fake 与真实 webhook 两条短信验收路径，明确真实短信所需配置和安全边界。
- [x] 记录服务器 P4 命令顺序、PowerShell/API 测试样例、验收清单和当前仍需用户提供的授权信息。
## 2026-08-10：本地登录错误运行态诊断
- [x] 本地 seed 已更新已有用户密码哈希，并补齐五个测试账号。
- [x] 管理端 5174 使用 `admin/123456` 真实登录成功并进入工作台。
- [x] H5 5185 使用 `demo01/123456` 真实登录成功；刷新后会话恢复。
- [x] H5 账号安全确认层真实可见；点击取消后未提交密码修改。
- [ ] 3100 旧运行实例的 `operator` 角色仍显示为 `user`，需用户确认后才重启该本地服务。
## 2026-08-10：目标服务器只读状态复核
- [x] `192.168.0.162:22` TCP 可达，但 SSH root 认证实际失败：`Permission denied (publickey,password)`。
- [x] 目标 3100 旧 API 可访问；`/api/health` 返回 404，课程接口返回 200。
- [x] 目标 5174、5185、5432 不可达；无法证明当前 Compose/PostgreSQL 管理端、H5 和数据库验收条件成立。
- [x] 目标公开支付开关读取为 `onlineWechatEnabled=true`、`onlineAlipayEnabled=true`，与非支付目标冲突。
- [ ] 未获得安全 SSH 授权和用户确认前，不执行服务器支付配置关闭、迁移、备份恢复或回滚。
## 2026-08-10：非支付公开开关回归门禁
- [x] 增加 `PAYMENT_ADAPTER=disabled` 时公开微信/支付宝开关强制为 `false/false` 的自动化测试。
- [x] 完整后端测试 15 套件、70 用例通过；后端构建、根目录 verify 和 diff 检查通过。
- [ ] 目标服务器仍返回公开支付开关 `true/true`，须取得 SSH 和维护授权后修正并复验。

## 2026-08-10：C 端退出登录确认弹窗视觉优化

- [x] 将退出登录确认改为统一确认层的危险操作变体，增加退出图标和明确按钮文案。
- [x] 为 html portal 增加内联重要样式，修复按钮退化为浏览器默认样式的问题。
- [x] 5185 真实验证取消分支和确认退出分支；H5 构建、根目录 verify、差异检查通过。
- [x] 新增 `Docs/Summary/2026-08-10_C端退出登录确认弹窗视觉优化验收.md`。

## 2026-08-10：课程管理 UI 需求追加
- [x] 课程新增表单增加开始/结束日期时间选择、草稿缓存、重置和新增校验。
- [x] 管理端课程列表按创建时间倒序，最新新增课程显示在最前。
- [/] 重新构建、自动化校验和本地浏览器回归进行中。

## 2026-08-10：时间与发票流程追加
- [x] 管理端课程截止时间、Banner 时间统一为日历时间选择；Banner 关联课程支持搜索选择。
- [x] C 端已驳回发票支持修改后重新申请，后端新增受控重申请接口并保留审计链路。
- [x] C 端报名支付弹窗打开时隐藏底层原生选择控件，修复控件盖顶。
- [x] H5、管理端构建及相关回归校验通过。

## 2026-08-10：本轮 H5/P2 复核
- [x] 刷新 5185 H5 后复核账号安全确认层：确认层位于业务弹窗之上；取消后临时输入保留且未提交密码修改；再次刷新后 `demo` 会话和账号安全入口恢复。
- [x] P2 后端学员关系、档案、导出和跨账号权限专项复跑：`student-api.spec.ts` 与 `admin-security-boundaries.spec.ts` 共 10 个测试通过。
- [ ] 管理端页面级导出浏览器证据本轮未新增：已有管理端标签不属于当前浏览器自动化会话；未读取 Cookie/存储、未使用底层绕过，仍单独标记为证据缺口。

## 2026-08-10：重启后 H5 与 P2 复核补充

- [x] 仅重启 5185 H5 进程；3100/3199/5174 未停止，5185 恢复监听且根页面 HTTP 200。
- [x] 5185 重启后重新接管当前标签并刷新：会话恢复到“我的/订单”页面，账号信息和账号安全入口存在。
- [x] 账号安全确认层真实覆盖业务弹窗；取消后确认层消失、临时密码保留；关闭业务弹窗并刷新后回到“我的”，未执行最终密码修改。
- [x] `student-api.spec.ts` 与 `admin-security-boundaries.spec.ts` 共 10 个测试通过；3100/3199 登录账号角色均为 admin/operator/user 的预期映射。
- [x] 3100/3199 真实读取权限复核：普通用户访问管理端学员接口返回 403，operator 的 reveal 导出仍为脱敏文件名，跨账号订单支付状态返回 404。
- [ ] 管理端页面级导出下载证据仍受浏览器旧标签无法接管影响；不读取浏览器存储、不使用底层绕过。

### 本轮错误与处理

| 错误 | 处理 |
|---|---|
| 首次用 `Start-Process pnpm.cmd` 启动 H5 触发 PowerShell `Path/PATH` 字典键冲突 | 改用 `cmd.exe /c` 调用 pnpm，5185 恢复监听 |
| 首次 H5 健康轮询在启动编译窗口内超时 | 读取进程/端口状态后采用不同启动方式并重新检查，未重复盲目重启 |
| 管理端浏览器旧标签不属于当前自动化会话 | 重新列出当前标签后仍无法接管，保留为证据缺口，不绕过会话 |

## 2026-08-10：继续目标材料交接文档
- [x] 新增材料提交说明，覆盖档案合并授权、SSH 公钥、服务器 `.env.docker`、短信前置条件和导出下载证据。
- [x] 明确敏感信息不通过聊天/Git 提交，并区分只读验收与需要单独确认的写操作。
- [ ] 等待用户提供 SSH 可用身份、服务器项目目录和脱敏环境状态；收到后先执行目标服务器只读验收。

## 2026-08-10：5185 H5 真实运行态复核补充
- [x] 使用新浏览器标签完成 `demo01` 登录、进入“我的”和刷新恢复验证。
- [x] 验证账号安全确认层真实存在、层级覆盖业务弹窗，取消后临时输入保留且未提交密码。
- [x] 关闭业务弹窗并刷新，确认会话、页面入口恢复正常；新增 Summary 留痕。
- [ ] 继续推进可在本地完成的 P1/P2/P3 证据或修复；P2 合并授权和 P4 服务器验收仍需外部输入。

## 2026-08-10：SSH 配置差异诊断
- [x] 读取 SSH config，确认 `guanxiang-server`（49.234.113.222）与目标服务器 `192.168.0.162` 是不同 Host。
- [x] 对两个 Host 做 TCP 和 BatchMode 只读 SSH 对比，均保留认证失败证据；确认当前命令行没有 ssh-agent/私钥凭据。
- [x] 新增 SSH/VSCode 登录差异 Summary，给出使用临时公钥和确认目标主机的后续步骤。
- [ ] 等待用户确认 P4 验收目标并提供该目标的可用 SSH 身份；在此之前不执行服务器写操作。

## 2026-08-10：VSCode 交互式 SSH 认证确认
- [x] 从 VSCode Remote-SSH 日志确认实际目标为 `192.168.0.162`，并确认密码提示、密码响应和远程 VSCode Server 启动成功。
- [x] 修正 BatchMode 失败的解释：命令行未提供交互式密码，不能据此判定目标服务器拒绝认证。
- [ ] 在用户已登录的 VSCode 远程终端执行服务器只读身份、项目目录、Docker/Compose 和环境文件存在性检查。
- [ ] 迁移、重启、备份恢复、回滚及数据库写操作继续等待单独维护授权。

## 2026-08-10：P2 导出页面证据补齐
- [x] 新建管理端标签，展开用户与运营并进入学员管理；点击导出后确认 Downloads 文件真实生成。
- [x] 校验最新导出 JSON 的完整性、数量、脱敏字段、截断标记和手机号掩码。
- [x] 新增 P2 导出页面下载 Summary；保留独立浏览器 download 事件未捕获的限制说明。
- [ ] 在 VSCode `SSH: 192.168.0.162` 远程窗口执行服务器只读身份、项目目录、Docker/Compose和 `.env.docker` 存在性检查。

## 2026-08-10：P4 服务器 SSH 前置条件已确认
- [x] 在远程 VSCode 终端确认 root、主机名、项目目录、Compose 文件、Docker 和 Compose 版本。
- [ ] 继续执行 `.env.docker` 脱敏状态、Compose config、容器状态和 API health 只读检查。
- [ ] 维护授权后再考虑迁移、容器重启、上传卷、备份恢复和回滚；当前不执行这些写/副作用操作。

## 2026-08-10：H5 隔离 API 复核与服务器版本差异
- [x] 确认本地 `3100` 被 VSCode Remote-SSH 端口转发占用，返回的是服务器旧 API；本地 `3199` 返回当前代码的 `status=ok,database=ok`。
- [x] 重启本地 5185 H5，并临时使用本地 3199 API 完成 `demo01` 登录、刷新恢复、账号安全确认层和取消不提交复核。
- [x] 记录确认层真实 z-index 高于业务弹窗，取消后临时输入保留；未执行最终密码修改。
- [ ] 服务器 HEAD `5f41bf7` 与本地工作区 HEAD `16ff2b8` 不一致；需先确认部署源/同步代码，再进行服务器构建、迁移、重启和回滚验收。
- [ ] 服务器容器内部 `/api/health` 仍返回 404；在正确代码部署后重新验证 `status=ok,database=ok`。
- [x] 确认 `5f41bf7` 是本地 `16ff2b8` 的祖先；本地当前提交包含健康路由，但工作区仍有未提交业务/前端修改，不能未经确认直接打包全部工作区覆盖服务器。
- [x] H5 构建、API 构建、管理端构建、后端 15 套件/70 测试和根目录 `verify` 均通过。
