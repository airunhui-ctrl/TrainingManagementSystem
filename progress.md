# 进度记录

## 2026-08-10：开票重新申请接口运行态修复
- 截图中的 `Cannot POST /api/invoices/INV-DEMO-006/reapply` 根因是本地 3100 API 进程未加载新增的重申请路由；源码、dist 和隔离测试均已包含该路由。
- 已补充 `verify` 对 `/invoices/:id/reapply`、前端专用调用和链路字段的静态门禁，并统一 C 端业务弹窗层级为 1000。
- 已重启本地 3100 API；带登录令牌访问不存在的重申请编号返回业务化 404（而非 `Cannot POST`），证明路由已注册。
- 后端 15/15 测试套件、70/70 用例通过；H5 构建完成；`pnpm.cmd run verify` 和 `git diff --check` 通过。

## 2026-08-09：P2 学员账号授权选择器实现

- 管理端“授权账号”由手工输入用户 ID 改为可搜索、可选择的普通用户账号弹窗，支持关系类型选择和统一二次确认。
- 普通用户访问学员关系、授权、解除、默认关系、履历和合并管理接口的 403 边界测试已补充并通过。
- 管理端 UI 静态验收、TypeScript/Vite 构建、后端关系与权限测试均通过。
- P2 仍未关闭：需要在运行态完成成功/取消、默认关系、解除、合并、脱敏导出和审计可见性复核。

## 2026-08-09：修改密码二次确认层级再次加固

- 在 `frontend/client-uni/src/common/confirm.ts` 增加 `enforceConfirmLayer`，统一重新设置确认根节点、遮罩和内容层级，并在首次渲染、`requestAnimationFrame`、0ms 和 32ms 延迟渲染后再次校正。
- 所有 C 端重要确认继续复用 `showClientConfirm`；业务弹窗保持 `90`，确认根/遮罩/内容保持 `2147483645/2147483646/2147483647`，高于 uni-h5 系统弹窗 `2147483000/2147483001/2147483002`。
- `pnpm.cmd run verify` 与 `pnpm.cmd --dir frontend/client-uni build:h5` 均通过。运行中的 5185 服务需要重启或热更新后，再在“我的 → 账号与安全 → 保存新密码”中做浏览器现场复核。

## 2026-08-09：修改密码二次确认 Top Layer 修复

- 复核截图后确认问题表现为确认框被“账号与安全”业务弹窗遮挡，而不是密码校验逻辑失败。
- C 端所有二次确认继续统一走 `showClientConfirm`；H5 改为优先使用 `<dialog>.showModal()` 的浏览器 Top Layer，普通 z-index 仅作为旧 WebView 回退。
- 保留并复核统一层级：业务弹窗 `90`，系统弹窗 `2147483000/2147483001/2147483002`，确认框 `2147483645/2147483646/2147483647`。
- `pnpm.cmd run verify`、C 端 H5 构建、`git diff --check` 已通过；待用户重启 5185 服务并 Ctrl+F5 做浏览器现场复核。

## 2026-08-09：C 端确认弹窗层级最终修复

- 修复“我的 → 账号与安全 → 保存新密码”二次确认可能被业务弹窗遮挡的问题。
- C 端业务弹窗统一恢复为 `z-index: 90`；系统弹窗和重要确认继续使用独立高层级。
- 已检查业务、消息、我的、报名、学员五类弹窗页面，重要二次确认均使用 `showClientConfirm`。
- 已通过 `pnpm.cmd run verify`、`pnpm.cmd --dir frontend/client-uni build:h5`、`pnpm.cmd --dir frontend/admin-react test:ui` 和 `git diff --check`。

## 2026-08-09：非支付上线主线重新建立

- 重新核对当前代码、后端路由、前端调用、测试基线和 `Docs/Summary`；确认“按钮未实现”不能仅凭界面判断，主要风险已转为运行态写操作和部署证据。
- 明确本阶段不把在线微信/支付宝作为放行条件，继续保持 `PAYMENT_ADAPTER=disabled`；线下收款码、凭证上传和人工审核属于当前内部测试范围。
- 建立 P1→P2→P3→P4→P5 执行顺序：先业务写闭环，再用户/学员安全，再运营，再服务器部署，最后冻结非支付版本。
- 当前下一步：补齐 P1 报名明细刷新失败/重试证据，然后进入 P2 成功路径与权限/脱敏复核。

## 2026-07-30

- 已创建本次分析任务的规划文件。
- 下一步读取 Prisma schema、后端服务和前端页面，记录可验证的数据关系。
- 已完成 Prisma schema、订单/模板/报名明细/用户管理服务、控制器、管理端模块和种子数据核对；关键发现已写入 `findings.md`。
- 已生成 `Docs/Summary/用户管理_学员管理_报名信息存储关系分析.md`，包含结论摘要、关系图、数据流、代码依据和优化建议。
- 已检查最终 Markdown 文件存在，路径和章节结构正确；任务完成。
- 当前支付目标复核：确认微信/支付宝点击调用 `createPaymentIntent` + `requestNativePayment`，原生成功后同步调用 `payOrder`；线下转账弹窗提供“复制全部”，复制订单号、金额、收款户名、开户行、账号和备注。
- 验证：C 端 H5 构建通过；后端 Jest 4 套件、10 个测试通过；后端 TypeScript 检查通过。
- 额外验证：`build:mp-weixin` 仍受现有 UniApp alpha 与 Vue 3.4.21 依赖不兼容影响，报 `isInSSRComponentSetup` 未导出；未扩大本次支付功能改动范围处理该既有依赖问题。
- 已按最新需求完善 `Docs/Summary/用户管理_学员管理_报名信息存储关系分析.md`：新增学员管理的业务意义、目标分层模型、`Student`/`AccountStudent`/`Enrollment` 设计、历史回填、去重合并、事务双写、后台/C 端改造、兼容切换、测试对账和分阶段交付步骤。

## 2026-07-31（独立学员档案文档复核）

- 对照当前 `schema.prisma`、`mvp.controller.ts`、`mvp.service.ts` 复核总结文档；确认当前学员管理仍是订单 `participants` 展开结果，不是独立学员表。
- 补充文档需要明确的实现落点：`CreateOrderDto` 兼容增加 `studentId`、订单模板版本快照、迁移幂等键、手机号冲突人工处理、事务双写、后台分页/权限和回滚验收。
- 已更新 `Docs/Summary/用户管理_学员管理_报名信息存储关系分析.md`：修正当前 `User` 字段与微信登录表述，并新增“落实到当前代码的文件与接口”“数据一致性、验收与回滚标准”两节。
- 已校验文档章节、JSON 示例和 Mermaid 关系图均能以 UTF-8 正常读取；本次只修改 Markdown 与规划记录，没有改动业务代码或数据库。

## 2026-07-31（持续目标完成度审计）

- 对照目标逐项核验：C 端导航已改为“订单”；“我的”页已有个人资料、账号与安全、密码修改、微信一键登录入口；课程图片已贯通管理端上传、后端媒体读取、首页卡片/轮播和详情 Banner；学员列表已将姓名、手机号、课程前置；报名汇总已增加课程人数图表、查看详情和已支付/未支付分区。
- 运行时发现 3100 端口原先是旧后端实例，`/auth/wechat-login` 返回 404；已停止占用 3100 的旧实例并按当前代码重启，路由日志确认新接口已注册。
- 修复 H5 本地微信登录联调：微信容器不可用时仅在开发构建回退到稳定设备标识，生产构建仍提示使用微信客户端。
- 修复报名汇总统计口径：汇总的未支付人数包含所有非“已取消”、非“已支付”的其他状态；点击详情时排除已取消订单，避免详情人数与汇总人数不一致。
- 验证通过：API/管理端/C 端构建、后端 4 个测试套件（10 个测试）、管理端 UI 静态验收、HTTP 登录/profile/wechat-login/courses/enrollment-summary、管理端和 C 端开发服务器响应。
- 记录的验证限制：课程图片 multipart 上传测试被执行审批层拒绝，未重复尝试；浏览器自动化访问本地地址被当前浏览器安全策略拒绝，因此改用 HTTP、构建和源代码静态检查完成验收。

## 2026-07-31（报名汇总视觉优化与重复修复）

- 将 `EnrollmentSummaryChart` 改为按已支付、未支付、其他状态分段的彩色进度条；总报名人数仍以轨道长度体现。
- 增加鼠标悬停和键盘聚焦提示，展示课程、总报名、各状态人数及百分比；零报名课程不再显示误导性的彩色短条，并补充窄屏布局。
- 定位截图中的重复课程根因：模块切换时旧的 `/admin/enrollments` 请求晚返回，覆盖了当前 `/admin/enrollment-summary` 数据。通过 `loadVersion` 请求版本守卫、切换模块清空旧数据并调整 effect 顺序修复。
- 管理端构建、UI 静态验收和 `git diff --check` 已通过。

## 2026-07-31（依据新截图调整分页布局）

- 将报名汇总页的列表分页移到上方数据表格紧下方，避免分页按钮被误认为控制下方图表。
- 为汇总图表增加独立分页（每页 5 门课程），图表分页与上方列表分页互不影响；图表卡片底部显示当前页和总页数。
- 已通过管理端 TypeScript/Vite 构建、UI 静态验收和 `git diff --check`。

## 2026-07-31（学员域重构与真实联动基线）

- 已使用 artifact-tool 更新 `Docs/FeatureList/培训管理系统功能清单.xlsx`：标准功能 105 项、原子功能 402 项；已实现标准功能 22 项、已实现原子功能 214 项。
- 已清除 `WEB-STU-001` 的误导性“已实现”标记，并新增 C 端已有学员/我的学员、管理端学员档案/账号关系/报名履历/迁移对账/权限脱敏，以及服务端数据模型、事务双写、Mock 脱离和首版验收功能。
- 已生成现状总结：`Docs/Summary/2026-07-31_学员域重构与真实联动项目现状总结.md`。
- 已生成后续任务规划：`Docs/Plans/2026-07-31_脱离Mock的前后端数据库联动任务规划.md`，分为 P0～P9，并为每阶段绑定 FeatureList 编号、完成标准、测试命令和同步要求。
- 已保存根目录副本：`project_baseline.md`、`implementation_plan.md`。
- Excel 校验通过：工作表、标准/原子新增行、产品统计、公式错误扫描均正常；已渲染并检查使用说明、标准功能、详细原子功能和模块统计预览。
- 本轮只完成基线、清单和规划文档，没有提前实现 `Student`/`AccountStudent`/`Enrollment`，后续开发应从规划 P1 开始，并遵守迁移、双写、对账和回滚门禁。

## 2026-07-31（学员域数据库设计方案）

- 已基于现状总结、学员关系分析和 P0–P9 规划生成 `Docs/Plans/2026-07-31_学员域数据库设计方案.md`。
- 方案覆盖目标逻辑模型、`Student`、`AccountStudent`、`Enrollment`、模板版本、订单兼容字段、合并审计、迁移批次/异常记录、索引约束、事务数据流、权限脱敏和发布验收。
- 已保存根目录副本 `database_design.md`，两份文件 SHA256 一致。
- 本次仍未修改 Prisma schema；下一步按规划 P1 实施前需先完成数据库备份和业务口径确认。

## 2026-07-31（P1 学员域数据模型实施）

- 已备份当前 SQLite 主库：`backend/api/data/training.pre-p1-20260731.db`。
- 已新增 Prisma 模型：`Student`、`AccountStudent`、`RegistrationTemplateVersion`、`Enrollment`，并补充 User/Course/Order/Template 关系。
- 已新增 `backend/api/prisma/migrations/0002_student_domain/migration.sql`，迁移器按 0001 → 0002 执行，SQLite `user_version` 更新为 5。
- FeatureList 已同步将 `WEB-DATA-001` 及 3 条原子功能标记为已实现；当前清单统计为 23 条标准功能、217 条原子功能已实现。
- 已在测试库和主库执行迁移；四张新表和索引存在且为空，主库原有 20 条 `Order.participants` 快照保留，未执行历史回填。
- 已增加迁移回归断言；后端 4 个测试套件、10 项测试和后端构建通过，Prisma schema validate 通过。
- `prisma generate` 仍受 Windows 查询引擎文件锁的 `EPERM rename` 影响；已用当前生成客户端成功查询四个新模型，环境问题已记录，未影响 schema validate、迁移和当前后端测试/构建。

## 2026-07-31（P2 历史快照回填与对账完成）

- 新增 `0003_student_backfill_ops` 迁移：`StudentMigrationBatch` 记录批次/游标/统计，`StudentMigrationIssue` 记录逐订单逐报名人异常；SQLite `user_version=6`。
- 新增 `backend/api/prisma/backfill-student-domain.js`，支持 dry-run、批次 ID、数据库文件参数、模板版本快照、手机号规范化、断点游标和确定性 ID 幂等。
- 副本库通过 dry-run、正式回填、坏 JSON/手机号姓名冲突异常和重复运行验证；异常不会静默丢失或错误合并。
- 主库回填完成：20 个订单、21 个报名人、21 个 Student、21 个 AccountStudent、21 个 Enrollment，异常 0、订单/课程对账差异 0；同批次复跑新增 0。
- FeatureList 已更新：`WEB-STU-007`、`WEB-DATA-003` 及 7 条原子功能打勾，统计为 25/105 标准、224/402 原子。
- 测试通过：`backend/api/test/backfill.spec.ts`；下一阶段 P3 开始改造下单 DTO 和事务双写。

## 2026-07-31（P3 下单事务双写完成）

- `CreateOrderDto` 支持每个报名人的可选 `studentId`，旧版 `participants[].data` 请求保持兼容。
- `MvpService.createOrder()` 已在同一 Prisma transaction 内写入订单快照、模板版本、Student、AccountStudent、Enrollment 和课程人数；确定性 ID/唯一键避免重复履历。
- 已有学员复用要求当前账号存在 active 关系；越权、冲突或容量失败会整体回滚，不留下订单半成品。
- 取消/退款同步关联 Enrollment 为 `cancelled`；订单快照保留原始表单数据。
- FeatureList 已同步 `WEB-DATA-002` 和 4 条原子功能，统计为 26/105 标准、228/402 原子。
- 后端全量测试 5 套件、12 项通过，Nest 构建通过；下一阶段进入 P4 学员 API、权限、合并和审计。

## 2026-07-31（P4 学员 API 与权限完成）

- 新增 `/admin/student-profiles` 学员档案分页、详情、编辑、启用/停用和脱敏导出接口；旧 `/admin/students` 订单快照接口保留。
- 新增账号关系授权、解除、默认报名人维护；新增跨课程 Enrollment 履历查询、手机号/姓名/公司匹配候选和软合并接口。
- 合并事务检测重复履历，关系/履历迁移和源档案 `mergedIntoId/status=merged` 同步提交；冲突时整体回滚。
- 列表/导出默认脱敏敏感字段，关键查看/编辑/授权/解除/合并/导出动作写入 AuditLog。
- FeatureList 已更新：`WEB-STU-003`～`WEB-STU-008` 和 21 条原子功能完成；统计为 32/105 标准、245/402 原子。
- `backend/api/test/student-api.spec.ts` 及后端全量 5 套件、12 项测试和 Nest 构建通过；下一阶段 P5 开始管理端页面拆分。

## 2026-07-31（P5 管理端档案/报名明细拆分完成）

- 管理端学员管理切换到 `/admin/student-profiles`；报名明细切换到 `/admin/enrollment-records`，分别对应 Student 和 Enrollment 数据源。
- 学员档案详情展示授权账号、状态和履历统计；报名履历详情展示模板 ID/版本、订单支付状态、责任账号和 `formPayload` 快照。
- 旧 `/admin/students`、`/admin/enrollments` 接口保留，汇总页面继续兼容；管理端加载版本守卫未移除。
- FeatureList 已更新：`WEB-ENR-002`、`WEB-ENR-003` 和 8 条原子功能完成；统计为 34/105 标准、253/402 原子。
- 管理端 Vite 构建和新报名明细查询测试通过；下一阶段进入 P6 C 端“我的学员”和报名选择。

## 2026-07-31（P6 C 端学员体验完成）

- C 端报名页新增账号学员选择：支持默认学员自动回填、已有学员复用、临时填写和逐报名人切换；提交会携带可选 `studentId`。
- C 端“我的”页新增独立学员管理区：支持新增、编辑、设置默认报名人和解除关系，解除不删除历史报名。
- 修复后端控制器重复方法名 `students`，管理端兼容接口改名为 `adminStudents`，恢复 TypeScript 编译与生产化回归。
- FeatureList 已更新：`MP-ENR-007`、`MP-ME-007` 及 7 条原子功能完成；统计为 36/105 标准、260/402 原子。
- 验证通过：`pnpm.cmd --dir frontend/client-uni build:h5`、`pnpm.cmd --dir frontend/admin-react build`、`pnpm.cmd --dir backend/api build`、`pnpm.cmd --dir backend/api test -- --runInBand test/production.spec.ts`。
- 下一阶段 P7：双读对账、切换开关、旧读回退和观察期报告。

## 2026-07-31（P7 双读对账与切换完成）

- 新增 `/admin/student-domain/reconciliation`，核对旧订单快照、Enrollment 数量/取消状态、课程有效报名数和已支付订单履历。
- 新增 `/admin/student-domain/read-mode`，默认 `legacy`；切换 `new` 前执行无差异门禁，`legacy` 可回退且不改数据。
- `/admin/enrollments`、`/admin/students` 兼容接口支持按模式选择旧读/新读。
- FeatureList 已更新：`WEB-DATA-004` 和 3 条原子功能完成；统计为 37/105 标准、263/402 原子。
- `student-api.spec.ts` P7 测试 2/2、Nest 构建通过。
- 下一阶段 P8：Mock 脱离、环境变量模板和微信/支付 adapter 门禁。

## 2026-07-31（P8 Mock 边界与渠道 adapter 完成）

- 删除未引用 `frontend/client-uni/src/common/mock.ts`；业务页面扫描不再发现静态 demo 数据依赖。
- 新增 `backend/api/src/channel-adapters.ts`，微信登录生产必须 real 配置，支付未配置只返回 `ready=false` 的明确禁用结果。
- 新增三端 `.env.example`，统一 API、数据库、JWT、上传目录和渠道配置。
- FeatureList 已更新：`WEB-DATA-005` 和 4 条原子功能完成；统计为 38/105 标准、267/402 原子。
- 测试/构建通过：adapter 2/2、生产化 API 5/5、Nest、管理端 Vite、C 端 H5。
- 下一阶段 P9：端到端回归、发布清单、备份和回滚演练。

## 2026-07-31（P9 首版回归、发布与回滚完成）

- 全量后端测试 7 套件/16 项通过；生产化 API、学员域、adapter、迁移和回填均覆盖。
- Nest、管理端 Vite、C 端 H5、Prisma schema validate、mock 扫描和 `git diff --check` 全部通过。
- 发现并修正 SQLite WAL 直接复制备份缺失问题，改用 checkpoint + `VACUUM INTO` 生成 `training.pre-p9-20260731-wal-safe-2.db`，版本 6、数据量 20/21/21/21 一致。
- 发布与回滚证据清单：`Docs/Summary/2026-07-31_P9_首版发布与回滚验收清单.md`。
- FeatureList 已更新：`WEB-DATA-006` 和 3 条原子功能完成；统计为 39/105 标准、270/402 原子。
- P0～P9 规划项全部完成；上线前仍需部署方提供真实微信/支付签名服务配置。

## 2026-08-03 C 端独立“我的学员”页面优化启动

- 已确认报名模板自动回填范围与其他字段手动填写边界。
- 已确认现有学员 API 和账号关系数据可复用，无需新增数据库表或破坏 `Order.participants`。
- 已创建本阶段计划；待实现独立页面、入口位置、当前学员匹配和姓名首字母分组。

## 2026-08-03 C 端独立“我的学员”页面优化完成

- 新增 `frontend/client-uni/src/pages/students/students.vue` 和页面路由。
- “我的”页将“我的学员”入口放到“账号与安全”下方、“我的积分”上方，移除原内嵌学员列表及其重复维护弹窗。
- 页面按显式默认、账号手机号/姓名/企业匹配当前学员；无匹配时展示空状态；其他 active 学员按中文拼音首字母分组。
- 报名页原有字段回填逻辑保持不变：常用字段自动回填，模板其他字段继续手动填写。
- 验证通过：`pnpm.cmd run build:client`、`pnpm.cmd run verify`、`git diff --check`；运行态 `/students` 返回 15 个 active 学员关系。

## 2026-08-03 单机服务器部署方案

- 已核对生产入口、构建产物和端口：API `3100`、管理端 `dist`、C 端 H5 `dist/build/h5`。
- 已确认当前 H5/管理端资源使用绝对 `/assets`，部署方案采用两个域名/独立 Nginx server 块，避免 `/admin`、`/h5` 子路径资源冲突。
- 已新增 `Docs/Summary/2026-08-03_单机服务器部署指南.md` 和 `Docs/Plans/2026-08-03_单机部署实施方案.md`。
- 已记录 SQLite 单实例约束、WAL-safe 备份、systemd、Nginx、首次验收和 P9 回滚门禁；未在未授权情况下连接或修改远程服务器。

## 2026-08-03 C 端登录页简化

- 登录页字段文案由“演示账号”改为“账号”，账号和密码初始值均为空，取消自动填充。
- 删除微信一键登录按钮、快捷账号入口和“MVP 演示密码统一为 123456”提示；同步清理“账号与安全”中的微信绑定提示。
- 登录页改为简洁卡片布局，保留账号密码登录、密码显示/隐藏和原有登录接口。
- `pnpm.cmd --dir frontend/client-uni build:h5`、`pnpm.cmd run verify`、`git diff --check` 通过；本地 H5 视觉复核确认输入框为空且上述文案已从构建产物移除。

## 2026-08-03 注册/找回密码与生产化准备

- 后端新增 `POST /api/auth/register`、`/api/auth/password-reset/request`、`/api/auth/password-reset/confirm`，验证码按哈希保存，支持过期、错误次数限制、一次性使用和旧刷新令牌吊销。
- C 端登录页新增“立即注册”“忘记密码”入口，并新增注册页、找回密码页；开发环境可显示 `devCode`，生产环境不返回验证码。
- 新增 `PasswordResetChallenge` Prisma 模型、`0004_password_reset` SQLite 迁移和密码找回 webhook/fake/disabled 配置模板。
- 新增 `backend/api/test/auth-registration-reset.spec.ts`，注册、重复账号、密码找回、挑战复用和未知账号不泄漏测试 3/3 通过；后端构建通过。
- Prisma Client 重新生成受 Windows 查询引擎文件被运行中 Node 进程占用影响（EPERM），现有生成客户端已含新模型，待停止占用 3100 的旧服务后再执行 `db:generate` 作为环境清理步骤。
- 当前生产化仍需真实微信登录场景、商户支付渠道、短信/邮件找回渠道和目标 PostgreSQL/MySQL 实例信息；个人微信/支付宝收款码不能替代商户支付接口。
- 最终验证：`pnpm.cmd run build:client`、管理端构建、根目录 `verify` 和 `git diff --check` 通过；C 端仅有既存 Sass 弃用警告。
- 全量后端 Jest 在 5 分钟内无输出并超时，未收到断言失败；新增注册/找回密码专项测试已独立 3/3 通过。由于 3100 仍有运行中的 Node 服务，未擅自停止服务重跑 Prisma generate。
- 内测数据库已执行 `pnpm.cmd --dir backend/api db:migrate`，`user_version=7` 且 `PasswordResetChallenge` 表存在；随后仅重启占用 3100 的旧 API 实例，新 `/api/auth/password-reset/request` 已返回 201，5174/5185 前端服务未中断。

## 2026-08-04 注册/找回密码完善与真实支付安全门禁

- 新增 `PaymentTransaction` 模型和 `0005_payment_transactions` 迁移，内测库升级到 `user_version=8`；重新生成 Prisma Client 成功。
- 在线支付不再允许客户端调用 `/orders/:id/pay` 直接改为已支付；新增支付状态查询、微信/支付宝回调入口、订单号/金额校验、回调验签后幂等入账。
- 补充微信支付 v3 H5/Native 下单签名、支付宝 WAP 下单签名、微信 AES-GCM 回调解密和支付宝 RSA2 回调验签代码；未配置真实商户参数时仍明确返回不可用。
- C 端报名页和订单页改为发起支付意图、跳转/调用原生支付并轮询服务端状态，不再显示伪造二维码或依据前端点击宣称支付成功。
- 新增 `payment-integrity.spec.ts`；注册/找回密码、支付安全门禁专项测试 4/4，通过生产化 API 5/5，后端/C 端/管理端构建和 `verify` 通过。
- 真实渠道仍需用户在服务器环境变量注入商户号、AppID、证书、私钥、公钥、HTTPS 回调地址；个人收款码仍只用于线下凭证审核。
## 2026-08-04 项目现状核验与后续执行清单

- 已核验仓库代码、Docs/Summary、Docs/Plans、Prisma schema 和 SQLite 实例，结论写入 `Docs/Summary/2026-08-04_项目现状核验与内测到生产执行清单.md`。
- 只读查询确认 SQLite `user_version=8`；User=12、Course=6、Order=21、Student=22、AccountStudent=22、Enrollment=22，PaymentTransaction=0、PasswordResetChallenge=0。
- 明确区分“代码已实现”和“真实业务未实测”：学员档案 P1-P9 需要业务场景闭环；微信/支付宝需要商户渠道和 HTTPS 回调，个人收款码仅适用于线下凭证审核。
- 当前优先级确定为：P10 学员档案场景验收 → P11 PostgreSQL 测试迁移 → P12 支付沙箱/小额回调验收 → P13 生产切换。
## 2026-08-04 PostgreSQL 配置边界修复

- 用户确认目标为 PostgreSQL、H5+小程序、内部测试验证码；服务器为内网地址，尚无域名。
- 发现并修复 `PrismaService` 将 PostgreSQL/MySQL `DATABASE_URL` 当作 SQLite 文件路径的缺口。
- 为 SQLite migrate/seed 增加 PostgreSQL/MySQL URL 保护，避免误执行 SQLite 流程。
- 新增 `backend/api/prisma/prepare-postgresql.cjs`、`export-postgresql.cjs`、`import-postgresql.cjs` 及对应 npm scripts，用于生成 PostgreSQL schema/migration、导出 SQLite 快照和导入对账。
- 新增 `Docs/Summary/2026-08-04_内测环境配置与数据库实例说明.md`，说明数据库实例字段、内网无域名对支付回调的限制以及用户仍需提供的信息。
## 2026-08-04 微信小程序 JSAPI 支付补齐

- 发现用户已选择小程序，但原支付适配器只有微信 H5/Native；新增 JSAPI 下单、`openid` 读取和小程序 `requestPayment` 参数签名。
- 微信登录接口支持 `mini_program`、`h5`、`official_account` 场景；默认小程序 `code2Session`，H5/公众号走网页 OAuth。
- 新增 `backend/api/test/payment-jsapi.spec.ts`；API 构建、注册/找回密码和支付专项测试通过。
## 2026-08-04 完成度审计与回归

- 后端全量 Jest：11 个测试套件、25 项通过。
- API 构建、C 端 H5 构建、管理端构建、根目录 `verify` 和 `git diff --check` 通过；仅保留 Sass/Vite 弃用提示。
- 本轮仍未执行远程服务器写入、PostgreSQL 安装或真实商户支付，因为服务器系统/数据库实例/公网回调信息尚未确认。
## 2026-08-04 SQLite → PostgreSQL 导出导入工具

- 新增只读 `export-postgresql.cjs`：导出 `user_version`、24 张业务表、行数和 SHA-256；已在当前 SQLite 上实际验证，导出数据包含 User=12、Order=21、Student=22、Enrollment=22 等计数。
- 新增 `import-postgresql.cjs`：要求 PostgreSQL `DATABASE_URL` 和已生成 PostgreSQL Client，按外键依赖顺序导入并回报源/目标行数。
- 发现 `.mjs` 与 CommonJS `require` 冲突，已统一改为 `.cjs` 并通过 Node 语法检查和 SQLite 导出验证。
- 当前未执行 PostgreSQL 导入，因为服务器实例和连接信息尚未确认，避免误连或覆盖未知数据库。
- 导入安全策略已固定：刷新令牌和找回密码挑战不迁移，切库后强制重新登录。
## 2026-08-04 服务器 SSH 探测

- `192.168.0.162:22` 可达，但无 SSH 公钥认证，未尝试密码登录，也未执行远程写入。
- 新增 `Docs/runtime/check-postgres-server.sh`，用于服务器本地输出非敏感的系统、Node、PostgreSQL、服务和端口信息。

## 2026-08-04 服务器基础信息回传

- 收到服务器终端信息：Ubuntu 24.04 系列、Node.js v22.22.1；`psql` 客户端未安装。
- 已更新 PostgreSQL 内测环境说明、任务计划和发现记录。
- 当前仍未执行远程安装或写入；等待 PostgreSQL 服务状态、监听端口和数据库列表的非敏感输出。

## 2026-08-04 个人收款码线下支付闭环

- 管理端支付设置新增微信/支付宝收款码上传。
- C 端报名账单和订单支付记录弹窗展示个人收款码，并保留凭证上传与管理端审核状态机。
- 后端新增收款码图片安全存储、公开读取接口和服务层测试；API、管理端、C 端构建通过，专项测试通过。
- 在线微信/支付宝仍需商户号、应用密钥、证书和公网 HTTPS 回调，个人收款码不能替代。

## 2026-08-04 渠道配置自检

- 新增管理员只读接口 `/api/admin/integration-readiness`，用于服务器注入密钥后检查缺项、HTTPS 回调和找回密码渠道状态。
- 自检不返回密钥值；生产模式下 fake/disabled 适配器不会被标记为可上线。
- 管理端新增“渠道自检”页面，展示同一自检结果；管理端构建通过。

## 2026-08-04 PostgreSQL 初始化脚本

- 新增 Ubuntu 24.04 PostgreSQL 初始化脚本，支持安装服务、创建 `training_app`/`training_management`、交互式设置密码和输出无密码连接模板。
- 脚本未在当前 Windows 环境远程执行；需要用户在 `192.168.0.162` 服务器本地运行并回传非敏感检查结果。
- 新增带确认门槛的 SQLite → PostgreSQL 测试迁移脚本；尚未执行真实迁移，避免在未知实例或未确认维护窗口下写入。
## 2026-08-04 本地到服务器部署手册

- 已核对旧单机部署文档、Plans、PostgreSQL 初始化/迁移/自检脚本、package scripts、API 环境变量和支付回调路由。
- 已确认下一份文档需要覆盖：本地发布、Windows 上传、服务器初始化、PostgreSQL 实例、迁移对账、systemd、Nginx、无域名限制、真实支付配置、P10-P13 顺序、回滚和用户回传清单。
- 已写入 `Docs/Summary/2026-08-04_本地到服务器部署及PostgreSQL与真实支付配置实施手册.md`，并完成 UTF-8、敏感信息、旧 `rsync`/图片说明和 `git diff --check` 检查。
- 根据用户服务器截图补充了 `/opt/training-management` 直接目录部署、缺少 `Docs/runtime` 时的补传步骤，以及 JWT_SECRET 暴露后的轮换提醒。
- 发现的部署错误：手册中的 `/opt/training-management/current` 是推荐版本软链接路径，不是用户当前服务器已经存在的目录；已记录并修正文档。
- 初步评估 Docker：当前项目尚无 Docker 配置且未使用 Redis；建议采用 Compose 容器化 API/PostgreSQL/Nginx，Redis 暂不加入，数据库和上传文件必须使用持久化卷。
- 已按用户确认落地 Docker Compose：PostgreSQL 16、API migration、NestJS API、管理端和 H5；新增 `Dockerfile.api`、`Dockerfile.web`、`docker-compose.yml`、Nginx 配置、`.dockerignore`、`.env.docker.example` 和 Docker 部署方案。
- 已更新 README Docker 快速入口；Compose 静态配置和 `pnpm.cmd run verify` 通过。本地 Docker daemon 未运行，未伪造镜像构建通过结论，需在服务器执行真实 build。
- 用户服务器执行 GitHub 更新时遇到 `detected dubious ownership`；已确认是 root 与 training 文件所有者不一致，已在 Docker 部署方案中补充安全处理步骤。
- 用户按 training 用户执行 Git 状态检查成功，权限问题已解除；发现 `frontend/admin-react/tsconfig.tsbuildinfo` 被本地构建修改，尚未擅自恢复。
- 已向用户说明 Git 无法判断未提交修改来自人工还是构建工具；建议查看 `git diff --word-diff` 后再恢复该生成缓存文件。
- 用户实际 diff 显示为 TypeScript 增量缓存的单行 root 路径变化，执行 restore 后该修改消失；检查过程生成了未跟踪 `.lesshst`，已说明清理方法。
- 用户服务器已触发 GitHub SSH 主机指纹确认；待确认后检查 `origin` SSH URL 和 training 用户的 GitHub deploy key。
- 用户检查 `/opt/training-management/.ssh/id_ed25519.pub` 时文件不存在；下一步需确认现有 `.ssh` 文件或为 training 创建 deploy key，再重新确认 GitHub 主机指纹。
- 用户已生成 `id_ed25519`/`id_ed25519.pub`，已说明复制 `.pub` 单行公钥到 GitHub Deploy keys，私钥文件不得上传。
- 用户确认 GitHub 账号已存在一把 `Company-Server` SSH Key；下一步改为核对指纹，必要时通过同一页面的 `New SSH key` 添加当前服务器公钥，不再要求用户寻找 Deploy keys。
- 已确认用户疑问的关键点：Linux root/training 各自拥有独立 `~/.ssh`，GitHub 账号中的旧 key 与 training 新生成 key 不一致时，必须添加 training 的 `.pub`，不能复用或复制 root 私钥。
- 用户完成 root/training 指纹对比，确认 GitHub 已登记的是 root key；已明确下一步为新增 training 公钥到 GitHub，保持 Git 操作与仓库所有者一致。
- 用户已完成 Docker 相关提交的 GitHub 拉取；当前进入服务器 Docker 安装、`.env.docker` 配置、PostgreSQL 容器启动和数据迁移阶段。
- 发现服务器 SSH 密钥位于项目根目录导致 `?? .ssh/`；已补充本地 `.gitignore`/`.dockerignore` 防护，并要求服务器先将密钥移出仓库再构建镜像。
- 用户已完成 SSH 文件迁移；`env: git: 权限不够` 是命令换行/反斜杠用法问题，下一步用不带反斜杠的单行 Git/SSH 命令验证。
- 当前阻塞仍在服务器侧：等待用户执行 PostgreSQL 初始化并回传非敏感自检结果。

## 2026-08-06 管理端上线前完善（支付暂缓）

- 复核 `frontend/admin-react/src/App.tsx` 与 `backend/api/src/mvp/mvp.controller.ts`：订单、开票、用户、学员档案等后端接口均已存在，主要问题是前端操作直接执行、缺少确认/统一异常反馈/重复提交保护，学员档案详情缺少状态与关系维护入口。
- 管理端订单操作已调整：已支付订单退款增加二次确认；待审核订单继续进入支付凭证审核弹窗；其他状态给出明确提示。
- 管理端开票操作已调整：仅待处理申请可操作，通过时要求人工输入真实发票号码，不再生成 `MOCK-*` 发票号；驳回增加确认。
- 管理端用户操作已调整：启用/禁用和密码重置增加确认、错误提示和操作锁；重置成功会显示后端返回的临时密码。
- 管理端学员档案详情已新增：编辑姓名/手机号/邮箱/公司/部门/职务、启用/停用档案、设置默认账号、解除账号关系；操作后刷新详情、列表并保留后端审计。
- 验证通过：管理端构建、根目录 `pnpm.cmd run verify`、后端 Jest 11 套件/25 项测试、管理端静态 UI 验收、`git diff --check`。
- 本阶段仍明确暂缓真实微信/支付宝支付，不以商户配置作为本阶段完成条件；线下凭证审核继续保留。
- 下一步：增加管理端按钮/API 对照验收，补齐学员关系授权和报名履历查看入口，随后执行浏览器逐模块回归并更新 `Docs/Summary`。
- 本地运行态已启动 API `3100` 与管理端 `5174`；HTTP 登录、工作台、订单、开票、学员档案、用户、反馈、模板和报名汇总接口均返回成功。
- 浏览器控制层拒绝接管 `localhost:5174`，原因是本机 URL 安全策略；未使用绕过方式，已改用本地 HTTP 运行态和静态验收作为当前证据，浏览器逐项验收仍待用户可访问的浏览器环境。
- 尝试提交本阶段变更时，沙箱无法写入 `.git/index.lock`（`.git` 目录只读）；未修改 Git 权限，代码与留痕文件仍保留在工作区，需用户在正常 Git 终端提交/推送。
- 最新回归：后端 12 套件/29 项测试通过；管理端构建通过；根目录 verify、管理端静态 UI 验收和 `git diff --check` 通过。
- 学员档案详情继续完善：打开详情时同时读取报名履历；新增授权账号入口，并保留默认账号/解除关系操作，前端管理端构建再次通过。
- 管理端登录页已移除账号/密码自动填充、默认密码文案，并改为表单提交支持回车登录；开发种子数据未修改，仅保留给测试环境使用。
- 扩展 `frontend/admin-react/scripts/verify-ui.mjs`：静态检查订单、开票、用户、学员档案按钮对应 API、请求锁、随机临时密码入口和默认密码残留；脚本通过。
- 新增 `backend/api/test/p10-scenarios.spec.ts`，覆盖一个账号替多个学员报名、切换默认报名人、模板必填字段失败时事务不留半成品；P10 专项 2/2 通过。
- 消息通知边界已收紧：当前只允许保存站内消息；短信/邮件在前端标记“待接入”，后端拒绝将预留渠道当作已发送，并新增回归测试。
- 修复学员详情刷新边界：编辑、授权、默认账号和解除关系后继续保留报名履历；编辑接口失败时保留编辑弹窗，不误报成功。
- 报名明细列表的动作文案由“处理”改为“查看详情”，移除不会执行任何业务变更的“处理入口”提示，避免管理端误以为该按钮能直接处理报名。

## 2026-08-06 上线前非支付功能审计与补齐（本轮）

- 完成当前代码基线审计，确认根目录 verify、管理端构建、管理端 UI 静态验收、后端全量测试均通过；后端 12 套件/30 项通过。
- 修复管理端审计日志无效“处理”按钮：审计列表现在为只读，不再触发无意义操作。
- 修复报名明细和学员列表重复“查看详情”按钮：按详情入口/操作入口划分，保留单一入口。
- 修复订单与开票的状态误操作：订单仅“待审核/已支付”显示审核或退款；开票仅“待处理”显示通过/驳回。
- 课程、Banner 删除加入二次确认；避免误删后才依赖后端拒绝。
- 接入现有学员档案合并 API：管理端详情可输入目标档案 ID，确认后迁移履历并关闭源档案，成功后刷新/关闭详情。
- 后端新增课程报名状态/截止时间/名额门禁，课程视图返回 `registrationOpen` 与关闭原因；新增回归测试覆盖已下架、已截止、正常开放三种情况。
- 新增静态验收断言，防止后续重新引入无效按钮、重复详情、状态误操作和无确认删除。
- 当前尚未做浏览器逐模块人工验收；本机此前浏览器控制层拒绝接管 localhost，后续需要在可访问浏览器环境验证视觉与点击链路。
- 完成最新代码构建和运行态核验：API build、管理端 build、C 端 H5 build、根目录 verify、管理端静态验收和 `git diff --check` 均通过。
- 在本机临时端口 3101 验证新编译 API 返回 `registrationOpen`/关闭原因，并在 3100 恢复运行最新 API；管理员登录后工作台、课程、学员档案、渠道自检、审计列表均返回 HTTP 200。
- C 端 H5 构建耗时约 180 秒，但最终 `DONE Build complete`；仅有 Sass legacy/import 弃用警告，没有构建错误。
- 本轮发现旧开发进程重启时出现一次 `EADDRINUSE`，已停止旧监听进程并用独立日志确认 3100 最新 API 正常监听；未改动服务器服务。

## 2026-08-06 P1 报名模板复制与安全删除

- 后端新增 `DELETE /api/admin/templates/:id` 和 `MvpService.removeTemplate`。
- 删除规则：模板被课程引用时拒绝；模板已经产生报名历史版本时拒绝；只有未使用且无历史版本的模板允许删除，并写入审计日志。
- 管理端模板编辑弹窗新增“另存为副本”和“删除模板”，复制通过已有保存接口创建新模板，不覆盖原模板。
- 静态 UI 验收新增模板复制/删除入口断言。
- 验证结果：后端 12 套件/31 项通过；管理端构建和 `test:ui` 通过。
- 2026-08-06：启动“可上线非支付版本”续作。重新核对工作区、管理端模块、控制器路由、Prisma 模型和已有验证证据；确认基础按钮闭环已大部分接入，下一批重点是优惠规则生命周期、积分流水、消息范围/阅读、审计筛选差异、学员导出脱敏和发票文件状态预留。
- 2026-08-06：完成 P1-1 优惠规则生命周期增强。新增同人数门槛/重叠课程范围冲突提示，新增停用/启用入口、删除接口和二次确认；所有操作写审计并使用统一请求锁。新增后端回归覆盖冲突、停用和删除；API 构建、管理端构建、UI 静态验收、根目录 verify、专项测试和 `git diff --check` 均通过。
- 2026-08-06：完成 P1-2 积分流水查询。新增 `GET /admin/points/:userId/ledger` 分页接口和管理端“查看流水”入口，展示变更时间、正负积分、原因及流水编号；补充回归测试并通过 API/管理端构建、UI 静态验收、根目录 verify 和 diff 检查。
- 2026-08-06：完成 P1-3 审计筛选增强。审计资源支持操作者、关键字、开始/结束时间和操作类型组合筛选；积分调整审计详情补充变更原因。新增回归覆盖操作类型+关键字筛选；后端 12/12 专项测试、API/管理端构建及 UI 静态验收通过。
- 2026-08-06：本轮阶段基线回归曾通过后端全量 12 套件/34 项；加入消息、导出、发票和 HTTP 回归后，最终后端全量为 12 套件/36 项，API build、管理端 build、管理端 `test:ui`、根目录 `verify` 和 `git diff --check` 均通过。真实浏览器逐模块验收、C 端消息展示入口、电子发票文件上传、Docker PostgreSQL 迁移仍未完成。
- 2026-08-06：完成 P1-4 消息目标范围与阅读状态。管理端消息表单支持目标用户/课程 ID、开始/结束展示时间，保存时将逗号/换行文本转换为数组，编辑时回填本地时间；后端校验目标用户/课程存在性，并将订单与未取消报名履历合并用于课程目标匹配。
- 2026-08-06：新增消息场景回归：全量可见、指定用户、指定课程、未来/过期时间过滤、标记已读返回 `readAt`、不可见账号不能标记；当时后端全量 12 套件/35 项通过，随后加入发票/导出/HTTP 回归后最终为 12 套件/36 项，API build、管理端 build、`test:ui` 通过。
- 2026-08-06：`prisma generate` 两次因 Windows 查询引擎 DLL 文件锁返回 EPERM；现有生成客户端已确认包含 `MessageRead`，未强杀未知 Node 进程，问题留待维护窗口处理。
- 2026-08-06：P1-5 首次导出回归因测试夹具缺少 `course-2` 模板必填的企业规模字段失败；已记录并补齐夹具，未放宽正式报名校验。
- 2026-08-06：完成 P1-5 学员导出收紧：导出按关键字/状态筛选，最多 1000 条并显式返回截断标记；默认手机号、邮箱脱敏，审计记录包含脱敏/截断信息，管理端默认只提供脱敏导出。
- 2026-08-06：完成 P1-6 发票文件状态预留：申请创建为“未生成”，开票通过为“待上传”，驳回为“不适用”；后端增加待处理状态门禁和发票号必填，管理端展示发票文件状态。专项测试 16/16、API/管理端构建和 UI 静态验收通过。
- 2026-08-06：P2 HTTP 回归增强：生产 API 套件新增消息创建/阅读、学员筛选脱敏导出、发票文件状态和重复开票门禁断言；生产 API 5/5 通过，普通用户管理接口 403、令牌轮换和重启持久化继续通过。
- 2026-08-06：P3 Docker 准备：Compose 静态配置通过；管理端/H5 宿主端口改为 18080/18081，避开服务器既有 8080/8081 占用；新增 `Docs/runtime/backup-postgres-docker.sh`，生成 custom dump 和 SHA-256 校验。当前本机 Docker daemon 未运行，真实镜像构建、PostgreSQL 导入和恢复演练需在服务器执行。
- 2026-08-06：尝试在 Windows 用 `bash -n` 检查备份脚本时被本机 Bash 服务权限限制，已改以字节编码检查和人工审阅脚本；脚本需在 Ubuntu 服务器执行语法/运行验证。
- 2026-08-06：P2 自动化验收完成：`production.spec.ts` 5/5 通过，后端全量 12 套件/36 项通过；权限、状态门禁、重复开票、消息阅读、脱敏导出和重启持久化均有 HTTP 证据。P2 标记完成，P3 仅剩服务器 Docker/PostgreSQL 实测和恢复演练。

## 2026-08-06：启动可上线非支付版本主线

- 目标重新聚焦为：支付申请期间先完成管理端、C 端和 API 的非支付业务闭环，不把真实在线支付作为当前发布门槛。
- 基线审计确认：管理端大部分按钮已有 API 和状态门禁；新增发现消息缺少显式启停/删除生命周期动作，反馈重复处理会覆盖原回复。
- 已更新 `task_plan.md`、`findings.md`，固定阶段 1（消息生命周期与反馈重复处理门禁）、阶段 2（C 端消息中心/发票文件链路）、阶段 3（浏览器人工验收）、阶段 4（Docker/PostgreSQL 发布验收）。
- 下一步立即实现阶段 1，完成后记录后端测试、管理端构建、静态按钮/API 验收和 diff 检查证据，并同步更新计划状态。

## 2026-08-06：阶段 1 完成——消息与反馈生命周期

- 新增管理端消息启用/停用、删除 API 和 UI 入口；删除会级联清理阅读记录并写入审计。
- 反馈处理增加“仅待处理可回复”状态门禁，重复回复返回明确错误，原回复保持不变。
- 验证：`backend/api` 的 `mvp.spec.ts` 15/15 通过；API build、管理端 build、管理端 `test:ui` 和 `git diff --check` 通过。
- 阶段 1 已标记完成；下一步进入阶段 2，优先补齐 C 端消息展示入口，再处理发票文件上传链路。

## 2026-08-06：阶段 2 前置完成——C 端消息中心

- 新增 C 端“我的消息”页面：展示当前账号可见消息、未读标记、消息详情和打开即标记已读。
- “我的”页新增消息入口及未读数量徽标；H5/小程序共用同一 API 和页面实现。
- 验证：C 端 H5 构建通过；仅有既有 Sass legacy/import 弃用警告，无构建错误。
- 阶段 2 尚未完成，下一步是发票文件上传/下载链路和管理端对应按钮，完成后再做全量回归。

## 2026-08-06：阶段 2 完成——消息中心与发票文件状态链路

- 管理端“开票管理”在记录状态为“已开票/待上传”时显示“上传发票”，接入真实文件上传接口；已上传记录不允许无确认覆盖。
- 后端新增管理员/所属用户发票文件读取路由，限制 PDF/PNG/JPEG 和 10MB 上限，保存文件元数据并审计。
- C 端业务页展示发票文件状态和文件名；消息中心、导出脱敏、审计筛选等非支付能力均已纳入当前代码线。
- 阶段 2 已标记完成；下一步进入阶段 3：管理端/C 端逐模块人工验收，记录真实点击、接口响应、数据库状态和错误提示。
## 2026-08-06：可上线非支付版本续作——修复管理端详情与反馈按钮

- 发现报名明细/学员列表的操作列实际上没有详情按钮：原因是 `detailOnlyModule` 禁用了操作，但 `detailHandler` 同时被置为空。
- 修复 `frontend/admin-react/src/App.tsx`：为报名明细、学员档案传递唯一“查看详情”入口；反馈记录仅待处理状态显示“回复处理”。
- 更新 `frontend/admin-react/scripts/verify-ui.mjs`，增加上述按钮连接和反馈状态门禁断言。
- 验证：管理端 `test:ui` 通过；管理端 build 通过；后端 Jest 12 套件/39 项通过。
- 下一步：在浏览器逐模块点击验收，随后继续处理仍缺少请求锁、详情内容或操作反馈的管理端模块。
- 浏览器验收补充：第一次热更新后发现学员列表出现两个“查看详情”按钮；已恢复 `detailOnlyModule` 的通用操作禁用条件，当前每行只显示一个详情按钮。
- 实际打开学员详情成功，基础资料、授权账号、报名履历、编辑/启停用/合并入口均可见；列表手机号脱敏，详情请求返回明文供受保护编辑使用。
- 开票运行态复核：发现旧 API 进程未加载最新 `invoiceView`，重启本地 3100 后，“发票文件状态”和“上传发票”按钮均正常显示；待处理/已驳回状态门禁符合预期。
- 本轮非支付写操作收紧：模板、Banner、消息、系统配置、积分、反馈和课程删除统一使用 `runOperation` 请求锁；课程保存增加并发门禁，避免重复提交。
- 回归结果：管理端 UI 静态验收、管理端 build、根目录 verify、后端 12 套件/39 项测试和 `git diff --check` 全部通过。
- 系统配置和操作审计人工验收通过：配置新增/编辑入口可见，审计筛选可用且列表保持只读；管理端关键模块按钮验收继续推进。
- 2026-08-06：阶段 3 浏览器人工验收启动；已接管管理端 `http://localhost:5174/login` 的登录态，操作审计页实际返回 62 条记录并显示操作类型/操作者/起止时间筛选，后续按模块逐项记录点击和接口结果。
- 2026-08-06：人工点击复核遇到控制层限制：课程管理按钮 DOM 可见且无禁用，但语义、DOM、坐标三种点击均未改变页面，控制台无错误；不把该次结果误判为业务缺陷，转用自动化/API 与服务器侧验收，并保留浏览器环境待后续补测。
- 2026-08-06：重新接管管理端标签后完成自动化回归：后端 12 套件/39 项、管理端 `test:ui`、根目录 `verify` 均通过；工作台、课程管理、报名汇总可切换。课程管理运行态显示 6 条课程、状态筛选/导出/新增/编辑入口，新增课程弹窗可打开并关闭，未提交测试数据。
- 2026-08-06：完成管理端运行态模块抽查：报名汇总/明细、订单、开票、学员、用户、优惠、反馈、消息、积分、渠道自检、系统配置和审计均能进入；订单详情、学员档案详情、反馈回复弹窗可打开并关闭，已处理反馈不显示回复按钮。
- 2026-08-06：修复线下支付凭证审核状态门禁，新增重复审核回归；`mvp.spec.ts` 17/17、API build、管理端 build 通过。下一步转入 C 端非支付验收和服务器 Docker/PostgreSQL 迁移/备份恢复。
- 2026-08-06：完成 C 端代码/构建核对：业务页的支付记录、线下凭证双区弹窗、开票记录、消息、我的学员入口均存在并接入 API；H5 构建通过（仅 Sass 弃用警告）。全量后端 12 套件/40 项、管理端静态验收、根目录 verify、diff 检查均通过。

## 2026-08-06：非支付上线收口——密码安全与电子发票访问

- 修改密码接口删除空请求回退 `123456`；控制器将原始请求交给服务层，服务层对缺失、空值和少于 6 位的密码统一返回明确错误。
- 新增回归断言：`undefined`、空字符串和 5 位密码均不能修改密码；测试账号仍可继续使用种子密码登录，不影响测试数据兼容性。
- 电子发票上传后的访问地址改为用户受保护路由 `/api/invoices/:id/file`，不再把管理员专用路径返回给 C 端；管理员仍可通过同一路由读取。
- C 端“开票记录”在文件状态为“已上传”时新增“查看电子发票”入口，下载时携带登录令牌并交由设备打开文档。
- 验证：`mvp.spec.ts` 18/18、生产化 API 5/5、后端全量 12 套件/41 项、管理端 `test:ui`、根目录 `verify`、C 端 H5 构建均通过；仅有既有 Sass 弃用警告。
- 当前阶段仍未完成：C 端真实设备逐点击、服务器 Docker/PostgreSQL 迁移/对账/恢复演练；支付适配器继续保持 `PAYMENT_ADAPTER=disabled`。

## 2026-08-06：管理端发票复核入口补齐

- 已上传电子发票的管理端记录新增“查看发票”按钮，使用管理员鉴权的 Blob 读取接口打开文件；待处理记录仍显示“驳回”，待上传记录仍显示“上传发票”。
- DataTable 操作标签支持按行状态动态显示，避免已上传记录继续出现误导性的上传/处理按钮。
- 管理端静态验收新增该入口断言；管理端构建、`test:ui`、根目录 `verify` 和 `git diff --check` 通过。
- 生产化 HTTP 回归新增管理员上传后普通用户读取电子发票文件的断言，确认权限链路闭环。

## 2026-08-06：非支付上线收口——待支付订单关闭

- 复核功能清单后确认，管理端订单列表缺少“待支付订单手动关闭”能力；前端旧筛选还包含服务端不会产生的“已退款”状态。
- 后端新增 `POST /admin/orders/:id/close`：仅允许订单状态为“待支付”，事务内将订单置为“已取消”、回补课程名额、同步取消报名履历，并写入“关闭待支付订单”审计。
- 重复关闭、已支付订单或待审核订单会被状态门禁拒绝，不会重复回补名额。
- 管理端订单主操作按状态显示“关闭订单/审核凭证/退款”，移除“已退款”筛选项；危险操作增加二次确认并复用请求锁。
- 验证：后端全量 12 套件/42 项通过；生产化 API 覆盖关闭订单接口；管理端 `test:ui`、管理端 build、根目录 `verify` 均通过。
- 下一步：继续形成 C 端人工验收证据，并在服务器完成 Docker/PostgreSQL 迁移、备份恢复和非支付内测发布；支付保持 `PAYMENT_ADAPTER=disabled`。

## 2026-08-06：C 端非支付人工验收补充

- 本地运行态已确认：API `3100`、管理端 `5174`、C 端 H5 `5185` 均在监听。
- 使用开发种子账号 `demo` 进入 C 端后，首页课程列表、课程状态、报名入口和底部导航均正常显示。
- “我的”页实际显示账号资料、积分、个人资料、账号安全、我的消息、我的学员和反馈入口；“我的消息”页面能打开并展示空状态。
- “我的学员”页面实际显示“暂无当前学员”空状态、其他学员数量、按首字母分组以及编辑/设为默认/解除入口，符合当前账号无默认学员时的产品约定。
- “业务/订单”页面实际加载订单、状态筛选、支付记录、开票记录和浏览记录；待支付订单展示微信/支付宝入口、线下凭证入口和取消报名，已支付订单展示开票入口。
- 打开“提交线下支付凭证”弹窗成功：上半部显示对公转账账户名称、开户银行、账号和备注，下半部显示凭证选择、上传说明和提交审核按钮；本次未上传文件、未改变业务数据。
- 当前人工证据仍覆盖 H5 浏览器，不等同于微信小程序真机验收；发票文件打开、消息已读写入和提交/审核写操作需在专用测试数据上继续验证。

## 2026-08-07：非支付可上线总规划与管理端已访问标签页

- 重新汇总当前代码、测试、部署和历史留痕，确认当前发布目标是“非支付内部测试候选版本”；真实在线支付继续不作为本轮门槛。
- 管理端 18 个模块完成静态按钮/API/状态门禁复核。当前剩余重点是运行态请求证据、服务器 Docker/PostgreSQL 迁移、备份恢复和回滚，而不是继续堆叠没有后端能力的按钮。
- 新增管理端已访问标签页：导航到模块时自动记录标签；点击标签切换；关闭非首页标签后自动切换到相邻标签；首页“首页”标签不可关闭。
- 标签使用当前浏览器 `sessionStorage` 保存，刷新会话后仍可恢复；退出登录清理标签，浏览器禁用存储时不影响当前会话使用。
- 更新管理端样式和 `verify-ui.mjs` 静态断言，覆盖高亮、关闭按钮、横向滚动和窄屏适配。
- 验证：管理端 build 通过、管理端 `test:ui` 通过、根目录 `pnpm.cmd run verify` 通过、`git diff --check` 通过。
- 下一步：运行态验证多个标签的访问/切换/关闭/刷新；随后执行服务器 PostgreSQL 测试迁移、数据对账、备份恢复和非支付内测发布检查。

### 本轮运行态限制

- 本机已有旧进程占用 `3100`、`5174`，新启动的管理端自动落到 `5176`；未强杀未知 Node 进程，避免破坏用户当前服务。
- 当前浏览器控制策略拒绝接管 `localhost:5174/login` 标签，因此无法把标签点击、关闭和刷新恢复写成浏览器实测通过；代码和静态验收已通过，运行态证据留待可接管浏览器或用户手动验收补齐。
- 只读 HTTP 复核：`http://[::1]:5174/login` 返回 200，`http://127.0.0.1:3100/api/courses?page=1&pageSize=1` 返回 200；新临时 Vite 进程未保持在 5176，未继续强行覆盖已有 5174 实例。
- 后端全量回归重新执行完成：12 个套件、42 项测试全部通过（约 29 秒）。

## 2026-08-07：管理端页面紧凑化与列表内部滚动

- 非工作台主容器新增 `page-main`，桌面端固定在视口高度内；工作台使用 `dashboard-main` 保持自身滚动，不被列表页规则影响。
- 删除非工作台页面的大标题占位，仅保留已访问标签、面包屑、紧凑搜索区、功能区和列表区。
- 搜索/操作区与控件高度调整为约 36–58px；表格区 `min-height: 40vh` 并通过 `overflow: auto` 在表格区域内部滚动，避免整个页面跟随列表变长。
- 列表增加复选框、全选当前列表和“已勾选 N 条”提示；导出按钮统一显示“导出”，未勾选导出筛选结果，勾选后导出选中数据。
- 学员导出保持后端脱敏接口，其他服务端分页模块最多读取 1000 条并对截断情况提示。
- 验证：管理端 build、`test:ui`、根目录 `pnpm.cmd run verify`、后端 12 套件/42 项、`git diff --check` 全部通过。
- 运行态限制未变化：当前浏览器控制层仍不能可靠接管旧 localhost 实例，需在最新实例或用户手动刷新后补充不同分辨率截图证据。

## 2026-08-07：标签页、面包屑与退出登录布局修正

- 根据最新界面截图，已访问标签页单独占据顶部一行，不再和退出登录按钮争用宽度。
- 非工作台页面的面包屑导航与紧凑文本形式的“退出登录”按钮放在同一行；工作台仍在顶部操作区显示退出按钮。
- 标签区继续支持横向滚动；桌面端释放列表区垂直空间，窄屏通过 header/面包屑行自动适配。
- 验证：管理端 build、`test:ui` 和 `git diff --check` 通过；后端和根目录校验沿用本轮前一轮已通过结果。

## 2026-08-07：管理端按钮与部署入口复核

- 对照管理端模块清单、`frontend/admin-react/src/App.tsx` 操作入口和 `backend/api/src/mvp/mvp.controller.ts` 路由，课程、Banner、模板、报名、订单、开票、学员、用户、优惠、反馈、消息、积分、配置、审计等当前入口均能找到对应 API 或明确只读行为。
- 未发现新的“显示按钮但没有后端实现”的缺口；后续重点仍是运行态请求、状态变化和审计证据，而不是继续增加静态按钮。
- 修正 README 中过期的管理端/H5 端口说明：由 8080/8081 改为当前 Compose 的 18080/18081，并提示更换端口时同步防火墙规则。
- 当前工作区仍包含前序功能实现的未提交改动，本轮未执行重置、覆盖或删除操作。

## 2026-08-07：面包屑视觉对齐

- 面包屑改为无边框、无背景的纯文本导航，分隔符改为轻量斜杠，更接近参考图。
- 桌面端页面顶部第一行内容左右缩进与列表内部一致：面包屑从内容左边界开始，退出登录固定到内容右边界；第二行标签页独占并支持横向滚动。
- 移动端保留自动换行，避免窄屏面包屑和退出按钮互相遮挡。
- 验证：管理端 build、`test:ui`、`git diff --check` 通过。
## 2026-08-07：搜索区紧凑化与多页签状态缓存

- 已将桌面端非工作台搜索区压缩：搜索区高度约 48px，输入框/下拉框 32px、13px 字号，按钮与间距同步收紧，减少筛选条件对列表空间的占用。
- 已补齐多页签 Keep-Alive 等价实现：切换前保存当前模块数据、筛选、分页、表单、弹窗、勾选和详情状态；切回时恢复；关闭标签和退出登录时清理缓存。
- 已接入列表滚动容器查询，保存并恢复当前标签的横向/纵向滚动位置。
- 验证通过：管理端 build、`test:ui`、根目录 `pnpm.cmd run verify`、后端 12 套件/43 项测试、`git diff --check`。
## 2026-08-07：部署可靠性加固

- API Docker 镜像的 build/runtime 阶段均显式安装 `openssl` 和 `ca-certificates`，消除 Prisma 在 `node:22-bookworm-slim` 中无法识别 libssl 的运行风险。
- 新增 `Docs/runtime/verify-compose-release.sh`：在服务器上无破坏地检查 `PAYMENT_ADAPTER=disabled`、Compose 配置、迁移容器退出码、PostgreSQL/API 健康状态及管理端/H5 HTTP 入口。
- 本地无法执行真实 Docker smoke test：Docker Desktop daemon 未启动，且 `.env.docker` 为服务器私密文件；脚本语法与真实容器检查需在 Ubuntu 服务器执行。
## 2026-08-07：本地服务运行态复核

- 当前监听：API `3100`、C 端 H5 `5185`、管理端旧实例 `5174` 和新实例 `5176`。
- HTTP 只读复核通过：API `http://127.0.0.1:3100/api/courses?page=1&pageSize=1` 返回 200；管理端 `[::1]:5174/login`、`[::1]:5176/login` 返回 200；H5 `127.0.0.1:5185` 返回 200。
- 管理端绑定 IPv6 loopback，使用 `127.0.0.1:5174` 测试会连接失败，不能误判为服务未启动；应使用 `[::1]:5174` 或通过浏览器访问 `localhost`。

## 2026-08-07：继续推进非支付可上线版本

- 重新审计管理端 18 个模块按钮与后端控制器，暂未发现新的无接口按钮。
- 发现报名明细默认返回完整手机号和表单快照，属于上线前敏感信息边界缺口；计划先完成列表脱敏、受保护详情接口和回归测试，再进入服务器 PostgreSQL/备份恢复阶段。
- 本轮开始前后端全量测试基线为 12 套件/43 项通过。

## 2026-08-07：报名明细脱敏与详情审计完成

- 报名明细列表默认只返回脱敏手机号，不再携带完整 `formPayload`；新增管理员单条详情接口供详情弹窗按需读取完整快照。
- 详情读取保留关联订单/支付凭证加载，并记录“查看报名履历”“查看学员档案”审计动作。
- 回归：后端 12 套件/43 项、API build、管理端 build、`test:ui`、根目录 `verify`、`git diff --check` 全部通过。

## 2026-08-07：服务器发布连通性核查

- `192.168.0.162:22` 可达，但当前本地没有可用的 SSH 公钥/代理凭据，非交互登录被服务器拒绝。
- 服务器端 Docker/PostgreSQL 迁移、对账、备份恢复仍保持未验收状态；下一步按 `Docs/Summary/2026-08-07_非支付可上线版本现状总览与实施规划.md` 阶段 E/F 执行，并归档命令输出。
- 修正 Docker Compose 部署文档中的容器内路径：`pnpm db:migrate:postgres`、`pnpm db:import:postgres`、`node prisma/export-postgresql.cjs`，避免 `/app/backend/api/backend` 路径重复错误。
- 新增 `Docs/Summary/2026-08-07_非支付服务器执行与验收清单.md`，把拉取、构建、迁移、对账、smoke check、备份恢复和发布门槛整理成服务器可执行清单。

## 2026-08-07：管理端搜索区进一步紧凑化

- 根据最新界面反馈，进一步缩小非工作台页面搜索区的输入框、筛选下拉框、查询/重置按钮及控件间距，减少筛选条件对列表可视区域的占用。
- 保留窄屏下的自适应换行规则；管理端 build、`test:ui` 已通过。

## 2026-08-07：报名明细课程筛选闭环修复

- 发现管理端报名明细的“关联课程”筛选错误复用了 `status` 参数，导致按课程筛选时服务端可能返回空结果或错误结果。
- 后端 `GET /admin/enrollment-records` 新增 `courseId` 查询条件；管理端列表查询和无勾选导出均改为发送 `courseId`。
- 生产化 API 回归新增课程筛选断言；管理端 build、`test:ui` 和该回归测试通过。
- 课程筛选下拉选项改为从完整课程列表加载，不再受当前报名明细分页结果限制；导出 JSON 的筛选字段也按模块实际字段记录（例如 `courseId`）。

## 2026-08-07：管理端服务端分页导出修复

- 发现服务端分页接口单页上限为 100，管理端原先直接请求 `pageSize=1000` 实际只会导出 100 条，和页面“最多 1000 条”提示不一致。
- 现改为按 100 条分页拉取，最多汇总 1000 条后导出；超过 1000 条仍通过 `truncated` 字段和页面提示明确告知。
- 管理端 build、`test:ui` 已通过。

## 2026-08-07：学员新读模式报名汇总字段兼容修复

- 发现切换 `student.readMode=new` 后，`/admin/enrollments` 返回 `orderStatus`，管理端报名汇总详情仍按旧字段 `paymentStatus` 分组，已支付人员会被误归为未支付。
- 兼容接口现在统一补齐 `paymentStatus`（优先使用订单状态，取消履历明确标为已取消），并增加双读模式回归断言。
- 学员 API 专项测试、API build、管理端 `test:ui` 和 `git diff --check` 通过。

## 2026-08-07：本轮非支付回归基线更新

- 后端全量回归 12 个套件/43 项通过；API build、管理端 build、管理端 `test:ui`、根目录 `verify` 和 `git diff --check` 通过。
- C 端 H5 重新构建通过，仅保留 uni-app/Sass 的弃用提示，不影响构建产物。
- 本机只读运行态仍可达：API `3100`、管理端 `5174/5176`、C 端 H5 `5185` 均返回 HTTP 200；这只能作为本地运行证据，不能替代服务器发布验收。

## 2026-08-07：管理端非支付操作闭环专项回归

- 新增 `backend/api/test/admin-operations.spec.ts`，覆盖课程、Banner、报名模板、优惠规则、消息、系统配置、积分、用户启停/密码重置和反馈回复等管理端写操作。
- 专项回归 3/3 通过；后端全量回归达到 13 个套件/46 项测试通过。
- 管理端 UI 静态验收新增操作路由矩阵检查；管理端 build、`test:ui`、根目录 `verify` 和 C 端 H5 build 均通过。
- 新增 `Docs/Summary/2026-08-07_管理端非支付操作闭环回归记录.md`。当前剩余阻断转移为真实浏览器证据、服务器 Docker/PostgreSQL 迁移对账及备份恢复演练。

## 2026-08-07：搜索区中等屏幕紧凑化收口

- 复核发现中等屏幕断点仍保留较宽的 145/175/92px 控件尺寸，已分别收紧为 135/155/84px；桌面端 145/165/88px 和窄屏自适应规则保持不变。
- `frontend/admin-react/scripts/verify-ui.mjs` 新增搜索区宽度断言，覆盖桌面端和中等屏幕断点，避免后续样式回退。
- 静态验收脚本通过；直接执行 TypeScript 检查和 Vite 生产构建均成功（Vite 5.79 秒）；API 生产构建、管理端非支付专项 3/3 回归和根目录 `verify` 也通过。`git diff --check` 仅提示既有 LF/CRLF 行尾转换，无空白错误。

## 2026-08-07：阶段 D 浏览器人工验收准备

- 新增 `Docs/Summary/2026-08-07_阶段D浏览器人工验收记录模板.md`，将管理端和 C 端非支付验收拆分为 D-01～D-17。
- 模板要求记录请求 URL/状态码、数据库前后状态、审计编号和截图，并明确旧服务、旧 bundle、写操作回滚和高风险场景放行门槛。
- 当前阶段 D 尚未宣称完成：浏览器控制层仍可能无法接管旧 localhost 实例，需在可接管浏览器或用户手动验收环境补齐逐项证据；完成后再进入服务器 PostgreSQL 迁移和备份恢复。

## 2026-08-07：Compose 发布环境门禁增强

- `Docs/runtime/verify-compose-release.sh` 新增非敏感配置自检：拒绝默认数据库密码、默认 JWT 占位值、少于 32 位的 JWT_SECRET，以及错误的 `DATABASE_URL` 主机名。
- 脚本仍不会输出密码或密钥，只在配置不合格时输出变量名和修复方向；支付适配器继续强制检查为 `disabled`。
- 浏览器人工验收尝试接管现有 `http://localhost:5174/login` 标签时被浏览器 URL 安全策略拒绝；未通过绕过方式继续，D 阶段保持待验收。

## 2026-08-07：PostgreSQL 导入后对账脚本

- 新增 `backend/api/prisma/verify-postgresql-reconciliation.cjs` 和 `pnpm db:verify:postgres`。
- 脚本校验导出快照 SHA-256（若存在），比较可导入表行数、订单状态/金额、课程已报名人数，并输出学员/账号关系/报名履历计数；发现差异时以非零退出码结束。
- 服务器清单已更新为“导入后先执行对账，再启动管理端/H5”；本地未连接服务器 PostgreSQL，因此尚未宣称真实迁移通过。

## 2026-08-07：移动端遮挡与高风险操作确认修复

- 根据 4 张移动端截图，移除 `pages/students/students.vue` 重复的自定义“我的学员”顶部栏，保留 UniApp 原生导航标题，避免双层吸顶。
- `mine.vue`、`business.vue`、`students.vue`、`register.vue` 和消息弹窗补足底部安全区/可视高度；关键遮罩层提升到高于原生 Tab 栏，解决个人资料、账号与安全等弹窗底部按钮被覆盖的问题。
- 删除 `detail.vue` 课程简介右侧“富文本内容”徽标。
- 为取消报名、保存资料、修改密码、保存/设默认学员、提交支付凭证、开票申请、确认报名、删除报名人员和退出登录增加二次确认；取消时不会调用后端接口。
- 验证结果：`pnpm.cmd build:client` 通过（仅保留既有 Sass 弃用提示）、`pnpm.cmd verify` 通过、`git diff --check` 无空白错误。真实设备点击验收仍待补充。
- 本地 H5 浏览器复核已能打开 `5185`，但当前会话未登录并被重定向到登录页，未读取浏览器存储或猜测账号密码；因此本轮未冒充完成已登录页面的视觉验收。

## 2026-08-07：C 端操作按钮与二次确认层级修复

- `frontend/client-uni/src/pages/business/business.vue` 统一订单操作按钮的高度、圆角、边框、字号和颜色，补充 `button::after` 清理，避免 H5 默认边框造成按钮样式不一致。
- “取消报名”保留二次确认，但改为透明背景、无边框的文字按钮；仍保留可点击高度，避免误触风险增加。
- `frontend/client-uni/src/App.vue` 增加非 scoped 的全局 `uni-modal`、`.uni-mask`、`.uni-modal` 和 `.uni-actionsheet` 层级规则（2000），保证 `uni.showModal` 在账号与安全、支付、报名、学员等业务弹窗之上显示。
- 复核 `frontend/client-uni/src/pages/detail/detail.vue`，源码中已不存在“富文本内容”字样；该提示不会再由当前源码渲染。
- `pnpm.cmd build:client` 通过；仅有既有 Sass/uni-app 弃用提示，`git diff --check` 无空白错误。真实设备视觉验收仍待完成。
- 复核构建产物确认层级规则已进入全局 `index-*.css`（选择器不带 `data-v-*`），并执行 `pnpm.cmd verify` 通过。

## 2026-08-07：二次确认弹窗层级恢复

- 复核发现账号安全、支付凭证、开票、报名、学员和消息详情等业务弹窗统一使用 `z-index: 1000`；消息详情页已移除旧的 `90` 层级定义。
- `frontend/client-uni/src/App.vue` 统一调整为：外层 `uni-modal`/遮罩 `3000`，确认框内容 `.uni-modal` `3001`，并同步覆盖 actionsheet/picker，避免任何二次确认弹窗落到当前业务弹窗下面。
- H5 运行态验证“修改密码”确认框：`uni-modal=3000`、`.uni-mask=3000`、`.uni-modal=3001`；随后点击取消，未修改测试账号密码。
- `pnpm.cmd build:client` 通过；仅有既有 Sass/uni-app 弃用提示。

## 2026-08-07：管理端重要保存与处理操作二次确认补齐

- 发现管理端课程、报名模板、Banner、优惠规则、消息、系统配置、积分、反馈、学员档案等保存/处理按钮仍可直接提交，和“重要保存、取消、审核需要二次确认”的交互要求不一致。
- 新增统一 `confirmAction`，并接入课程创建/编辑、模板创建/编辑/复制、Banner 保存、优惠规则保存、消息发布/编辑、系统配置保存、积分调整、反馈回复、学员档案编辑、线下支付凭证审核、开票通过等非支付操作。
- 删除、退款、关闭订单、停用/启用、发票驳回、密码重置等原有确认逻辑保留；确认取消时不发送 API 请求。
- `frontend/admin-react/scripts/verify-ui.mjs` 增加重要保存和审核确认静态断言。
- 管理端 build、`test:ui`、根目录 `verify`、目标后端回归和 `git diff --check` 已通过；真实浏览器点击证据仍列入阶段 D。

## 2026-08-07：全量回归收口

- 后端全量 Jest：13 个套件、48 项测试全部通过。
- API `nest build`、管理端 TypeScript/Vite build、管理端 `test:ui`、C 端 H5 build、根目录 `verify` 全部通过。
- C 端构建仅保留既有 uni-app/Sass 弃用提示；未发现编译错误。
- 当前仍不能将本地回归替代阶段 D 浏览器逐点击证据，以及服务器 PostgreSQL 迁移、对账、备份恢复和回滚演练。

补充：C 端当前源码的确认弹窗层级已进一步统一为外层/遮罩 `4000`、内容 `4001`；此前记录中的 `2000`/`3000` 属于历史修复值，不代表当前源码。

本地只读 HTTP 复核：API `3100`、管理端旧实例 `5174`、本轮管理端实例 `5176`、C 端 H5 `5185` 均返回 `200`。`5174` 仍可能是旧 bundle，正式验收前需只保留最新管理端实例。
## 2026-08-07：C 端二次确认弹窗层级最终恢复

- 根因确认：`frontend/client-uni/src/App.vue` 中的 `display: block !important` 覆盖了 `uni.showModal` 关闭时写入的内联 `display: none`，导致确认弹窗遮罩残留，并被误判为层级低于业务弹窗。
- 已删除强制显示规则；业务弹窗保持 `z-index: 1000`，系统确认遮罩/内容统一为 `4000/4001`，actionsheet/picker 为 `4000`，取消后不应残留遮挡。
- 已复核账号与安全、支付凭证、开票、报名、学员、消息详情等 C 端弹窗，没有发现高于系统确认框或异常低于业务层的定义。
- `pnpm.cmd build:client` 通过；源码不再包含 `display: block !important`，构建产物包含 `z-index: 4000/4001`。下一步用 `5185` 刷新后人工验证修改密码确认/取消流程。
## 2026-08-07：C 端二次确认弹窗层级修复

- 定位到 `frontend/client-uni/src/App.vue` 的首段 z-index 规则被同一行 `//` 注释吞掉，`uni-modal` 根节点规则实际未生效。
- 修复 uni-app `systemDialog` 外层页面的层级：从框架注入的 999 提升到 4000；确认框遮罩为 4000，对话框为 4001。
- 统一 C 端业务弹窗 `.modal-mask` 为 1000，覆盖支付凭证、开票、报名、账号安全、学员和消息等页面，保证修改密码等二次确认始终显示在最上层。
- 验证：`pnpm.cmd --dir frontend/client-uni build:h5` 通过；仅保留 Sass legacy/import 弃用警告，无构建错误。

## 2026-08-07：二次确认弹窗层级恢复复核

- 复核发现旧版 `App.vue` 首段注释与选择器曾粘连，导致部分 z-index 规则被注释吞掉；已恢复为独立、可编译的全局选择器。
- 当前层级统一为：业务弹窗 1000；`systemDialog` 外层及确认遮罩 4000；确认框内容 4001；action sheet/picker 4000。
- 验证：C 端 H5 构建、根目录 `verify`、`git diff --check` 均通过；真机/可控浏览器点击验收仍待补充。

## 2026-08-07：管理端非支付功能矩阵建立

- 重新盘点 `frontend/admin-react/src/App.tsx` 的 18 个模块、操作分发和加载逻辑，并逐项对照 `backend/api/src/mvp/mvp.controller.ts` 的管理路由。
- 新增 `Docs/Summary/2026-08-07_管理端非支付功能矩阵.md`，区分“前端已接线”“后端接口闭环”“自动化证据”和“真实浏览器待验收”，不再把静态按钮存在误报为上线完成。
- 当前判断：课程、Banner、模板、订单线下流程、开票、学员档案、用户、优惠、反馈、消息、积分、配置和审计均能找到真实接口；下一阶段重点转为失败路径、状态门禁、脱敏、审计和运行态证据。
- 新增管理端回归场景：普通 C 端用户访问 `/admin/courses` 返回 403；管理员创建的“待发布”课程可在管理端查看，但公共 `/courses` 不返回该课程；专项测试 4/4 通过。
- 本轮验证：管理端 `test:ui`、管理端 build、API build、C 端 H5 build、`pnpm.cmd run verify` 均通过；后端专项回归为 `admin-operations` 4/4、`production + p10` 8/8、认证/迁移/MVP/学员 28/28。

## 2026-08-08：非支付上线规划续审

- 重新读取当前工作区、管理端功能矩阵和历史计划，确认本轮按“功能矩阵 → 缺口修复 → 自动化回归 → 浏览器/服务器证据”推进。
- 首个可复现问题：管理端 `test:ui` 因验收脚本仍匹配旧版 `window.confirm` 实现而失败；当前 `App.tsx` 实际使用 `requestDialog`/`confirmAction`，保存、审核和处理操作仍有应用内二次确认。
- 下一步：更新静态验收断言以匹配现行确认实现，然后重新执行 UI/build/根目录验证；若通过，再继续核对未实现按钮和错误路径。

## 2026-08-08：P0 完成与 P1 内容管理加固

- 修正 `frontend/admin-react/scripts/verify-ui.mjs` 对确认弹窗的过时断言；当前 `requestDialog`/`confirmAction` 统一确认逻辑已被正确验证。
- 管理端 `test:ui`、管理端 build、根目录 `verify` 均通过；后端全量 Jest 13 套件/49 项通过。
- 进入 P1：后端 `saveCourse`、`saveBanner`、`saveTemplate` 增加内容边界校验，拒绝空值、负数、非法时间、名额不一致和选择字段无选项等脏数据。
- 新增 `mvp.spec.ts` 非法内容管理回归，21/21 通过；API build 通过。
- 下一步继续补充 P1 浏览器成功/失败/取消/重复提交证据，并开始 P2 用户与学员权限及敏感数据边界。

## 2026-08-08：P2/P3 第一批边界加固

- 用户管理：不存在用户的启停操作改为明确业务错误。
- 学员档案：补齐姓名/手机号/邮箱校验，已合并档案不可编辑、启停、授权，也不可作为合并目标；关系类型不能为空。
- 运营优惠：拒绝非法人数门槛、折扣比例和不存在课程，避免后端静默修正运营数据。
- 验证：`student-api.spec.ts` 4/4、`mvp.spec.ts` 22/22、API build 通过。
- 下一步继续补充用户旧令牌失效、学员关系越权、敏感字段网络响应和消息/积分/反馈/配置幂等回归。

## 2026-08-08：P4 原生对话框收口

- 富文本编辑器链接输入和图片错误提示已接入管理端统一应用内对话框/通知，源码不再使用 `window.prompt/window.alert`。
- `test:ui` 增加原生对话框回退检查，管理端 UI 验收和 build 通过。
- 下一步：继续完善弹窗 Escape/焦点、空状态和错误恢复，并准备 P5 真实浏览器逐按钮验收。

## 2026-08-08：C 端确认弹窗根节点层级加固

- 复核 `uni.showModal` 的 uni-h5 实际挂载点为 `body > #u-a-m > uni-modal`；此前仅提升子节点层级，根节点仍为普通静态节点，存在被页面 stacking context 影响的兼容风险。
- `frontend/client-uni/src/App.vue` 现将 `#u-a-m` 根节点固定为 `z-index: 4000`，确认遮罩保持 `4000`，确认框内容保持 `4001`；业务弹窗统一为 `1000`，并限制根节点仅由实际确认框接收点击。
- 已在运行中的 `5185` 页面复核“账号与安全 → 修改密码”：确认框显示在业务弹窗上方，实际计算层级为 `#u-a-m=4000`、遮罩 `4000`、内容 `4001`。
- 验证：`pnpm.cmd --dir frontend/client-uni build:h5` 通过，`git diff --check` 通过；仍需在真实小程序/H5 设备上补充取消、关闭和其他确认场景的人工证据。

## 2026-08-08：P2 会话失效与学员越权回归加固

- 发现管理端“重置后旧登录令牌会失效”的产品约定原先只吊销 refresh token，旧 access token 仍可能在有效期内继续访问。
- `User` 增加持久化 `sessionVersion`；登录/刷新令牌携带版本，`JwtGuard` 每次请求比对数据库版本。用户启停、个人改密、管理员重置密码和验证码找回密码都会递增版本，使旧 access token 立即失效。
- 新增 SQLite `0008_auth_session_version` 迁移，并让兼容迁移器对已有数据库条件补列；PostgreSQL 部署继续由 `prepare-postgresql.cjs` 从当前 Prisma schema 生成。
- 增加回归：禁用/重新启用后旧令牌仍失效、管理员重置密码后旧令牌失效、账号只能操作自身授权的学员档案。
- 验证：后端全量 Jest 13 套件/53 项通过，API build、根目录 verify、git diff 检查通过；本地 API 已恢复监听 3100。

### 下一步

1. 补 P3 消息/积分/反馈/配置的失败、幂等和审计回归，修正文档中仍标为旧实现的描述。
2. 进入 P5 浏览器逐按钮验收，优先覆盖登录、课程待发布、学员关系、消息定向、开票驳回和线下凭证审核。
3. 服务器发布前重新执行 PostgreSQL schema 生成、迁移、导入对账和备份恢复门禁。

## 2026-08-08：C 端修改密码二次确认层级恢复

- 复核发现 C 端业务弹窗统一为 `1000`，而 `uni.showModal` 动态挂载的 `#u-a-m` 根节点在不同 uni-h5 版本中可能没有稳定的定位/层级，导致“账号与安全 → 修改密码”的确认框落到业务弹窗下面。
- `frontend/client-uni/src/App.vue` 现统一约定：业务弹窗 `1000`；系统确认/选择浮层使用独立 stacking context，遮罩 `2147483000`、内容 `2147483001`，并兼容 `#u-a-m`、`data-type="systemDialog"`、`.uni-mask` 和 `.uni-modal` 等实际挂载结构。
- 不覆盖 `display`，保留 uni-app 自己的显示/隐藏状态；根节点不接收点击，实际确认框和遮罩接收点击，避免确认框显示正常但无法操作。
- 验证：`pnpm.cmd --dir frontend/client-uni build:h5` 通过；`git diff --check` 通过。需要在运行中的 H5 页面重新验证修改密码、取消报名、提交支付凭证、保存资料、提交开票等确认场景。

## 2026-08-08：C 端二次确认层级统一复核完成

- 五个包含业务弹窗的页面已统一使用业务层级变量，并保留 `z-index: 1000` 兼容兜底。
- 运行态已在 `http://localhost:5185/#/pages/mine/mine` 打开账号安全并触发“修改密码”确认框：确认框可见，计算层级为 `#u-a-m=2147483640`、遮罩 `2147483640`、内容 `2147483641`，高于业务弹窗 `1000`。
- `pnpm.cmd --dir frontend/client-uni build:h5`、`pnpm.cmd run verify`、`git diff --check` 均通过。

## 2026-08-08：C 端修改密码确认框层级恢复（本轮）

- 根据运行截图复核“我的 → 账号与安全 → 修改密码”二次确认框可能被业务弹窗遮挡的问题。
- 将全局系统弹窗层级恢复为项目约定的根节点/遮罩 `10000`、确认内容 `10001`；业务弹窗继续固定为 `1000`。
- `App.vue` 补充 `#u-a-m > uni-modal` 及新旧 `uni-modal` mask/container 类名选择器，保留 uni-app 内联 `display` 控制，不强制显示隐藏节点。
- 根目录静态校验已同步更新到 `10000/10001`，避免后续层级回退。
- 验证：C 端 H5 构建、根目录 `verify`、`git diff --check` 均通过；运行态 CSS 变量为业务 `1000`、系统遮罩 `10000`、内容 `10001`。
- 已复核管理端自定义确认框：普通业务弹窗 `1000`，确认遮罩/内容分别为 `3000/3001`，二级报名详情为 `1100`；管理端 UI 静态验收通过。
- 最新 C 端 H5 开发服务已监听 `127.0.0.1:5185`，实际样式响应包含 `1000/10000/10001` 规则；浏览器需强制刷新以清除旧 bundle。

## 2026-08-08：非支付运营输入边界加固

- `saveMessage` 增加标题、内容、目标数量和 `enabled` 类型门禁；`adjustPoints` 增加安全整数、原因长度和 Prisma Int 范围门禁；`saveSystemConfig` 增加配置键格式及值/说明长度门禁。
- 新增 `backend/api/test/mvp.spec.ts` 边界回归，覆盖超长消息、非法启用类型、超量目标、非法积分和配置键/值/说明。
- 验证：`mvp.spec.ts` 23/23、API build、`git diff --check` 通过；全量回归随后更新为 13 套件/56 项。

## 2026-08-08：账号与我的学员资料边界加固

- 账号资料更新增加姓名、手机号、邮箱格式和重复绑定校验；手机号/邮箱统一规范化保存。
- “我的学员”新增入口增加邮箱格式校验，并规范化带空格的手机号和大小写邮箱，保证匹配与回填稳定。
- 验证：学员档案与认证专项 2 套件/10 项通过，API build 和 diff 检查通过。

## 2026-08-08：非支付基线全量回归

- 后端 13 套件/56 项、API build、管理端 `test:ui`、管理端 build、C 端 H5 build、根目录 `verify` 和 `git diff --check` 全部通过。
- 当前代码侧 P0～P4 第一/二批边界加固没有引入回归；剩余主阻断项转为 P5 浏览器人工证据和 P6 服务器迁移/恢复证据。

## 2026-08-08：C 端修改密码二次确认层级再次恢复

- 针对“修改密码确认框仍可能落在账号与安全弹窗下面”的反馈，复核业务弹窗与 `uni.showModal` 的根节点、遮罩、内容容器三层结构。
- 保持 C 端业务弹窗为 `1000`，系统确认根节点/遮罩为 `10000`，确认内容为 `10001`；并对 `.uni-modal`/dialog container 增加固定定位、居中和同层级强制规则，避免 uni-h5 默认 `z-index: 999` 抢回层级。
- 该规则覆盖修改密码、保存资料、退出登录、取消报名、支付凭证、开票、报名提交、删除报名人和我的学员操作等所有 `uni.showModal` 二次确认。
- 验证：C 端 H5 构建、根目录 `verify`、`git diff --check` 通过；产物 CSS 已包含确认内容的固定定位与 `10000/10001` 层级。
- 下一步：重启/刷新当前 H5 开发服务后，在浏览器逐项确认“修改密码”及其他确认框的可见性和可点击性。

## 2026-08-08：C 端确认框层级选择器加固

- 在 `frontend/client-uni/src/App.vue` 增加 `--client-system-modal-layer: 10000` 别名，并显式覆盖 `#u-a-m > uni-modal`、`.uni-mask`、`.uni-modal` 直接子节点。
- 业务弹窗仍为 `1000`，系统确认遮罩为 `10000`，确认内容为 `10001`；未强制设置 `display`，保留 uni-app 关闭状态控制。
- 根目录 `verify` 新增别名及直接子节点选择器断言，防止后续 uni-h5 升级或样式重构导致层级回退。

## 2026-08-08：非支付可上线规划启动

- 完成管理端 18 个模块的源码/路由初审：主要按钮已有真实操作分发和受保护 `/admin/*` 接口，当前重点从“是否有按钮”转为运行态请求、状态门禁、错误恢复、脱敏、审计和发布恢复证据。
- 新增 `Docs/Summary/2026-08-08_非支付版本可上线规划与实施状态.md`，明确 P0～P5 范围、执行顺序和支付暂缓决策。
- 当前开始 P0 运行态审计；已连接本地管理端并读取用户管理列表，下一步逐模块检查按钮加载、空状态、失败反馈和数据变化。
## 2026-08-08：C 端修改密码二次确认层级修复

- 更新 `frontend/client-uni/src/App.vue`：新增系统确认根节点层级变量 `--client-system-modal-root-layer: 11000`，并将 mask/container/dialog 固定到 `11000/11001`。
- 更新 `mvp/scripts/verify.mjs`：静态门禁检查业务弹窗与系统确认弹窗的层级关系及兼容选择器。
- 已完成运行态复核：账号与安全弹窗打开后点击保存新密码，确认框显示在上层；取消/确定按钮均可点击。
- 已完成验证：`pnpm.cmd --dir frontend/client-uni build:h5`、`pnpm.cmd exec node mvp/scripts/verify.mjs`、`git diff --check`。
- 下一步：刷新/重启当前 5185 H5 服务后，补做真实浏览器与小程序容器的取消、确认、重复打开验收。
## 2026-08-08：P5 管理端模块运行态首轮验收

- 完成管理端真实登录和 17 个模块逐项导航验收，所有模块均能打开对应标签并加载数据/只读状态。
- 当前未发现导航失效、模块请求错误或按钮完全缺少实现的运行态证据；关键操作已有 API 路由和 56 项后端回归覆盖。
- 已修正部署文档口径：Docker Compose 使用 `18080/18081`，旧 `8080/8081` 仅保留为 legacy systemd/Nginx 方案。
- 下一步：继续补充成功/取消/失败路径的浏览器记录，并把服务器 `192.168.0.162` 的 Compose、PostgreSQL 迁移对账、备份恢复输出归档后再判定非支付内测放行。
## 2026-08-08：P6 Docker/GitHub 发布可靠性加固

- 修复 `.gitignore` 过宽导致 `Docs/runtime` 脚本无法随 GitHub 发布的问题；仅部署脚本放行，文档和日志仍保持忽略。
- 新增 `.gitattributes`，确保 Linux shell 脚本以 LF 行尾检出。
- 更新 README 和服务器验收清单，增加脚本跟踪检查与无需再次手工 `scp` 的部署路径。
- 复核 Dockerfile：API build/runtime 阶段均安装 `openssl` 和 `ca-certificates`；服务器需执行 `docker compose ... build --no-cache` 以替换旧镜像。
- 本机 Docker daemon 当前不可用，因此镜像构建和服务器迁移/恢复不能宣称完成；下一步必须在 `192.168.0.162` 执行发布门禁脚本并回传非敏感结果。

## 2026-08-08：P5 管理端运行态验收第二轮与 P4 弹窗操作栏修复

- 真实登录态已逐项打开课程、报名模板、Banner、报名汇总、报名明细、订单、开票、学员、用户、消息和反馈模块；均能加载列表/详情并显示对应操作入口。
- 验收了课程新增空值提示、订单关闭取消、开票驳回理由+二次确认取消、用户启停取消、学员档案编辑、报名汇总状态详情和报名明细详情；取消路径未触发对应写操作。
- 发现并修复 `frontend/admin-react/src/styles.css` 的长弹窗操作栏问题：`.modal-actions` 与 `.app-dialog-actions` 现在使用 sticky 底部栏，弹窗内容保持内部滚动，移动端同步调整边距。
- 修复 `frontend/admin-react/src/App.tsx`：学员档案编辑弹窗提交期间向 `SimpleModal` 传递 `busy`；支付设置输入、上传和保存按钮提交期间禁用。
- 更新 `frontend/admin-react/scripts/verify-ui.mjs`，加入操作栏 sticky 层的静态门禁。
- 验证通过：管理端 `test:ui`、管理端生产构建；运行态计算样式为 `.modal-actions { position: sticky; bottom: -28px; }`，课程弹窗 `overflow: auto`。
- 下一步：继续补充成功写操作/服务端失败响应的浏览器证据，再进入服务器 Docker/PostgreSQL 迁移、备份恢复和非支付放行门禁。

## 2026-08-08：P2 权限与敏感数据边界回归

- 新增 `admin-security-boundaries.spec.ts`：覆盖全部管理端主要读取/写入路由，普通用户请求均返回 HTTP 403。
- 新增敏感字段回归：报名履历列表手机号脱敏且不返回 `formPayload`；单条详情返回完整手机号和表单快照；学员列表隐藏 `phoneNormalized` 并脱敏手机号/邮箱，详情按受保护接口揭示完整值。
- 测试使用隔离库创建临时订单和履历，避免依赖种子数据是否包含学员报名记录。
- 专项测试 2/2 通过；下一步继续补 P2 的旧令牌失效、跨账号学员关系越权和审计记录，以及 P3 运营模块幂等证据。
- 已补充旧 access token 吊销和跨账号学员关系操作回归；专项测试现为 3/3 通过，下一步转入 P3 运营模块幂等与失败恢复证据。

## 2026-08-08：P3 反馈边界加固

- 反馈提交现在只保存规范化的分类和内容，拒绝超长内容；反馈回复增加长度上限，避免直接 API 写入异常大文本。
- 反馈列表和处理响应改为白名单字段，历史记录中的额外 payload 不再通过管理端接口返回。
- 新增回归验证超长提交、超长回复和额外字段不会落库；`mvp.spec.ts` 24/24 通过。
- 下一步继续核对消息目标范围/时间窗、积分调整并发幂等、系统配置更新和审计筛选的失败恢复证据。

### 本轮全量回归补充

- 根目录 `pnpm.cmd run verify` 通过。
- 后端 `nest build` 通过；Jest 13 套件 / 56 项通过。
- 管理端 `test:ui` 和生产构建通过。
- C 端 `build:h5` 通过，仅保留 Sass/uni-app 既有弃用提示。
- `git diff --check` 通过；未发现新增空白错误。

## 2026-08-08：P1 课程搜索与刷新闭环修复

- 回归测试发现课程列表关键字不匹配课程 ID，导致编辑成功后按列表中显示的 ID 刷新为空，形成“按钮没有实现”的假象。
- `backend/api/src/mvp/mvp.service.ts` 已让公共课程列表和管理端课程列表同时匹配 `id/title/subtitle/instructor`。
- `backend/api/test/admin-operations.spec.ts` 已增加编辑后刷新、删除后刷新和课程维护/删除审计断言；该套件 5/5 通过。
- 下一步继续补充 Banner、报名模板、报名明细和订单的刷新、失败恢复和取消不写入证据。

## 2026-08-08：C 端修改密码二次确认层级再次加固

- 统一层级关系为：业务弹窗 `1000` < 系统确认根节点/遮罩 `2147483646` < 系统确认内容 `2147483647`。
- `frontend/client-uni/src/App.vue` 增加 `#u-a-m` 直接子节点及 dialog/container 兼容选择器，避免不同 uni-h5 版本把修改密码确认框放到账号安全弹窗下面。
- 管理端 `app-dialog-backdrop/app-dialog` 同步提升为 `2147483646/2147483647`，Portal 挂载方式保持不变。
- 根目录 verify、管理端 UI 验收/构建、C 端 H5 构建和 diff 检查通过；待重启 H5 并强刷后做运行态确认。

## 2026-08-08：非支付版本真实接口基线复核

- 本地运行态确认：API `http://127.0.0.1:3100`、管理端 `http://127.0.0.1:5174/login`、C 端 `http://127.0.0.1:5185/` 均返回成功响应。
- 管理员令牌逐项请求 18 个管理端入口，最终全部返回 HTTP 200；之前出现的短暂失败来自重复开发进程占用端口，清理重复实例后复测通过。
- 报名明细列表返回脱敏手机号且不泄露 `formPayload`；单条详情返回完整表单快照，学员档案列表同样默认脱敏。
- 当前“按钮没实现”的代码级/接口级证据已进一步排除，剩余工作集中在写操作的浏览器证据和服务器发布恢复门禁。
- 下一步：记录每个非支付写操作的成功刷新、失败恢复、取消无写请求、重复点击和审计结果，然后进入 Docker/PostgreSQL 迁移对账。

## 2026-08-08：P1 Banner 写操作运行态闭环

- 通过真实管理员接口验证：非法空标题保存返回 400；临时 Banner 新增返回 201。
- 新增后刷新列表可见 1 条，删除后刷新列表为 0 条，证明按钮对应的新增、删除和刷新链路可用。
- 审计列表记录了 Banner 维护和删除动作；临时数据已清理，现有业务数据未保留测试 Banner。
- 下一步继续按同一标准验证课程、报名模板、订单、开票、学员、用户、消息、积分和配置。

## 2026-08-08：P1 课程与报名模板写操作运行态闭环

- 在当前源码 API 实例上验证课程：非法空标题 400；新增“待发布” 201；管理端刷新可见；公共课程列表隐藏待发布课程；编辑 200；删除 200；刷新后消失。
- 验证报名模板：重复字段 400；合法新增 201；编辑 200；删除 200；刷新后记录消失。
- 测试使用 UTF-8 请求体；PowerShell 直接传字符串时会产生编码差异并造成假性 400，浏览器 Fetch 使用 UTF-8，不属于业务代码缺陷。
- 临时课程和模板均已清理。下一步进入订单、开票和报名明细写操作证据。

- 已将 API 重新启动到当前源码并恢复监听 `3100`；健康请求和管理员登录复测通过。此前 `3101` 仅作为隔离验证实例，验证后已关闭。

## 2026-08-08：C 端修改密码确认框层级兼容性加固

- 检查运行态 DOM 后确认 `uni.showModal` 使用 `body > #u-a-m > uni-modal`，现行层级为业务弹窗 `1000`、系统根节点/遮罩 `2147483646`、确认内容 `2147483647`。
- `frontend/client-uni/src/App.vue` 新增 `uni-modal__mask`、`uni-modal__container` 和直接子节点的全局层级规则，覆盖不同 uni-h5 版本的确认框承载结构。
- `mvp/scripts/verify.mjs` 已增加对应静态断言；C 端 H5 构建、根目录 verify、`git diff --check` 均通过。
- 下一步：重启/强制刷新当前 5185 H5 服务，逐项确认修改密码、保存资料、取消报名、凭证提交、开票和学员操作的显示与点击行为；小程序端仍需真机复核。

## 2026-08-08：非支付可上线主计划启动

- 完成当前工作区基线复核：管理端、C 端和 API 的主要入口/接口均已存在，问题集中在写操作运行证据和发布门禁，不再把静态按钮数量当作完成度。
- 将后续工作拆为 P1 核心业务写操作、P2 用户/学员权限脱敏、P3 运营系统幂等、P4 交互与 Docker/PostgreSQL 发布门禁、P5 非支付内部发布。
- 当前进入 P1，优先验证订单关闭/取消、线下支付凭证上传/审核、开票处理和报名明细刷新；支付在线渠道继续保持禁用。

## 2026-08-08：P1 订单/凭证/开票接口闭环

- 修复后端状态门禁：已取消订单不可重复取消；审核中的支付凭证不可重复上传；已驳回开票订单允许重新申请。
- C 端订单页同步调整：审核中的凭证显示“审核中”而不再开放重复上传；已驳回开票订单可再次勾选申请。
- 新增 `backend/api/test/p1-write-operations.spec.ts`，通过隔离 SQLite 数据库验证订单取消、管理端关闭/退款、凭证上传/驳回/通过、开票处理/重新申请、电子发票上传、报名明细查询和审计。
- 目标回归：P1 测试 2/2 通过；后续需在真实浏览器中补充点击/取消/刷新/网络面板证据，再进入 P2 用户与学员权限脱敏。

## 2026-08-08：C 端二次确认弹窗层级恢复

- 修复“修改密码”确认框显示在账号与安全业务弹窗下面的问题，补齐当前 uni-h5 实际使用的 `#u-a-t` 容器，并保留旧版 `#u-a-m` 兼容规则。
- 统一层级：业务弹窗 `1000` < 系统确认根节点/遮罩 `2147483646` < 确认内容 `2147483647`；管理端确认弹窗同步采用同一高层级契约。
- 已覆盖修改密码、保存资料、退出登录、取消报名、凭证提交、开票、报名提交、删除报名人及我的学员操作等确认入口。
- 已通过 C 端 H5 构建、管理端 UI 验收、根目录 `verify` 和 `git diff --check`。
- 验收要求：重启或刷新 `5185` H5 后，逐项打开上述确认框，确认弹窗在业务弹窗之上且遮罩、取消、确定均可点击；小程序端需再用真机复核。
## 2026-08-08：C 端二次确认层级再次加固

- 针对“账号与安全”弹窗内修改密码的确认框仍可能被遮挡的问题，复核发现仅提升内容节点层级不足，必须同时固定动态挂载宿主。
- `frontend/client-uni/src/App.vue` 现将 `#u-a-t`/`#u-a-m` 系统确认宿主固定为全屏层，并统一业务弹窗 `1000`、系统根节点 `2147483000`、遮罩 `2147483001`、确认内容 `2147483002`。
- 规则覆盖 `uni-modal`、`systemDialog`、`.uni-mask`、`.uni-modal__mask`、container/dialog/content 及动态宿主直接子节点，未覆盖 `display`，保留 uni-app 开关状态。
- `node mvp/scripts/verify.mjs` 通过；H5 构建完成（仅保留既有 Sass 弃用提示）；`git diff --check` 通过。
- 待用户刷新/重启 5185 H5 后，继续验证修改密码、保存资料、退出、取消报名、凭证提交、开票提交、报名和学员操作等全部确认框的显示、取消、确定与重复打开。
## 2026-08-08：P3 运营模块并发与审计边界补强

- 完成积分调整原子边界校验：事务内按当前余额条件更新，失败不会生成积分流水。
- 完成消息创建/更新语义门禁：POST 不接受客户端 ID，PATCH 必须提供资源 ID。
- 完成审计时间筛选校验和反馈审计脱敏，避免无效筛选静默通过或写入用户反馈原文。
- 验证：`backend/api` 的 `mvp.spec.ts` 25/25 通过；下一步进入 P4 Docker/PostgreSQL、上传持久化、备份恢复和发布门禁。
## 2026-08-08：修改密码二次确认层级补强

- 已将 C 端业务弹窗和系统确认弹窗保持明确的层级关系，覆盖修改密码、资料保存、退出、订单、支付凭证、开票、报名和学员操作。
- 已增加 `onLaunch` + `onMounted` 双入口安装运行时层级守卫，动态确认宿主出现后继续校正，不覆盖 uni-app 的显示/隐藏状态。
- 验证通过：`pnpm.cmd --dir frontend/client-uni run build:h5`、`node mvp/scripts/verify.mjs`、`git diff --check`。
- 待办：刷新当前 5185 H5 页面，用真实登录账号完成可视化点击留痕；小程序容器需独立复核。
## 2026-08-08：C 端修改密码二次确认弹窗根节点修复

- [x] 复核 C 端所有 `uni.showModal` 二次确认入口，业务弹窗统一为 `1000`，系统确认根节点/遮罩/内容统一为 `2147483000/2147483001/2147483002`。
- [x] `frontend/client-uni/src/App.vue` 对未挂在 `body` 下的 `#u-a-m` 执行根节点迁移，消除页面祖先 stacking context 导致的确认框被业务弹窗遮挡问题。
- [x] C 端 H5 build、根目录 verify、git diff check 通过。
- [ ] 重启 5185 H5 服务并强制刷新后，补充真实浏览器/小程序容器的修改密码、取消、确认和重复打开验收证据。

## 2026-08-08：C 端二次确认层级运行时加固

- [x] 针对“修改密码确认框被账号与安全弹窗遮挡”再次复核所有 C 端 `uni.showModal` 入口及页面业务弹窗层级。
- [x] `App.vue` 增加 style/class/data-type 变化监听、渲染周期归一化和数值 z-index 兜底；仅在值变化时写入 inline `!important`，避免观察器循环。
- [x] C 端 H5 构建、根目录 `pnpm.cmd run verify`、`git diff --check` 通过。
- [ ] 仍需重启/刷新 5185 后完成真实账号点击验收，并在小程序容器复核。
## 2026-08-08：管理端列表加载、错误与重试闭环

- [x] 管理端列表、工作台和渠道自检页补齐加载状态、接口失败状态和“重新加载”入口，避免把请求失败误显示为空列表。
- [x] 保留 Keep-Alive 标签页快照和请求版本门禁，切换标签页时不会让旧请求覆盖当前页面。
- [x] 管理端 build、`test:ui`、根目录 `verify` 和 `git diff --check` 通过。
- [ ] 仍需在浏览器中模拟接口失败并完成重试、刷新、分页和标签页缓存的人工证据。
## 2026-08-08：管理端电子发票上传重复刷新修复

- [x] 移除发票上传成功后多余的第二次列表请求，统一由 `runOperation` 负责刷新和错误反馈。
- [x] 管理端 build、UI 静态验收、根目录 verify 和后端 15 套件/64 项回归通过。
## 2026-08-08：管理端查询失败请求安全化

- [x] 查询、重置、回车搜索和列表重试统一使用 `requestLoad`，失败由列表错误态承接，不再产生未处理 Promise。
- [x] 静态校验增加 `requestLoad` 门禁，管理端构建、UI 验收和根目录 verify 通过。
## 2026-08-08：管理端写入成功与列表刷新失败解耦

- [x] `runOperation` 不再把写入成功后的列表刷新失败误报为写入失败；页面保留错误态和重试入口。
- [x] 增加根目录静态门禁，管理端构建和验证通过。

## 2026-08-08：C 端二次确认弹窗层级修复

- [x] 定位 uni-h5 新版 `data-type="systemDialog"` 默认 `z-index: 999` 覆盖业务弹窗 `1000` 的原因。
- [x] 统一 `#u-a-m`、`uni-modal`、`systemDialog` 页面及新旧 mask/container/dialog 类名的层级：业务弹窗 1000，系统确认根节点/遮罩/内容为 2147483000/2147483001/2147483002。
- [x] C 端 H5 构建、根目录 verify 通过；待刷新 5185 服务后补充真实浏览器和小程序容器的点击/取消复核。

## 2026-08-08：管理端写入与刷新错误隔离补强

- [x] 课程新增/编辑统一接入 `runOperation` 请求锁，保存成功后列表刷新失败不会被误报为课程保存失败。
- [x] 线下支付凭证审核统一接入 `runOperation`，审核写入成功后刷新失败不会覆盖真实成功结果；审核弹窗仅在写入完成后关闭。
- [x] 学员档案关系、状态、合并等操作区分写入结果与详情/列表刷新结果，失败时给出可重试提示。
- [x] 并发操作已有进行中请求时统一提示“另一个操作正在处理中”，避免按钮点击无反馈。
- [x] 管理端 UI 门禁、构建、根目录 verify 和 `git diff --check` 均通过。
- [x] 后端隔离回归 `admin-operations.spec.ts`、`admin-security-boundaries.spec.ts`、`p1-write-operations.spec.ts` 共 3 套/11 项通过，API build 通过。
- [ ] 下一步进入 P4：服务器 Compose/PostgreSQL、上传卷、备份恢复和回滚门禁；在线支付继续保持 disabled。
## 2026-08-08：管理端核心写操作运行时验收

- 已在当前运行 API `3100` 使用管理员账号完成课程、Banner、报名模板的新增、编辑、删除和刷新核对。
- 临时数据已清理；审计接口可检索对应的维护/删除记录。
- 结果：课程、Banner、报名模板当前运行时闭环通过；订单、凭证、开票、学员、用户及运营模块继续按同一标准验收。

## 2026-08-08：非支付可上线主线重新启动

- 重新执行根目录 verify、管理端 UI 静态验收/生产构建和后端全量 Jest：分别通过、通过、15 套/64 项通过。
- 确认本机已有 3100/5174/5185 服务，未重复启动；管理端工作台和 17 个导航模块可进入。
- 当前执行顺序保持 P0 → P1 → P2 → P3 → P4 → P5：先逐按钮补真实运行证据和修缺口，再进行 Docker/PostgreSQL、上传持久化、备份恢复、回滚与非支付内部发布验收。

## 2026-08-08：管理端课程按钮真实浏览器闭环（本轮）

- 通过管理端真实会话展开“内容与课程”分组并进入“课程管理”；确认子菜单默认收起时，隐藏子项不能直接点击，展开分组后导航正常，不能将该现象误判为按钮未实现。
- 使用唯一临时课程完成“新增课程 → 二次确认 → 列表刷新 → 编辑打开 → 删除取消（记录仍在）→ 删除确认 → 列表刷新（记录消失）”闭环，临时数据已清理。
- 订单/线下凭证/开票的隔离后端回归重新执行：`p1-write-operations.spec.ts`、`admin-security-boundaries.spec.ts` 共 2 套、5 项通过。
- P1 尚未结束：仍需补充订单、支付凭证、开票和报名明细的真实浏览器状态门禁/取消/重复点击证据，再进入 P2/P3 的逐模块验收。

## 2026-08-08：管理端订单/凭证/开票/报名明细运行态核验（本轮）

- 订单：真实打开订单详情并关闭；关闭待支付订单的二次确认取消后，原订单仍保留且状态不变。
- 线下凭证：真实打开待审核凭证预览；驳回未填写审核备注时被前端拦截，审核弹窗保持打开，未产生写入。
- 开票：真实进入驳回理由输入和第二次确认；取消第二次确认后，`INV-DEMO-005` 仍为待处理。
- 报名明细：真实打开“报名履历详情”，展示受保护的完整详情与表单快照，关闭后列表正常保留；详情接口加载延迟不会被误判为按钮无效。
- P1 仍需补：至少一条隔离临时订单的真实关闭/凭证审核成功写入证据，以及服务器发布门禁。

## 2026-08-08：P4 上传卷持久化门禁补充

- 新增 `Docs/runtime/verify-upload-volume.sh`：在 API 容器上传目录写入隐藏临时标记，重启 API，等待 healthcheck 恢复后读取并自动清理。
- 该脚本不读取或打印数据库密码，不改业务表；只验证 Compose `upload_data` 卷的跨容器重启持久性。
- 服务器发布阶段需在 `verify-compose-release.sh` 通过后显式执行该脚本，再归档输出。

## 2026-08-08：C 端修改密码二次确认层级回归修复

- 复核发现运行时归一化逻辑不应把 `uni-modal` 子节点从 `#u-a-m`/`#u-a-t` 的 Vue 挂载根强行搬到 `body`，否则 uni-app 后续渲染可能将确认框恢复到默认 `z-index: 999`，再次落到业务弹窗（`1000`）下面。
- 已调整 `frontend/client-uni/src/App.vue`：仅提升真正嵌套的 `systemDialog` 根页面；`uni-modal` 保留原挂载树，同时统一根节点、遮罩和内容层级 `2147483000/2147483001/2147483002`，业务弹窗继续为 `1000`。
- 所有 C 端 `uni.showModal` 二次确认入口（修改密码、资料保存、退出登录、取消报名、凭证提交、开票、报名、学员操作等）继续复用同一层级契约。
- 验证通过：`pnpm.cmd --dir frontend/client-uni build:h5`、`pnpm.cmd run verify`。真实浏览器需刷新/重启 5185 后补做修改密码确认、取消和重复打开复核。

## 2026-08-08：C 端确认弹窗动态层级即时兜底

- `frontend/client-uni/src/App.vue` 的 `MutationObserver` 现在会在确认宿主插入或框架改写样式后立即执行一次层级归一化，再保留下一帧和延迟归一化，减少确认框先以 uni-h5 默认层级绘制在业务弹窗下面的窗口。
- 运行标记改为仅在值变化时写入，避免层级归一化自身触发观察器循环；全部 `uni.showModal` 入口仍共享同一层级契约。
- 验证通过：`pnpm.cmd run verify`、`pnpm.cmd --dir frontend/client-uni build:h5`、`git diff --check`。

## 2026-08-08：C 端二次确认弹窗层级修复留痕

- 已将业务弹窗、Toast、系统确认根节点/遮罩/内容统一为 `1000/2000/2147483000/2147483001/2147483002`，覆盖修改密码及全部 `uni.showModal` 二次确认入口。
- 已补充 `#u-a-m`、`#u-a-t`、`uni-modal`、`systemDialog` 和直接子节点兼容处理；动态宿主插入时同步归一化，避免确认框短暂沿用默认层级。
- `pnpm.cmd run verify`、C 端 `build:h5` 均通过，仅有 Sass 弃用警告；人工验收待有效登录会话和刷新 5185 服务后完成。
## 2026-08-08：非支付可上线版本规划启动

- 用户目标：先不等待在线支付，完成管理端非支付功能的真实可用性，并形成可上线版本。
- 已完成：盘点管理端模块、前端操作入口、后端管理接口和已有自动化测试；确认当前主要缺口是逐按钮运行验收与发布演练，而不是简单补充静态按钮。
- 本轮边界：在线微信/支付宝暂不作为放行条件；保留线下凭证审核链路；支付商户配置列为后续外部依赖。
- 下一步：按“内容维护 → 运营 → 用户/学员 → 订单/开票 → 权限/审计 → 部署恢复”顺序逐阶段实现，每阶段记录成功、取消、重复、失败和审计证据。
## 2026-08-08：C 端二次确认层级复核

- 复核账号与安全“修改密码”确认框及全部 `uni.showModal` 重要操作，业务弹窗统一为 `1000`，系统确认根节点/遮罩/内容统一为 `2147483000/2147483001/2147483002`。
- 复核管理端确认框，确认遮罩/内容为 `2147483646/2147483647`，Portal 挂载到 `document.body`。
- `verify`、C 端 H5 build、管理端 UI 验收/build、`git diff --check` 均通过；待刷新 5185 后完成真实账号人工点击验收。
## 2026-08-08：P1 浏览器验收第一条证据

- 在真实管理端会话中打开待支付订单的“关闭订单”确认框，点击“取消”后弹窗关闭、订单仍保留且状态不变。
- 课程新增表单空提交未创建数据，列表仍保持原内容；非法输入后端回归已覆盖 400 门禁。
- 下一步使用隔离临时数据补充课程/Banner/模板成功写入、刷新后可见、删除取消/确认和审计记录，再推进 P2 用户/学员。
## 2026-08-08：C 端反馈失败路径加固

- “问题反馈”增加内容裁剪/长度校验、二次确认、重复提交锁和 try/catch/finally 失败恢复；接口异常会显示可恢复提示。
- `mvp/scripts/verify.mjs` 增加反馈失败守护断言；C 端 H5 build、根目录 verify 通过。
## 2026-08-08：P1 课程管理真实成功闭环

- 使用唯一临时课程完成新增、二次确认、按标题刷新可见、编辑后删除、二次确认和刷新消失。
- 操作审计查询到课程维护与课程删除记录；临时数据已清理。
- P1 下一步：按同一标准验收 Banner、报名模板、订单关闭/线下凭证、开票和报名明细；支付在线渠道仍不纳入。
## 2026-08-08：P1 内容管理浏览器闭环扩展

- Banner 和报名模板分别完成临时新增、二次确认、查询刷新、删除确认、刷新消失。
- 操作审计均能查到维护与删除记录，临时数据已清理。
- 内容管理成功路径已覆盖课程/Banner/模板；下一步进入订单、线下凭证、开票和报名明细状态门禁。
## 2026-08-08：C 端二次确认弹窗层级回归修复

- 检测到 uni-h5 将确认框挂在 `#u-a-t` 时，将该容器提升到系统确认层 `2147483000`，必要时挂载到 `document.body`。
- 业务弹窗、Toast、系统确认保持 `1000/2000/2147483000/2147483001/2147483002`。
- `pnpm.cmd run verify` 与 C 端 `build:h5` 已通过；待刷新 5185 H5 做真实修改密码确认框验收。

## 2026-08-08：P1 反馈处理取消路径

- 真实管理端打开待处理反馈的“回复处理”弹窗，点击“取消”后弹窗关闭，列表内容和状态不变，未产生处理请求。
- 反馈成功回复继续使用后端隔离测试验证，避免污染现有演示数据。
## 2026-08-08：C 端二次确认弹窗层级回归修复

- 修复过宽的 uni-h5 兼容选择器：不再把确认框标题、正文和按钮强制定位为 fixed。
- 统一确认层级：业务弹窗 1000，系统确认宿主/遮罩/内容 2147483000/2147483001/2147483002；管理端保持 2147483646/2147483647。
- 真实 H5 验证“我的 → 账号与安全 → 修改密码 → 保存新密码”：确认框显示在业务弹窗上方，取消可关闭且不产生写请求。
- `pnpm.cmd run verify`、C 端 `build:h5`、`git diff --check` 已通过。
- 下一步：刷新 5185 后补齐其他 C 端确认入口和小程序容器复核。
## 2026-08-08：P4 管理端用户运营与配置运行态审计

- 学员档案详情、编辑取消、默认关系取消、解除关系取消已在真实管理端会话验证。
- 运营优惠完成临时新增、二次确认、分页刷新、删除取消、最终删除和审计查询，临时数据已清理。
- 积分管理完成零值/缺少原因校验、调整取消和积分流水查看；成功积分写入待隔离库补测。
- 系统配置完成编辑取消、临时新增确认、数据库清理和审计留痕；操作审计筛选/重置已验证。
- 记录文件：`Docs/Summary/2026-08-08_P4管理端用户运营与配置运行态审计记录.md`。
- 下一步：继续 C 端关键流程状态门禁，再进入服务器 Compose/PostgreSQL 对账与备份恢复。

## 2026-08-08：非支付上线规划重整与学员档案编辑边界修复

- 重新核对现有计划、代码、后端 15 套/64 项测试、管理端 UI 静态验收和生产构建；确认当前管理端主要按钮已有真实接口，剩余工作集中在逐按钮运行证据、状态门禁、部署恢复和发布审计，不是盲目补静态按钮。
- 发现学员列表返回脱敏手机号/邮箱时，管理端编辑入口曾尝试把脱敏值拼接成短值回写，导致“学员管理编辑”可能被误判为不可用。
- 已修复：脱敏手机号/邮箱进入编辑时留空；保存时仅提交操作员重新输入的完整敏感字段，留空不会覆盖服务器原值；补充编辑提示和静态门禁。
- `pnpm.cmd --dir frontend/admin-react test:ui`、管理端生产构建和 `git diff --check` 通过。
- 当前阶段：先继续 C 端报名、订单、凭证、开票、消息、学员状态门禁；支付在线渠道仍保持 disabled，之后再做 Docker/PostgreSQL 对账、备份恢复和回滚。
## 2026-08-08：C 端二次确认统一改为应用级高层弹窗

- 修复“我的 → 账号与安全 → 保存新密码”确认框被账号与安全业务弹窗遮挡的问题。
- 新增 `frontend/client-uni/src/common/confirm.ts`：H5 将确认框直接挂载到 `document.body`，非 H5 平台继续使用 `uni.showModal`。
- 统一所有重要确认入口（密码、资料、报名、支付凭证、开票、学员档案等）使用 `showClientConfirm`。
- 统一层级：业务弹窗 `1000`；确认根节点/遮罩/内容分别为 `2147483000/2147483001/2147483002`。
- 已通过根目录 `verify` 和 C 端 H5 构建；仍需刷新 5185 后使用真实登录会话做人工点击验收。

## 2026-08-08：C 端二次确认 z-index 回归修复

- 复核所有 C 端业务弹窗均为 `1000`，密码、资料、退出、报名、支付凭证、开票和学员操作均统一使用共享确认实现。
- `frontend/client-uni/src/common/confirm.ts` 的确认根节点、遮罩和内容改为内联 `!important` 层级，分别保持 `2147483000/2147483001/2147483002`，避免 uni-h5 后续渲染覆盖层级。
- H5 运行态已触发“修改密码”确认框，确认内容实际计算 z-index 为 `2147483002`，显示在账号与安全业务弹窗之上；取消和确定均可点击，未提交密码修改。
- `mvp/scripts/verify.mjs` 增加内联重要层级静态门禁；`verify`、H5 构建和 `git diff --check` 均通过。
- 记录文件：`Docs/Summary/2026-08-08_C端二次确认z-index回归修复.md`。
- 下一步：刷新/重启 5185 后完成各确认入口人工复核，再继续非支付内部发布验收和 Docker/PostgreSQL 持久化对账。

## 2026-08-08：确认层级常量统一补强

- 新增 `frontend/client-uni/src/common/modal-layer.ts`，`App.vue` 与 `common/confirm.ts` 共用同一组层级常量。
- 根目录 `verify` 已通过；运行态确认修改密码弹窗仍位于账号与安全弹窗之上。
- 本次本地 H5 构建在既有 uni 编译阶段停留并超时（输出 `os - Alias not found`），不将该次超时误记为构建通过；需刷新 5185 开发服务后复核。

## 2026-08-08：C 端错误态与重试闭环

- 完成首页、课程详情、订单/开票、消息、学员档案、报名和个人中心的加载失败状态改造。
- 请求失败不再直接清空数据或静默处理；页面显示失败原因和“重新加载”入口，成功返回空数组时仍显示“暂无数据”。
- 订单/开票和首页在已有旧数据时刷新失败会保留旧数据，并显示重试提示；消息标记已读失败会反馈但不阻断消息查看。
- 报名模板、报价和收款信息分别增加错误态/重试入口，学员档案回填失败时允许用户继续临时填写。
- 根目录 `verify` 新增 `clientErrorStatesHaveRetry` 静态门禁；`verify`、C 端 H5 构建和 `git diff --check` 均通过。
- 运行态检查首页、详情、消息、学员、订单和个人中心正常数据展示通过；尚需隔离环境模拟接口失败并补充重试成功证据。
- 记录文件：`Docs/Summary/2026-08-08_C端错误态与重试闭环.md`。
- 下一步：补充失败模拟与浏览器证据，再进入管理端写操作全量验收和 Docker/PostgreSQL 发布门禁。

## 2026-08-08：P1 C 端订单展示与取消闭环

- [x] C 端真实创建临时订单，完成报名确认、账单生成、线下转账信息展示和订单页跳转。
- [x] 验证取消确认的取消分支不写入；确认取消后 C 端、管理端刷新均显示“已取消”，审计记录可查。
- [x] 修复待选择支付渠道的订单误显示“微信支付”问题，改为“待选择支付方式”。
- [ ] 继续补充线下凭证上传/审核和开票成功路径的真实浏览器证据，再进入服务器发布门禁。
## 2026-08-09：修改密码二次确认弹窗层级人工复核

### 本轮复核补充

- [x] 修改密码确认框运行态位于账号与安全业务弹窗之上：业务弹窗 `1000`，确认根/遮罩/内容为 `2147483600/2147483601/2147483602`。
- [x] 复核密码、资料、退出、报名、凭证、开票、学员等重要 C 端操作统一调用 `showClientConfirm`；积分说明和反馈输入属于普通信息输入，不纳入二次确认层。
- [x] 根目录 `verify`、H5 构建和 `git diff --check` 通过。

- 已在运行中的 5185 H5 页面复现“我的 → 账号与安全 → 修改密码 → 保存新密码”确认框。
- 实际 DOM 层级为：业务弹窗 `1000`；确认根节点 `2147483600`；遮罩 `2147483601`；确认内容 `2147483602`，且确认内容是视口顶层元素。
- 确认框直接挂载在 `document.body`，取消按钮可关闭确认框，未触发密码修改请求。
- 已核对密码、资料、退出、报名、支付凭证、开票、学员档案等重要操作均统一调用 `showClientConfirm`；仅积分说明和反馈输入仍使用原生 `uni.showModal`，不属于二次确认。
- 根目录 `verify`、C 端 H5 构建、`git diff --check` 均通过；管理端确认层仍为 `2147483646/2147483647`，高于管理端业务弹窗 `1000`。

## 2026-08-09：非支付上线主线继续推进

- 重新盘点管理端模块、操作处理函数和后端路由；暂未发现大量“按钮无接口”的确定性缺口，剩余重点转为真实写操作证据和失败恢复。
- 已打开当前管理端运行会话，工作台统计与导航模块加载正常。
- 下一步优先执行订单/凭证/开票、用户/学员高风险操作的成功、取消、重复点击、刷新和审计核验；支付在线渠道继续不纳入本批次。

## 2026-08-09：规划恢复与自动化基线复核

- 已重新读取 `task_plan.md`、`progress.md`、`findings.md`，确认当前主线是非支付内部可上线版本，不将在线支付商户配置作为阻断条件。
- 当前自动化证据：根目录 `pnpm.cmd run verify` 通过；管理端 `test:ui` 通过；后端 15 套件、64 项测试通过。
- 管理端运行会话已可进入工作台并加载 18 个导航模块；读取链路正常，但写操作仍按 P1/P2/P3 逐项补齐运行态证据。
- 本批次下一步：先验证订单/线下凭证/开票/报名明细，再进入用户与学员；所有临时业务数据必须唯一、隔离并清理。

## 2026-08-09：P1 订单、凭证与开票运行闭环

- 通过 C 端真实报名创建唯一临时订单 `HX-1786210554512-579e76`，选择线下对公转账并上传临时凭证。
- C 端二次确认的取消分支验证通过；确认提交后订单从“待支付”变为“待审核”，重复提交入口被状态门禁隐藏。
- 管理端凭证审核验证了驳回理由必填、驳回确认取消和审核通过；通过后列表刷新为“已支付”，C 端刷新后同步显示“已支付”。
- C 端提交开票申请并在管理端完成开票通过、发票号码填写和电子发票上传；管理端刷新后显示“已开票/已上传”。
- 验收中发现并修复开票申请成功后弹窗不关闭的问题：提交中调用了会阻止关闭的普通关闭函数，现改为成功后显式关闭。
- 当前临时订单和发票用于运行证据保留，后续完成审计核对后再按唯一编号清理或标记为验收数据。
## 2026-08-09：P1/P2 隔离回归补强

- 运行 `admin-operations.spec.ts`、`admin-security-boundaries.spec.ts`、`p1-write-operations.spec.ts`、`p10-scenarios.spec.ts`，结果为 4 套件、13 项通过。
- 订单关闭/退款、凭证审核、开票处理、状态门禁、普通用户权限和旧令牌吊销均有回归证据。
- 新增学员档案导出回归：默认导出手机号脱敏且 `sensitiveFieldsMasked=true`；管理员显式 `reveal=true` 才返回明文，并保持 `phoneNormalized` 一致。
- 管理端 `test:ui` 和生产构建通过；本轮没有向当前运行数据库写入临时业务数据。
- 当前仍未把 P1 标记为完全放行：浏览器运行态成功路径、刷新失败/重试和服务器发布门禁仍待完成。

## 2026-08-09：P4 健康检查门禁补强

- 新增无需登录的 `GET /api/health`，实际执行数据库 `SELECT 1`，成功返回 `status=ok`、`database=ok` 和时间戳；数据库不可用时返回 503/degraded。
- Docker Compose API healthcheck 已从业务接口 `/api/courses` 切换为 `/api/health`，发布时可以明确区分 API 进程和数据库连接问题。
- `production.spec.ts` 与根目录 `verify` 已纳入健康检查契约并通过。
- 历史阻断：3100 旧 API 实例曾使新健康路径返回 404，已在本轮维护窗口重启项目 API 处理。
- 后端全量回归随后通过：15 套件、65 项测试；新增健康检查与导出边界测试均纳入结果。
- P3 运营回归新增后，全量回归更新为 15 套件、66 项测试；优惠规则冲突、消息已读幂等、配置非法键和积分零值均有门禁证据。
- 已在本机维护窗口仅重启项目 API 进程：`GET /api/health` 返回 200 且 `database=ok`，`/api/courses` 返回 200；管理端 5174 与 C 端 5185 均返回 200。旧 API 进程阻断已解除。

## 2026-08-09：Compose 发布脚本与健康检查统一

- `Docs/runtime/verify-compose-release.sh` 已同步使用 `/api/health`，并校验返回体中的 `status=ok` 与 `database=ok`。
- 根目录 `verify` 增加发布脚本健康探针门禁，防止 Compose healthcheck 与发布验收脚本再次分叉。
## 2026-08-09：C 端修改密码二次确认最高层级修复

- 将 C 端 body-level 确认根/遮罩/内容统一提升为 `2147483645/2147483646/2147483647`，与管理端最高层级契约一致，避免 uni-h5 后续系统层覆盖确认框。
- 确认根节点和遮罩增加可见性内联兜底；所有重要 C 端操作仍共用 `showClientConfirm`。
- `pnpm.cmd run verify`、C 端 `build:h5`、`git diff --check` 均通过。
- 运行态待刷新 5185 后复核修改密码、资料保存、退出、报名、订单/凭证/开票、学员操作等确认入口。
## 2026-08-09：修改密码二次确认弹窗层级恢复复核

- 已复核 C 端“我的 → 账号与安全 → 保存新密码”的真实 H5 运行态：账号与安全业务弹窗 `z-index=1000`，确认根/遮罩/内容分别为 `2147483645/2147483646/2147483647`，确认内容位于业务弹窗之上。
- 已全量扫描 C 端和管理端二次确认入口：C 端重要操作统一使用 `showClientConfirm`，管理端统一使用 Portal 挂载的 `app-dialog-backdrop/app-dialog`；未发现残留 `2147483600/2147483601/2147483602` 源码层级。
- 已通过根目录 `verify`、管理端 `test:ui`、C 端 H5 构建和 `git diff --check`。本轮未提交真实密码修改，仅点击了确认框的取消路径进行层级验证。
- 若浏览器仍显示旧层级，需停止旧的 5185 H5 进程后重新启动并强制刷新，避免旧 HMR 页面缓存。
## 2026-08-09：非支付版本差距复核

- 重新扫描管理端按钮、API 调用和后端路由后，未发现可证明的“大量静态占位按钮”；主要缺口转为真实浏览器写操作和失败恢复验收。
- 已将当前执行批次锁定为 P1：订单/凭证/开票/报名明细；完成后按 P2 用户与学员、P3 运营配置推进。
- 本轮不接入在线支付，支付适配器继续保持 disabled。
## 2026-08-09：P1 管理端浏览器验收准备

- 已打开管理端登录页并确认前端服务可达。
- `admin/admin` 登录失败，已停止继续猜测凭据；待使用项目已有测试账号或用户提供的授权账号后继续 P1。
## 2026-08-09：P1 订单页选择器修正

- 首次点击“关闭订单”命中已访问标签的关闭按钮，页面回到工作台；已确认没有触发后端写入。
- 已记录该前端验收陷阱，后续使用 exact 名称和表格行作用域继续验证。
## 2026-08-09：P1 管理端高风险写操作运行态回归

- [x] 订单关闭取消/确认：状态从“待支付”变为“已取消”，成功提示和列表刷新正常。
- [x] 退款取消/确认：状态从“已支付”变为“已取消”，成功提示和列表刷新正常。
- [x] 开票驳回：理由输入、二次确认、取消分支和成功状态均正常。
- [x] 线下凭证驳回：空理由阻止、二次确认取消、填写理由后成功驳回均正常。
- [ ] 继续补审计记录可见性和报名明细刷新失败/重试证据；完成后关闭 P1。

## 2026-08-09：P1 报名明细详情与刷新恢复验收

- 使用本地授权管理端会话进入“报名与交易 → 报名明细”，列表成功加载 29 条记录（第 1 页 5 条）。
- 点击第 1 条“查看详情”后，详情弹窗显示学员与课程、订单状态、所属账号、模板编号/版本和当次报名表单快照，确认报名明细不是静态占位入口。
- 关闭详情并刷新页面后，“报名明细”已访问标签仍保留；切回该标签后列表重新加载并恢复 29 条记录，证明标签导航和刷新后的数据恢复路径可用。
- 当前 P1 仍未关闭：需要在隔离环境模拟报名明细接口失败，验证错误态保留与“重新加载”恢复，并核对对应审计记录可见性。

## 2026-08-09：P2 用户与学员档案运行态回归

- 用户管理：对普通用户 `demo01` 执行“重置密码”，确认框确认后成功返回临时密码；随后使用临时密码登录 API，并通过个人密码接口恢复为 `123456`，避免污染测试账号。
- 用户启停：`demo01` 先从启用切换为停用，列表状态变为“否”；再次确认启用后恢复为“是”。
- 学员档案：打开 `stu-0401e1f0eeb72f12a9cbd5fa` 详情，保存原有公司字段，确认后成功提示“学员档案已更新”，详情与列表刷新正常。
- 操作审计：审计列表可见本轮“重置用户密码”“禁用用户”“启用用户”“学员档案更新”和“查看学员档案”记录。
- P2 未关闭：仍需完成关系授权/解除、默认关系、档案合并、脱敏导出和普通用户跨账号权限验证。

## 2026-08-09：P4 服务器可达性检查

- 对 `root@192.168.0.162` 进行只读 SSH 检查，返回 `Permission denied (publickey,password)`。
- 未继续猜测密码，也未修改服务器；Docker/PostgreSQL 的真实迁移、备份恢复和回滚必须等服务器配置好可用 SSH 密钥或由用户在服务器终端执行部署命令。
- 本地 P4 静态脚本仍可继续校验，但不能把本地结果当作服务器验收证据。
## 2026-08-09：C 端二次确认弹窗 z-index 统一恢复

- 已复核 C 端所有重要二次确认入口：修改密码、资料保存、退出登录、报名、取消报名、支付凭证、开票、学员档案保存/默认/解除/删除等，均通过 `showClientConfirm`。
- C 端层级契约统一为：业务弹窗 `90`；系统弹窗 `2147483000/2147483001/2147483002`；确认 Portal 根/遮罩/内容 `2147483645/2147483646/2147483647`。
- 确认框不再依赖 HTML `dialog` Top Layer，统一使用挂载到 `document.body` 的固定 Portal，并在首帧、下一帧、0ms、32ms 重新加固层级，避免被账号与安全、支付等业务弹窗覆盖。
- 管理端确认遮罩/内容继续使用 `2147483646/2147483647`，并增加 `!important`，保证高于管理端业务弹窗。
- 已通过：`pnpm.cmd run verify`、C 端 `build:h5`、管理端 `test:ui`、`git diff --check`。
- 人工验收时请刷新/重启 5185 H5 服务后再验证“我的 → 账号与安全 → 保存新密码”，旧 HMR 页面可能仍缓存旧层级实现。

## 2026-08-09：P1.1 管理端列表刷新失败恢复

- 修复 `frontend/admin-react/src/App.tsx`：列表刷新失败不再清空最近一次成功数据；首次加载失败仍显示完整错误页，已有数据刷新失败时保留列表并显示“刷新失败 + 重新加载”。
- 新增 `.list-refresh-error` 样式和管理端静态门禁，覆盖订单、报名明细、开票、用户、学员等服务端分页模块的局部恢复体验。
- 验证通过：管理端 `test:ui`、管理端生产构建、根目录 `pnpm.cmd run verify`、`git diff --check`。
- 下一步：继续 P2 关系授权/解除、默认关系、合并、脱敏导出和跨账号权限运行态验收；P1 的隔离接口失败模拟仍需可控代理或独立服务进程。

## 2026-08-09：P2 管理员账号安全边界补强

- 后端 `setUserEnabled` 新增两条硬性保护：管理员不能停用当前登录的管理员账号；系统不能停用最后一个启用的管理员账号，避免后台整体失去管理入口。
- 新增 `admin-security-boundaries.spec.ts` 回归，覆盖普通用户 403、敏感字段脱敏、旧令牌吊销、跨账号关系边界以及当前/最后管理员保护。
- 后端全量回归：15 个套件、67 项测试通过；API 构建、根目录 verify 和差异检查通过。
- P2 仍未完成：关系授权/解除、默认关系、合并、脱敏导出和普通用户跨账号权限还需浏览器运行态逐项留证。

## 2026-08-09：P2 学员档案合并目标选择器实现

- 发现管理端“合并到其他档案”仍使用原生输入框手工填写目标 ID，虽然可以调用接口，但容易误选、输错，也与账号授权选择器的交互不一致。
- 新增 `StudentMergeDialog`：按姓名、手机号、公司或档案 ID搜索活动档案，排除当前档案和已合并档案，选择后进入统一二次确认。
- 合并请求仍调用原有 `/admin/student-profiles/:id/merge`，并沿用写入锁、详情/列表刷新失败提示和审计链路；删除旧的手工输入实现，避免维护人员误用。
- 管理端 UI 静态验收、TypeScript/Vite 生产构建和根目录 `verify` 均通过。
- P2 下一步：在真实会话验证授权/取消、默认关系、解除关系、合并选择、脱敏导出和普通用户跨账号权限，并记录审计可见性。

- P2 后端专项回归：`student-api.spec.ts` 与 `admin-security-boundaries.spec.ts` 共 10 项通过。

## 2026-08-09：课程报名人数一致性边界补强

- 课程编辑页将“已报名人数”改为只读，并明确由报名、取消报名和退款自动维护。
- 后端 `saveCourse` 拒绝编辑时手工修改已有报名人数；新课程报名人数必须从 0 开始；缩减名额也不能低于当前报名人数。
- 管理端回归新增手工修改报名人数应返回 400 的断言；`admin-operations` 与 `student-api` 共 13 项测试通过。
- 下一步继续补 P2 关系/导出/跨账号运行态，随后进入 P3 运营模块。
- 中途全量回归发现非法课程名额错误文案与既有测试契约不一致，已调整校验顺序和文案；`mvp.spec.ts` 25/25 通过。

## 2026-08-09：非支付上线主线启动

- 重新盘点管理端 18 个导航模块、前端操作处理函数和 Nest 管理路由；确认主要按钮已有真实接口，后续不再以“按钮数量”作为完成标准。
- 当前主线锁定为：不等待微信/支付宝，先完成订单/凭证/开票/报名明细、用户/学员、消息/积分/优惠/反馈/配置/审计的业务闭环，再做 Docker/PostgreSQL 发布验收。
- 已将本次盘点结论写入 `findings.md`；下一步按 P1 → P2 → P3 逐项补成功、取消、重复点击、失败重试、刷新和审计证据，每阶段同步 `Docs/Summary` 和 `task_plan.md`。

## 2026-08-09：P3 运营模块并发安全补强

- 重新扫描管理端 18 个模块，确认主要操作入口均有真实前端请求和后端 `/admin/*` 路由；“按钮未实现”需与空数据、状态门禁和刷新失败区分。
- 将反馈处理改为带 `status='待处理'` 条件的原子更新，避免快速双击或并发请求覆盖已处理反馈。
- 新增并发回归：两个同时处理请求一成功一失败，原始回复保持不变；`mvp.spec.ts` 26/26、API 构建通过。
- 当前下一步：继续补 P1/P2/P3 浏览器运行态证据；服务器 Docker/PostgreSQL 仍是 P4 的独立阻断项。

## 2026-08-09：报名明细失败与重试隔离运行态验收

- 使用独立模拟 API 让 `/api/admin/enrollment-records` 首次返回 503，管理端“报名明细”正确显示错误态和“重新加载”。
- 恢复接口后点击“重新加载”，列表恢复并显示模拟报名记录；再次让接口失败并查询时，旧列表仍保留，同时显示“刷新失败 + 重新加载”。
- 该证据验证了失败恢复 UI，不涉及真实数据库或支付；隔离 API、管理端进程和浏览器标签已在验收后关闭。
## 2026-08-09：修改密码二次确认 Portal 恢复到 body

- 定位到确认框实现挂载在 `html` 直系节点与运行态 uni-h5 DOM 层叠上下文不一致，可能导致确认框落在“账号与安全”弹窗下面。
- 已将 `frontend/client-uni/src/common/confirm.ts` 和 `frontend/client-uni/src/App.vue` 统一改为 `document.body` 末尾 Portal，并保留根/遮罩/内容 `2147483647/2147483646/2147483647` 的 `!important` 层级。
- 已扫描修改密码、资料、退出、报名、订单/凭证/开票、学员档案等全部二次确认入口；均继续使用 `showClientConfirm`。
- C 端 H5 构建、根目录 `verify` 和 `git diff --check` 已通过；待刷新 5185 后进行运行态点击复核。

## 2026-08-09：非支付主线运行态复核续接

- 本地 API 已在隔离端口 `3199` 启动，`GET /api/health` 返回 `200`，响应包含 `status=ok`、`database=ok`；管理端 Vite 已在 `5174` 启动并可返回登录页。
- 使用本地授权管理会话做只读接口复核：订单列表返回 28 条，报名明细返回 29 条，学员档案返回 27 条，操作审计返回 143 条记录；这些接口均可达且响应结构符合管理端数据表要求。
- 已确认审计接口包含本轮高风险操作记录（重置密码、用户启停、学员档案查看/更新），报名明细接口返回模板版本、订单状态、账号和脱敏手机号等字段。
- 后端专项回归重新执行：`admin-operations.spec.ts`、`admin-security-boundaries.spec.ts`、`p1-write-operations.spec.ts`、`student-api.spec.ts` 共 4 套件、19 项全部通过；管理端 `test:ui`、C 端 H5 构建和根目录 `verify` 均通过。
- 当前浏览器自动化连接被本地 URL 安全策略拦截，本轮没有绕过策略；因此新增的是 API/构建证据，不把它误记为完整浏览器证据。P1/P2 继续保持未关闭，下一步在可控的浏览器或用户已授权会话中补关系操作、导出和审计筛选。

## 2026-08-09：P3 运营模块只读运行态复核

- 管理会话只读访问 `/admin/messages`、`/admin/points`、`/admin/configs`、`/admin/discount-rules`、`/admin/feedbacks` 和 `/admin/audits` 均返回 200；当前分别读取到 6、13、6、6、3 和 143 条记录（审计接口另带操作类型集合）。
- 该结果证明 P3 各模块已有可读数据源和统一管理接口；P3 的写入成功/取消/重复点击/失败恢复仍需在允许的浏览器会话中逐项验证，不能用只读结果直接关闭 P3。

## 2026-08-09：全量后端回归复跑

- `pnpm.cmd --dir backend/api test -- --runInBand` 通过：15 个测试套件、68 项测试全部通过。
- 覆盖认证/注册/找回密码、学员档案、管理操作、权限边界、P1 写操作、P3 并发、迁移回填、支付完整性和渠道适配器；在线支付测试仍是适配器安全门禁，不代表本轮启用真实商户支付。
## 2026-08-09：修改密码二次确认层级再次加固

- 针对“确认框在账号与安全弹窗下面”的反馈，统一复核 C 端业务弹窗、uni-h5 系统弹窗和自定义确认 Portal 的层级契约。
- C 端业务弹窗固定为 `90`，确认 Portal 固定挂载到 `document.documentElement`，根/遮罩/内容为 `2147483645/2147483646/2147483647`，并补齐四边定位与独立 stacking context。
- `pnpm.cmd run verify` 与 C 端 H5 构建均通过；仍需在重启后的 5185 H5 页面完成人工点击复核。

## 2026-08-09：C 端修改密码确认框真实 H5 运行态复核

- 本地 API 使用 `PORT=3100` 启动后，`GET /api/health` 返回 `200`，响应为 `status=ok,database=ok`；管理端 `5174` 和 C 端 `5185` 页面可访问。
- 通过 C 端注册流程创建临时本地验收账号并登录，进入“我的 → 账号与安全”，填写两次相同的新密码后点击“保存新密码”。
- 确认框真实显示在“账号与安全”父弹窗之上；运行时读取到确认根/遮罩/内容层级分别为 `2147483645/2147483646/2147483647`，三层均覆盖全屏或确认内容区域。
- 点击“取消”后确认根节点移除，账号与安全父弹窗仍保持打开，未提交最终密码修改；该结果验证了本轮最初遗留的覆盖关系和取消分支。
- 本地启动脚本曾因旧 H5 依赖缓存路径导致启动日志出现 `uni-h5.es.js` 解析错误；清晰区分为开发环境缓存/进程问题，当前 5185 页面已恢复可访问。临时验收账号保留在本地 SQLite，未上传外部环境。

## 2026-08-09：P2 学员关系管理浏览器运行态复核

- 使用现有本地管理员账号 `operator` 登录 `5174`，进入“用户与运营 → 学员管理”。列表真实加载 27 条档案，详情页显示授权关系和报名履历。
- 对学员档案 `stu-032180e84b045c16963b7e27` 通过账号搜索选择器选择 `demo01`，验证关系类型选择、继续授权和二次确认；确认后详情显示关系数量从 1 增加到 2。
- 将 `demo01` 设置为默认报名人，详情显示“默认报名人”；随后通过二次确认解除关系，关系数量恢复为 1，历史报名履历仍保留。
- 操作审计页按“授权学员关系”筛选后显示 1 条本次授权记录；未把接口结果或静态代码当作浏览器证据。
- 学员档案“导出”按钮在当前浏览器控制环境未触发可捕获下载事件，未宣称脱敏导出浏览器验收完成，P2 仍未关闭。

## 2026-08-09：本轮门禁复跑

- `pnpm.cmd run verify` 通过，确认 HTML Portal、确认层级、失败重试和管理端状态门禁均为 true。
- `pnpm.cmd --dir frontend/admin-react test:ui` 通过。
- `pnpm.cmd --dir frontend/client-uni build:h5` 通过；仅有 uni-app/Sass 依赖弃用提示，无构建错误。
- 支付配置检索仍显示发布脚本要求 `PAYMENT_ADAPTER=disabled`；本轮未启用真实在线支付。

## 2026-08-09：P1 退款刷新持久化与审计接口复核

- 管理端订单 `HX-1786210554512-579e76` 已通过退款二次确认，随后使用新的 `operator` 管理会话重新读取订单列表，订单状态仍为“已取消”，说明写入结果已持久化到当前本地数据库。
- 通过 `/api/admin/audits?action=退款完成` 筛选到本次记录：操作者为 `operator`，详情为 `HX-1786210554512-579e76`；筛选结果与订单 ID 一致。
- 对同一已取消订单再次调用退款接口返回 HTTP 400，未产生第二次退款副作用；这补充了后端终态门禁和幂等拒绝证据。
- 当前证据是“真实接口刷新/审计”复核，不冒充管理端浏览器刷新和审计筛选点击证据；P1 仍需在浏览器会话可控时补齐真实页面路径。

## 2026-08-09：P2 脱敏导出与普通用户权限接口复核

- 使用 `operator` 管理会话读取 `/api/admin/student-profiles/export?limit=1`，响应带 `sensitiveFieldsMasked=true`，手机号为掩码格式且不包含 `phoneNormalized`。
- 使用同一管理员会话读取 `reveal=true`，响应带 `sensitiveFieldsMasked=false`，手机号为完整 11 位并包含 `phoneNormalized`；该接口级结果与代码中的管理员明文揭示边界一致。
- 使用未修改的 `demo01/123456` 普通用户会话访问 `/api/admin/student-profiles`，返回 HTTP 403；未继续尝试已被重置密码的 `demo` 账号，也未修改任何账号密码。
- 以上是接口响应内容和权限边界证据；导出文件由前端 Blob 下载生成，仍需在浏览器中实际捕获并检查文件内容后，才能关闭 P2 导出项。

## 2026-08-09：续接时浏览器控制限制

- 5185 H5、5174 管理端和 3100 API 均可通过 HTTP 访问；但当前浏览器控制会话在创建/导航本地标签时返回标签页不属于当前会话的绑定错误。
- 未采用原始 CDP、换浏览器或其他绕过方式，因此本轮没有把 API/静态检查冒充为 H5 浏览器运行态验收；待浏览器会话恢复后优先补做重启/刷新后的确认弹窗和管理端点击证据。

## 2026-08-09：续接后回归门禁

- 后端全量回归：15 个测试套件、68 项测试通过。
- 根目录 `pnpm.cmd run verify`、管理端 `test:ui`、C 端 `build:h5` 和 `git diff --check` 均通过；H5 构建仅有 Sass/uni-app 弃用提示，没有编译错误。
- 本轮没有启用真实微信/支付宝支付，`PAYMENT_ADAPTER=disabled` 约束保持不变。

## 2026-08-09：P1 浏览器闭环与报名明细失败恢复

- 5185 H5 刷新后再次验证“我的 → 账号与安全 → 保存新密码”：确认根、遮罩、内容的运行时层级分别为 `2147483645/2147483646/2147483647`，父业务弹窗层级为 `90`；点击取消后确认根移除、父弹窗和输入保留，未提交密码。
- 5174 管理端刷新后订单 `HX-1786210554512-579e76` 仍显示“已取消”；操作审计页面筛选“退款完成”显示首条 `operator / HX-1786210554512-579e76`。另核对“关闭待支付订单”“线下支付审核通过/驳回”“开票处理”“上传支付凭证/电子发票文件”“查看报名履历”等 P1 审计筛选均有记录。
- 使用独立 3198 模拟 API 和 5175 管理端浏览器完成报名明细失败恢复：首次 503 显示错误态；接口恢复后点击“重新加载”显示 `enr-mock`；已有数据上再次 503 时，旧记录仍保留并显示“刷新失败：模拟报名明细接口暂时不可用”和“重新加载”。隔离进程随后已停止。
- 结合既有订单/凭证/开票成功、取消、重复提交和状态门禁证据，P1/P1.1 已达到当前计划的闭环条件。

## 2026-08-09：P2 合并选择器与脱敏导出浏览器复核

- 在“学员管理”中搜索 `验收`，打开临时源档案 `UI凭证验收学员-868937`，合并选择器搜索并选择临时目标 `UI验收临时学员-144610`，最终确认文案明确说明会迁移报名履历并标记源档案为已合并；未点击最终“确定”，因此数据未改变。
- 真实点击学员导出后页面提示“已导出 4 条脱敏学员档案”；操作审计筛选“导出学员档案”首条为 `4/4 条，脱敏`。浏览器下载事件仍未被控制面捕获，尚不能声称已经检查下载文件字节内容；接口级导出内容和脱敏字段已另行复核。

## 2026-08-09：非支付运行配置纠偏

- 发现 3100 API 原先因本地开发配置缺少环境变量而使用 `fake` 支付适配器，公共支付设置也返回微信/支付宝为 true。
- 已在本地 `.env` 明确设置 `PAYMENT_ADAPTER=disabled` 并安全重启 3100；重启后 `/api/admin/integration-readiness` 报告三个支付适配器均为 `disabled`，`/api/payment-settings/public` 返回 `onlineWechatEnabled=false`、`onlineAlipayEnabled=false`。
- 本次只关闭本地非支付版本的在线支付能力，没有配置或调用真实微信/支付宝渠道。

## 2026-08-09：续接后 5185 H5 修改密码确认运行态复核

- 当前 3100 API、5174 管理端和 5185 H5 均可访问；H5 刷新后使用既有本地账号 `demo01/123456` 登录，未修改该账号密码。
- 进入“我的 → 账号与安全”，填写两次临时新密码并点击“保存新密码”后，确认框真实显示在账号与安全业务弹窗之上；运行时读取到确认根/遮罩/内容层级分别为 `2147483645/2147483646/2147483647`，业务弹窗遮罩为 `90`。
- 点击“取消”后确认根节点移除，账号与安全父弹窗仍存在，两个密码输入值仍保留；未点击最终“确定”，未产生密码修改写入。
- 该证据覆盖本轮遗留的重启/刷新后真实浏览器路径；不代表密码提交成功，也不改变 P2/P3/P4/P5 的未完成状态。

## 2026-08-09：P2 脱敏导出文件捕获续接

- 管理端真实登录 `operator/123456` 后进入“用户与运营 → 学员管理”，先展开导航组再打开学员列表；页面真实显示 27 条档案和“导出”按钮。
- 触发“导出”后未产生浏览器标准 `download` 事件；页面实现是 `Blob + URL.createObjectURL + anchor.click()`，与既有记录一致。
- 检查本机用户 Downloads 目录也未发现本次生成的 `student-profiles-masked.json`，因此本轮没有把“导出按钮已触发”升级为“已检查磁盘文件字节”；后续仍需保留该证据差异。

## 2026-08-09：P3 反馈、消息、积分浏览器写操作

- 反馈管理：打开 `FB-DEMO-008` 回复表单后先点击取消，列表保持待处理；重新填写“已记录您的课程收藏建议，后续评估排期。”，经过“操作确认”后保存，刷新后的列表显示“已处理”、回复内容保留且该行不再显示“回复处理”。
- 消息通知：新增临时消息 `验收临时消息-20260809`，取消分支未写入；确认发布后列表出现唯一 `MSG-1786282970119-152ce0`，随后通过停用确认将启用状态从“是”改为“否”，保留该临时记录以便审计追踪。
- 积分管理：对 `u-demo-01/陈晓雯` 的取消分支未写入；确认 `+1` 后余额由 36 变为 37，再确认 `-1` 恢复为 36；积分流水弹窗显示两条相反变更和对应流水编号，证明写入、恢复与流水可见。
- 以上均为本地非支付验收数据；未触碰真实支付或服务器环境。反馈 `FB-DEMO-008` 的处理是既有待处理测试记录的真实状态变化，不能声称已回滚。

## 2026-08-09：P3 优惠规则、系统配置与审计筛选

- 优惠规则：在新增表单输入最低人数 `1`、折扣 `1.2` 时保存被拒绝并显示“请填写有效的人数门槛和 0~1 折扣比例”；取消无写入。随后新增 `99` 人、`0.95` 折临时规则，确认后在第 2 页真实出现，再经停用确认变为“否”。
- 系统配置：编辑 `invoiceDays` 时先取消并确认列表仍为 `7`；随后确认改为 `8`，列表刷新显示 `8`，再确认恢复为 `7`，最终列表恢复原值。
- 操作审计：在真实审计页面选择“处理反馈”并查询，显示本轮 `operator / FB-DEMO-008` 记录；同一页面已观察到本轮积分、优惠规则和系统配置维护记录位于最新审计结果中。
- P3 当前仍保持未关闭：消息/积分/规则/配置的失败重试和重复提交需要更多证据，审计筛选还应补充关键操作类型后再形成阶段结论。

## 2026-08-09：续接后自动化、支付门禁与 P4 SSH 复核

- 根目录 `pnpm.cmd run verify` 通过，所有静态发布门禁为 true；管理端 `test:ui` 通过；管理端构建和 C 端 `build:h5` 均通过（仅有 uni-app/Sass 弃用提示）。
- 后端全量 Jest 通过：15 个测试套件、68 项测试；`git diff --check` 通过。
- `GET /api/health` 返回 200 且数据库为 ok；受保护 readiness 使用 `operator` 管理会话复核为 `channels.paymentAdapter=disabled`、`wechatPayment.adapter=disabled`、`alipayPayment.adapter=disabled`；公共支付设置 `onlineWechatEnabled=false`、`onlineAlipayEnabled=false`。
- P4 只读复核：`192.168.0.162:22` TCP 可达，但 BatchMode SSH 被服务器直接关闭，未取得 Docker/PostgreSQL 读权限；未猜测密码、未执行远程写操作。服务器迁移、容器重启、备份恢复和回滚仍为外部阻塞。

## 2026-08-09：P3 优惠规则冲突行为续测

- 新增优惠规则时先输入与 `rule-2` 相同的最低人数 `2`、折扣 `0.95`，经过二次确认后接口仍保存记录，并在列表显示 `冲突规则=rule-2`；这证明当前产品语义是“允许保存并显式提示冲突”，不是硬失败拒绝。
- 将该冲突测试记录 `rule-1786283589509-4a7ab8` 停用后，原 `rule-2` 的冲突提示恢复为“无”；新增记录状态为“否”。该测试记录已停用，没有保留新的有效优惠规则。
- 该行为需要在 P3 发布清单中作为业务边界说明；若产品要求冲突必须阻止保存，应另立需求并修改后端，而不是把当前运行态误判为失败重试已完成。

## 2026-08-09：普通账号管理入口边界复核

- 在管理端退出 `operator` 后使用 `demo01/123456` 尝试登录，页面明确返回“账号或密码错误”，普通用户不能进入管理端会话；随后恢复 `operator/123456` 管理员会话。
- 该页面证据与既有普通用户访问管理接口返回 403 相互补充；没有继续猜测其他账号密码，也没有修改普通用户数据。

## 2026-08-09：P3 审计筛选补齐

- 真实操作审计页面分别筛选“停用消息”“用户积分调整”“优惠规则维护”“系统配置维护”，均返回本轮 `operator` 记录。
- “用户积分调整”筛选显示 `+1` 与 `-1` 两条相反流水；“优惠规则维护”筛选显示冲突测试的 `conflicts:["rule-2"]` 以及随后停用的 `enabled:false`；“停用消息”筛选显示临时消息编号；“系统配置维护”筛选显示 `invoiceDays` 的变更与恢复。
- P3 的真实成功/取消/边界/恢复/审计路径已较完整，但网络失败重试和重复点击的页面级证据仍不足，不能在本轮直接标记 P3 完成。

## 2026-08-09：P3 重复提交竞态修复与回归

- 复核 `frontend/admin-react/src/App.tsx` 后发现 `runOperation` 只读取异步 React state 的 `operationKey`，同一事件循环内的极快重复提交存在竞态窗口。
- 已增加 `operationKeyRef` 作为同步门闩：进入操作前先检查 ref，成功占用后才更新 state；finally 仅在 ref 仍属于当前 key 时清理，避免并发操作互相解锁。
- `frontend/admin-react/scripts/verify-ui.mjs` 已增加 ref 门闩静态断言。
- 验证：管理端 UI 静态验收通过；管理端构建通过；根目录 verify 通过；后端 15 套件/68 项测试通过；`git diff --check` 通过。
- 该修复增强了实际防护，但本轮应用内浏览器 URL 安全策略仍阻止 5185 页面动作，因此网络失败重试和双击行为仍未取得独立页面级证据。

## 2026-08-09：P2 导出短号码脱敏修复

- 直接读取管理端导出 HTTP 响应时发现历史异常手机号 `123132` 原样返回，虽然响应声明 `sensitiveFieldsMasked=true`；根因是旧 `maskPhone` 对长度小于 7 的值直接原样返回。
- 已修复 `maskPhone`：合法 11 位手机号返回前三后四掩码；所有其他非空值全部返回等长星号，避免异常/短值泄漏。
- 新增 `admin-security-boundaries.spec.ts` 回归，验证短号码默认导出为 `******`；专项测试通过。
- 重启 3100 后再次读取默认导出响应：HTTP 200、27 个手机号字段、未掩码数量 0、`sensitiveFieldsMasked=true`、没有 `phoneNormalized`。
- 该证据是 HTTP 响应正文检查；In-app Browser 仍未捕获标准 download 事件，不能据此声称 Downloads 文件已验收。

## 2026-08-09：P2 导出附件响应标准化

- 为 `/admin/student-profiles/export` 增加标准附件响应头 `Content-Disposition: attachment; filename="student-profiles-masked.json"`，并在权限边界测试中断言文件名。
- 重启 API 子进程后真实检查：HTTP 200、附件文件名正确、`sensitiveFieldsMasked=true`、27 个手机号字段未掩码数量为 0、无 `phoneNormalized`。
- 浏览器标准 download 事件和 Downloads 文件仍未捕获；本轮结论保持为“HTTP 附件契约已验证”，不升级为“浏览器落盘文件已验证”。

## 2026-08-09：P2 明文导出附件命名修正

- 发现管理员 `reveal=true` 时仍返回 `student-profiles-masked.json` 的误导性文件名，已修正为 `student-profiles.json`。
- 运行态验证：默认导出返回 `attachment; filename="student-profiles-masked.json"`，明文导出返回 `attachment; filename="student-profiles.json"`；两者均 HTTP 200。

## 2026-08-09：最新运行态复核

- API `/api/health` 返回 `status=ok,database=ok`。
- readiness：`paymentAdapter=disabled`，微信支付和支付宝支付适配器均为 `disabled`；公共在线支付开关均为 `false`。
- 重启后的默认/明文导出附件文件名分别为 `student-profiles-masked.json` 与 `student-profiles.json`。

## 2026-08-09：HTTP 下载字节证据补记

- 从标准 HTTP 附件响应下载到一次性临时文件并读取实际字节：`13651` 字节，SHA-256 为 `25273DD028EB26041A2ECA772FB017C8CAF6A53B8B38CE2E4E55F309E217E82D`。
- 内容检查结果：`sensitiveFieldsMasked=true`；手机号字段 27 个；未掩码数量 0；不含 `phoneNormalized`。临时文件随后已清理。
- 该结果证明 HTTP 下载内容满足默认脱敏边界，但不证明浏览器 Downloads 目录存在落盘文件。

## 2026-08-09：本轮浏览器策略复核

- 尝试从现有应用内浏览器标签接管 5185 H5 时再次被 URL 安全策略拒绝。
- 未使用原始 CDP、其他浏览器、替代 URL、Cookie/存储读取或其他绕过方式；P2 浏览器下载、P3 页面级失败重试与重复点击证据继续保持未完成。

## 2026-08-09：续接后的自动化与本地运行态复跑

- `pnpm.cmd run verify` 通过。
- 后端全量 Jest 通过：15 套件、68 项测试。
- 管理端 `test:ui`、管理端生产构建、C 端 H5 构建和 `git diff --check` 均通过。
- 本地 `/api/health` 返回 200 且数据库为 ok；`/api/payment-settings/public` 返回 200，在线微信/支付宝开关均为 false。
- 工作区 `.env` 中 `PAYMENT_ADAPTER=disabled`。管理 readiness 接口本轮未带 token，返回 401；不将其误记为运行态失败，也不猜测或读取凭据。

## 2026-08-09：P3 剩余缺口代码审查

- 已确认管理端 `runOperation` 使用 `operationKeyRef` 同步互斥和 state UI 门禁；消息、积分、优惠、系统配置、反馈等写操作均复用该路径。
- 已确认列表刷新失败不会清空最近成功数据，并提供局部“重新加载”入口。
- 这些发现支持静态/自动化门禁，但不升级当前缺失的浏览器级网络失败和双击证据。

## 2026-08-09：学员档案操作重复提交竞态修复

- `operateStudentDetail` 已增加同步 `studentOperationKey`/`operationKeyRef` 门闩，覆盖学员启停、关系授权/默认/解除和合并等写操作。
- finally 按当前 key 清理 ref 与 state，避免重复操作或旧请求清理新请求状态。
- 验证通过：管理端 `test:ui`、管理端生产构建、根目录 `verify`、后端 15 套件/68 项测试、`git diff --check`。
- 仍未将静态/自动化结果冒充独立浏览器双击证据。

## 2026-08-09：学员详情刷新恢复缺口审查

- 确认详情刷新失败目前只通过 toast 提示“重新打开详情”，详情面板没有直接重载动作；列表刷新失败则已有局部重试按钮。
- 下一步补充详情重载回调和按钮，并覆盖静态门禁与构建验证。

## 2026-08-09：学员详情刷新直接重试入口完成

- 新增 `reloadStudentDetail`，详情面板新增“重新加载详情”按钮，复用受保护的详情和报名履历读取接口。
- 写操作后的详情刷新失败提示已改为指导用户点击“重新加载详情”；详情和列表同时失败时提示分别重试。
- 验证通过：管理端 `test:ui`、管理端生产构建、根目录 `verify`、后端 15 套件/68 项测试、`git diff --check`。
- 页面级故障注入和重试点击仍未能在应用内浏览器中完成，不能把静态/自动化结果当作浏览器证据。

## 2026-08-09：管理端上传路径复核

- 已确认主要管理端写操作统一经过请求锁；课程图片和收款码上传仍需单独检查其上传中状态是否能阻止快速重复提交。

## 2026-08-09：管理端上传锁缺口确认

- 课程图片与收款码上传函数未占用统一 `operationKeyRef`；课程图片输入也未在上传期间禁用。
- 下一步将两条上传路径接入 `runOperation`，并补静态断言和构建回归。

## 2026-08-09：管理端上传重复提交门禁完成

- 课程图片上传已接入 `runOperation('course-image-upload', ...)`，并在上传期间禁用文件输入。
- 微信/支付宝收款码上传已按渠道接入统一请求锁；上传时支付设置弹窗进入 busy 状态。
- 验证通过：管理端 `test:ui`、管理端生产构建、根目录 `verify`、后端 15 套件/68 项测试、`git diff --check`。
- 真实浏览器快速重复选择文件的证据仍受应用内浏览器 URL 策略限制。

## 2026-08-09：C 端业务写操作锁复核

- 已确认凭证上传、取消报名、开票和在线支付均有请求期间 ref 锁；下一步确认 `loadAll` 刷新失败时旧数据和重试入口的显示语义。

## 2026-08-09：C 端刷新恢复静态复核完成

- 已确认 `loadAll` 失败保留旧数据并显示“重新加载”，首次无数据失败不会显示空列表误导。
- 根目录 verify 已覆盖 C 端写锁和错误重试契约；真实断网/重试页面证据仍未取得。

## 2026-08-09：C 端与服务器只读复核

- C 端 H5 构建通过；根目录 verify 通过；`/api/health` 返回 200 且数据库为 ok；公共在线微信/支付宝均关闭。
- 目标服务器 22 端口可达，但本机 SSH agent 不可用，无法在不猜密码的前提下取得 Docker/PostgreSQL 服务器证据；未执行远程写入。

## 2026-08-09：学员档案操作锁竞态修复前记录

- 代码复核发现 `operateStudentDetail` 仍仅依赖 `operationKey` state；学员关系、启停和合并等操作的快速双击需要补同步 ref 门闩。

## 2026-08-09：续接运行环境与浏览器策略记录

- 本轮按现有脚本尝试恢复本地服务；API 3100 最终启动成功，`GET /api/health` 返回 `status=ok,database=ok`。H5 5185 启动过程先出现端口占用提示，等待残留启动过程完成后最终恢复监听。
- 重新接管已有 5185 标签并刷新时，应用内浏览器 URL 安全策略拒绝了该本地页面动作。未绕过策略，也未使用原始 CDP、其他浏览器或替代自动化方式。
- 现有 `task_plan.md`、`findings.md` 和 H5 总结文档已保存此前重启/刷新后的修改密码确认 Portal 运行态证据；当前新增缺口是 P3 网络失败重试和重复提交的页面级独立证据。

## 2026-08-09：管理端列表网络失败与恢复重试运行态复核

- 管理端当前未提交 `.env.local` 使用 3199 API；本轮在 3199 启动后进入真实“学员管理”页面，列表显示 27 条记录，手机号默认脱敏。
- 暂停本地 3199 API 后点击“查询”，页面真实显示“刷新失败：Failed to fetch”，同时保留原有 5 条列表，不误显示空列表。
- 恢复 3199 API 后点击“重新加载”，页面错误提示消失，列表恢复为 27 条、第 1/6 页；该证据补齐管理端列表刷新失败/旧数据保留/直接重试的一条页面级闭环。
- 点击“导出”后应用内浏览器仍未收到标准 `download` 事件；导出页面触发与 HTTP 字节检查继续分开记录，不能升级为浏览器 Downloads 落盘证据。
- 本轮复跑结果：根目录 `verify`、管理端 UI 静态验收、管理端构建、C 端 H5 构建、后端 15 套件/68 项测试均通过。

## 2026-08-09：5185 H5 运行态续接边界

- 5185 H5 服务当前正常监听并可返回根页面；3100/3199 API 均健康，数据库状态为 ok。
- 重新建立应用内浏览器后仍无法创建新的可控标签；没有尝试替代 URL、替代浏览器、原始 CDP 或读取 Cookie/存储。
- 既有“修改密码确认 Portal 位于业务弹窗之上、取消不提交”的刷新后证据继续有效，但本轮没有新增 C 端页面级证据；P2 浏览器下载和 C 端 P3 失败/重复操作证据仍未完成。

## 2026-08-09：C 端消息与课程详情刷新恢复修复

- 消息页刷新失败时不再通过互斥分支隐藏已有消息；旧消息继续显示，错误提示提供局部“重新加载”入口。
- 课程详情刷新失败时不再清空已加载课程；已有详情继续显示，错误提示提供局部“重新加载”入口；无已有详情的首次失败仍显示完整错误页。
- 新增静态门禁 `clientRefreshPreservesLoadedData`，并通过根目录 verify、C 端 H5 构建、管理端 UI 静态验收和 `git diff --check`。
- 真实 C 端断网/重试页面证据仍受应用内浏览器会话限制，未将本次源码修复当作页面级证据。

## 2026-08-09：C 端学员关系同步操作锁

- 修复 `students.vue` 中保存学员、设为默认和解除关系在确认弹窗期间可能重复触发的问题。
- 新增 `studentOperationKey`，在确认前同步占用，取消确认、请求成功和异常均通过 `finally` 释放。
- 操作期间禁用相关编辑、默认、解除、关闭和保存入口，并增加静态门禁 `clientStudentsHaveSynchronousWriteLock`。
- `pnpm.cmd run verify`、`pnpm.cmd --dir frontend/client-uni build:h5` 和 `git diff --check` 均通过；构建仅有既有 Sass/uni-app 弃用提示。
- 独立浏览器快速双击/网络失败重试证据仍受应用内浏览器当前无法建立可控标签的限制，未用静态结果替代运行态证据。

## 2026-08-09：C 端“我的”页写操作同步锁

- 发现个人资料、密码修改和反馈提交均在确认框返回后才设置 busy 状态，确认等待期间存在重复触发窗口。
- 新增 `mineOperationKey`，覆盖保存资料、保存密码和反馈提交；反馈的可编辑输入框等待期也纳入锁范围。
- 操作期间禁用保存/关闭入口；新增 `clientMineHasPreConfirmationWriteLocks` 静态门禁。
- 首次回归因旧反馈断言耦合固定换行失败，已改为语义令牌断言；随后 `verify`、H5 构建、管理端 UI 验收、后端 15 套件/68 项测试和 `git diff --check` 全部通过。
- 应用内浏览器接管 H5 标签仍被 URL 安全策略拒绝；没有新增 C 端页面级双击或失败重试证据。

## 2026-08-10：5185 H5 服务恢复尝试

- 本轮检查发现 5185 实际没有监听；使用项目现有 `Docs/runtime/start-client-h5.cmd` 在隐藏进程中启动 H5。
- 启动后通过 `Invoke-WebRequest http://127.0.0.1:5185/` 确认 HTTP 200，说明服务层已恢复。
- 应用内浏览器已有 H5 标签显示启动前的 `ERR_CONNECTION_REFUSED` 页面；接管后从 `data:` 错误页导航到同一 5185 地址被 Browser Use URL 策略阻止。
- 未使用替代地址、其他浏览器、原始 CDP、Cookie/存储读取或其他绕过；页面级运行态证据继续保持缺失。

## 2026-08-10：H5 修改密码确认框真实运行态复核

- 5185 服务恢复后，通过新应用内浏览器标签登录 `demo01/123456`，进入“我的 → 账号与安全”。
- 填写两次相同的新密码，点击“保存新密码”后真实确认 Portal 出现；计算样式显示业务弹窗 `z-index=90`，确认根/遮罩/内容为 `2147483645/2147483646/2147483647`。
- 点击确认框“取消”后，确认 Portal 节点数量为 0，父业务弹窗数量为 1 且仍显示“账号与安全”；未点击“确定”，没有提交密码修改。
- 该项从“仅有既有历史证据”升级为当前 H5 新标签的真实运行态证据；其他 C 端入口仍待逐项验证。

## 2026-08-10：学员关系确认锁运行态复核

- 新 H5 标签进入“我的 → 我的学员”，加载 2 条其他学员。
- 点击第一条“解除”后确认框出现；运行时读取到确认根存在，6 个编辑/设为默认/解除操作均带 `action-disabled`。
- 仅执行到确认框和取消前状态检查，未确认解除关系；页面级保存/默认/失败重试证据仍未完成。

## 2026-08-10：学员关系取消分支补充

- 解除关系确认取消后，列表仍显示 2 个解除入口，确认 Portal 已移除。
- 设为默认确认框可正常打开并在取消后恢复列表；未改变默认关系。
- 新增学员填写临时姓名后打开保存确认框；点击取消后保存编辑弹窗仍保留，确认 Portal 已移除，未写入数据。
- 本轮仍未点击任何“确定”，避免污染测试数据；网络失败/双击请求次数和成功后刷新对账证据仍待补齐。

## 2026-08-10：C 端网络失败注入尝试边界

- 为验证学员列表旧数据保留，先确认 3100 由 Node PID 监听并停止该单个子进程。
- 项目开发监视器立即以新 PID 恢复监听，`/api/health` 仍返回 200；本次没有形成真实断网状态。
- 未继续猜测或终止未知父子进程树，避免影响其他本地服务；C 端网络失败/重试页面证据继续未完成。

## 本轮续接：H5 恢复与导出下载边界

- 复核运行态：`http://127.0.0.1:3100/api/health` 返回 HTTP 200、`status=ok,database=ok`；`http://127.0.0.1:5185/` 返回 HTTP 200。
- 原 H5 错误页标签从 `data:` 错误页刷新到 5185 再次被应用内浏览器 URL 安全策略阻止；未绕过该策略。
- 通过同一浏览器会话已有空白标签正常加载 5185，真实 DOM 显示首页及“我的”入口；进入“我的”后显示测试账号 `demo01`，说明可继续进行受控 H5 验收。
- 管理端学员管理页真实点击“导出”，本次 `waitForEvent('download')` 10 秒超时且页面未提供可见下载完成提示；随后对 Downloads 目录中的最新同名文件完成独立字节校验，文件落盘证据与本次事件捕获结果分开记录。
- 本轮验证通过：`pnpm.cmd run verify`、`pnpm.cmd --dir frontend/admin-react test:ui`、`pnpm.cmd --dir frontend/client-uni build:h5`、`git diff --check`。

## 本轮补充：P2 导出文件落盘验证

- 在 `C:\Users\EDY\Downloads` 发现最新 `student-profiles-masked (5).json`，大小 `19910` bytes，写入时间 `2026-08-09 23:50:50.8308766 +08:00`。
- 文件导出时间为 `2026-08-09T15:50:50.825Z`，与本地文件时间对应；正文确认 `total=27`、27 个手机号字段全部包含掩码、`sensitiveFieldsMasked=true`、`truncated=false`，未出现 `phoneNormalized`，文件以 JSON 对象闭合。
- SHA-256 为 `C89AAD9D67619F0261B1672943690FF3BF2EF0599BFC452FFDF586A52942EFC0`。
- 该结果证明 Downloads 文件已实际落盘；应用内浏览器本轮的标准 `download` 事件仍未捕获，两个证据等级分别记录。

## 本轮补充：可控 H5 密码确认取消复核

- 通过同一可控 H5 标签进入“我的 → 账号与安全”，填写两次相同的新密码并打开确认框。
- 真实 DOM 父链显示确认根 `.client-confirm-root` 挂载在 `HTML` 下，`z-index=2147483645`；确认内容 `.client-confirm-dialog` 为 `2147483647`；业务弹窗 `.modal-mask` 为 `90`。
- 点击“取消”后 `.client-confirm-root` 数量为 0，业务弹窗仍为 1；未提交密码修改。

## 本轮续接：C 端反馈快速双击幂等证据

- 真实 H5 `demo01` 进入“问题反馈”，提交唯一临时内容 `C端双击幂等验收-1786291386284`。
- 在最终确认按钮上执行快速双击；页面最终只显示一次“反馈已提交”，确认框关闭。
- 只读查询当前 SQLite 数据库，按唯一内容返回 1 条记录：`FB-1786291402614-77aeb3`，用户 `u-demo-01`，状态 `待处理`。
- 该证据证明本次 C 端快速双击没有产生重复反馈记录；仍不能等同于底层浏览器网络请求计数。

## 本轮错误记录：临时 H5 故障注入未启动

- 尝试在独立端口 `5190` 启动临时 H5，以便把 Vite `/api` 代理切换到不可用的 `3198`。
- `Start-Process` 两次因 PowerShell 环境变量 `PATH` 键冲突未启动；直接 `pnpm exec vite` 找不到 bin；直接 Vite CLI 又因 uni-app 工作目录/依赖解析失败。
- 临时配置已清理；未修改跟踪中的 Vite 配置，未停止或重启现有 5185/3100 服务。
- 因此本轮仍未取得 C 端网络失败/旧数据保留/重试按钮页面级证据；不能把该尝试当作失败恢复验证。

## 2026-08-10：C 端消息页网络失败、旧数据保留与重试恢复

- 在可控 H5 标签 `http://127.0.0.1:5185/#/pages/messages/messages` 中先加载成功，页面显示 6 条真实消息。
- 针对已确认的 API 开发进程做短时停止；点击消息页“刷新”后，页面真实显示“刷新失败：请求失败：500”和“重新加载”，原有 6 条消息标题、时间和渠道仍保留。
- 使用项目 API 启动方式恢复服务；健康接口返回 `status=ok,database=ok`。随后点击“重新加载”，错误提示消失，6 条消息重新显示。
- 该证据证明消息页的网络失败、旧数据保留和重试恢复闭环；不扩大解释为全部 C 端页面，其他页面仍需按页面契约独立验收。

## 本轮新增证据边界

- 尝试通过页面只读评估读取 `performance` 资源条目，以统计刷新双击后的底层 API 请求数；当前评估环境中的 `performance` 不可用，三种语法/访问方式均未取得计数。
- 因此不把刷新双击请求数记为已证明；继续沿用消息页失败/旧数据保留/重试闭环和反馈最终数据库仅一条记录两类证据。
- 目标服务器 `192.168.0.162:22` 的本轮只读连通性/SSH 检查超时；未猜测凭据、未执行远程写入，P4 仍未关闭。

## 本轮收尾验证

- 根目录 `pnpm.cmd run verify` 通过，所有静态门禁为 `true`。
- `git diff --check` 通过；仅有既有工作树换行提示。
- API `/api/health` 返回 HTTP 200，`status=ok,database=ok`；H5 根页面此前已返回 HTTP 200。

## 2026-08-10：C 端课程详情刷新失败与重试恢复

- 修改 `frontend/client-uni/src/pages/detail/detail.vue`：顶部新增“刷新”入口，`retryLoad` 在加载期间直接返回，避免重复刷新请求；保留已有 `course` 数据直到新请求成功。
- H5 真实进入 `course-1` 课程详情，基线显示标题、费用、报名人数和完整课程简介。
- 停止 API 后点击“刷新”，页面显示“刷新失败：请求失败：500”和“重新加载”，旧课程内容仍完整显示。
- 恢复 API 后点击“重新加载”，错误提示消失，课程详情恢复正常。
- `pnpm.cmd run verify` 通过，新增 `clientDetailHasRefreshControl=true`；C 端 H5 构建通过。

## 2026-08-10：C 端订单业务页刷新失败与重试恢复

- 修改 `frontend/client-uni/src/pages/business/business.vue`：订单页顶部新增“刷新”入口，`loadAll` 增加 `loadInFlight` 门闩，刷新期间重复触发直接返回。
- H5 真实进入订单页，基线显示 2 条订单及其状态、金额和凭证提示。
- 停止 API 后点击“刷新”，页面显示“业务数据加载失败”和“重新加载”，原订单数据仍显示。
- 恢复 API 后点击“重新加载”，错误提示消失，订单和状态恢复正常。
- `pnpm.cmd run verify` 通过，新增 `clientBusinessHasRefreshControl=true`；C 端 H5 构建通过。

## 2026-08-10：C 端学员档案刷新失败与重试恢复

- 修改 `frontend/client-uni/src/pages/students/students.vue`：页面新增“刷新”入口，`load` 增加 `loadInFlight` 门闩，刷新期间或写操作期间入口呈禁用状态。
- H5 真实进入“我的学员”，基线显示 2 条其他学员及编辑/设为默认/解除入口。
- 停止 API 后点击“刷新”，页面显示“学员档案加载失败”和“重新加载”，2 条学员记录及操作入口仍保留。
- 恢复 API 后点击“重新加载”，错误提示消失，2 条学员列表恢复正常。
- `pnpm.cmd run verify` 通过，新增 `clientStudentsHasRefreshControl=true`；C 端 H5 构建通过。

## 本轮三类 C 端刷新门禁收尾

- 本轮代码变更覆盖课程详情、订单业务、学员档案三个页面的刷新入口和刷新中门闩。
- 根目录 `pnpm.cmd run verify`、C 端 H5 构建和 `git diff --check` 均通过。
- API `/api/health` 最终返回 HTTP 200，数据库状态为 `ok`；测试过程中停止的本地 API 已恢复。

## 2026-08-10：续接后恢复本地 H5 验收会话

- 重新核对规划、进度、发现和最近总结；工作区既有改动全部保留，未执行回退或清理。
- 当前本地 API `http://127.0.0.1:3100/api/health` 返回 HTTP 200，数据库状态为 `ok`；H5 `http://127.0.0.1:5185/` 返回 HTTP 200。
- 应用内可控浏览器的旧标签绑定已失效；按浏览器故障规范重新创建标签后，H5 首页真实加载成功，可见课程内容和“我的”入口。
- 管理端 `5174` 本轮启动脚本未在检查窗口内恢复监听；尚未把该结果当作业务验收证据，后续继续单独诊断。

- 新 H5 标签从首页进入“我的”，真实显示测试账号 `demo01`、账号与安全、我的学员、消息和反馈等入口；会话无需重新输入登录凭据。

- 进入“我的学员”后真实显示当前学员为空、其他学员 2 人（演示学员 2、演示学员 8），每条提供编辑/设为默认/解除入口；下一步验证设为默认成功和刷新对账。

- 点击第一条“设为默认”后真实出现确认框，提示“后续报名将优先回填该学员信息，确定继续吗？”，确认框提供取消和确定按钮；确认前未执行写入。

- 点击一次“确定”后，页面显示“默认学员已更新”；演示学员2进入“当前学员”区并标记“当前”，其他学员从 2 人变为 1 人，证明成功写操作反馈和列表状态更新均发生。

- 点击“刷新”后重新加载页面，演示学员2仍为当前学员、演示学员8仍为其他学员；默认关系成功状态完成刷新对账。

- 新增学员入口实际显示为“＋ 添加学员”，已用可见文本定位到唯一入口；准备使用唯一临时姓名验证保存成功路径，避免与既有档案混淆。

- 新增学员表单真实打开，字段包括姓名、手机号、企业、部门、职务、邮箱和“报名时优先使用该学员”；尚未提交。

- 首次按 `placeholder` 定位字段失败：uni-app H5 实际输入元素未保留 placeholder 属性。读取真实 DOM 后确认表单输入按顺序为姓名、手机号、企业、部门、职务、邮箱，后续改用结构定位。

- 使用 DOM 顺序填入唯一临时资料：姓名“验收临时学员-20260810-01”、手机号 `19900000042`、企业“验收临时企业”；点击保存后真实打开确认框，提示“保存后将更新学员档案，确定继续吗？”。

- 点击确认一次后页面显示“学员已保存”，其他学员从 1 人变为 2 人，并出现唯一临时档案及其手机号、企业信息。

- 保存成功后点击“刷新”，临时档案仍存在且字段一致，当前默认学员仍为演示学员2；保存成功和刷新持久化对账完成。

- 已按唯一临时姓名定位到对应学员行，行内只有该记录自己的编辑/设为默认/解除按钮；准备执行解除关系清理，不触碰其他学员。

- 解除确认提示明确说明历史报名记录仍保留；确认后页面显示“已解除关系”，临时档案从其他学员列表移除，其他学员数量恢复为 1。

- 清理后点击“刷新”，临时关系未恢复，页面仍显示当前学员演示学员2、其他学员仅演示学员8；临时测试数据已从账号关系列表中清理并完成刷新对账。

- 对演示学员8的“设为默认”执行 Playwright `dblclick` 未打开确认框，页面状态未变化；该事件与 uni-app H5 的双 tap 不等价，未将其当作重复提交门禁证据，改用两个紧邻语义点击继续检查。

- 两个紧邻普通点击同样未打开确认框；按钮仍可见且 `isEnabled=true`，控制台无 error/warn。未继续重复点击，也未产生演示学员8的写入证据；需结合源码诊断该交互边界。

- 源码确认 `makeDefault` 先占用 `studentOperationKey` 再等待确认；直接点击文本 span 的事件在当前浏览器控制层未触发可见确认框，改点其父级操作节点后确认框正常出现。点击取消后确认框消失、当前默认仍为演示学员2；已取得取消安全证据，但未取得底层重复请求次数证据。

- 重新打开“新增学员”表单，表单为空且保存按钮可见；开始验证空姓名校验失败后的表单保留与恢复输入。

- 空姓名点击“保存学员”后真实显示“请输入学员姓名”，新增表单仍保留，既有默认/其他学员列表未变化；确认是前端校验失败而非档案写入。

- 在失败后同一表单填入第二个唯一临时姓名/手机号/企业，点击保存重新打开确认框；失败状态可恢复到正常保存前置状态。

- 点击确认后页面显示“学员已保存”，临时学员 02 出现在其他学员列表；保存失败→恢复填写→确认成功闭环完成。

- 通过唯一姓名定位临时学员 02 的关系行，解除确认框再次显示历史报名保留提示；尚未点击最终解除确认。

- 确认解除后页面显示“已解除关系”，刷新后临时学员 02 仍不存在；列表恢复为当前演示学员2和其他演示学员8，失败恢复测试数据已清理。

- 本轮自动化复跑：根目录 `pnpm.cmd run verify` 通过；后端 15 个测试套件/68 个测试通过；管理端 `test:ui` 通过；C 端 H5 构建通过；`git diff --check` 通过。
- 管理端 5174 初始即时检查未监听：启动脚本和直接子进程启动均遇到当前 PowerShell 环境重复 `Path/PATH` 键错误；未修改环境配置，随后延迟启动恢复。

- 延迟启动完成后，管理端 `5174` 与其 `.env.local` 指向的 API `3199` 均返回 HTTP 200；真实管理端登录页已加载。当前没有已授权管理员测试账号，本轮不猜测或重复提交密码。

- 3199 API 运行态检查：`GET /api/payment-settings/public` 返回 HTTP 200 且 `onlineWechatEnabled=false`、`onlineAlipayEnabled=false`；未授权访问 `/api/admin/integration-readiness` 和 `/api/admin/student-profiles` 均返回 HTTP 401。

## 2026-08-10：管理端授权运行态恢复

- README 明确记录演示管理员账号 `admin/123456`；使用该文档凭据登录 5174 成功，真实工作台加载课程、报名、订单、开票、运营和操作审计导航。
- 这不是猜测密码；此前 `admin/admin` 失败不再作为当前账号结论。下一步转入 P1 管理端订单/凭证/开票和审计可见性验收。

- 管理端工作台真实加载；订单与支付按钮 DOM 为 `<button title="订单与支付">`，第一次正则定位点击后页面未切换，已记录并改用精确按钮定位继续诊断。

- 精确 Playwright 点击和可视 DOM 节点点击订单按钮均未切换模块，工作台仍显示首页；未据此判断订单接口失败，准备刷新管理端标签恢复 React 会话后重试。

## 2026-08-10：续接后本地服务与 H5 会话复核

- 重新核对当前工作区、规划文件和最近验收记录；既有修改全部保留，未执行清理、回退或重置。
- 当前已有本地进程监听 API 3100、配置 API 3199、管理端 5174 和 H5 5185；四个地址的 HTTP 检查均返回 200，3100/3199 `/api/health` 均为 `status=ok,database=ok`。
- 尝试再次启动同样服务时，API/H5/管理端分别报告端口已被占用；未杀掉未知进程，也未修改当前 PowerShell 的重复 `Path/PATH` 环境键。
- 应用内浏览器原有管理端登录页和 H5“我的学员”标签均可接管；H5 当前真实显示“暂无当前学员”和 2 条其他学员关系，说明登录会话仍可继续用于运行态验收。
- 最近遗留的 C 端“账号与安全 → 保存新密码”确认层级运行态验证尚未完成，下一步转到“我的”页面继续。

## 2026-08-10：C 端修改密码确认层级运行态闭环

- H5 真实进入“我的 → 账号与安全”，填写一致的临时测试值 `TestPassword-2026`。
- 点击“保存新密码”后真实出现“确认修改密码”确认框；截图确认白色确认框和遮罩显示在账号与安全业务弹窗之上，取消/确定按钮可见。
- 点击“取消”后确认层消失，账号与安全表单仍保留；未点击最终“确定”，未提交密码修改。
- 证据已写入 `Docs/Summary/2026-08-10_C端修改密码确认层级运行态验收.md`。

## 2026-08-10：P1 管理端订单退款与关闭运行态验收

- 恢复 `admin/123456` 授权会话后，进入已创建的“订单与支付”标签；订单列表真实显示 28 条数据。
- 退款成功：订单 `HX-1786204869428-cecd61` 初始为已支付，确认退款后页面提示“订单已退款，课程名额已回退”，状态变为已取消；点击查询刷新后仍为已取消且退款按钮消失。
- 关闭成功：筛选待支付订单，订单 `HX-1785832507481-c41cc5` 确认关闭后页面提示“待支付订单已关闭，课程名额已回退”，待支付列表由 8 条变为 7 条；刷新查询后该订单不再出现。
- 证据已写入 `Docs/Summary/2026-08-10_P1订单退款关闭运行态验收.md`。
- 重复退款/关闭的后端失败断言尚未单独闭环；当前 UI 已在成功刷新后移除对应操作入口，不能把入口消失扩大解释为再次请求失败。

## 2026-08-10：线下凭证审核发现种子资产缺失

- 管理端“待审核”筛选真实找到 `ORDER-DEMO-002`，点击“审核凭证”返回“支付凭证文件不存在”。
- 核对种子和存储目录后确认：种子引用 `demo-proof-002.png`，但仓库/当前本地存储未提供该文件；未修改审核业务逻辑。
- 下一步仅补齐与种子路径一致的本地演示 PNG 文件，然后重新执行审核通过/驳回运行态验收。

## 2026-08-10：线下凭证驳回与重新提交

- 补齐本地演示种子资产：在 `backend/api/storage/payment-proofs/demo-proof-002.png` 放入与种子引用一致的 PNG 文件；未修改审核业务逻辑。
- 管理端重新打开 `ORDER-DEMO-002` 的凭证审核弹窗，真实显示凭证预览和驳回/通过按钮。
- 输入驳回原因“凭证验收驳回：测试文件不清晰”，经过二次确认后页面提示“支付凭证已驳回”，待审核列表变为 0 条。
- H5 `demo01` 订单页真实显示该订单回到“待支付”和“上次凭证未通过”原因；选择新的本地测试凭证并确认上传后，页面显示“待审核”“凭证已提交，等待管理端审核到账”，且不再显示重复提交入口。
- 首次上传尝试把“打开上传弹窗”和“触发文件选择器”混为一步，文件选择器超时且未提交；后续按实际两步 UI 流程完成，未重复错误操作。

## 2026-08-10：P1 凭证审核通过、审计与开票运行态验收

- 管理端审核通过 `ORDER-DEMO-002` 后，待审核列表为 0；筛选并搜索订单显示“已支付 / offline”，C 端刷新同一订单显示“已支付”并出现“选择开票/申请开票”。
- 操作审计未筛选列表顶部显示本轮退款、关闭、凭证上传、驳回和审核通过记录；筛选“退款完成”返回 11 条，首条为本轮退款订单。
- 开票成功：`INV-DEMO-005` 填写发票号 `P1-DEMO-INV-20260810-005`，处理为已开票并上传 PNG，刷新后保持已上传。
- 开票失败恢复：`INV-DEMO-006` 上传 `.gitattributes` 得到类型限制错误；随后上传合法 PNG 成功，刷新后保持已上传。
- 证据已写入 `Docs/Summary/2026-08-10_P1凭证开票审计运行态验收.md`。

## 2026-08-10：续接本地服务与浏览器会话

- `Invoke-WebRequest` 实测 API 3100/3199 健康检查均返回 `status=ok,database=ok`；管理端 5174 与 H5 5185 根页面均可访问。
- 重新启动尝试因已有实例占用端口而自动退出，未杀掉未知进程、未修改环境变量或业务数据。
- 应用内浏览器现有标签仍可接管：管理端标签位于开票管理，H5 标签位于订单页；后续继续使用已授权管理会话和现有 C 端会话。

## 2026-08-10：P2 账号停用与旧令牌运行态验收

- 使用 `admin/123456` 和 `demo01/123456` 获取授权令牌；停用 `u-demo-01` 后，旧访问令牌访问 `/profile` 返回 HTTP 401。
- 重新启用账号后，同一旧令牌仍返回 HTTP 401；重新登录返回 HTTP 200，证明 `sessionVersion` 令牌失效门禁有效且恢复路径可用。
- 审计接口真实返回本轮“禁用用户”和“启用用户”记录；账号最终保持启用状态。

## 2026-08-10：P2 跨账号读取与脱敏导出运行态验收

- `demo01` 自有订单 2 条、`demo02` 自有订单 1 条；两个账号查询对方订单号均返回 `items=[]、total=0`，直接读取对方订单支付状态返回 HTTP 404；读取本人资源未越权。
- 未授权访问学员档案导出返回 HTTP 401。
- 管理端标准导出接口默认返回 `sensitiveFieldsMasked=true`，手机号显示为 `182****0668`；请求 `reveal=true` 返回明文手机号和 `phoneNormalized`，且 `operator` 与 `admin` 当前同属 `role=admin`，两者均可请求明文导出。该权限边界已记录，未修改角色模型。
- 导出审计真实返回本轮 admin/ operator 的“导出学员档案”记录；标准管理端 UI 导出路径不传 `reveal=true`。

## 2026-08-10：P3 反馈管理取消、处理与刷新运行态验收

- 反馈 `FB-DEMO-006` 打开“回复处理”弹窗后点击取消，弹窗关闭且仍保持“待处理”。
- 重新打开填写“已记录电子发票下载建议，纳入后续版本评估。”，点击保存后出现二次确认；确认后真实提示“反馈已处理”，列表显示“已处理”和回复内容。
- 点击查询刷新后 `FB-DEMO-006` 仍为“已处理”，完成成功后刷新持久化对账；审计需在后续汇总中核对。

## 2026-08-10：P3 消息与积分管理运行态验收

- 消息 `MSG-1786282970119-152ce0`：打开“启用消息”确认框后取消，状态保持“否”；再次确认启用后页面显示“是”，随后确认停用并点击查询刷新，最终恢复为“否”。
- 积分 `u-demo-02`：打开调整弹窗后取消；确认 `+1` 后页面积分由 52 变 53 并提示“积分已调整”，随后确认 `-1` 恢复为 52，刷新后仍为 52。
- 积分流水真实显示本轮 `+1` 和 `-1` 两条记录；审计接口真实返回对应的“启用消息”“停用消息”“用户积分调整”和“处理反馈”记录。
- 一次积分自动化序列因管理端会话短暂回到登录页而超时，未据此判定业务失败；重新登录后按确认步骤拆分复跑成功，最终数据已恢复原值。

## 2026-08-10：本轮验收复核

- `pnpm.cmd run verify` 通过，全部静态门禁为 `true`。
- `git diff --check` 通过；仅报告现有工作区的 LF/CRLF 转换提示，没有空白错误。

## 2026-08-10：5185 H5 重启后真实运行态验收

- 只停止占用 5185 的 Node PID `21140`（路径为 `D:\Environment\nodejs\node.exe`），未终止 API、管理端或未知进程；重新启动 `pnpm.cmd --dir frontend/client-uni dev:h5`。
- H5 Vite 日志显示 `http://127.0.0.1:5185/` ready，根页面 HTTP 200；日志中的 `os - 找不到别名` 和 Sass legacy API/deprecation 为开发工具提示，不影响启动。
- 原 H5 标签在服务重启后成为失效标签，按应用内浏览器规则通过现有用户标签重新接管；重启后登录态不假定保留，使用 `demo01/123456` 重新登录成功并进入首页/我的页面。
- 真实进入“我的 → 账号与安全”，填写一致临时密码后确认层显示在业务弹窗之上；截图和 DOM CUA 显示取消/确定，点击取消后确认层消失、密码表单仍保留，未执行最终密码修改。

## 2026-08-10：P1 当前运行 API 失败态复核

- `p1-write-operations.spec.ts`：2 个测试、2 个断言套件通过，覆盖订单取消、凭证重复提交/重复审核、开票重复处理/重复上传/重新申请、重复关闭和重复退款。
- 当前 3199 API 对已完成演示数据的重复请求真实返回 HTTP 400：重复审核、重复退款、重复关闭、重复开票处理分别返回状态门禁错误；失败请求未产生新的业务写入。
- 当前审计查询接口返回 HTTP 200；失败重复请求不写成功审计，既有成功动作的审计记录仍可按 action/keyword 查询。

## 2026-08-10：本轮续接错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| H5 旧标签在服务重启后不属于当前浏览器控制会话 | 1 | 丢弃旧标签绑定，按应用内浏览器用户标签精确接管新标签；未使用 Cookie/Storage/CDP 绕过 |
| H5 登录按钮未暴露为 Playwright button role | 1 | 读取当前 DOM 后使用可见“登录”文本节点定位，登录成功 |

## 2026-08-10：非支付内部测试发布文档收尾

- [x] 已创建 `Docs/Summary/2026-08-10_非支付内部测试发布报告.md`，结论为可进入本地/内网非支付内部测试，暂不批准生产部署或目标服务器发布。
- [x] 已创建 `Docs/Summary/2026-08-10_支付后续接入清单.md`，明确支付适配器、商户资料、回调验签、幂等、沙箱/小额验收和分阶段开关要求。
- [x] 发布报告记录自动化、P1/P2/P3、C 端失败恢复、支付禁用和运行服务证据，并区分已验证项与阻断项。
- [x] 最终冻结条件保持：不执行学员档案合并、不修改远程服务器、不启用真实微信/支付宝支付。
- [ ] 页面级快速双击底层请求次数、学员合并明确确认、目标服务器 P4 和 `operator`/`admin` 明文揭示权限决策仍未关闭。

## 2026-08-10：P2 管理角色与敏感字段权限收敛

- [x] 新增 `backend/api/src/auth/roles.ts`，明确 `admin`、`operator`、`user` 三种角色；管理端守卫允许前两者，JWT/刷新令牌保留真实角色。
- [x] 学员档案详情、报名履历详情和导出接口改为仅 `admin` 可揭示明文；operator 仍可管理业务但返回脱敏字段。
- [x] 种子和 SQLite 迁移保留 operator 角色；本地 `training.db` 的 `u-operator` 已事务性校正为 `operator`，会话版本递增以使旧会话重新鉴权。
- [x] 后端权限/生产/迁移/MVP 相关 4 个测试套件共 38 个测试通过；API 构建通过；`git diff --check` 通过。
- [x] 3199 真实运行态：operator 登录返回 `role=operator`，dashboard HTTP 200；admin 详情手机号为明文，operator 详情和 `reveal=true` 导出均为 `182****0668`，无 `phoneNormalized`。
- [x] API 3100/3199、管理端 5174、H5 5185 健康/根页面均 HTTP 200；支付公共设置仍为微信/支付宝 `false`。
- [ ] 页面级快速双击底层请求次数、学员合并明确确认和目标服务器 P4 仍未完成。
## 2026-08-10：C 端问题反馈最终确认快速双击运行态复核（补充）

- 复用当前 H5 `demo01` 会话，刷新后从“我的”进入“问题反馈”；填写唯一测试内容 `页面级快速双击幂等性验证-20260810`。
- 点击首层“确定”进入“确认提交反馈”二次确认；仅对最终确认层的“确定”执行快速双击。页面最终只显示一次“反馈已提交”，二次确认关闭。
- 只读核对本地 `backend/api/data/training.db`：该唯一内容匹配 `count=1`，记录为 `FB-1786305748966-315ff2`、用户 `u-demo-01`、状态“待处理”。
- 因浏览器评估环境不能可靠提供底层网络请求计数，仍不声称请求次数为 1；本次已取得页面反馈结果 + 数据库唯一落库两级证据。
## 2026-08-10：续接后恢复 H5 可控标签

- 旧 `h5Tab` 绑定已失效；按浏览器会话规则仅从现有用户标签中重新接管 `http://127.0.0.1:5185/#/pages/mine/mine`，未读取 Cookie/Storage，也未使用 CDP 绕过。
- 当前可控 H5 页面真实显示账号 `demo01` 的“我的”页，包含账号安全、我的学员、消息、反馈等入口；服务未因旧绑定失效而判定为业务失败。
## 2026-08-10：管理端授权会话恢复

- 从现有用户标签中接管管理端 5174 页面，真实 DOM 显示已登录工作台、报名与交易导航和订单列表；当前会话具备继续做运行态验收的权限。
- 当前订单列表真实显示 28 条记录，后续优先沿 P1 补报名明细失败/重试和审计可见性；不把登录成功或列表存在当成功能闭环。
## 2026-08-10：P1 报名明细真实页面基线

- 管理端授权会话进入“报名明细”，真实列表显示 29 条记录、课程筛选、查询/重置、导出和详情入口；当前默认手机号为脱敏显示。
- 该页面基线可用于下一步故障注入与重试验收；尚未把页面可见或列表成功误记为失败恢复已完成。
## 2026-08-10：P1 报名明细断网失败态真实验收

- 已验证 3199 监听者为单个 Node PID `17820`，仅停止该进程制造 API 断网；未触碰 3100、5174、5185 或数据库写操作。
- 管理端报名明细点击“查询”后真实显示 `刷新失败：Failed to fetch` 与“重新加载”按钮，同时保留原有 5 条列表数据、筛选和详情入口。
- 该结果证明失败态不会错误清空已加载报名明细；下一步恢复 3199 后点击“重新加载”，核对错误消失和数据恢复。
## 2026-08-10：P1 报名明细失败恢复闭环完成

- 恢复单个 3199 Node API 后，健康检查返回 HTTP 200、`status=ok,database=ok`。
- 点击管理端“重新加载”后，`刷新失败` 和“重新加载”入口消失，报名明细恢复为 29 条、第 1/6 页，原列表内容可继续查看。
- 本次未执行任何业务写操作；故障注入仅停止/恢复本地 3199 API，未修改数据库。
## 2026-08-10：P1/P3 审计页面真实可见性

- 管理端展开“系统管理”后进入“操作审计”，页面真实显示 230 条记录、操作类型筛选、操作者筛选、时间筛选和查询按钮。
- 首屏可见本轮 H5“提交反馈”审计及 operator/admin 学员查看、脱敏导出审计记录，证明成功操作能在管理端审计页被观察到。
- 下一步用已有订单退款/关闭操作类型筛选，补充 P1 成功操作的审计可见性证据。
## 2026-08-10：P1 审计筛选运行态补齐

- 操作审计页按操作类型筛选“退款完成”，真实返回 11 条记录；首屏包含 admin/operator 操作人、订单编号和创建时间。
- 这补齐了 P1 所需的成功操作审计可见性；与报名明细的失败/重试恢复证据结合后，P1 当前可以进入收尾核对。
## 2026-08-10：本轮自动化与服务收尾验证

- `pnpm.cmd run verify` 通过，静态门禁全部为 `true`。
- 后端 15 个测试套件、68 个测试通过；管理端 `test:ui`、管理端生产构建、C 端 H5 构建均通过。
- `git diff --check` 通过（仅有既有 LF/CRLF 转换提示）。
- API 3100/3199、管理端 5174、H5 5185 均 HTTP 200；3199 健康检查为 `status=ok,database=ok`，公共支付开关 `onlineWechatEnabled=false`、`onlineAlipayEnabled=false`。

## 2026-08-10：续接本轮启动

- 已重新读取 `task_plan.md`、`progress.md`、`findings.md` 及最新非支付内部测试发布报告。
- 已恢复真实管理端/H5 浏览器标签；管理端停留操作审计筛选，H5 停留“我的”页。
- P1 报名明细断网恢复、审计筛选和 H5 5185 重启后确认层级均视为已完成证据，不重复执行。
- 下一步：选择 P3 运营模块执行可恢复失败/重试与重复提交验证；完成后补跑相关自动化/构建并更新发布留痕。

## 2026-08-10：P3 入口诊断

- 首次点击“反馈管理”未切换标签；DOM 确认入口唯一，未把该交互现象误报为业务接口失败。
- 将改用刷新后精确点击并读取标签变化，必要时先展开“用户与运营”菜单，再继续 P3 失败恢复验证。

## 2026-08-10：P3 反馈失败态与服务恢复

- 反馈页真实完成断网失败态验证：旧列表保留、明确错误和重试入口均出现。
- 3199 已恢复并通过健康检查；一次错误工作目录启动导致管理端进入登录页，已停止错误实例并按 `backend/api` 工作目录重新启动；重新登录后反馈列表恢复。

## 2026-08-10：P3 服务恢复后重新授权

- 已纠正服务工作目录并恢复管理端授权，原有演示数据可见；误启动产生的零字节根目录数据库文件已清理。
- 记录了一次严格选择器冲突，后续使用精确名称完成反馈页恢复确认；不把自动化定位错误记为业务失败。

## 2026-08-10：本轮自动化与发布边界复核

- 根目录 `pnpm.cmd run verify` 通过，所有静态门禁为 `true`。
- 后端 Jest 15 个测试套件、68 个测试全部通过；管理端 `test:ui`、管理端生产构建和 C 端 H5 构建全部通过。
- `git diff --check` 通过；仅有既有 LF/CRLF 转换提示。
- 运行态：3100、3199、5174、5185 均在监听；3199 `/api/health` 返回 HTTP 200 且 `status=ok,database=ok`。
- 根目录 `.env` 明确为 `PAYMENT_ADAPTER=disabled`；3199 `/api/payment-settings/public` 返回 HTTP 200，`onlineWechatEnabled=false`、`onlineAlipayEnabled=false`。
- 本轮未启用真实微信/支付宝、未执行学员档案合并、未向目标服务器写入；发布结论仍限于本地/内网非支付内部测试。

## 2026-08-10：新一轮 H5 重启后运行态验证启动

- 5185 根页面 HTTP 200；重新接管真实 H5 标签后，`demo01` 的“我的”页面可见，账号/积分/消息数据正常。
- 下一步进入“账号与安全”验证保存新密码确认层的可见性和取消后表单保留；不会点击最终确认。

## 2026-08-10：H5 入口定位调整

- 可见文本节点点击没有触发交互；已确认真实页面使用 `uni-view.menu-row` 父级承载点击事件，改用父级行定位，不修改代码。

## 2026-08-10：H5 账号安全父级行复核

- 父级行唯一文本为“账号与安全登录账号和密码›”；常规点击仍未打开弹窗，未把它误报为业务接口失败。
- 下一步检查真实可点击子节点/事件语义，避免重复点击造成未知写入。

## 2026-08-10：H5 入口事件诊断错误

- 账号安全行 HTML 已确认；页面上下文 `el.click()` 不适用于该抽象节点并报错，未改变页面或业务数据。
- 后续改用浏览器提供的原生交互/tap 能力，不使用 CDP、Storage 或脚本绕过。

## 2026-08-10：H5 浏览器交互能力核对

- 已核对当前浏览器控制层公开的定位、快照和页面交互入口；下一步确认 locator 是否存在原生点击方法及目标行的可点击状态。

## 2026-08-10：H5 账号安全行状态

- 账号安全行唯一、可见、可用，仍未通过原生 click 打开弹窗；未继续盲目重复操作，准备检查页面截图/交互层状态。

## 2026-08-10：H5 账号安全交互层进一步证据

- 截图与计算样式确认入口行可见、可点击区域正常且没有遮罩；键盘 Enter 因焦点目标不匹配失败，未改变页面。
- 不使用脚本或 CDP 绕过；保留此前已完成的重启后确认层证据，当前新一轮仅记录交互桥接边界。

## 2026-08-10：H5 刷新后账号安全弹窗恢复

- 刷新真实 H5 标签后，按父级行点击成功打开“账号与安全”弹窗；下一步填写临时值并验证确认层，最终不提交密码。

## 2026-08-10：管理端 P3 会话恢复

- 已重新接管管理端真实标签，反馈管理页授权正常，当前显示 14 条记录；未新增或处理反馈数据。
- 下一步继续 P3 运营模块的页面级失败重试/重复操作证据。

## 2026-08-10：管理端消息模块基线

- 已从真实管理端会话进入消息通知页，列表显示 7 条；首行既有临时消息当前为停用状态。
- 选择该临时记录做启用/恢复验证，避免新增数据；尚未执行写操作。

## 2026-08-10：管理端消息启用快速双击与恢复

- 既有临时消息启用确认最终确定按钮快速双击后，页面只显示一次“消息已启用”，状态变为“是”；随后确认停用恢复为“否”。
- 未新增消息；底层网络请求次数未统计，不将页面结果扩大为请求次数证明。

## 2026-08-10：管理端消息列表故障注入准备

- 已确认并停止单个 3199 Node 进程 `6920`，没有停止其他本地服务或执行业务写入。
- 开始验证消息列表失败态与重试恢复。

## 2026-08-10：管理端消息列表失败与重试恢复

- 3199 停止后消息页显示 `刷新失败：Failed to fetch` 和“重新加载”，5 条旧消息仍保留。
- 正确工作目录恢复 API 后健康检查通过；点击重试后列表恢复为 7 条，临时消息仍为停用，未产生额外写入。

## 2026-08-10：本轮最终回归核对

- `pnpm.cmd run verify` 通过，所有静态门禁为 `true`；`git diff --check` 通过（仅有既有换行转换提示）。
- 3199 健康检查为 `status=ok,database=ok`，公共支付开关为 `onlineWechatEnabled=false`、`onlineAlipayEnabled=false`；3100/3199/5174/5185 均在监听。
- 本轮 H5 和 P3 消息运行态证据已同步到 `task_plan.md`、`findings.md` 和非支付内部测试发布报告；P3 其他模块、P2 合并、P4/P5 边界仍未关闭。

## 2026-08-10：新一轮管理端标签恢复

- 重新接管现有管理端标签 `58`，授权消息通知页正常显示 7 条记录，临时消息仍停用。
- 下一步选择 P3 积分/优惠/系统配置中可安全恢复的现有记录继续验证。

## 2026-08-10：系统配置模块基线

- 已进入系统配置页，列表显示 6 条；选择既有 `invoiceDays=7` 做修改后恢复验证，不新增配置。

## 2026-08-10：系统配置非法值分支

- 输入 `not-a-number` 后保存进入操作确认层，未点击确认、未写入数据库；先取消并退出编辑，再继续合法值恢复验证。

## 2026-08-10：系统配置非法值取消恢复

- 取消确认和编辑后，列表恢复显示 `invoiceDays=7`；未写入非法值、未产生额外审计记录。

## 2026-08-10：系统配置列表故障注入准备

- 已停止单个 3199 Node 进程 `4188`，未停止其他服务或执行配置写操作。
- 开始验证系统配置列表失败/重试恢复。

## 2026-08-10：系统配置列表失败与重试恢复

- 3199 断网后系统配置页显示失败提示和“重新加载”，5 条旧配置保留；恢复 API 后重试，列表恢复 6 条且 `invoiceDays=7` 不变。
- 未产生配置写入；系统配置失败/重试页面证据已补齐。

## 2026-08-10：本轮 P3 回归与文档收尾

- 根目录 `pnpm.cmd run verify` 通过；后端 15 个测试套件/68 个测试、管理端 `test:ui`、管理端构建、C 端 H5 构建和 `git diff --check` 均通过。
- 3199 健康检查为 `status=ok,database=ok`，在线微信/支付宝开关为 `false`/`false`；四个本地服务均正常监听。
- 本轮补齐 P3 系统配置非法值取消和列表失败/重试证据；P3 仍未整体关闭，P2 合并、P4/P5 外部/发布边界保持不变。

## 2026-08-10：H5 密码保存入口定位边界

- 两个密码框已填入临时值；文本节点点击未弹出确认层，读取该节点 HTML 超时，未重复点击或提交密码。
- 需要改用弹窗内真实可点击父级的结构定位，继续验证确认层。

## 2026-08-10：密码保存按钮实际状态

- 真实 DOM 显示保存按钮已处于 `disabled/loading`，确认层未出现；暂停进一步点击，先确认请求是否完成或出现错误提示。

## 2026-08-10：密码保存按钮忙状态未解除

- 等待后按钮仍处于 `disabled/loading`，页面未显示确认层、成功或失败提示；暂停重复操作，下一步使用截图核对可见层。

## 2026-08-10：H5 密码确认层截图证据

- 截图确认“确认修改密码”层和遮罩位于账号安全弹窗之上，取消/确定均可见；DOM 快照未暴露该系统层。
- 下一步只点击取消，核对账号安全表单和临时输入仍保留，不执行密码修改。

## 2026-08-10：H5 密码确认层取消分支闭环

- 点击唯一“取消”后确认层消失，账号安全弹窗和两个临时密码输入值仍保留；未提交密码修改。
- 重启/刷新后 H5 确认层遗留项现已获得本轮独立截图和取消分支证据。

## 2026-08-10：继续执行积分/优惠页面验收

- 重新接管管理端真实浏览器会话，当前页面为“系统配置”，列表显示 6 条，`invoiceDays=7`。
- 已确认导航中存在“积分管理”和“运营优惠”入口；下一步优先完成积分页面的取消、重复操作、断网保留旧数据、重试恢复和审计证据。

## 2026-08-10：积分入口定位

- 重新接管后，“积分管理”按钮可见且唯一，真实 DOM 为带 `title="积分管理"` 的导航按钮。
- 常规点击后仍停留在系统配置，尚未判定为业务故障；继续使用该真实按钮的强制交互并观察路由/页面状态。

## 2026-08-10：积分入口第二次交互观察

- 对唯一的 `button[title="积分管理"]` 进行一次强制 UI 点击并等待，页面仍停留在系统配置；未发生积分业务写入。
- 已暂停继续重复点击，改用键盘可访问交互验证入口事件，避免无效重复操作。

## 2026-08-10：积分入口键盘交互结果

- `Enter` 键交互使积分按钮获得浏览器活动态，但页面内容和“系统配置”选中标签未改变。
- 代码核对确认 `navigate('points')` 应切换 React 状态；当前更像是浏览器标签绑定与页面事件状态不同步，下一步用已访问标签交互对照，不执行写操作。

## 2026-08-10：积分管理页面已进入

- 先切换到已访问的“消息通知”标签使“用户与运营”导航组处于展开状态，再点击“积分管理”成功进入真实页面。
- 积分列表当前 14 条；选择 `u-demo / 培训用户 / 128` 作为可恢复测试对象，后续调整后恢复原值，避免留下业务数据变化。

## 2026-08-10：积分调整取消分支

- 打开 `u-demo` 的积分调整表单，临时填入 `+5` 和取消分支原因后点击“取消”。
- 表单关闭，列表仍为 14 条且 `u-demo` 积分保持 `128`；未产生积分流水或业务写入。

## 2026-08-10：积分调整快速重复确认

- 对 `u-demo` 执行 `+1`，原因“验收临时调整-随后恢复”，进入真实“操作确认”层。
- 对“确定”执行快速双击后只出现一次“积分已调整”，列表积分从 `128` 变为 `129`，确认层和表单均关闭；未把页面结果等同于底层请求次数。
- 下一步执行 `-1` 恢复原值，并查看积分流水与审计记录。

## 2026-08-10：积分恢复第一次定位错误

- 恢复操作首次按旧列表值 `128` 定位行，因快速重复确认后页面已刷新为 `129`，定位器无匹配并超时；未发生新写入。
- 已记录该错误，改用当前值 `129` 定位，不重复失败选择器。

## 2026-08-10：积分恢复完成

- 使用当前 `129` 行执行 `-1`，确认层快速双击后仅出现一次“积分已调整”。
- `u-demo` 已恢复为 `128`，列表仍为 14 条；当前净业务值恢复原状，两个临时调整流水保留用于审计核对。

## 2026-08-10：积分流水核对

- `u-demo` 的“积分流水”真实弹窗显示当前积分 `128`，新增两条临时记录：`+1 验收临时调整-随后恢复` 与 `-1 验收临时恢复原值`。
- 流水编号分别为 `PL-1786309015693-5b1aff`、`PL-1786309062592-1cde58`；页面数据与净值恢复一致。
- 下一步切换“操作审计”并按积分相关动作筛选，补齐审计可见性证据。

## 2026-08-10：积分审计入口定位错误

- 关闭积分流水时，弹窗同时存在两个同名“关闭”按钮，严格定位器拒绝选择；未发生数据变化。
- 改用弹窗内第一个同名关闭按钮，再切换既有“操作审计”标签，避免重复相同选择器。

## 2026-08-10：积分操作审计筛选

- “操作审计”真实筛选为“用户积分调整”，列表共 6 条；顶部两条对应本轮 `demo: -1` 与 `demo: 1`，操作者均为 `admin`。
- 记录编号为 `LOG-1786309062597-5cc19a5d` 与 `LOG-1786309015695-357fcd61`；审计可见性与积分流水交叉一致。
- 下一步回到积分列表，注入 3199 读取失败，验证旧积分列表保留和重试恢复。

## 2026-08-10：积分列表失败与重试恢复

- 只停止监听 3199 的 API 进程 `PID 16288`，未停止 3100/5174/5185，也未执行积分写入。
- 积分列表点击“查询”后真实显示 `刷新失败：Failed to fetch` 和“重新加载”，原有 14 条旧列表保留，`u-demo=128` 未变。
- 以 `backend/api` 工作目录恢复 3199，健康检查 HTTP 200 且 `status=ok,database=ok`；点击“重新加载”后错误消失，列表恢复 14 条，积分值仍为原值。

## 2026-08-10：运营优惠页面进入

- 真实进入“运营优惠”，列表共 8 条；第一条 `rule-1786283589509-4a7ab8` 当前为停用，适合作为启用后恢复原状态的测试对象。
- 规则列表展示启用状态筛选、编辑和启用/停用操作；后续不新增规则，避免留下额外业务数据。

## 2026-08-10：优惠规则编辑取消分支

- 打开第一条停用规则，临时将最低人数改为 `3`、折扣改为 `0.88`，点击“取消”。
- 弹窗关闭，规则仍为 `2 / 0.95 / 否`；未新增或修改优惠规则。

## 2026-08-10：优惠规则启用快速重复确认

- 第一条规则进入“启用优惠规则”确认层；对“确定”快速双击后只出现一次“优惠规则已启用”。
- 规则状态由“否”变为“是”，列表仍为 8 条；启用后页面真实显示与 `rule-2` 的冲突关系，未把静态按钮存在当成功能完成。
- 下一步停用该规则恢复原状态，再按审计筛选核对启用/停用动作。

## 2026-08-10：优惠规则恢复第一次定位错误

- 恢复停用时，按规则编号和“是”匹配同时命中第一、第二行，因为第二行的冲突规则列也包含该编号；严格定位器拒绝多匹配，未发生写入。
- 改用第一条数据行的精确文本并选择其第一个操作按钮，避免重复同一失败定位。

## 2026-08-10：优惠规则恢复完成

- 对启用后的第一条规则进入“停用优惠规则”确认层，快速双击“确定”后只出现一次“优惠规则已停用”。
- 第一条规则已恢复为 `2 / 0.95 / 全部课程 / 无冲突 / 否`，列表仍为 8 条；未留下净状态变化。
- 下一步筛选“优惠规则维护”审计并验证断网重试。

## 2026-08-10：优惠规则操作审计筛选

- 操作审计筛选“优惠规则维护”后共 7 条；本轮顶部两条分别为 `enabled:false` 和 `enabled:true`，详情显示 `minPeople=2`、`discountRate=0.95`，启用时冲突为 `rule-2`，停用后冲突为空。
- 记录编号为 `LOG-1786309334245-b1a0d9de`、`LOG-1786309288416-d41ad668`，与页面状态切换和恢复闭环一致。
- 下一步回到优惠规则列表注入 3199 读取失败，验证旧规则保留与重试恢复。

## 2026-08-10：优惠规则列表失败与重试恢复

- 只停止 3199 当前 API 进程 `PID 6856`，未停止其他本地服务，未执行规则写入。
- 点击优惠规则“查询”后真实显示 `刷新失败：Failed to fetch` 和“重新加载”，原有 8 条规则保留；第一条仍为 `2 / 0.95 / 否`，无冲突。
- 恢复 3199 后健康检查 HTTP 200 且 `status=ok,database=ok`；点击“重新加载”后错误消失，列表恢复 8 条，第一条仍为原状态。

## 2026-08-10：P3 积分/优惠专项与回归收尾

- 积分与优惠的取消、快速重复确认、恢复原状态、流水/审计和 3199 失败重试均已完成页面级验证；底层网络请求次数仍不作声称。
- 复跑通过：根目录 `verify`、后端 15 套件/68 测试、管理端 UI 校验、管理端构建、C 端 H5 构建、`git diff --check`。
- 当前本地服务均恢复监听：3100、3199、5174、5185；3199 健康为 `status=ok,database=ok`；微信/支付宝在线支付开关仍为 `false/false`。
- P3 尚保留“其他模块剩余重复提交边界”收敛项；P2 合并、P4 目标服务器和生产发布边界不变。

## 2026-08-10：继续会话 H5 复核开始

- 当前 5185 H5 真实标签已接管，页面为 `demo01` 的“我的”→“账号与安全”，两个临时密码输入值仍在，未执行密码提交。
- 另有一个同一 H5 地址的错误标签；不使用该错误标签，继续基于健康运行的真实标签完成重启/刷新后的验证。

## 2026-08-10：H5 重启刷新后账号安全入口

- 仅重启占用 5185 的 Node 进程 `PID 2512`，恢复后根页面 HTTP 200；未停止 3100/3199/5174。
- 真实 H5 标签刷新后仍保持 `demo01` 登录态，进入“我的”页面；通过唯一父级菜单行打开“账号与安全”，弹窗正常显示，未读取 Cookie/Storage。
- 当前密码框为空，下一步只填入临时值并验证确认层取消分支，不提交密码修改。

## 2026-08-10：H5 密码确认触发

- 两个密码框仅填入临时值 `TestPassword-2026`，保存按钮唯一匹配且点击后业务弹窗仍保留，未发生密码修改成功提示。
- DOM 快照未暴露系统确认层；下一步用真实页面截图核对确认层是否位于业务弹窗之上，然后只点击“取消”。

## 2026-08-10：H5 密码确认层视觉证据

- 截图真实显示“确认修改密码”系统确认层、遮罩和“取消/确定”按钮位于“账号与安全”业务弹窗之上；与重启/刷新后的页面状态一致。
- 尝试用业务层 `uni-button` 定位“取消”未命中，因为系统层未暴露到该 DOM 语义树；未重复点击或触发密码修改，下一步改用可见文本定位诊断后取消。

## 2026-08-10：H5 重启刷新后密码确认取消闭环

- 重启 5185 并刷新真实 H5 标签后，`demo01` 会话恢复；账号安全弹窗可见。
- 填入两个临时密码后点击保存，截图确认系统“确认修改密码”层位于业务弹窗之上；最终确认未点击。
- 通过唯一可见文本“取消”关闭系统确认层后，账号安全业务弹窗和两个临时密码输入值仍保留；未产生密码修改成功提示或写入。

## 2026-08-10：P2 运行态读取初次诊断

- 现有 `admin/123456`、`operator/123456`、`demo01/123456`、`demo02/123456` 可登录；`demo/123456` 当前登录失败，未继续使用该账号。
- 首次脚本误假设学员列表存在 `phone` 字段并直接解析导出 JSON；实际导出 JSON 含重复键 `username/userName`，PowerShell 解析失败，但请求本身已返回管理员明文文件名、运营人员脱敏文件名、普通用户 401 和跨账号支付状态 404。
- 不把这次脚本输出当作完整 P2 结论，下一步改用字段名诊断和正则提取布尔标志，不输出敏感值。

## 2026-08-10：P2 跨账号/脱敏导出运行态复核完成

- 3199 真实 API 读取：管理员学员详情 HTTP 200，手机号为明文且含 `phoneNormalized`；运营人员详情 HTTP 200，手机号脱敏且不含 `phoneNormalized`。
- 管理员标准导出 HTTP 200、`sensitiveFieldsMasked=true`、文件名 `student-profiles-masked.json`；管理员显式 `reveal=true` 返回 `false`、文件名 `student-profiles.json`。
- 运营人员显式 `reveal=true` 仍返回脱敏文件名和 `sensitiveFieldsMasked=true`；普通用户 `demo01` 访问管理端导出返回 HTTP 403。
- `demo01`/`demo02` 各自订单列表分别为 2/1 条，互读对方订单支付状态均 HTTP 404；导出审计接口 HTTP 200，存在“导出学员档案”记录。
- P2 当前保留的限制为学员合并需明确授权，以及浏览器底层请求次数不可可靠统计；未执行任何写操作。

## 2026-08-10：本轮最终回归校验

- 根目录 `pnpm.cmd run verify` 通过；后端 15 个测试套件/68 个测试通过；管理端 UI 校验通过；`git diff --check` 通过（仅有 LF/CRLF 提示）。
- 3199 健康检查仍为 `status=ok,database=ok`；3100/3199/5174/5185 均在监听；微信/支付宝在线支付开关仍为 `false/false`。
- 本轮没有代码业务修改，主要补充 H5/P2 运行态证据和文档；未执行真实支付、密码最终确认、学员合并或远程写入。

## 2026-08-10：续接后的本地回归与健康检查

- `pnpm.cmd run verify` 通过；`git diff --check` 通过，仅有换行格式提示。
- 3100 与 3199 的实际健康端点 `/api/health` 均返回 HTTP 200、`status=ok`、`database=ok`；5174 与 5185 根页面均返回 HTTP 200。
- 首次探测 API 根路径得到 404，复核代码路由后确认这是预期的无根路由行为，不记录为服务故障。
- 本轮未修改业务代码、未执行真实支付、未合并学员档案、未向目标服务器写入。

## 2026-08-10：P3 消息启停并发门禁补强

- 将 `backend/api/src/mvp/mvp.service.ts` 的消息启停改为目标状态条件的原子更新；重复/竞争请求不再重复迁移状态或写入启停审计。
- 在 `backend/api/test/admin-operations.spec.ts` 新增并发回归：两个同时启用请求得到 `201/400`，最终启用状态正确，相关启用审计仅 1 条。
- 定向测试 8/8 通过；完整后端 15 个测试套件、69 个测试通过；根目录 verify、管理端 UI 校验/构建、H5 构建和 `git diff --check` 通过。
- 首次定向测试仅因新断言把 Nest POST 成功状态写成 200 而失败，已修正为 201；未发现业务逻辑错误。
- 已新增 [P3 消息启停并发状态门禁验收](Docs/Summary/2026-08-10_P3消息启停并发状态门禁验收.md)，并修正 P2 旧总结中已被最新运行态推翻的 operator 权限历史描述。

## 2026-08-10：P3 规则与系统配置重复保存审计去重

- 优惠规则已有记录的重复相同值保存改为目标值条件更新；系统配置已有记录的重复相同值保存改为值/说明条件更新。
- 新增回归验证：规则与配置各自并发重复提交均成功，但每类维护审计只增加 1 条。
- 定向测试 9/9 通过；完整后端 15 个测试套件、70 个测试通过；根目录 verify、管理端 UI 校验/构建和 H5 构建通过。
- 首次测试按规则 ID 查询审计得到 0 条，原因是规则维护详情未承诺包含 ID；已改为按操作类型比较数量增量，测试通过。
- 积分调整仍不去重，每条积分变更保留独立流水；未执行真实支付、学员合并或远程写入。

## 2026-08-10：P4 Compose 预检与环境模板

- `export-postgresql.cjs`、`import-postgresql.cjs`、`verify-postgresql-reconciliation.cjs` 均通过 `node --check`。
- 新增 `.env.docker.example`，包含非支付 Compose 所需配置形状，真实 `.env.docker` 仍不创建、不提交。
- 本地 `docker compose config` 首次因缺少 `POSTGRES_PASSWORD` 和 `.env.docker` 失败；使用进程级占位值复核后仍因 `.env.docker` 不存在而拒绝，未启动容器或数据库。
- 更新服务器验收步骤，要求在目标服务器复制模板并替换专用密码/密钥；P4 真实 SSH、迁移对账、卷持久化、备份恢复和回滚仍未验收。

## 2026-08-10：P4 预检收尾

- 根目录 `pnpm.cmd run verify`、三个 PostgreSQL 脚本 `node --check` 和 `git diff --check` 通过。
- 3100/3199 `/api/health` 均为 HTTP 200、数据库 `ok`；公共在线支付开关保持关闭。
- 本地预检未启动 Docker、未连接 PostgreSQL、未写入 `.env.docker`；真实 P4 仍等待目标服务器环境与 SSH 权限。

## 2026-08-10：P2/P3/P4 当前状态审计

- 核对既有 P2 Downloads 证据：标准脱敏导出文件已实际落盘，27 条记录、未掩码手机号为 0、不含 `phoneNormalized`。
- 计划状态更新：P3 的页面/后端成功、取消、失败重试、刷新、幂等与审计证据已闭环；不把浏览器底层请求次数解释为已统计。
- P2 仅保留不可逆学员档案合并授权项；P4 仍受目标服务器 SSH 与 `.env.docker` 外部配置阻塞。

## 2026-08-10：目标服务器网络可达性复核

- `Test-NetConnection 192.168.0.162 -Port 22` 返回 `TcpTestSucceeded=True`。
- 该结果仅证明 TCP 网络路径可达，不代表 SSH 身份认证或命令执行权限；未尝试猜测用户名/密码，未远程写入。

## 2026-08-10：P4 shell 脚本检查限制

- 尝试使用 `bash -n` 检查 `Docs/runtime/*.sh`，Windows Bash/WSL 服务返回 `E_ACCESSDENIED`；随后环境探测确认 `bash.exe`/`wsl.exe` 存在但服务不可用。
- 未将该结果记录为脚本语法错误；当前 `.sh` 需在目标 Linux 服务器或可用 Unix shell 中验证。

## 2026-08-10：本轮执行与 H5 运行态复核

- 本地门禁复跑通过：`pnpm.cmd run verify`、三个 PostgreSQL 脚本 `node --check`、`git diff --check`；3100/3199/5174/5185 TCP 均可达。
- 重新获取浏览器会话后打开 5185，使用已确认的 `demo01/123456` 登录成功；点击“我的”进入账号安全，刷新后仍恢复 `demo01` 会话。
- 临时填写两个密码并点击“保存新密码”后，真实截图显示“确认修改密码”系统层覆盖业务弹窗；点击“取消”后确认层消失，业务弹窗和临时输入值保留，未执行最终确认或密码写入。
- 浏览器首次使用了旧失效标签绑定，提示标签不属于当前会话；重新获取浏览器会话和标签后恢复，记录为测试环境会话问题，不作为应用失败。
- 本轮未修改业务代码、未执行真实支付、未执行学员合并、未向目标服务器写入。

## 2026-08-10：完整自动化与构建门禁复跑

- 后端 `pnpm.cmd test` 通过：15 个测试套件、70 个测试，0 失败。
- 后端 `pnpm.cmd run build`、管理端 `test:ui`、管理端 `build`、H5 `build:h5` 和根目录 `pnpm.cmd run verify` 全部通过。
- H5 构建仅输出 Sass `@import`/legacy API 及 uni-app 更新提示，未造成构建失败。
- 本轮没有新增业务代码修改；P3 按当前自动化与运行态证据保持完成，P2 合并和 P4 服务器仍未授权/未执行。

## 2026-08-10：规划状态一致性修正

- 修正 `task_plan.md` 顶部阶段状态与末尾最新证据不一致的问题：P1/P3 已完成，P2/P4 进行中，P5 已完成本地/内网非支付冻结。
- P5 的“完成”仅表示内部测试版本及其限制清单已冻结，不表示生产发布；P2 学员合并和 P4 目标服务器验收仍明确保留为未完成项。
- 本轮一次长连接端口探测超时、一次 PowerShell 管道语法错误均已改用短超时 TcpClient 和对象收集方式解决；未造成业务写入或服务变更。

## 2026-08-10：外部阻塞审计收尾

- 本地/内网非支付内部测试版本已具备完整自动化、构建、健康检查、H5 运行态和权限/脱敏/审计留痕。
- 学员档案合并仍未执行，原因是缺少用户明确的源/目标档案授权；未猜测或替代执行。
- 目标服务器 P4 仍无法执行，原因是缺少 SSH 身份和服务器 `.env.docker`；未猜测凭据、未远程写入。
- 底层快速双击请求次数继续标记为不可可靠统计，不影响已验证的页面状态门禁结论。

## 2026-08-10：目标恢复后的外部状态复核

- 目标服务器 `192.168.0.162:22` TCP 仍可达，但本机 `ssh-add -l` 显示 SSH agent 未运行，未尝试猜测或替代凭据。
- 工作区仍不存在真实 `.env.docker`；没有新的服务器环境配置或 SSH 授权可用于 P4。
- 当前对话未提供学员合并的源档案 ID、目标档案 ID及明确执行授权，因此未执行合并。
- 尝试只读检查本机 `C:\Users\EDY\.ssh` 配置时被 Windows 文件权限拒绝；未绕过权限、读取私钥或猜测 SSH 用户名。

## 2026-08-10：SSH、登录和短信验证操作指南

- 新增 `Docs/Summary/2026-08-10_继续非支付目标与SSH短信验证操作指南.md`。
- 文档基于当前认证控制器、认证服务、短信/密码找回适配器、H5 页面、测试用例、种子账号和 Compose 配置编写。
- 已明确区分开发 fake 验证码与真实 webhook 短信；未把 `devCode` 或页面按钮存在解释为真实短信发送完成。
- 已记录 SSH 公钥授权、服务器 `.env.docker`、短信 webhook 请求格式、登录测试账号、PowerShell/API 测试命令和 P2/P4 剩余授权项。
## 2026-08-10：本地登录错误与 H5 运行态复核
- 本地管理端 5174 使用 `admin/123456` 登录成功，进入工作台；截图中的旧登录错误未在当前运行态复现。
- H5 5185 退出后使用 `demo01/123456` 登录成功，刷新后会话恢复并进入“我的”。
- H5“账号与安全”保存密码流程显示系统确认层；点击取消后未提交密码修改。
- 登录种子和测试库已同步修正，后端完整测试 15/15 套件、70/70 用例通过，根目录 verify 通过。
- 3100 的旧运行实例仍有角色映射差异：operator 登录成功但返回 user；3199 返回正确 operator。未未经确认重启 3100。
- 本轮未连接目标服务器、未执行学员合并、真实短信或最终密码修改。
## 2026-08-10：目标服务器只读状态复核
- `192.168.0.162:22` TCP 可达，但 OpenSSH 认证失败：`Permission denied (publickey,password)`。
- 目标 3100 的 `/api/health` 返回 404，课程接口返回 200；5174、5185、5432 不可达，表现为旧 API 而非已验收的 Compose/PostgreSQL 版本。
- 目标公开支付设置读取为微信/支付宝在线开关 `true/true`，不能满足 `PAYMENT_ADAPTER=disabled` 的非支付发布门禁。
- 远程 admin 登录返回 HTTP 500；未继续猜测凭据或执行服务器写入。
- P4 继续保持外部阻塞，已记录具体证据和所需授权。

## 2026-08-10：本地 seed 执行与管理端登录复核
- 已执行 `pnpm.cmd --dir backend/api db:seed`，本地 `backend/api/data/training.db` seed 完成。
- 本地 `3100`、`3199` 的 `/api/health` 均返回 HTTP 200，`status=ok`、`database=ok`。
- `admin/123456` 在本地两个 API 登录均成功；未输出或保存访问令牌。
- 截图地址为 `http://localhost:5174/login`，属于本地管理端；本次未连接、修改或重启 `192.168.0.162`。

## 2026-08-10：C 端退出登录确认弹窗视觉优化
- `mine.vue` 的退出确认改为危险操作变体，按钮文案调整为“继续使用/退出登录”。
- `confirm.ts` 增加变体、退出图标和关键内联样式；`App.vue` 补充全局主题样式，避免 html portal 恢复浏览器默认按钮。
- 5185 真实截图确认新弹窗视觉与“我的”页面统一；取消后仍留在“我的”，确认后进入登录页。
- H5 构建、根目录 verify、git diff 检查全部通过；验收文档已新增。
## 2026-08-10：非支付公开支付开关回归门禁
- 新增公开支付设置回归断言：生产环境 `PAYMENT_ADAPTER=disabled` 时，数据库历史在线开关不会被公开接口暴露为可用。

## 2026-08-10：课程管理新增课程与排序
- 已完成开始/结束日期时间选择、时间段摘要、结束时间晚于开始时间校验。
- 已完成新增课程草稿保存、关闭后恢复、重置清空；编辑已有课程不写入新增草稿。
- 已完成报名模板为空时的明确提示，并在真实保存成功后清理草稿。
- 已将管理端课程列表排序改为创建时间倒序，新增课程显示在最前。
- 待完成：运行管理端 UI 校验、构建、根目录 verify，并用本地管理端浏览器执行课程新增/草稿/重置/排序回归。
- 后端完整回归 15/70 通过；后端构建、根目录 verify、git diff 检查通过。
- 目标服务器公开开关仍为 `true/true`，本地门禁已能覆盖该风险，但服务器修正仍需 SSH/维护授权。

## 2026-08-10：时间控件、课程搜索与发票重申请
- 已将课程报名截止、Banner 展示开始/结束时间统一为日期时间选择器；消息和审计时间筛选保持统一控件。
- 已将 Banner 关联课程改为可搜索课程名称/编号的选择器。
- 已完成已驳回发票重新申请的前后端闭环；`p1-write-operations.spec.ts` 通过。
- 已修复 C 端报名支付弹窗打开时底层原生选择控件盖顶问题。
- H5 构建、管理端 UI 校验、管理端构建、根目录 verify、git diff 检查均通过。
## 2026-08-10：开票重申请审核闭环补强

- C 端重新申请按钮调整为开票卡片右下角的紧凑操作按钮，避免独占整行造成空旷布局。
- 登录/注册后仅提示没有后续重申请记录的最新驳回申请；旧驳回记录保留审计但标记为已重新提交。
- 管理端新申请继续进入“待处理”，可再次通过或驳回；新增专项测试覆盖再次驳回、再次重申请和最终通过。
- 验证：`p1-write-operations.spec.ts` 2/2 通过，C 端 H5 构建通过，`git diff --check` 通过。

## 2026-08-10：H5 账号安全与 P2 后端边界复核
- 5185 H5 真实进入“我的 → 账号与安全”；点击保存后确认层位于业务弹窗之上，点击取消后确认层消失、临时密码仍保留且未提交修改。
- 关闭业务弹窗并刷新后，页面仍为 `#/pages/mine/mine`，账号 `demo` 和账号安全入口均恢复，证明本地会话可刷新恢复。
- `pnpm.cmd --dir backend/api test -- --runTestsByPath test/student-api.spec.ts test/admin-security-boundaries.spec.ts`：2 个套件、10 个测试通过。
- 尝试接管管理端已有标签时，浏览器返回标签不属于当前自动化会话；未读取浏览器存储、未使用底层绕过。管理端页面级导出证据继续单独标注。

## 2026-08-10：H5 重启后运行态与 P2 真实请求复核

- 仅重启 5185 H5，重启后端口恢复监听；3100/3199/5174 保持运行，API 健康检查仍为 `status=ok,database=ok`。
- 当前浏览器重新接管 5185 标签后刷新，页面恢复到订单；进入“我的”显示账号 `demo`、积分和“账号与安全”入口。
- 填写临时密码并点击“保存新密码”后，真实截图显示确认层覆盖账号安全业务弹窗；点击“取消”后两个输入值仍保留，未执行密码修改；关闭弹窗并刷新后回到“我的”。
- H5 当前标签控制台错误为空数组；未发送真实短信、未执行最终密码确认、未执行学员合并或远程写入。
- 3100/3199 登录 `admin`、`operator`、`demo01`、`demo02` 均成功，角色分别为 `admin`、`operator`、`user`、`user`；普通用户访问管理端学员接口为 403，operator 的 `reveal=true` 导出仍为 `student-profiles-masked.json`，跨账号支付状态为 404。
- P2 专项自动化：2 个测试套件、10 个测试全部通过。
- 管理端页面级导出仍未取得独立浏览器下载证据：旧标签接管失败，保持该证据缺口，不使用 Cookie/Storage 或底层绕过。

## 2026-08-10：继续目标所需材料提交说明
- 新增 `Docs/Summary/2026-08-10_继续非支付目标所需材料提交说明.md`。
- 文档详细说明了学员档案合并授权、SSH 公钥登录、服务器 `.env.docker` 脱敏配置、真实短信前置材料和管理端导出下载证据的提交方式。
- 明确禁止在聊天或 Git 中提交 SSH 私钥、数据库密码、JWT secret、短信 Token 和未脱敏学员数据；合并、迁移、重启、备份恢复、回滚和真实短信均需单独确认。

## 2026-08-10：5185 H5 新浏览器标签真实复核
- 新建 H5 测试标签后使用 `demo01` 登录成功；进入“我的”后刷新，账号信息和“账号与安全”入口保持存在。
- 打开账号安全并提交临时密码后，真实确认层标题为“确认修改密码”，根/遮罩/内容运行时层级为 `2147483645/2147483646/2147483647`。
- 点击确认层“取消”后确认层消失，两个临时密码输入值仍保留；关闭业务弹窗并刷新后回到“我的”，未执行最终密码修改。
- 新增 `Docs/Summary/2026-08-10_C端H5刷新后真实运行态复核补充.md`；本轮未连接目标服务器或执行其他外部写操作。

## 2026-08-10：SSH 配置与 VSCode 登录差异诊断
- 只读读取 `C:\Users\EDY\.ssh\config`：`guanxiang-server` 指向 `49.234.113.222`，显式使用 root 和密码认证；`192.168.0.162` 是另一条 Host 配置。
- SSH 目录只发现 `config`、`known_hosts`、`known_hosts.old`，未发现常见私钥；ssh-agent 无可用连接。
- `49.234.113.222:22` 与 `192.168.0.162:22` 均可达，但两个 Host 的 BatchMode 只读登录均认证失败。该结果与 VSCode 可能通过交互式密码登录另一台主机相符。
- 新增 `Docs/Summary/2026-08-10_SSH配置与VSCode登录差异诊断.md`；未读取私钥/密码库，未对任一服务器执行写操作。

## 2026-08-10：确认 VSCode 可交互登录目标服务器
- 根据 VSCode Remote-SSH 日志确认，VSCode 实际连接的是 `192.168.0.162`，使用 `C:\Windows\System32\OpenSSH\ssh.exe` 和 `ssh -T -D ... "192.168.0.162" sh`。
- 日志出现 `root@192.168.0.162's password:`、`Got password response`，随后解析出 Ubuntu/Linux 远程配置并成功启动 VSCode Server。
- 之前命令行 BatchMode 失败的原因是禁止密码交互，不能据此判定服务器账号密码无效；SSH 目标已从“不可登录”修正为“交互式登录已验证，待在该会话中进行只读 P4 检查”。
- 未读取密码、私钥或 VSCode 凭据，未执行服务器写操作。

## 2026-08-10：管理端学员导出页面下载复核
- 新建管理端浏览器标签，展开“用户与运营”后进入“学员管理”，点击“导出”后实际生成 `C:\Users\EDY\Downloads\student-profiles-masked (7).json`。
- UTF-8 JSON 校验通过：`total=29`、`items=29`、`sensitiveFieldsMasked=true`、`truncated=false`、不含 `phoneNormalized`、未脱敏手机号数量为 0；SHA-256 为 `4C022B4346A303A89D937EBF87EAB0ACB77007CC7FBA389EDFAB44B728CEE963`。
- 新增 `Docs/Summary/2026-08-10_P2导出页面下载复核补充.md`；浏览器独立 download 事件仍未稳定捕获，但页面操作与文件落盘证据已补齐。
- 用户提供的 `id/find` 结果来自本地 PowerShell，不是服务器远程终端；下一步需在 VSCode `SSH: 192.168.0.162` 窗口中执行只读服务器命令。

## 2026-08-10：P4 服务器 SSH 与运行时预检
- 用户在 VSCode `SSH: 192.168.0.162` 远程终端提供了只读结果：root 登录、主机名 `ctc-Vostro-3910`、项目目录 `/opt/training-management`、Compose 文件存在、Docker 29.1.3、Compose 2.40.3。
- root 属于 docker 组，SSH/网络/Docker 前置条件已确认；新增 `Docs/Summary/2026-08-10_P4服务器SSH与运行时预检补充.md`。
- 尚未读取或输出 `.env.docker` 内容，未执行 Compose、迁移、重启、备份恢复、回滚或数据库写操作；已准备下一组只读检查命令。

## 2026-08-10：H5 重启后隔离 API 真实运行态
- VSCode Remote-SSH 占用了本地 3100 端口并转发到服务器旧 API；本地 3199 的当前代码 API 健康检查返回 `status=ok,database=ok`。
- 仅重启本地 5185 H5，并通过运行时 `VITE_API_BASE_URL` 临时指向 3199；未修改仓库代码、数据库或服务器。
- 新浏览器标签中 `demo01` 登录成功；进入“我的”后刷新，仍停留在“我的”，账号与安全入口保持存在。
- 真实点击保存密码后确认层出现；业务弹窗 z-index `1000`，确认层 z-index `2147483647`；点击取消后确认层消失、两次临时输入仍保留，未执行最终密码修改。
- 新增 `Docs/Summary/2026-08-10_C端H5重启后隔离API真实复核.md`。

## 2026-08-10：服务器版本差异确认
- 服务器 HEAD 为 `5f41bf7`，镜像创建于 2026-08-05；本地工作区 HEAD 为 `16ff2b8`，部署源不一致。
- 服务器 API 容器内部 `/api/health` 返回 404，管理端/H5 宿主机入口返回 200；不能把旧镜像的 Compose `healthy` 状态等同于当前代码健康检查通过。
- 用户已明确授权服务器构建、Compose 替换、PostgreSQL migration、重启、备份恢复和回滚；但应先确定正确代码同步来源，保留服务器未跟踪导出文件和 override 文件，不直接清理。
- 本地 `16ff2b8` 包含服务器 `5f41bf7` 之后的健康路由；但本地工作区还有未提交的 API、管理端和 H5 修改。服务器部署前仍需确认是部署干净提交，还是先形成包含当前未提交修改的受控发布包。
- 本轮 H5 构建、API 构建、管理端构建、后端 15 套件/70 测试及根目录 `verify` 均通过；H5 构建仅有 Sass 弃用警告。
